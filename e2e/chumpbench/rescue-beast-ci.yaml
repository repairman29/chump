# ChumpBench track — the known-good RESCUE lap (DOC-072).
id: rescue-beast-ci
mode: RESCUE
repo: repairman29/BEAST-MODE
stack: javascript
state: red-ci
difficulty: medium
task: "The GitHub Actions workflow 'ml-pipeline' is failing on the default branch: its 'Setup Node.js' step errors with 'Some specified paths were not resolved, unable to cache dependencies'. Fix the root cause in the workflow so the ml-pipeline job passes. (The ML pipeline itself already succeeds — only the setup-node dependency-cache configuration is broken.)"
acceptance:
  kind: ci-green
  check: "the repo's CI checks all pass (no failing check-runs)"
budget:
  max_wall_clock_min: 30
  max_human_touches: 0
