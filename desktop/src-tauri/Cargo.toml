[package]
name = "chump-desktop"
version = "0.1.0"
description = "Tauri shell for Chump PWA (Cowork UI scaffold)"
edition = "2021"
rust-version = "1.74"

[lib]
name = "chump_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
