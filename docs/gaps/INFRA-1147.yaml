- id: INFRA-1147
  domain: INFRA
  title: "EFFECTIVE: weekly roadmap-update agent — read recent ships, propose ROADMAP.md diff for operator review"
  status: done
  priority: P2
  effort: s
  acceptance_criteria:
    - Agent script reads ROADMAP.md, git log (ships), chump gap list --status done; calls Claude (haiku-4-5 default, sonnet --high-fidelity) to propose unified diff
    - Never auto-merges; opens roadmap/weekly-update-YYYY-WW PR for operator review
    - "Emits 4 event kinds: roadmap_update_proposal_cost (model, tokens, USD), roadmap_update_proposal_opened (PR success), roadmap_update_proposal_failed (LLM/parse/PR error), roadmap_update_proposal_skipped (idempotency/NO_CHANGE)"
    - "Cost tracked via events ($0.01/run × 52 weeks = $0.52/yr); all 4 kinds in EVENT_REGISTRY.yaml"
    - "Failures silent (no half-baked PR); --force override same-week idempotency; smoke test: test-roadmap-update-agent.sh passes 13 assertions"
    - "Launchd plist runs Sunday 09:00 local with RunAtLoad=false"
  closed_pr: 1882

