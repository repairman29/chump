- id: EFFECTIVE-359
  domain: EFFECTIVE
  title: "EFFECTIVE: external-repo contract prompt must steer edits to str_replace (weak models clobber via write_file)"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"external-repo contract prompt must steer edits to str_replace (weak models clobber via write_file)\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3481

