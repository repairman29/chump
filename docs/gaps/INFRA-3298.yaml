- id: INFRA-3298
  domain: INFRA
  title: 'ZERO-WASTE: main.rs slice 2 — extract 5 author-time helper commands to dispatch_authoring'
  status: done
  priority: P2
  effort: m
  description: 'Slice 2 of INFRA-3287 (main.rs decomposition). Extract the 5 author-time helper command arms (add-env-var,
    emit-event, install-daemon, add-path-filter, add-raw-gh-allowlist — all INFRA-2399, all clean commands::X::run delegations)
    from main()''s argv chain into src/commands/dispatch_authoring.rs::try_dispatch(args)->Option<i32>. Same try_dispatch
    template as slice-1 dispatch_external (INFRA-3289). Pure move, zero behavior change.

    '
  acceptance_criteria:
  - the 5 author-time helper arms moved from main.rs into commands/dispatch_authoring.rs behind try_dispatch
  - cargo check --bin chump green (verified in a target dir OUTSIDE the worktree)
  - main.rs shrinks by the moved block
  - dispatch_authoring declared in commands/mod.rs; CLI surface of the 5 commands unchanged
  closed_date: '2026-07-05'
  closed_pr: 3185
