- id: INFRA-2384
  domain: INFRA
  title: 'inventory: backfill artifact.introducing_pr + introducing_gap via git-log → PR mapping (currently 0 of 4534 populated)'
  status: done
  priority: P0
  effort: m
  acceptance_criteria:
  - single-pass git-log --diff-filter=A walk populates introducing_pr + introducing_gap on artifact_index rows; target <60s
    on 4500-artifact / 3000-PR repo
  - 'activation_state recomputed via PR provenance: referenced when refs>2, dormant when refs<=2 from MERGED PR, orphan when
    reference_count=0 from MERGED PR, unknown if PR provenance not found'
  - chump inventory show <path> prints artifact profile with introducing PR, gap, activation; --json supported
  - chump inventory pr <pr_number> prints PR + every artifact it shipped with activation health; --json supported
  - test fixture scripts/ci/test-inventory-provenance-backfill.sh synthesizes 3 merge PRs adding 2 files each; asserts all
    6 artifacts linked + activation recomputed + CLI outputs match
  - post-backfill, >50% of artifacts have introducing_pr populated
  closed_date: '2026-06-01'
  closed_pr: 2936
