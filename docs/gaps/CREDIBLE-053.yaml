- id: CREDIBLE-053
  domain: CREDIBLE
  title: 'CREDIBLE: gap-ID allocator 60s ETag cache + CANONICAL_STATE_CONTRACT.md doc (followup to CREDIBLE-052)'
  status: done
  priority: P2
  effort: xs
  description: 'Optimization + documentation on top of CREDIBLE-052. The core collision

    prevention in CREDIBLE-052 calls gh pr list on every reserve; this gap

    adds a 60s cache keyed on gh response ETag or repo-tip SHA to avoid

    hammering the API on rapid successive reserves, and documents the

    cross-session contract in CANONICAL_STATE_CONTRACT.md.

    '
  acceptance_criteria:
  - reserve_next_id() caches the open-PR YAML inventory for 60s keyed by gh ETag header (or repo HEAD SHA as fallback) — rapid
    successive chump gap reserve calls within 60s reuse the cached list
  - Cache invalidated when ETag changes or 60s TTL expires; emits kind=gap_id_allocator_cache_miss vs kind=gap_id_allocator_cache_hit
    to ambient.jsonl
  - 'docs/process/CANONICAL_STATE_CONTRACT.md updated: adds section ''Drift Mode B — cross-session ID collision'' describing
    the two-source check (state.db + open PR branches) and what degrades protection (offline mode)'
  depends_on:
  - CREDIBLE-052
  notes: Decomposed from CREDIBLE-044. Blocked on CREDIBLE-052 shipping first.
  closed_date: '2026-05-14'
  closed_pr: 1714
