- id: INFRA-2045
  domain: INFRA
  title: "RESILIENT: wizard-daemon step4 outcome detection — prevent infinite dispatch wheel-spin"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "step4_dispatch_pickable_gaps classifies each dispatch entry as SHIPPED/PR_OPENED/FAILED/IN_FLIGHT on every tick; FAILED dispatches enter cooldown before re-dispatch; cooldown guard emits wizard_dispatch_cooldown; give-up guard emits wizard_dispatch_giveup and tags wizard_skip:true; schema migration handles old-shape files; DRY_RUN=1 leaves state file unchanged; event kinds registered in EVENT_REGISTRY.yaml and event-registry-reserved.txt; test T1-T6 all pass"
  notes: |
    Closed as duplicate: this exact feature already shipped under INFRA-2051
    (PR #2643, commit feb3133f). Verified all ACs hold on current main:
    test-wizard-daemon-dispatch-outcome.sh T1-T6 = 17/17 pass,
    test-wizard-daemon-dry-run-readonly.sh = 6/6 pass,
    test-wizard-daemon.sh = 35/35 pass. No code change needed.
  closed_pr: 2643
  outcome_id: MISSION-010

