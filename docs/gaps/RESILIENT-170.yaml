- id: RESILIENT-170
  domain: RESILIENT
  title: "RESILIENT: nightly cargo-audit red on yanked aes 0.9.0 (RUSTSEC-2026-0186) — add --ignore in cargo-audit-nightly.yml"
  status: open
  priority: P2
  effort: m
  description: |
    CORRECTION (2026-07-04): this is NOT the fleet-merge blocker. The schedule-only 'cargo audit (nightly)' (cargo-audit-nightly.yml) fails on yanked aes 0.9.0 (chump->superboring 0.1.11->aes-keywrap 0.9.0->aes =0.9.0). Fix = add '--ignore RUSTSEC-2026-0186' to the cargo audit --ignore list in .github/workflows/cargo-audit-nightly.yml (~line 55-62), matching the existing RUSTSEC ignores there. (NOT deny.toml — that config isn't what the nightly reads.) The ACTUAL fleet-freeze blocker is the required 'audit' context = audit.yml aggregator over audit-shard(1..4); shards 1 & 4 are RED on sccache/toolchain-install runner-infra errors, blocking every PR — tracked separately (see the RESILIENT trunk-red infra gap).
  acceptance_criteria:
    - RUSTSEC-2026-0186 added to deny.toml [advisories] ignore with an explanatory comment (chain + why-safe + durable-fix pointer)
    - cargo deny check advisories passes locally
    - the required 'audit' check goes green on the PR
    - no other deny.toml advisory is silently added or removed

