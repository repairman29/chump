- id: RESILIENT-231
  domain: RESILIENT
  title: "RESILIENT: bot-merge 300s stage budget (INFRA-1422) too tight for warm clippy --workspace --all-targets on this host — wedges legit ships"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"bot-merge 300s stage budget (INFRA-1422) too tight for warm clippy --workspace --all-targets on this host — wedges legit ships\" is implemented in the relevant RESILIENT code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

