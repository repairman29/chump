- id: INFRA-1796
  domain: INFRA
  title: "EFFECTIVE: opus-message v0 — addressed async DMs between Opus sessions via .chump-locks/opus-inbox/ (bridge until A2A Layer 2b RPC lands, INFRA-1759)"
  status: done
  priority: P1
  effort: xs
  acceptance_criteria:
    - "New scripts/coord/opus-message.sh CLI: opus-message.sh send --to <gap-id"
    - session-id
    - "all-opus> --from <my-session-id> --body \"<text>\" [--ref <pr-or-gap>] writes one JSONL line to .chump-locks/opus-inbox/<recipient>.jsonl with fields {ts, from, to, body, ref, read_at:null}"
    - opus-message.sh list [--unread] [--for <session-id>] reads the recipient's inbox and prints addressed messages, oldest first
    - opus-message.sh mark-read <message-id> sets read_at on the message in-place via atomic rewrite (tempfile + rename)
    - "Recipient resolution: --to gap:INFRA-NNN routes to whoever currently holds the lease for that gap (look up in .chump-locks/*.json); --to session:claim-X routes to a specific session; --to all-opus broadcasts to .chump-locks/opus-inbox/all-opus.jsonl which every session reads"
    - "SessionStart hook addendum: read .chump-locks/opus-inbox/<my-session-id>.jsonl + .chump-locks/opus-inbox/all-opus.jsonl; print unread count + first-line preview of latest 3 unread messages alongside the ambient digest"
    - Hook lookup discovers my-session-id via  env (set by chump claim) OR falls back to hostname-pid
    - Emit kind=opus_message_sent {to, from, ref} to ambient; register in EVENT_REGISTRY.yaml
    - "Smoke test scripts/ci/test-opus-message.sh: send a message gap-addressed, assert recipient's inbox has the JSON line, list --unread shows it, mark-read flips read_at, second list --unread shows it gone"
    - No CHUMP_OPUS_MESSAGE_SKIP bypass needed (this IS the lightweight escape hatch); operators can ignore inbox entries entirely if they choose
    - "Bridge gap: paved-cow path until INFRA-1759 (full RPC Layer 2b) lands; this gives addressed async TODAY for ~50 LOC instead of waiting on the full A2A stack"
  notes: |
    [2026-05-23T10:55:54Z] stuck cycle 1 → re-attempt 2026-05-23 (PR #2392 auto-closed) respawn-handled:2392
    [2026-05-23T10:55:54Z] stuck cycle 2 → re-attempt 2026-05-23 (PR #2391 auto-closed) respawn-handled:2391
  closed_date: '2026-05-24'
  closed_pr: 2402

