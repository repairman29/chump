- id: INFRA-1497
  domain: INFRA
  title: "RESILIENT: INFRA-1485 follow-up — async-ify 3 gh callsites in pr-explain handlers (web_server.rs:4177/4240/4312)"
  status: open
  priority: P2
  effort: s
  pillar: RESILIENT
  depends_on: ["INFRA-1485"]
  acceptance_criteria:
    - "All 3 gh callsites in pr-explain handlers (src/web_server.rs lines 4177, 4240, 4312) converted from std::process::Command to tokio::process::Command with .output().await"
    - "Containing handler fns are async; no synchronous std::process::Command remains on these lines"
    - "Smoke test scripts/ci/test-pr-explain-concurrent.sh: hit pr-explain endpoint 5× concurrently with gh artificially throttled, assert /api/health remains responsive (<500ms p95)"
    - "Use chump_gh wrapper if present (per CLAUDE.md cache-first reads + criticality tags); pr-explain calls are background-tier"
    - "No new clippy warnings; existing pr-explain tests still pass"
    - "Cross-link: INFRA-1485 #2211 — these are the named residual gh callsites that audit found"

