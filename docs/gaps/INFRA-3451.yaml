- id: INFRA-3451
  domain: INFRA
  title: "EFFECTIVE: comprehend_repo must be in the INTERACTIVE profiles too (WEB_SLIM + LIGHT_CHAT) — live ACP test proved the interactive agent can't reach it, only WORKER has it"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"comprehend_repo must be in the INTERACTIVE profiles too (WEB_SLIM + LIGHT_CHAT) — live ACP test proved the interactive agent can't reach it, only WORKER has it\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3350
  outcome_id: MISSION-010

