- id: INFRA-3460
  domain: INFRA
  title: "RESILIENT: untrack .chump/github_cache.db — the tracked regenerable cache churns dirty and blocks EVERY ship's autofetch guard (ship_autofetch_blocked_dirty)"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"untrack .chump/github_cache.db — the tracked regenerable cache churns dirty and blocks EVERY ship's autofetch guard (ship_autofetch_blocked_dirty)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-28'
  closed_pr: 3358
  outcome_id: MISSION-010

