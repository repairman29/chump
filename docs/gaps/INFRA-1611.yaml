- acceptance_criteria:
  - 'gap reserve command writes opened_date: YYYY-MM-DD to the YAML file at reservation
    time'
  - 'For existing gaps without opened_date: a backfill script uses git log to find
    the commit that added the gap file and sets opened_date from that commit date'
  - After backfill, chump gap audit-priorities shows non-zero ages for gaps older
    than today
  - 'CI scripts/ci/test-gap-opened-date-coverage.sh asserts: no open P0 or P1 gap
    has missing or placeholder opened_date'
  description: 'We are failing at gap metadata quality. 486 of 540 gaps (90%) have
    no opened_date field. The P0 aging census in CLAUDE.md Mission Driver and chump
    gap audit-priorities relies on opened_date to compute "age" — but audit-priorities
    shows every P0 as ''0d old'' because state.db was freshly imported and has no
    age data.


    Evidence:

    - python scan of docs/gaps/*.yaml: 486/540 gaps have no opened_date

    - chump gap audit-priorities output: all 20 P0 gaps show ''(0d old)'' — computed
    from import time, not from creation date

    - CLAUDE.md Mission Driver §4: ''P0 budget = 5 max'' — aging is the enforcement
    mechanism; blind aging = no enforcement

    - gap-doctor.py doctor crashes with ''no such table: gaps'' on fresh clone (INFRA-821);
    even after import, age is 0 for all gaps


    RED_LETTER#12 (2026-05-18).


    Falsifying condition: state.db stores a created_at column set at original reservation
    time (not import time) and audit-priorities uses that instead of YAML opened_date.'
  domain: INFRA
  effort: s
  id: INFRA-1611
  opened_date: '2026-05-18'
  priority: P1
  raised_by: cold-water
  raised_in: RED_LETTER#12
  status: open
  title: 'ZERO-WASTE: 486/540 gaps missing opened_date — P0 aging census blind; audit-priorities
    age column useless'
