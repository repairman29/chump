- id: META-118
  domain: META
  title: 'wedge auto-detection + auto-dispatch pipeline (wizard Step 7) — close the ''manual loop'' that ran today: detect
    novel wedge → classify → file gap → dispatch fix → cascade-unblock → circuit-break on admin-merge spam'
  status: done
  priority: P0
  effort: m
  description: 'TODAY''S MANUAL LOOP (the work the operator was asking about):\n  1. CI fails       → DETECTION ✓ (automated,
    gh API)\n  2. Read logs      → DIAGNOSIS ✗ (manual, ~30s/wedge)\n  3. File gap       → TRIAGE ✗ (manual)\n  4. Dispatch
    fix   → REMEDIATION ✗ (manual)\n  5. Admin-merge through other wedges → UNBLOCK ✗ (manual, plus accumulating risk)\n\nSteps
    2-5 ran 10 times today (once per wedge). That''s why ''running on auto'' still requires me. This umbrella closes the manual
    loop.\n\n5 SUB-GAPS (decompose at claim time):\n\nSub-gap 1: novel-wedge classifier daemon\n  - Scans ambient.jsonl +
    gh api pr-checks for CI failure patterns\n  - Classifies by (failing_test_name, first_error_line_signature)\n  - When
    >=N occurrences of same signature in window W → emit kind=wedge_class_detected\n  - Distinct from W-001..W-013 catalog
    (those are KNOWN; this finds NOVEL)\n\nSub-gap 2: wizard-daemon Step 7 (auto-wedge-file)\n  - Consume kind=wedge_class_detected\n  -
    Auto-create a gap: title=''detected wedge X: <signature>'', AC stub=''4 fixture cases reproduce + fix'', priority P1 size
    xs\n  - Tag with wedge_auto_filed:true so it can be distinguished from operator-filed\n\nSub-gap 3: wedge-fixer auto-dispatch\n  -
    For well-characterized templates (fmt-drift → cargo fmt --all; orphan-event → register in YAML; printf|grep → replace
    with case)\n  - Auto-dispatch Sonnet with templated prompt\n  - Tag PR with wedge_auto_fix:true\n  - Rate-limited: max
    N auto-fix dispatches per hour\n\nSub-gap 4: cascade-unblock detector\n  - When PR with wedge_auto_fix:true MERGES, identify
    all open PRs whose CI failed on the same signature\n  - Trigger gh pr update-branch on each (cascading rebase already
    exists at wizard Step 5)\n  - Emit kind=cascade_unblocked with count of unblocked PRs\n\nSub-gap 5: admin-merge frequency
    circuit-breaker\n  - When admin-merge-cycle runs >N times in window W → emit operator_action_needed kind=admin_merge_burst,
    pause further bypass\n  - Forces ''stop and triage'' instead of accumulating debt invisibly (today''s anti-pattern)\n  -
    Operator can override via --force-admin (already exists per RESILIENT-031)

    '
  acceptance_criteria:
  - 'Sub-gap 1 shipped: scripts/coord/novel-wedge-classifier.sh + launchd plist + EVENT_REGISTRY entry for kind=wedge_class_detected'
  - 'Sub-gap 2 shipped: wizard-daemon Step 7 consumes wedge_class_detected and creates gaps'
  - 'Sub-gap 3 shipped: scripts/coord/wedge-fixer-dispatch.sh with fixer-templates.yaml + smoke test for 3 templates (fmt/orphan-event/printf-grep)'
  - 'Sub-gap 4 shipped: wizard-daemon Step 5 (cascade rebase) reads wedge_auto_fix tag and unblocks matched PRs'
  - 'Sub-gap 5 shipped: scripts/ops/admin-merge-cycle.sh circuit-breaker with --burst-threshold default 3/hour'
  - 'End-to-end demo: synth a new wedge (e.g. fmt drift in a known file), run a no-op canary PR, verify the auto-loop detects-files-fixes-merges
    WITHOUT operator intervention'
  - 'Retirement criterion: 24h period where >=2 novel wedges auto-detected + auto-fixed + auto-cascaded without operator intervention'
  depends_on:
  - INFRA-2067
  - INFRA-2068
  - INFRA-2069
  - INFRA-2070
  - INFRA-2071
  notes: '[2026-05-27T19:35:13Z] Decomposed into 5 sub-gaps INFRA-2067..2071 on 2026-05-27 by curator-opus-shepherd. Sequencing:
    INFRA-2067 (classifier) → INFRA-2068 (auto-file) → INFRA-2069 (auto-fix dispatch) → INFRA-2070 (cascade unblock); INFRA-2071
    (admin-merge circuit breaker) is independent and can run in parallel. End-to-end demo (META-118 AC #6) requires all 5
    sub-gaps shipped.

    '
  closed_date: '2026-05-29'
  closed_pr: 2665
