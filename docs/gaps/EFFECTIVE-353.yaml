- id: EFFECTIVE-353
  domain: EFFECTIVE
  title: "EFFECTIVE: --execute-gap on an external gap runs the scout + hands the agent a placeholder, not the real filed job"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"--execute-gap on an external gap runs the scout + hands the agent a placeholder, not the real filed job\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3473

