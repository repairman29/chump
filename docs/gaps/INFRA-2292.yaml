- id: INFRA-2292
  domain: INFRA
  title: "RESILIENT: replace webkit2gtk-4.1 with libwebkit2gtk-4.1-0 in all CI apt-get blocks — package dropped from Ubuntu 24.04 noble"
  status: done
  closed_pr: 3717
  priority: P1
  effort: m
  description: |
    webkit2gtk-4.1 is a transitional package that existed in Ubuntu 22.04 jammy but was dropped in Ubuntu 24.04 noble. ubuntu-latest is now 24.04. The package is referenced in 7 separate apt-get install blocks in ci.yml (fast-checks, clippy, cargo-test, coverage, e2e-pwa, audit, tauri-cowork-e2e jobs). When runner pools fully migrate to noble these blocks will fail with 'E: Unable to locate package webkit2gtk-4.1'. Fix: replace webkit2gtk-4.1 with libwebkit2gtk-4.1-0 in each block (or remove the transitional line if libwebkit2gtk-4.1-dev already depends on libwebkit2gtk-4.1-0). Evidence: packages.ubuntu.com/noble/webkit2gtk-4.1 returns no such package; libwebkit2gtk-4.1-0 and libwebkit2gtk-4.1-dev both exist in noble. Audit source: docs/audits/CI_DEPENDENCY_AUDIT_2026-05-30.md (META-177 lane B, INFRA-2290).

    [2026-08-19] SYNC: already shipped in PR #3717 (commit ae5bda38,
    2026-08-13), which removed the bare `webkit2gtk-4.1 \` transitional
    line from all 5 apt-get install blocks in ci.yml — libwebkit2gtk-4.1-dev
    already pulls in libwebkit2gtk-4.1-0 transitively, verified there via
    `docker run ubuntu:24.04 apt-cache show libwebkit2gtk-4.1-0`. Reverified
    on this checkout: `grep -n webkit2gtk .github/workflows/ci.yml` shows
    only `libwebkit2gtk-4.1-dev` (5 occurrences) and `webkit2gtk-driver`
    (1 occurrence, tauri-cowork-e2e) — no bare `webkit2gtk-4.1` transitional
    package reference remains anywhere in the file. state.db re-imported
    this gap as status:open from stale docs/gaps/INFRA-2292.yaml — same
    split-brain class as INFRA-1811/INFRA-1865. Flipping status to done
    against PR #3717 (the PR that did the actual work) so the picker stops
    re-surfacing a gap that shipped 6 days ago.
  acceptance_criteria:
    - All apt-get install blocks in .github/workflows/ci.yml that reference webkit2gtk-4.1 replace it with libwebkit2gtk-4.1-0 (or remove the transitional name if libwebkit2gtk-4.1-dev already pulls it in transitively)
    - "CI fast-checks, clippy, cargo-test, coverage, and audit jobs all pass apt-get install without E: Unable to locate package errors on ubuntu-latest (24.04 noble)"
    - "A local verification step runs apt-cache show libwebkit2gtk-4.1-0 on ubuntu:24.04 docker image or documents evidence from GitHub runner docs that the package name resolves"
  outcome_id: MISSION-010

