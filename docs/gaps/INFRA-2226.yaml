- id: INFRA-2226
  domain: INFRA
  title: "RESILIENT P2: ci-runner-saturation throttle — when N PRs in-flight without merges > 20 min, chump fleet dispatch refuses new code-touching Sonnets (docs-only allowed); auto-resume when avg merge gap < 10 min"
  status: open
  priority: P2
  effort: s
  acceptance_criteria:
    - "TODO: what events emitted on success/failure/timeout"
    - "TODO: how cost tracked and reported to operator"
    - "TODO: failure-class taxonomy (distinguish transient vs permanent)"
    - "TODO: smoke test command to verify observability"
  outcome_id: MISSION-010

