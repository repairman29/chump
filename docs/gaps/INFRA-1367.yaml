- id: INFRA-1367
  domain: INFRA
  title: "RESILIENT: chump-commit.sh Rust-First-Bypass trailer silently lost when git commit -m is used"
  status: done
  priority: P2
  effort: xs
  acceptance_criteria:
    - "[\"Root cause: scripts/git-hooks/pre-commit-rust-first.sh reads Rust-First-Bypass: trailer from .git/COMMIT_EDITMSG, but `git commit -m foo` does NOT populate that file until AFTER pre-commit runs — so bypass trailers in inline messages are silently ignored\",\"Surfaced 2026-05-15 by ZERO-WASTE-001 agent which had to manually write the message to a file and `git commit -F` to make the trailer visible\",\"Fix in scripts/coord/chump-commit.sh: write the -m message to .git/COMMIT_EDITMSG (or pass through `git commit --message=` AFTER stamping the editmsg file) before invoking git. Alternatively, change pre-commit-rust-first.sh to read from $GIT_INDEX_FILE-derived merge-msg path\",\"Smoke test scripts/ci/test-rust-first-gate.sh adds case: invoke chump-commit.sh with `-m \\\"... Rust-First-Bypass: reason\\\"` against a triggered Rust-first violation; assert gate passes (bypass detected)\",\"Out of scope: refactoring pre-commit-rust-first.sh to read from -m directly via git stash/temp file (more invasive)\"]"
  closed_date: '2026-05-15'
  closed_pr: 2118
  outcome_id: MISSION-010

