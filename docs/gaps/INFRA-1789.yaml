- id: INFRA-1789
  domain: INFRA
  title: 'ZERO-WASTE: chump preflight runs subcommand --help regression check locally — INFRA-1246 catches stale CLI surfaces
    (INFRA-1762 Tier C #3)'
  status: done
  priority: P2
  effort: s
  acceptance_criteria: 1. src/preflight.rs gains a chump-subcommand-help regression gate, runs only when staged diff touches
    src/main.rs OR any src/*_cmd.rs file 2. Gate invokes the same iteration scripts/ci/test-chump-subcommand-help.sh uses
    — for every chump subcommand, assert --help exits 0 + prints non-empty Usage line 3. Exit non-zero with INFRA-1246 error
    format (subcmd=X exit=N stdout=Y) 4. Bypass via CHUMP_PREFLIGHT_SKIP_SUBCMDHELP=1 + emit preflight_subcmdhelp_bypassed
    5. Smoke test scripts/ci/test-preflight-subcmdhelp-gate.sh — stub a broken subcmd, assert gate catches it 6. This protects
    against the class of failures where a subcommand registers but its handler falls through to the LLM (real bug, INFRA-1238)
  closed_date: '2026-05-23'
  closed_pr: 2397
