- id: INFRA-724
  domain: INFRA
  title: "ZERO-WASTE: split main.rs phase 3 — extract fleet/ module group (fleet, fleet_capability, fleet_db, fleet_health, fleet_status, fleet_tool, fleet_velocity, cluster_mesh)"
  status: open
  priority: P2
  effort: m
  acceptance_criteria: |
    Criterion 1: All fleet-* modules (fleet, fleet_capability, fleet_db, fleet_health, fleet_status, fleet_tool, fleet_velocity, cluster_mesh) are extracted from main.rs into a `fleet/` module directory
    Criterion 2: main.rs imports from the `fleet` module rather than containing the implementations inline
    Criterion 3: Cargo build and `cargo test` pass with no functional changes
    Criterion 4: No public API surface change — callers of fleet functions continue to work unchanged

