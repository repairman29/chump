- id: MISSION-075
  domain: MISSION
  title: Blend the self-improving job-loop architecture + version ladder (V1->V1.5->V2->V2.5->V3/COTG) + branch-proving bench tracks into CHUMPOS_V1_ROADMAP.md (DOC-074); ground the real/partial/missing loop map + artifact
  status: done
  priority: P2
  effort: s
  acceptance_criteria:
    - "The change described by \"Blend the self-improving job-loop architecture + version ladder (V1->V1.5->V2->V2.5->V3/COTG) + branch-proving bench tracks into CHUMPOS_V1_ROADMAP.md (DOC-074); ground the real/partial/missing loop map + artifact\" is implemented in the relevant MISSION code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-02'
  closed_pr: 3448

