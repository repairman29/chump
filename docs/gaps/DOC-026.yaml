- id: DOC-026
  domain: DOC
  title: "EFFECTIVE: document 189 undocumented env vars — code reads 299 vars but .env.example only covers 180, separate internal runtime state from user config"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - .env.example documents all env vars chump reads (gap from 110 documented → 299 referenced in code)
    - "new scripts/ci/test-env-var-coverage.sh greps src/ for std::env::var calls and asserts each name is mentioned in .env.example or explicitly listed in scripts/ci/env-vars-internal.txt (allowlist for purely-internal vars)"
    - test added to fast-checks
  notes: |
    DESIGN NOTE (Opus): 189 undocumented vars is too many to doc by hand. Two-step: (1) script scripts/ci/extract-env-vars.sh greps src/ for std::env::var calls + emits a structured list (var name + file + comment-above context if present). (2) operator/Opus categorizes the list into TIERS — Tier 1 (operator-tunable, must doc), Tier 2 (debug-only, mark internal), Tier 3 (test-only, hide). Only Tier 1 (probably ~50 vars) goes in .env.example. Save the script output to docs/operations/ENV_VARS_RAW.md as an audit trail.
  closed_date: '2026-05-08'
  closed_pr: 1330

