- id: INFRA-1982
  domain: INFRA
  title: "MEDIUM: chump gap reserve title-similarity gate friction without preventing duplicate-PR class (critique M4)"
  status: open
  priority: P3
  effort: m
  acceptance_criteria:
    - "Failure: chump gap reserve enforces Jaccard >= 0.85 title similarity block (INFRA-1149). I bypassed CHUMP_GAP_RESERVE_NO_SIMILARITY=1 three times today. The gate doesn't actually prevent the real failure (duplicate PRs from different gap IDs)."

