- id: INFRA-1510
  domain: INFRA
  title: "MISSION: Payment infrastructure spike — Stripe vs Lemon Squeezy vs Paddle; first-pass evaluation"
  status: open
  priority: P2
  effort: s
  pillar: MISSION
  acceptance_criteria:
    - "Spike doc `docs/strategy/PAYMENT_PROVIDERS_2026.md` produced, comparing Stripe vs Lemon Squeezy vs Paddle across: (a) MoR status (Merchant of Record — who handles sales tax / VAT), (b) per-transaction fee at <$100/mo MRR, (c) developer/CLI experience, (d) self-hosted-friendliness (do they require server-side keys you can't run locally?), (e) chargeback risk model"
    - "Decision matrix at the end of the doc: which provider for which product (one-time license / monthly subscription / fleet metered billing) — the spike isn't a recommendation for ALL pricing, just a clear matrix"
    - "Each candidate has a 2-paragraph evaluation, no hand-wave: cite actual fee pages, link to their MoR/tax positioning page, note any deal-breaker (e.g., 'Stripe is not MoR — we collect tax ourselves in 30+ jurisdictions which is a non-starter for a one-person ship without an accountant')"
    - "Operator-facing summary section: '3 things to decide before picking' — e.g., do we want MoR? are we OK with US-only initially? what's our refund policy"
    - "NO integration code in this spike. This is research only. Implementation is a follow-up gap once a provider is chosen."
    - "Operator-action items flagged to chump-proprietary/OPERATOR_ACTIONS.md per existing reference (signing up, providing legal info) — NOT tracked in this gap registry"
    - "Done = doc committed + Jeff has reviewed; no provider chosen in this gap"
    - "Cross-link: prerequisite to any monetization. Connects to MEMORY: exec_summary_hardware_economics (cost model) and project_offline_local_llm_mission (revenue must support free-offline-tier)"


