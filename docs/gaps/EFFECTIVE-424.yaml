- id: EFFECTIVE-424
  domain: EFFECTIVE
  title: "EFFECTIVE: chump integrator on|off|status wrapper — batching toggle must never be an env-var fumble (COTG new-user safety)"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"chump integrator on|off|status wrapper — batching toggle must never be an env-var fumble (COTG new-user safety)\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

