- id: INFRA-1536
  domain: INFRA
  title: "RESILIENT: install-self-hosted-runner.sh multi-instance plist labels — current hardcoded com.chump.actions-runner collides on RUNNER_NAME=foo-2; need per-runner suffix in plist Label"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "scripts/setup/install-self-hosted-runner.sh: plist Label and StandardOut/StandardErr paths gain RUNNER_NAME suffix so multiple runners coexist; current hardcoded com.chump.actions-runner collides causing 'Bootstrap failed: 5: Input/output error' on the 2nd install"
    - Add CHUMP_RUNNER_SLOT env var (default 1) — if >1, plist Label=com.chump.actions-runner-<slot>, log files actions-runner-<slot>.log
    - "Idempotent: re-running install with same slot is no-op; new slot creates new plist"
    - "Smoke test scripts/ci/test-install-runner-multi-slot.sh: run install twice with different slots, assert 2 plists + 2 runners both online via gh api"
    - "Today's signal: spun up 2nd M4 runner inline by manually writing plist with com.chump.actions-runner-2 label after the installer's bootstrap failed"
    - Pairs with INFRA-1535 (autoscale) — autoscale will need this fix to provision 2+ runners on the same host
  notes: |
    Demoted P1→P2 2026-07-19 re-rank: open >60 days with no ship activity; P1 now reserved for current-cycle critical path (docs/ROADMAP.md). Re-promote freely if picked up.
  outcome_id: MISSION-010

