- id: INFRA-3339
  domain: INFRA
  title: "Migrate to cargo major-version bumps rejected in dependabot PR #3188 (broad clippy/test breakage)"
  status: open
  priority: P2
  effort: m
  description: |
    Dependabot PR #3188 (aes-gcm 0.10->0.11, jsonschema 0.46->0.47) failed 13 checks (clippy, cargo-test, fast-checks) — real API migrations, not flakes. Closed the PR to keep the queue clean; dependabot will re-file on the next major release. This gap tracks doing the migration deliberately: aes-gcm 0.11 changes the AEAD trait surface; jsonschema 0.47 changes validator construction.
  acceptance_criteria:
    - aes-gcm updated to 0.11.x with all call sites migrated and tests green
    - jsonschema updated to 0.47.x with validator call sites migrated
    - cargo clippy --workspace -D warnings green
    - no dependabot cargo-major PR open for these two crates
  notes: |
    PR #3188 closed 2026-07-18; YAML carried to main via PR (queue-cleanup batch).

