- id: EFFECTIVE-358
  domain: EFFECTIVE
  title: "EFFECTIVE: design pass — UI/brand/interaction review for shipped user-facing tools (factory L3)"
  status: open
  priority: P1
  effort: m
  description: |
    Factory-model L3 (design) — the only design capability today is the CSS token lint gate (INFRA-1590), a gate not a designer (docs/strategy/SOFTWARE_FACTORY_MATRIX_2026-08-05.md, DOC-079). Exploratory, deliberately last of the three missing-chair gaps: only matters once external user-facing tools flow through intake (EFFECTIVE-357). Rough shape: (a) a design-pass stage for user-facing tools the fleet ships — layout/spacing/typography/interaction review against a small written heuristic checklist; (b) per-product brand tokens compatible with CSS token discipline; (c) screenshot-based before/after evidence attached to the PR. P3 until EFFECTIVE-356/357 land.
  acceptance_criteria:
    - After intake produces requirements, a design pass emits a UI/brand/interaction spec so the shipped artifact is USABLE not just functional
    - "Proven: for a product gap the design chair emits an interaction+visual spec the implement stage consumes; the shipped artifact has coherent UI, not a raw CLI dump"
  outcome_id: COTG

