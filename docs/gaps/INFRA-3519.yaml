- id: INFRA-3519
  domain: INFRA
  title: "RESILIENT: remediation checkout must force (-f) over the dirty scratch clone — 'local changes would be overwritten'"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"remediation checkout must force (-f) over the dirty scratch clone — 'local changes would be overwritten'\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3404

