- id: INFRA-3447
  domain: INFRA
  title: "CREDIBLE: verify gate must RUN the gap's acceptance check, not just ask a model — deterministic verdict beats hallucination"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"verify gate must RUN the gap's acceptance check, not just ask a model — deterministic verdict beats hallucination\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3347
  outcome_id: MISSION-010

