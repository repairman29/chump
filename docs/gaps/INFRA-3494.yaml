- id: INFRA-3494
  domain: INFRA
  title: "CREDIBLE: [COTG-3.1] outcome-level verification (does the shipped thing SOLVE the problem?)"
  status: done
  closed_pr: 3593
  closed_date: "2026-08-10"
  priority: P1
  effort: m
  acceptance_criteria:
    - Outcome-level verification checks the shipped artifact against the user's original stated problem (not just CI green) and emits a PASS/FAIL solve-verdict with evidence.
  notes: |
    [2026-07-29T03:21:23Z] DISCOVERY PARTIAL ~45% -> FINISH (gate 5.1/5.3/6.2 depend on). src/external_verify_merge.rs is stronger than CI-green (proves fix fails-on-base/passes-on-head, no-regression). Add a post-merge LIVE outcome probe + promote src/browser.rs (static fetch) to real CDP.
    [2026-08-20T00:00:00Z] RECONCILE (2nd time): already shipped 2026-08-10 via cf55e83c (PR #3593, same gap ID in the commit trailer) — `run_outcome_probe`/`outcome_probe_matches` in crates/chump-verify/src/external_verify_merge.rs fetches a caller-supplied live URL post-`--apply`-merge and asserts an expected substring, emitting kind=outcome_probe_verified/outcome_probe_failed with evidence (url + note). Covered by unit tests at crates/chump-verify/src/external_verify_merge.rs:2893-2981. state.db has no open row for this ID (`chump gap preflight INFRA-3494` -> "not found in state.db"). The prior reconcile (859f2efe, PR #3978) set status:done but was accidentally clobbered back to status:open by a later bulk gap-queue commit (c941d77b, PR #3987) that didn't carry this file's edit forward — reapplying. The "promote browser.rs static fetch to real CDP" line above remains aspirational future scope beyond the single stated AC, not a blocker for this gap.
  outcome_id: COTG

