- id: INFRA-1415
  domain: INFRA
  title: "EFFECTIVE: chump claim --check-only — dry-run that warns on hot-file collision + stale state.db status before creating worktree (today: claimed INFRA-1368/1363 only to find them done)"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - chump claim accepts --check-only flag that runs all preflight gates without creating worktree or lease
    - "Gates: state.db status check (refuse if done/in-review), hot-file collision per INFRA-1394, AC TODO/placeholder check, base-branch up-to-date sanity, disk space >5GB"
    - "Output: structured table of pass/warn/fail per gate; --json variant for tooling"
    - Used by INFRA-1409 chump pr triage and by manual operator inspection
    - CI test scripts/ci/test-claim-check-only.sh fabricates gap-states for each fail scenario + asserts the right gate fires

