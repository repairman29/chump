- id: RESILIENT-025
  domain: RESILIENT
  title: 'pre-commit hook: commit subject must mention claimed gap ID (anti-off-rails defense, prevents wrong-gap commits
    like a44 2026-05-27)'
  status: done
  priority: P0
  effort: m
  description: 'Today 2026-05-27T14:42Z a Sonnet sub-agent (a44) dispatched on INFRA-2051 went off-rails: committed work titled
    ''fix(INFRA-2048+INFRA-2049)'' in the chump-infra-2051 worktree while holding the INFRA-2051 claim. Production-quality
    intelligent serializing (pre-applying prereq changes from another in-flight agent) but commit metadata didn''t match the
    claim, creating confused state + risk of duplicate PR. A 10-LOC pre-commit hook gate would have caught this instantly.
    Includes intentional escape via Off-Rails-Bypass trailer for legitimate prereq-integration commits.

    '
  acceptance_criteria:
  - scripts/git-hooks/pre-commit gains a claim-gap-mention check that BLOCKS commit when claim-lock exists in worktree and
    commit subject does not mention the claimed gap ID (case-insensitive substring match)
  - 'Bypass trailer ''Off-Rails-Bypass: reason'' overrides the check (audited via grep, emits kind=off_rails_bypassed to ambient.jsonl)'
  - 'scripts/ci/test-pre-commit-claim-gap-match.sh covers 4 cases: matching gap passes, non-matching fails, no claim file
    passes (operator manual edit), bypass trailer overrides'
  - Pre-commit overhead under 50ms on a typical commit (no perceptible delay)
  - Documented in CLAUDE.md under existing 'Hard rules' section plus AGENTS.md mirror
  closed_date: '2026-05-27'
  closed_pr: 2638
