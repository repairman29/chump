- id: MISSION-055
  domain: MISSION
  title: "MISSION: chump ingest <repo> — import a repo's DEFINED backlog (.beast-mode-tasks.json/beads/TODO) as fleet gaps"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"chump ingest <repo> — import a repo's DEFINED backlog (.beast-mode-tasks.json/beads/TODO) as fleet gaps\" is implemented in the relevant MISSION code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

