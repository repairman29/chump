- id: INFRA-2205
  domain: INFRA
  title: 'RESILIENT: chump-commit.sh _staged_paths unbound variable warning + chump-fleet-server ignores --help and binds
    7070'
  status: done
  priority: P3
  effort: xs
  description: 'Two small bash bugs surfaced repeatedly this session: (1) scripts/coord/chump-commit.sh at lines 671 and 710
    references _staged_paths under set -u when the array is empty, prints ''unbound variable'' twice per commit — same INFRA-1316
    class issue; (2) crates/chump-fleet-server binary ignores --help and other CLI args, starts the listener and binds port
    7070 unconditionally — caused a phantom ''address already in use'' surprise this session when a forgotten ''--help'' invocation
    held the port.

    '
  acceptance_criteria:
  - scripts/coord/chump-commit.sh assigns _staged_paths unconditionally before the INFRA-1833/INFRA-1853 blocks so it is always
    bound under set -u; no more 'unbound variable' warning during commits
  - 'crates/chump-fleet-server adds CLI arg parsing: --help prints usage and exits 0 without binding a port; --version prints
    crate version and exits 0; --port N is wired as alternative to CHUMP_FLEET_SERVER_PORT env var'
  - bash -n scripts/coord/chump-commit.sh exits clean
  - cargo clippy -p chump-fleet-server --all-targets -- -D warnings exits clean
  - cargo check --workspace exits clean
  closed_pr: 2763
