- id: INFRA-632
  domain: INFRA
  title: 'RESILIENT: bot-merge.sh portability for non-Chump repos — --branch-prefix, --pr-template, --required-checks'
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "done — --branch-prefix (default 'chump'), --pr-template (path), --required-checks (comma list) all implemented"
    - "done — BM_BRANCH_PREFIX / BM_PR_TEMPLATE / BM_REQUIRED_CHECKS env var aliases"
    - "done — scripts/ci/test-bot-merge-portability.sh: 14/14 tests covering prefix strip, flag parse, template substitution, check filtering, backward compat"
    - "done — existing Chump invocation (chump/ prefix, Chump CI gates) unchanged"
  closed_date: '2026-05-09'
  closed_pr: 1432
