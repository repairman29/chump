- id: DOC-035
  domain: DOC
  title: README rewrite anchored on the demo flow — Week 4 demo gate
  status: open
  priority: P2
  effort: s
  description: |
    Per ROADMAP.md Week 4 section: the README needs a rewrite anchored on the 
    5-minute June 6 demo flow:
    
    1. `brew tap repairman29/chump && brew install chump` — installs cleanly
    2. `chump init` — wizard, pinned deps, ~/.chump/config.toml
    3. `chump gen "add a /health endpoint to my axum server"` — single-shot coding task
    4. `chump orchestrate` — conversational loop driving the fleet
    5. `chump fleet-status` — real-time visibility
    
    The current README predates the Orchestrator MVP (Week 3) and doesn't 
    reflect the full demo flow. Should be a clear walkthrough that a solo dev 
    with a 24GB Mac and Ollama can follow.
  acceptance_criteria:
    - ""
    - " README walks through the 5-step demo flow end-to-end "
    - " yes "
    - "\n"
    - " README mentions `chump orchestrate` conversational loop "
    - " yes "
    - "\n"
    - " README references the offline/local LLM mission (Ollama, no paid API key required) "
    - " yes "
    - "\n"
    - " README includes a troubleshooting section for common install/init issues "
    - " yes "
    - ""

