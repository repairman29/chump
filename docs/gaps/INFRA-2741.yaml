- id: INFRA-2741
  domain: INFRA
  title: 'INFRA: mirror test-mission-picker.sh into preflight.rs — remove from parity exceptions'
  status: done
  priority: P2
  effort: xs
  acceptance_criteria:
  - src/preflight.rs contains the string literal "scripts/ci/test-mission-picker.sh" as an always-on Scripts-scope gate (GateKind::Scripts),
    following the plist-no-tmp-paths pattern — NO new CHUMP_*_SKIP bypass env var (would trip the EFFECTIVE-094 zero-bypass
    debt-ceiling gate)
  - test-mission-picker.sh is removed from scripts/ci/preflight-ci-parity-exceptions.txt and replaced with an INFRA-2741 breadcrumb
    comment, mirroring the INFRA-2350 removal style
  - bash scripts/ci/test-preflight-ci-parity.sh exits 0 with the gate counted under Mirrored (not Allowlisted)
  - chump preflight --scope scripts runs a mission-picker gate that passes (pure-Python, no network, <2s)
  closed_pr: 3033
