- id: EFFECTIVE-412
  domain: EFFECTIVE
  title: "WHEN_TO_CRATE: add the crate-first half (new subsystems born as crates + bin-bloat-guard CI gate)"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "The change described by \"add the crate-first half (new subsystems born as crates + bin-bloat-guard CI gate)\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-08'
  closed_pr: 3520
  outcome_id: CHUMPOS

