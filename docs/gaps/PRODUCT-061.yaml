- id: PRODUCT-061
  domain: PRODUCT
  title: "EFFECTIVE: MCP server discovery — chump mcp list shows available servers from local registry (PRODUCT-033 slice 1)"
  status: open
  priority: P1
  effort: xs
  depends_on: []
  acceptance_criteria:
    - "chump mcp list reads registry/mcp-servers.toml (new file) and prints name + description + transport"
    - "Registry ships with 5+ bundled server entries (filesystem, git, web-search, database, code-analysis)"
    - "chump mcp list --installed shows only servers configured in ~/.chump/config.toml"
    - "--json flag for machine-readable output"
