- id: INFRA-3489
  domain: INFRA
  title: "RESILIENT: [COTG-2.1] operator-intervention watchdog (log every human touch as a defect)"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"[COTG-2.1] operator-intervention watchdog (log every human touch as a defect)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  notes: |
    [2026-07-29T03:21:23Z] DISCOVERY NOT-STARTED -> BUILD. reuse emit_ambient_event() + gap-store to file self-healing gaps (src/operator_presence.rs is the INVERSE - detects absence).
  closed_date: '2026-07-30'
  closed_pr: 3384
  outcome_id: COTG

