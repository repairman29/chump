- id: INFRA-3539
  domain: INFRA
  title: THROWAWAY parity probe C — mutating-path test, delete me
  status: wontfix
  priority: P2
  effort: xs
  acceptance_criteria:
    - "The change described by \"THROWAWAY parity probe C — mutating-path test, delete me\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  notes: |
    THROWAWAY parity probe for INFRA-1965 slice-2 extraction (2026-08-07). Mutating-path parity confirmed. Disposed per archive-only.

