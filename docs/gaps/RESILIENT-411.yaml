- id: RESILIENT-411
  domain: RESILIENT
  title: recalibrate board-vitals (RESILIENT-371) PAGE thresholds to Jeff's real page bar — stop DMing on transient worker-silence lulls (a 49-min silence that self-resolved in ~1min paged him); page ONLY on genuinely-his, non-self-healing incidents, and residentize operator-recall's credential/credit pages
  status: in_progress
  priority: P1
  effort: s
  acceptance_criteria:
    - "ROOT CAUSE: board-vitals shipped with too-sensitive PAGE conditions. It DM'd Jeff on worker-silence >40min via TWO paths (worker_wedge + a novel-escalation-on-silence) — but worker lulls SELF-HEAL (INFRA-3832 cools-down/auto-blocks wedged/timed-out gaps), so a silence that recovers in ~1min is operational noise, not Jeff's problem. Observed live 2026-08-28T03:04Z: board_vitals_novel_escalation on worker_silent_min=49 -> real DM -> next beat incidents:0 (already recovered). Jeff's standing order: DMs = advisor conversation + ONLY pages he must act on, zero transient-lull pages."
    - "FIX — tighten the PAGE (Discord-DM) bar to genuinely-his, non-self-healing incidents; the [board-vitals] tick proof-of-life line still prints EVERY beat (journal observability, not a DM): (1) REMOVE the standalone worker_wedge page and the novel-escalation-on-worker-silence page — bare silence NEVER pages. (2) merge_stall (was merge_drought): page ONLY when the line is DEAD — 0 merges for the full drought window (default 3h) AND 0 PRs in flight (BLOCKED/green/pending = CI-lag, not a stall) AND worker not producing; 3h is far past any self-heal window, so this IS the 'unrecoverable wedge' case, and the LLM diagnosis now fires ONLY here. (3) disk >=90% (unchanged). (4) main-red sustained >=30m (unchanged)."
    - "ADD — residentize operator-recall.sh's (INFRA-626) halt-class credential/credit pages (its timer is inactive on CJ), same registered signals + thresholds: oauth_expired = >=3 oauth_token_refresh_failed in a 2h window AND no oauth_token_refreshed recovery since (routine refresh successes never page); cost_cap = any cost_cap_exceeded in 2h (sub cap exhausted / OpenRouter credit needed — the floor can't ship). These ARE Jeff's to fix, so they SHOULD page."
    - "TEST: scripts/ci/test-board-vitals.sh — new assertions: worker-silence-alone (stale worker signal, no stall/disk) emits NO page but still ticks incidents=0; oauth (>=3 fails, newest=failure) pages; oauth recovered (success after fails) does NOT page; cost_cap_exceeded pages. Suite 13 -> 18, all green. shellcheck -S error clean; event-registry coverage gate green."
    - "LIVE PROOF ON CJ (merged != running): after merge + organ-watchdog reset, healthy beats show `[board-vitals] tick incidents=0` in journalctl and send NO DM; a synthetic real incident (forced disk>=90 dry-run, or a 3h+ stall / oauth-fail fixture) still pages via notify-operator on the deployed path."
  outcome_id: CHUMPOS
  evidence: |
    COMMAND: grep board_vitals_novel_escalation .chump-locks/ambient.jsonl (CJ, 2026-08-28T03:04) | OUTPUT: {"kind":"board_vitals_novel_escalation","worker_silent_min":"49"} then board_vitals_page_sent worker_silent_unexplained, then next beat board_vitals_tick incidents:0 — a real DM on a lull that self-resolved in ~1min | THEORY: worker-silence is self-healing (INFRA-3832) so it must not page on its own; only a 3h-dead-line (no merge + nothing in flight + worker idle) or a credential/credit need is genuinely the operator's to act on | ALT: rejected 'just raise the silence threshold' — any bare-silence page races the self-heal and cries wolf; the correct bar folds unrecoverable-wedge into the 3h merge_stall AND adds the credential/credit pages that ARE his to fix (residentizing operator-recall's proven signals rather than inventing new ones)
  closed_pr:
