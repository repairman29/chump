- id: INFRA-1908
  domain: INFRA
  title: 'EFFECTIVE P1: chump curator wake-template CLI — generates paste-ready bootstrap text per curator role; one-time-per-window
    operator nudge that starts curators'' self-tending /loop'
  status: done
  priority: P1
  effort: xs
  acceptance_criteria:
  - scripts/coord/curator-wake.sh — prints paste-ready bootstrap text per curator role (target/handoff/ci-audit/shepherd/decompose/md-links)
  - Each template includes — (1) inbox-read incantation, (2) /loop 5m with standard orchestration prompt, (3) loop_started
    ACK message, (4) CHUMP_SESSION_ID export
  - --copy flag copies first curator's template to clipboard via pbcopy on macOS
  - --role <name> generates just one curator's template
  - --all prints all 6 separated by clear dividers (default)
  - Smoke test scripts/ci/test-curator-wake.sh asserts CLI prints 6 templates each containing the 4 required pieces
  - 'Operator workflow: bash scripts/coord/curator-wake.sh --role handoff --copy then paste into handoff Claude Code window'
  closed_date: '2026-05-24'
  closed_pr: 2485
