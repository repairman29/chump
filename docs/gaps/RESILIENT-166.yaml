- id: RESILIENT-166
  domain: RESILIENT
  title: "RESILIENT: trunk red — fleet_self_sync + stall_deferred_build emitted but unregistered, audit-shard(2) red fleet-wide"
  status: done
  priority: P0
  effort: xs
  acceptance_criteria:
    - "1. Register 'fleet_self_sync' (emitter scripts/ops/github-webhook-receiver.py, RESILIENT-152) and 'stall_deferred_build' (emitter scripts/dispatch/worker.sh, RESILIENT-157) in docs/observability/EVENT_REGISTRY.yaml events: list with kind/emitter/trigger/consumers/fields_required. 2. 'CHUMP_REGISTRY_GATE_MODE=strict bash scripts/ci/test-event-registry-coverage.sh' passes (emit-without-register: 0). 3. No other registry drift."
  closed_pr: 3172
  evidence: |
    2026-06-22: audit-shard(2) [scripts/ci/test-event-registry-coverage.sh] fails on every fresh PR (#3171 et al.) with 'emit-without-register: 2' — fleet_self_sync (webhook-receiver.py:444, since RESILIENT-152 #3147) + stall_deferred_build (worker.sh:1286, since RESILIENT-157 #3150) emitted but never registered. audit-required red => fleet-wide merge stall (0 merges/64min). Deterministic, not flake; confirmed by running the gate locally.
  evidence: |
    2026-06-22: audit-shard(2) [scripts/ci/test-event-registry-coverage.sh] fails on every fresh PR (#3171 et al.) with 'emit-without-register: 2' — fleet_self_sync (webhook-receiver.py:444, since RESILIENT-152 #3147) + stall_deferred_build (worker.sh:1286, since RESILIENT-157 #3150) emitted but never registered. audit-required red => fleet-wide merge stall (0 merges/64min). Deterministic, not flake; confirmed by running the gate locally.

