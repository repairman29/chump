- id: INFRA-1448
  domain: INFRA
  title: 'MISSION: Rust-first gate emit on BLOCK (not just BYPASS) — restore audit trail'
  status: done
  priority: P2
  effort: xs
  acceptance_criteria:
  - scripts/git-hooks/pre-commit-rust-first.sh emits kind=rust_first_blocked to ambient.jsonl when it BLOCKS a commit (today
    only the BYPASS-USED path emits)
  - Block emit includes files list and inferred reasons (hot-path / state-mutator / >200 LOC)
  - kind=rust_first_blocked added to docs/observability/EVENT_REGISTRY.yaml + scripts/ci/event-registry-reserved.txt (hook-emitted,
    not grep-scannable)
  - 'Audit reveals: 161k-line ambient.jsonl has zero rust_first_* events → after fix we should see emits during the next normal
    session of fleet commits'
  closed_pr: 2188
