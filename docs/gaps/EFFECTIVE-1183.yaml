- id: EFFECTIVE-1183
  domain: EFFECTIVE
  title: "EFFECTIVE: Add honest degradation to Upshift CLI surface (registry‑data explain) (EFFECTIVE-370 slice)"
  status: done
  priority: P2
  effort: s
  depends_on: [["EFFECTIVE-1181"]]
  acceptance_criteria:
    1. `upshift explain` works when the AI bucket is unavailable, returning a non‑AI explanation
    2. When the AI bucket is quota‑exhausted, the CLI prints a clear banner stating the quota status using existing credits machinery
    3. No stack traces or hangs occur in either path
    4. CLI test harness blocks the AI bucket and asserts correct fallback output and exit code
  notes: |
    [chump harvest check 'EFFECTIVE']
    === primitives_index match for 'EFFECTIVE' ===

    === cluster keyword match for 'EFFECTIVE' ===

    === extracted_primitives (per-file, line-refd) match for 'EFFECTIVE' ===

    === repo-description match for 'EFFECTIVE' ===

    === HARVEST_ROADMAP.md mention of 'EFFECTIVE' (deep-scan findings) ===
      102:| **G1** | `EFFECTIVE: investigate INFRA-1719 vs echeo/src/shredder.rs — confirm harvest lineage or file consolidation` | INFRA | EFFECTIVE | P1 |
      103:| **G2** | `EFFECTIVE: vendor BEAST-MODE HITL approval flow into chump preflight + bot-merge (Marcus trust gate)` | INFRA | EFFECTIVE | P0 (Marcus blocker) |
      104:| **G3** | `EFFECTIVE: extract chump-coord-mesh crate from chump-proprietary, consumed by both private + public mesh layer` | INFRA | EFFECTIVE | P1 |
      105:| **G4** | `EFFECTIVE: vendor echeo::ShipVelocityScore as Chump gap-value scorer for routing_outcomes (INFRA-1764)` | INFRA | EFFECTIVE | P1 |
      214:| `EFFECTIVE: harvest bot-simulation-service synthetic-load generator into Chump fleet test harness (CP-008)` | EFFECTIVE | P2 |
      215:| `EFFECTIVE: vendor mock-services (Anthropic / OpenAI / Stripe / Supabase containers) into Chump CI fixture layer (CP-009)` | EFFECTIVE | P1 |
      216:| `EFFECTIVE: compare project-forge OKR schema vs Chump state.db gap schema — extract any superior primitives (CP-010)` | EFFECTIVE | P2 |

    === cross-pollination briefs mentioning 'EFFECTIVE' ===
      /home/jeff/Projects/chump/docs/arsenal/cross-pollination/CP-001-neural-farm-into-chump.md
      /home/jeff/Projects/chump/docs/arsenal/cross-pollination/CP-007-acp-alignment.md
      /home/jeff/Projects/chump/docs/arsenal/cross-pollination/CP-016-project-forge-okr.md

    [2026-09-11T06:35:00Z] Shipped as repairman29/upshift PR #31 (external_repo gap): https://github.com/repairman29/upshift/pull/31 — added src/lib/explain.test.ts covering honest degradation of `upshift explain` when the AI bucket is blocked. AC1 (registry-data explain works with zero AI credits, no --ai) and AC2/AC3 (--ai path prints the existing quota-exhausted banner from credits.ts and calls process.exit(2) cleanly, no throw/hang) were already implemented in src/lib/explain.ts and src/lib/credits.ts; this PR adds the AC4 CLI test harness that blocks the AI bucket (resetCredits(0)) and asserts fallback output + exit code. PR merged (squash).
    [2026-09-11T06:35:30Z] Closed via chump gap import (row did not yet exist in this worktree's local state.db/state.sql — synced late from a sibling fleet session): chump's own INFRA-1392 proof-of-merge guard only scans chump's local main for the gap ID, but this gap's dependency (EFFECTIVE-1181) and sibling slices are external_repo:repairman29/upshift work — the actual code landed as repairman29/upshift PR #31 (merged), a different repo entirely. closed_pr=31 refers to that external PR number, not a chump PR.
  closed_pr: 31
  skills_required: "external_repo:repairman29/upshift"
  outcome_id: READY-GATE
