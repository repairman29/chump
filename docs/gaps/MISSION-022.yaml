- id: MISSION-022
  domain: MISSION
  title: "MISSION: Verify fleet can push new branch to repairman29/BEAST-MODE via configured auth (MISSION-015 slice)"
  status: open
  priority: P2
  effort: s
  acceptance_criteria:
    - The fleet is configured with appropriate GitHub credentials (e.g., a PAT) with write access to `repairman29/BEAST-MODE`.
    - The fleet successfully pushes its new branch (containing the changes for the picked gap) to `github.com/repairman29/BEAST-MODE`.
    - The new branch is visible on GitHub in the `repairman29/BEAST-MODE` repository.
    - Fleet logs confirm successful push.
  depends_on: [MISSION-021]
  skills_required: "external_repo:repairman29/BEAST-MODE"
  outcome_id: MISSION-010
  evidence: |
    Superseded by done end-state slices: MISSION-025 (zero-touch PR MERGE in BEAST, PR#3062) + MISSION-020 (clone/prepare, PR#3205) demonstrate the full pick->commit->push->openPR->merge chain end-to-end; this intermediate verify-slice is subsumed.

