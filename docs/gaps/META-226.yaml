- id: META-226
  domain: META
  title: "META-209 prereq — chump-fleet-bot GitHub account provisioning + scoped PAT (admin:repo for repairman29/chump only); replaces operator-PAT inheritance with dedicated bot identity"
  status: closed
  priority: P1
  effort: xs
  acceptance_criteria:
    - "1. GitHub account chump-fleet-bot created (or repairman29-bot, or similar reserved name)\n2. PAT generated with admin:repo scope LIMITED to repairman29/chump only (NOT user-wide); 1-year expiry\n3. Token stored in 1Password / macOS Keychain under 'chump-fleet-bot-pat'\n4. ~/.chump/chump-fleet-bot.env file (mode 600) with GITHUB_TOKEN= line that scripts/coord/auto-admin-merge-daemon.sh (META-209) will source\n5. Verify: gh auth login --with-token < ~/.chump/chump-fleet-bot.env; gh auth status reports chump-fleet-bot identity\n6. Operator action — this gap is operator-execution, chump cannot create GitHub accounts on operator's behalf"
  notes: |
    Filed under META-209 prereq sweep. The AI-agent admin-merge protocol needs a dedicated identity separate from operator's PAT. xs because all the heavy lifting (account creation + token mgmt) is operator-hands; chump's job is just to consume the env file.
  closed_pr: 3115
  outcome_id: MISSION-010

