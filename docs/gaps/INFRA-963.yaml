- id: INFRA-963
  domain: INFRA
  title: "RESILIENT: opus-curator scheduled task silently dead for 28h — bash syntax errors on jq-fail, no exit-code surfaced to scheduler"
  status: done
  priority: P0
  effort: s
  acceptance_criteria:
    - "Diagnosis: opus-curator scheduled task (scheduled-tasks MCP, */10 * * * *) shows enabled=true and a fresh nextRunAt, but lastRunAt is 2026-05-12T00:06Z — ~28h ago. Manual `bash scripts/coord/opus-curator.sh` reveals two bash syntax errors at lines 81 and 86 (\"syntax error in expression (error token is 0)\") whenever `chump gap audit-priorities --json` emits anything that breaks jq parsing. The scheduled-tasks runner appears not to report these failures to lastRunAt, masking the outage."
    - "Fix lines 75-89 of scripts/coord/opus-curator.sh to defensively coerce P0_COUNT and VAGUE_COUNT to integers (e.g. `P0_COUNT=$(echo \"$AUDIT\" "
    - " jq -r \".p0_count // 0\" 2>/dev/null "
    - " head -1 "
    - " tr -dc 0-9); P0_COUNT=${P0_COUNT:-0}`)"
    - Same defensive treatment for jq usage in audit_waste() (~line 113), audit_pillar_balance() (similar `[[ $EFFECTIVE -lt 2 ]]` lines), audit_pr_stuck() (`[[ $STUCK_COUNT -gt 0 ]]`)
    - "Test: scripts/ci/test-opus-curator-handles-jq-fail.sh — pipe a non-JSON fixture into the audit_gaps() and audit_waste() functions and assert script exits 0 with no bash syntax errors"
    - "Companion: file a follow-up gap for the scheduled-tasks runner to track + surface non-zero exit codes (so future curator deaths are visible immediately)"
    - "Why: META-055 audit assumed the curator was running healthily; this gap unmasks a dead curator for 28h. Wave 1 stability gate cannot be evaluated until this lands."
    - "Empirical: my session detected zero curator_decision events in ambient for 30 min before this gap was filed, despite scheduled-tasks claiming */10 cadence."
  closed_date: '2026-05-13'
  closed_pr: 1644

