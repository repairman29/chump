- id: INFRA-3423
  domain: INFRA
  title: "MISSION-EFFECTIVE: pillar starved — only 0 pickable xs/s/m gaps as of 2026-07-25"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - "The change described by \"pillar starved — only 0 pickable xs/s/m gaps as of 2026-07-25\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

