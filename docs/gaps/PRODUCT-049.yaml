- id: PRODUCT-049
  domain: PRODUCT
  title: 'MISSION: publish Chump to JetBrains ACP Agent Registry'
  status: done
  priority: P2
  effort: xs
  acceptance_criteria:
  - Chump is listed in the JetBrains ACP Agent Registry with correct metadata (name, description, capabilities, install instructions)
  - Registry entry points to chump --acp as the launch command
  - Entry includes supported ACP version (v1+v2) and tool capabilities
  - Install instructions cover brew tap + brew install + chump init
  shipped_pr: https://github.com/agentclientprotocol/registry/pull/308
  notes: 'Registry entry submitted via PR #308 to agentclientprotocol/registry.

    Files created: chump/agent.json + chump/icon.svg in repairman29/registry fork.

    Binary archives for darwin-aarch64, darwin-x86_64, linux-aarch64, linux-x86_64 (v0.1.2).

    Auth: terminal type (local-first binary).

    Install instructions in PR body: brew tap repairman29/chump && brew install chump && chump init.

    '
  closed_date: '2026-05-16'
