- id: INFRA-3390
  domain: INFRA
  title: "RESILIENT: bot-merge.sh silently dies (set -e) on cache_lookup_pr_by_branch miss + unbound $REPO var"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - bot-merge.sh survives a cache_lookup_pr_by_branch miss without exiting under set -e (miss is logged and handled), and $REPO is guarded against unbound expansion; a lookup miss no longer silently kills the merge.

