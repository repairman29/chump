- id: INFRA-3390
  domain: INFRA
  title: "RESILIENT: bot-merge.sh silently dies (set -e) on cache_lookup_pr_by_branch miss + unbound $REPO var"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"bot-merge.sh silently dies (set -e) on cache_lookup_pr_by_branch miss + unbound $REPO var\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

