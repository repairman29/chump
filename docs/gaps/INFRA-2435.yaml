- id: INFRA-2435
  domain: INFRA
  title: "ZERO-WASTE P0: chump gap reserve checks title-similarity against open-PR titles (not just other gaps)"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - chump gap reserve --title X also runs Jaccard similarity vs all currently-open PR titles (gh pr list --json title)
    - Warn at >=CHUMP_GAP_RESERVE_PR_SIMILARITY_WARN (default 0.65)
    - Block at >=CHUMP_GAP_RESERVE_PR_SIMILARITY_BLOCK (default 0.85) with refusing message listing the candidate-duplicate PRs
    - "Bypass: --force-duplicate flag (audit-logged via kind=gap_reserve_pr_similarity_bypassed)"
    - "Smoke test: mock open PR titled \"fix pipefail-race patterns\"; chump gap reserve --title \"replace pipefail-race printf grep patterns\" → blocked with PR # cited"
    - Companion to INFRA-1149 (gap-vs-gap similarity); together they close the 2 holes that let INFRA-2343 + INFRA-2347 ship the same fix
  outcome_id: MISSION-010

