- id: META-287
  domain: META
  title: "META: AGENT_START_HERE north-star + session-start injection + fleet-brief merge-age honesty (cut confused-agent re-folding)"
  status: done
  priority: P2
  effort: m
  description: |
    Agents repeatedly get confused / off-mission on this large repo (1584-line core docs + 545 total .md; the load-bearing mission/anti-patterns drown). Add ONE short target-agnostic north-star docs/process/AGENT_START_HERE.md that points at the LIVE mission source (cat ACTIVE_MISSION + mission-scoreboard.sh; never hard-codes a target repo), inject it at session-start ahead of the fleet brief, and fix fleet-brief.sh which false-reported fleet-looks-healthy while the fleet was dead 13h (only alerts on daemons-dead, missing the daemons-alive-but-zero-merges case). Born from 2026-06-08: an agent had all docs in context yet trusted the lying brief, hand-ground bot-merge 30 turns, trusted a broken auth probe.
  acceptance_criteria:
    - docs/process/AGENT_START_HERE.md exists, target-agnostic (names no specific repo), points at ACTIVE_MISSION + mission-scoreboard, lists ground-truth-first ritual + 3 confusion traps + stop-condition
    - north-star injected into session-start context ahead of the fleet brief
    - fleet-brief.sh downgrades fleet-looks-healthy whenever last origin/main merge older than ~2-3h regardless of daemon-liveness or blocked-PR count
    - "regression: alive-daemons + no-merge-in-threshold reports NOT-healthy"
  notes: |
    INFRA-1435 dup-archive (similarity 100%): keeping META-286; reason: MISSION-ZERO-WASTE queue scrub 2026-08-20 (Jeff directive: no waste): archive exact-duplicate auto-filed gaps; keep lowest ID canonical; depends_on rewired; archived+recoverable
  outcome_id: MISSION-010

