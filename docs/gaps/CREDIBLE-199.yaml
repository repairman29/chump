- id: CREDIBLE-199
  domain: CREDIBLE
  title: "CREDIBLE: external-repo agent commits chump's own logs/chump.log — ship-path junk guard misses it"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"external-repo agent commits chump's own logs/chump.log — ship-path junk guard misses it\" is implemented in the relevant CREDIBLE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3474

