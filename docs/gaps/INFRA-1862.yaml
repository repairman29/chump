- id: INFRA-1862
  domain: INFRA
  title: 'A2A WORLD-CLASS: extend INFRA-1115 broadcast/inbox into a real coordination mesh with delivery guarantees + role-typed
    routing (META-074 child B)'
  status: done
  priority: P1
  effort: l
  description: "Child B of META-074. Operator priority #2: \"A2A world class.\"\n\nToday's pain (concrete):\n  - My STUCK\
    \ broadcast at 18:54Z used reason=unspecified (broadcast.sh\n    positional-arg confusion) — orchestrator had to ask for\
    \ the\n    actual reason. Delivery worked; semantic correctness didn't.\n  - When the daily-driver-session closed 3 of\
    \ my PRs at 21:29Z while\n    my fixes were in flight, no advance signal. The closure was an\n    automated action with\
    \ no broadcast.\n  - curator-opus-ci-audit dispatched INFRA-1834 to me at 21:33Z —\n    cross-curator routing worked,\
    \ but there's no role-based\n    capability discovery (it could have routed to curator-opus-handoff\n    instead and I'd\
    \ never know).\n  - No delivery guarantees: I check my inbox manually each turn.\n    At 50 workers, every agent polling\
    \ every turn = wasted compute.\n\nWhat \"world class\" means concretely:\n  a) Typed events with strict schemas (today's\
    \ broadcast.sh accepts\n     INTENT/HANDOFF/STUCK/DONE/WARN/ALERT but the field shapes drift)\n  b) Role-based capability\
    \ discovery (Opus instances publish their\n     role-skills; routers dispatch to best-fit owner not to whoever\n     claimed\
    \ first)\n  c) Delivery guarantees (push notifications via NATS — already partly\n     in INFRA-1118 A2A Layer 1a — replace\
    \ polling inboxes)\n  d) Atomic claim-handoff (handing off a gap mid-stream shouldn't\n     require the receiver to re-claim;\
    \ current pattern leaves a 10-30s\n     hand-over gap)\n  e) Audit trail: every send/receive logged for post-hoc analysis\n"
  acceptance_criteria:
  - 'Schema validation: scripts/coord/broadcast.sh rejects malformed events with a clear error pointing at the expected shape
    (today''s --to-recipient + STUCK reason field defaulted to ''unspecified'' on positional-arg misuse)'
  - 'Capability manifest: each curator session publishes role + skills (e.g. role=curator, skills=[rust,docs,sql,a2a]) to
    a known KV; routers query by skill match BEFORE dispatching'
  - 'Delivery guarantees Tier 1: NATS-backed delivery (extends INFRA-1118 Layer 1a) for high-priority events (STUCK, ALERT,
    dispatch). Tier 2: file inbox as fallback for low-priority'
  - 'Push notifications: agents subscribe to their session''s inbox via NATS push (not poll). Reduces polling overhead by
    100×'
  - 'Atomic handoff: chump gap handoff <gap> --to <session> atomically transfers the lease + claim record + worktree pointer
    in a single transaction; receiver auto-resumes without manual re-claim'
  - 'Audit trail: every broadcast logged to ambient.jsonl with sender/recipient/event/correlation_id; nightly digest summarises
    mesh health (delivery rate, response latency, dead-letter count)'
  - 'Backward-compat: existing broadcast.sh callers continue to work; new typed-schema is opt-in via --strict flag; flip to
    default after 7-day clean window'
  - 'Smoke: scripts/ci/test-a2a-mesh.sh spawns 3 ephemeral sessions, exchanges messages across all 6 pair-directions, asserts
    100% delivery + <100ms median latency'
  notes: "DECOMPOSED 2026-05-24T17:42Z by wizard (owning the offline decompose-curator task per operator 17:31Z directive).\
    \ 5 sub-slices filed:\n  - INFRA-1944 slice A: broadcast.sh delivery-confirmation receipts (P1/s)\n  - INFRA-1945 slice\
    \ B: role-typed routing via fleet-registry.jsonl (P1/s)\n  - INFRA-1946 slice C: dead-letter queue + operator-recall page\
    \ (P1/s)\n  - INFRA-1947 slice D: opt-in NATS transport with file-fallback (P2/m)\n  - INFRA-1948 slice E: schema validation\
    \ + 4 ambient kinds (P1/xs, ships last)\nRecommended ship order: E (gates) → A → B → C → D. Slices A-C usable independently\
    \ for single-machine fleet; D enables Pi-mesh.\n"
  closed_date: '2026-05-25'
  closed_pr: 2535
