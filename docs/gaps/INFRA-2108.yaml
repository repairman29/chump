- id: INFRA-2108
  domain: INFRA
  title: "EFFECTIVE P1: chump onboard <repo-url> — first-touch repo scan that reads TODO/README/IMPLEMENTATION/recent commits and proposes a prioritized backlog of derelict-scoped gaps"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "- New command: `chump onboard <repo-url-or-path> [--clone-to PATH] [--max-gaps N]`. Behavior:\n  1. If URL, clone shallow to ~/.chump/external/<owner>/<repo>/ (or --clone-to dir). If path, use it.\n  2. Read repo intent inputs:\n       - README.md (top section)\n       - CLAUDE.md / AGENTS.md if present (likely has roadmap framing)\n       - ideas/TODO.md, IMPLEMENTATION.md, ROADMAP.md, docs/ROADMAP.md (whichever exist)\n       - Last 20 git commit messages (signals what's been worked on recently)\n       - Open GitHub issues + PRs (paginated, summarized)\n       - Top-level package.json / Cargo.toml / pyproject.toml for tech stack hints\n  3. Send to provider cascade with a discovery prompt: \"Given this repo's intent docs, recent activity, and open issues, propose 5-10 next-step gaps in chump-gap YAML shape (id, domain, title, priority, effort, AC). Tag each with confidence (high/med/low) and source-of-evidence (which input file said it).\"\n  4. Surface as either:\n       - markdown table to stdout for operator triage, OR\n       - --apply mode that runs `chump gap reserve` for each and tags `external_repo: <owner>/<repo>` so they're scoped + filterable\n  5. Persist the scan result to ~/.chump/external/<owner>/<repo>/onboard-scan-<ts>.json so subsequent runs can diff and only surface delta.\n- Demo target: `chump onboard https://github.com/ehippy/derelict` should produce a backlog that mentions: (1) Character backend (router + ElectroDB entity), (2) Frontend character creation form, (3) Scenario template loading — because TODO.md ranks them and Character interface already exists in shared.\n- Why this matters: The customer-arc (Marcus M-B canonical demo) needs the operator to demo \"point chump at my repo, watch it tell me what to ship next.\" Without this, demos fall back to /health-style generic work that doesn't engage the maintainer's roadmap.\n- Cross-references: INFRA-1318 (Liaison Phase 2 webhook cache) is the *continuous* surface; this is the *initial discovery* surface. They're complementary, not duplicates. Harvester is internal-only; this is external-first-touch.\n- Discovered during: ehippy/derelict demo on 2026-05-28, where I picked a demo-safe /health endpoint instead of engaging with ideas/TODO.md's \"Next 6 Features\" list. Patrick's real next step is character.router + frontend creation form; we shipped a generic health probe."
  depends_on: [META-123, INFRA-2116, INFRA-2110]
  notes: |
    [2026-05-29T02:29:55Z] META-123 C2 — chump onboard CLI. Re-scoped as canonical child of META-123. Depends on INFRA-2116 (schema for ~/.chump/external/<owner>/<repo>/ layout) and INFRA-2110 (curator-opus-scout role) — the CLI is the operator entry point that invokes Scout to populate the schema. Implementation note: the existing src/onboard_repo_tool.rs is a Tool-trait scaffolding for the chump-brain path; the new chump onboard CLI is operator-facing (different surface). Reuse repo_path helpers + provider cascade primitives.
  closed_date: '2026-05-29'
  closed_pr: 2706
  outcome_id: MISSION-010

