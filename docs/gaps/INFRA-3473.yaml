- id: INFRA-3473
  domain: INFRA
  title: "RESILIENT: fleet doesn't re-rebase ARMED PRs when main advances — they drift to CONFLICTING/DIRTY and sit until a human notices"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"fleet doesn't re-rebase ARMED PRs when main advances — they drift to CONFLICTING/DIRTY and sit until a human notices\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3370
  outcome_id: MISSION-010

