- id: RESILIENT-1098
  domain: RESILIENT
  title: apex watchdog-of-watchdog must be a node-external daemon, not the operator session
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"apex watchdog-of-watchdog must be a node-external daemon, not the operator session\" is implemented in the relevant RESILIENT code path(s)."
    - "At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change."
    - "cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests."
  outcome_id: RESILIENT-000
