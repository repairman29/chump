- id: INFRA-768
  domain: INFRA
  title: "EFFECTIVE: Review-as-Handoff — multi-agent self-heal of CI failures via structured PR comments. Reviewer-agent diagnoses failure, posts standardized comment with failure surface + root cause + apply-this-diff + verification, tagged [handoff:apply by=<id> verified=<bool>]. Author-agent re-engages on its own PRs, applies trusted handoffs, runs full-suite tests, pushes. Caps at 1 re-engagement per session. Spec: docs/architecture/REVIEW_AS_HANDOFF.md. Decompose via chump gap decompose (PRODUCT-063 dogfood). Sub-gaps will cover: comment template+lint (s), [handoff:apply] ACL (s), worker.sh re-engagement loop (m), chump review --serve daemon (m), ambient telemetry (xs), e2e smoke test (s)."
  status: done
  priority: P1
  effort: l
  closed_date: '2026-05-08'
  closed_pr: 1360

