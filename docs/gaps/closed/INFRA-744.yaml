- id: INFRA-744
  domain: INFRA
  title: "EFFECTIVE: declarative mcp-servers.json config for MCP server discovery and settings"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "A chump-mcp.json (or section in config.toml) declares MCP servers: binary path, env vars, enabled/disabled, per-project overrides"
    - chump reads this config instead of scanning CHUMP_MCP_SERVERS_DIR for binaries
    - Config is checked into the repo so MCP setup is portable across machines
    - chump mcp list shows configured servers and their status (running/stopped/error)
    - chump mcp restart <server> restarts a specific server
  opened_date: '2026-05-08'
  closed_date: '2026-05-11'
  closed_pr: 1467

