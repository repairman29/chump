- id: CREDIBLE-027
  domain: CREDIBLE
  title: "CREDIBLE: scratch-commit / mass-deletion guard — CI fails any PR whose net diff vs main deletes >100 lines from files unrelated to its title/body, OR contains commits with vague-titles ('first', 'init', 'wip', 'unrelated change'); would have caught #1441's 378k-line wipe"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "scripts/ci/check-mass-deletion.sh + test-mass-deletion-guard.sh run on every PR"
    - "[SHIPPED in #1459] Rule A — vague commit titles: subjects matching /^(first|init|wip|unrelated change|temp|scratch|tmp)$/i fail CI"
    - "[SHIPPED in #1459] Rule B — mass deletion vs scope: net diff vs main deleting >100 lines from files NOT referenced in PR title/body fails CI"
    - "[FOLLOWUP CREDIBLE-038, merged #1476] Rule C — file-count blast: PRs titled `chore(gaps):` or `docs:` touching >50 files fail CI"
    - "Each failure surfaces as a structured PR review comment listing rule + offending commits/paths"
    - "Emits ambient kind=pr_scratch_commit_blocked with pr, rule_fired, evidence[]"
    - "Test fixtures replay against historical PRs: FAIL #1441 (rule A + B); PASS #1443, #1439, #1432 (legitimate cross-cutting)"
    - "Allow-list label `cross-cutting-acknowledged` bypasses rule B"
    - "Documented in docs/process/PR_HYGIENE.md"
  closed_date: '2026-05-11'
  closed_pr: 1459
  notes: "AC restored post-stale-binary-wipe (INFRA-825 fix). Partial impl shipped in #1459 (rules A + B); rule C in CREDIBLE-038 (#1476). Required-gate flip tracked in CREDIBLE-043 (#1477)."

