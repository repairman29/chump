- id: INFRA-671
  domain: INFRA
  title: "CREDIBLE: half-implementation detector — when PR cargo-test fails with 'no method named X found for struct Y' AND X/Y was introduced by the PR's own diff, don't auto-merge; file completion gap. Caught manually on PR #1256 (--tokens flag wired but render_text_tokens unimplemented)."
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - new pr-triage-bot job 'half-impl-detector' fires on cargo-test failure with 'no method named X found' or 'cannot find function X' AND X appears in PR's diff as a callsite (not definition)
    - disables auto-merge on the PR + comments + files completion gap
    - "test: scripts/ci/test-half-impl-detector.sh — synth PR adding 'foo()' call but not 'fn foo()', assert auto-merge disarmed"
  closed_date: '2026-05-08'
  closed_pr: 1342

