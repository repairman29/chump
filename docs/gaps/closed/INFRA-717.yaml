- id: INFRA-717
  domain: INFRA
  title: "EFFECTIVE: dispatch prompt self-verify — workers must run cargo check + clippy + symbol-resolution check before bot-merge. Cuts half-impl rate (PR #1256 class)."
  status: done
  priority: P0
  effort: xs
  acceptance_criteria:
    - "scripts/dispatch/worker.sh prompt construction adds 3 self-verify steps: (a) cargo check --workspace (b) cargo clippy --workspace --fix --allow-dirty (c) symbol-resolution check (grep diff for new method/fn calls; verify each defined in diff or main)"
    - prompt template tested via scripts/ci/test-dispatch-prompt-self-verify.sh — synth diff with orphan call, assert prompt rejects
    - "measure: half-impl rate (PR #1256 pattern) drops to <5% over 7-day window"
  closed_date: '2026-05-08'
  closed_pr: 1321

