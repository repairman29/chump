- id: INFRA-669
  domain: INFRA
  title: "RESILIENT: pr-triage-bot — auto-rerun class=flake failures (ftue/ollama, network blip) instead of just commenting. Eliminates manual 'gh run rerun --failed' cycle observed 4x in 2026-05-07 cascade."
  status: done
  priority: P0
  effort: xs
  acceptance_criteria:
    - pr-triage-bot.yml's file-fix-gap job, when classify-failure tags class=flake or class=infra-broken, calls 'gh run rerun --failed' on the run instead of just commenting
    - cap reruns at 3 attempts (read run_attempt from API); on attempt 3+ fail comment 'investigation needed'
    - "test: scripts/ci/test-pr-triage-bot-rerun.sh — synth a flake check_run, assert rerun API was called"
  closed_date: '2026-05-08'
  closed_pr: 1297

