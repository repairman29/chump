- id: PRODUCT-052
  domain: PRODUCT
  title: 'EFFECTIVE: chump gen — fallback to ===FILE=== blocks when model lacks tool-use (PRODUCT-046 slice 3)'
  status: done
  priority: P1
  effort: xs
  depends_on:
  - PRODUCT-050
  acceptance_criteria:
  - When model returns no tool_calls for 2 consecutive turns, fall back to ===FILE=== text parsing
  - Fallback emits a warning log line so operator knows tool-use is not working
  - Existing ===FILE=== single-shot path still works identically to pre-PRODUCT-050 behavior
  - 'Test: CHUMP_GEN_STUB_FILE fixture works in both tool-use and fallback modes'
  closed_date: '2026-05-10'
  closed_pr: 1418
