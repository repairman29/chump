- id: INFRA-682
  domain: INFRA
  title: "RESILIENT: CI path filter allowlist structural fix — auto-detect unmatched sole-diff paths instead of relying on comment (INFRA-272)"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - ".github/workflows/ci.yml 'changes' job emits warning when a PR diff matches NONE of the code: paths-filter allowlist"
    - "new scripts/ci/check-path-filter-coverage.sh runs in fast-checks; fails if PR has no matching code: pattern AND has gated job dependents"
    - "test: scripts/ci/test-path-filter-allowlist.sh — synth PR touching only new top-level dir, assert warning fires"
  closed_date: '2026-05-10'
  closed_pr: 1411

