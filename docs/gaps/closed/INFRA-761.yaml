- id: INFRA-761
  domain: INFRA
  title: 'RESILIENT: pre-push cargo-test full-suite gate — kill ''I ran scoped tests'' lie. scripts/git-hooks/pre-push runs
    ''cargo test --bin chump --tests'' (full suite, not scoped) before every push to a chump/* branch. Bypass: CHUMP_TEST_GATE=0
    with required Test-Gate-Bypass: trailer. Catches stale tests in unrelated files (EVAL-026 #1349 missed neuromodulation.rs:340
    because author ran scoped test in env_flags.rs only).'
  status: done
  priority: P1
  effort: s
  closed_date: '2026-05-08'
  closed_pr: 1363
