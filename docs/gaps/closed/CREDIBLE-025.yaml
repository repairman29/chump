- id: CREDIBLE-025
  domain: CREDIBLE
  title: "CREDIBLE: tag worker model in ambient events + per-model ship-rate breakdown — so Sonnet/Haiku/Opencode comparisons stop being vibes"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "Every ambient.jsonl event from a worker includes BOTH `model` (e.g., claude-sonnet-4-6, claude-haiku-4-5) AND `harness` (claude-code-ide, opencode-bigpickle, fleet-dispatcher)"
    - "[SHIPPED in #1457] scripts/dispatch/model-ship-rate.sh + scripts/coord/broadcast.sh tag model field; scripts/ci/test-model-ship-rate.sh passes"
    - "[FOLLOWUP CREDIBLE-037, merged #1476] harness-tagging half — ambient-emit.sh --harness flag, fleet-status --by-harness, waste-tally --by-harness"
    - "scripts/dispatch/fleet-status.sh --by-model renders breakdown tables: ships, waste rate, silent_agent count, P50 cycle time"
    - "chump waste-tally --by-model and chump health --by-model show breakdown for ad-hoc operator review"
    - "Test fixture: synthetic ambient events across multiple model+harness pairs"
    - "Migration note: pre-CREDIBLE-025 events without model/harness fields are tagged harness=legacy"
  closed_date: '2026-05-11'
  closed_pr: 1457
  notes: "AC restored post-stale-binary-wipe (INFRA-825 fix). Partial impl shipped in #1457 (model side); harness side in CREDIBLE-037 (#1476)."

