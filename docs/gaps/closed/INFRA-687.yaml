- id: INFRA-687
  domain: INFRA
  title: "ZERO-WASTE: split main.rs — extract lib.rs for mod declarations + cli/ for subcommand routing from 5341-line monolith"
  status: done
  priority: P1
  effort: m
  notes: |
    TOO BIG FOR FLEET — split main.rs is a many-week refactor; fleet (haiku) will produce broken splits.
    Decomposed into 5 phased sub-gaps for human-guided pickup:
      INFRA-722: extract cli/ (dispatch, routes, platform_router, rpc_mode)
      INFRA-723: extract providers/ (provider_bandit/cascade/quality, streaming_provider, local_openai, mistralrs_provider)
      INFRA-724: extract fleet/ (fleet, fleet_capability, fleet_db, fleet_health, fleet_status, fleet_tool, fleet_velocity, cluster_mesh)
      INFRA-725: extract memory/ (memory_db, memory_graph*, memory_tool, belief_state, episode_db, episode_extractor)
      INFRA-726: wire lib.rs with pub mod declarations and crate-level pub use
  closed_date: '2026-05-08'
  closed_pr: 1311

