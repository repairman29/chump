- id: INFRA-810
  domain: INFRA
  title: "RESILIENT: stale core.worktree in main .git/config poisons show-toplevel for all linked worktrees"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - git config --local core.worktree in main checkout prints nothing
    - git -C <any-tmp-worktree> rev-parse --show-toplevel returns the correct path
    - gap-claim.sh runs identity check and calls git worktree repair on mismatch
    - chump gap reserve from main checkout writes YAML to main repo docs/gaps/
    - git worktree repair exits 0 after fix
  closed_date: '2026-05-10'
  closed_pr: 1419

