- id: PRODUCT-304
  domain: PRODUCT
  title: "[pvc][external_repo:repairman29/pvc][gcp-exit] Move Peak Vinyl Club members DB + auth from Firebase (peakvinyl) to hosted Supabase (free slot) with nightly backups"
  status: open
  priority: P1
  effort: m
  description: |
    peakvinyl.club already hosts on Vercel; only data + auth move. Firestore collections: members, posts, settings, events, supportingMembers, transactions, filings, community_events, applications, outreach_leads, inviteCodes, admin_users. Firebase Auth in ~30 files (website/lib/firebase-client.ts, firebase-admin.ts, admin pages, member APIs) plus functions/src/index.ts. Real 501(c)(7) member data: do first, verify counts. Rough shape: (a) Postgres schema + RLS mirroring firestore.rules, (b) import script from export, (c) swap firebase-client/admin libs for supabase-js, (d) port the Cloud Function to a Vercel route/cron, (e) admin custom-claim to Supabase role. Target (Jeff 2026-09-26, workspace-docs/GCP_EXIT.md): pvc is the one app with real members, so it goes to HOSTED Supabase, not the shared cuphead stack. Uses the second FREE project slot: the paused 'Echeo' project (mgeydloygmoiypnwaqmn) must be exported and then deleted by Jeff first (irreversible, operator action). Free tier pauses idle projects and has no backups, so: a Vercel daily cron pings the project (Olive's keep-alive pattern) and a nightly pg_dump + storage copy lands in OCI Object Storage (cuphead account) and on CJ with 14-day retention. If managed backups are wanted later, upgrade this one org to Pro ($25/mo). Code uses supabase-js only. Keep Sign in with Google. Firebase email/password members keep working: import from the PRODUCT-303 auth:export and verify Firebase scrypt hashes on first sign-in, or document why a reset flow was needed.
  acceptance_criteria:
    - Every Firestore collection has a Postgres table; row counts match the PRODUCT-303 export ledger
    - Existing members sign in with their current password and with Google on a preview deploy
    - RLS enforces the same member/admin access as firestore.rules (tests for member, admin, anon)
    - Keep-alive cron is live and the project has not paused over 8 days; a nightly backup exists off Supabase (OCI Object Storage + CJ) and one restore is proven
    - No firebase or firebase-admin import remains in website/ or functions/; production peakvinyl.club runs on Supabase
  depends_on: [PRODUCT-303]
  notes: |
    DISPATCH (Jeff 2026-09-26): P1, queued behind its depends_on. Do not start until every dependency is done: the PRODUCT-303 export ledger must exist in workspace-docs/GCP_EXIT.md, because migration row counts are verified against it. Operator-only prerequisite: free the second Supabase free-tier slot. Jeff exports, then deletes, the paused 'Echeo' project (mgeydloygmoiypnwaqmn); that is irreversible and a worker must not do it. If the slot is not free, page with that exact ask and stop. Any node may do the code work (repo: repairman29/pvc).
  skills_required: "external_repo:repairman29/pvc"

