- id: INFRA-3542
  domain: INFRA
  title: "RESILIENT: pr-failure-auto-rescue — terminal close+refile for unfixable-red armed PRs, and schedule the daemon (COTG self-heal)"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"pr-failure-auto-rescue — terminal close+refile for unfixable-red armed PRs, and schedule the daemon (COTG self-heal)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-09'
  closed_pr: 3533

