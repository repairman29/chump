- id: INFRA-1255
  domain: INFRA
  title: A2A — correlation_id schema + inbox-reap auto-clears on matching DONE/ACK
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "[\n  \"scripts/coord/broadcast.sh: every event gets corr_id field. Default: gap-id (when applicable) else current branch name else ts\",\n  \"STUCK / HANDOFF / INTENT include corr_id matching the gap-id; DONE / ACK include same corr_id\",\n  \"scripts/coord/inbox-reap.sh runs nightly (cron) AND in-process via bot-merge.sh hook (INFRA-1253)\",\n  \"Reap rules: clear inbox messages where (DONE seen w/ same corr_id) OR (older than CHUMP_INBOX_TTL_DAYS, default 7)\",\n  \"Emits kind=inbox_reaped with counts per session/event-type\",\n  \"Test: build inbox with mixed messages, run reap, verify DONE-matched ones gone + ttl-older ones gone + recent un-DONE remain\"\n]"
  notes: |
    [2026-05-14T16:09:42Z] Lifecycle correlation is the missing piece. With corr_id every event chain (INTENT->STUCK->HANDOFF->DONE) becomes traceable + auto-cleanable. Enables future a2a metrics (time-to-resolution by message type).
  closed_date: '2026-05-14'
  closed_pr: 1900

