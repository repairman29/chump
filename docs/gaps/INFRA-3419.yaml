- id: INFRA-3419
  domain: INFRA
  title: "RESILIENT: pr-stuck-cluster — 3 PRs blocked >2h as of 2026-07-24"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - "The change described by \"pr-stuck-cluster — 3 PRs blocked >2h as of 2026-07-24\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

