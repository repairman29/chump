- id: MISSION-065
  domain: MISSION
  title: "CHUMPOS Phase 0: Hetzner substrate + disk-floor gate + worker-scaling formula + systemd daemon parity"
  status: done
  priority: P2
  effort: xl
  acceptance_criteria:
    - "The change described by \"Hetzner substrate + disk-floor gate + worker-scaling formula + systemd daemon parity\" is implemented in the relevant MISSION code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  notes: |
    Decomposed into 7 slices: MISSION-068, MISSION-069, MISSION-070, MISSION-071, MISSION-072, MISSION-073, MISSION-074
  closed_date: '2026-07-28'
  closed_pr: 3344
  outcome_id: CHUMPOS
  evidence: |
    Measured 2026-07-27: 82GB cargo cache + 6 reactive disk-reaper daemons; CPX32 8GB RAM caps concurrent Rust builds; 9 daemons failed to install (launchd disabled-state EIO, shared-target path); GitHub API 2791 calls/24h w/ graphql exhaustion. Substrate/disk/scaling are unbounded and macOS-hostile.

