- id: INFRA-2458
  domain: INFRA
  title: "RESILIENT P1: main-preflight-watchdog-daemon.sh gate-name extraction returns literal '—' on certain failure modes"
  status: open
  priority: P0
  effort: s
  acceptance_criteria:
    - "[\"Trigger: 2026-06-02 ~23:30Z; INFRA-2457 was auto-filed with failing_gates=[\\\"—\\\"] (literal em-dash); test-gap-preflight-ac-gate.sh actually exits 0 (6/6 PASS) when invoked directly\",\"Investigate scripts/coord/main-preflight-watchdog-daemon.sh gate-name extraction logic — probably awk/grep that mismatches on certain output shapes\",\"Fix the parser so failing_gates carries real gate names (or empty array on no-fail) — never literal dashes\",\"Smoke test scripts/ci/test-main-preflight-watchdog-parser.sh: mock preflight output with [preflight] FAIL — at least one gate; assert no gate extracted as literal dash\"]"

