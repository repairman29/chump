- id: CREDIBLE-042
  domain: CREDIBLE
  title: "CREDIBLE: make CREDIBLE-026 (PR scope-vs-title detector) a required CI check on every PR, not advisory — applied retroactively would catch PR #1444 silent META-044 revert + PR #1469 bundle false-title claim"
  status: done
  closed_pr: 1663
  priority: P1
  effort: xs
  acceptance_criteria:
    - ".github/workflows/ci.yml lists `pr-scope-honesty` (or whatever job name CREDIBLE-026 shipped) as a REQUIRED check via branch protection rules — currently it runs but is not blocking"
    - "Updates the branch protection ruleset for `main` (via repo settings or .github/branch-protection.yml if managed-as-code) to include the check"
    - "Before merging this gap's PR, retroactively run CREDIBLE-026's checker against the last 30 days of merged PRs and produce a report of historical violations — informs whether the gate is calibrated correctly before flipping to required"
    - "Allow-list label `cross-cutting-acknowledged` still bypasses the rule (already in CREDIBLE-026 AC)"
    - "Test: a synthetic PR with chore(gaps) title + a src/ change → CI fails BEFORE merge (today it merges anyway because the check is advisory)"
    - "Documented in docs/process/PR_HYGIENE.md as 'this is required, not advisory'"
    - "Pairs with CREDIBLE-043 (mass-deletion required) so the two CREDIBLE-026/027 checkers both become enforcing gates simultaneously"
