- id: CREDIBLE-030
  domain: CREDIBLE
  title: 'CREDIBLE: add unit tests to src/main.rs — CLI dispatch + arg parsing (6365 LOC, decomp of CREDIBLE-004 child 1/4)'
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
  - 'Extract pure-function subcommand handlers from src/main.rs into tests/main_dispatch.rs — start with the 5 highest-traffic
    subcommands: chump gap reserve, claim, show, list, ship'
  - 'Each test exercises: valid args → expected exit code (0) + expected stdout markers; invalid args → exit 2 + usage hint
    in stderr'
  - 'Test the dispatch table itself: every subcommand string in main.rs''s match arm has a corresponding test that asserts
    the right handler is called (cargo test main::dispatch_table_complete)'
  - Mock state.db via tempdir + chump gap reserve in test fixtures (do NOT touch the repo's state.db); each test isolates
    its own .chump dir
  - 'Coverage target: >40% line coverage on src/main.rs measured by `cargo tarpaulin --bin chump --include-files src/main.rs`
    (today: 0%)'
  - Document any subcommand whose handler is too entangled to test as-is, file a CREDIBLE-* follow-up gap proposing refactor
    — don't force-test untestable code
  - 'CI gate: `cargo test --lib main` and `cargo test --test main_dispatch` both pass; the new test target is in cargo''s
    default test set'
  closed_date: '2026-05-15'
  closed_pr: 1856
