- id: META-167
  domain: META
  title: "[UMBRELLA] Fleet wire best-in-class v1 — reactor table + sub-second push + capability publish + MCP surface + JetStream durable consumers (unblocks META-155 HOLD)"
  status: open
  priority: P0
  effort: l
  description: |
    Umbrella to elevate the fleet wire from "delivery works" (proven 2026-05-30T07:37Z by META-RECURSIVE PROOF V3, recipient_count=2) to "reply works + sub-second + capability-aware + MCP-exposed + restart-safe." Unblocks META-155 HOLD by shipping a concrete reactor-table design. 9 slices, mostly parallel-dispatchable via META-069 Sonnet fleet. Behind feature flag CHUMP_FLEET_WIRE_V1 (default OFF until E2E green: broadcast → 5 reactors emit votes → consensus_result fires within 30s).
  acceptance_criteria:
    - "Slice a (DESIGN, FOUNDATIONAL): docs/strategy/FLEET_WIRE_V1_2026-05-30.md design doc — reactor table per role with (kind in / decision class / cooldown rules / dedupe fingerprint); inotify/kqueue spec; capability publish spec; MCP surface spec; NATS JetStream durable consumer spec; 5-day rollout plan"
    - "Slice b: decompose-loop reactor — react to FEEDBACK kind=proposal on umbrella gaps; vote +1 if AC count >= 3 + status open + no existing sub-gaps; vote -1 if AC vague; emit kind=decompose_reactor_voted with reasoning"
    - "Slice c: ci-audit-loop reactor — react to FEEDBACK kind=proposal that references regression_attributed events; vote per blame-bot suspect_commits count; cross-reference with current CI cluster state via existing tick subcommand"
    - "Slice d: handoff-loop reactor — react to FEEDBACK kind=proposal that proposes new Contract type or modifies existing; vote +1 if Contract schema is well-formed (Input/Output/Validate/prompt/ModelTier all present); file follow-up if Contract overlaps existing"
    - "Slice e: target + shepherd + md-links reactors (bundle 3): target votes per pillar-alignment with active demo-target; shepherd votes per existing wedge-class match (uses VOA-001 schema); md-links votes +1 if proposal touches docs/ (lane match), -1 otherwise"
    - "Slice f: scripts/coord/lib/inbox-watcher.sh inotify (Linux) / fswatch (macOS) helper; per-curator daemon watches .chump-locks/inbox/<self>.jsonl for new lines; emits SIGUSR1 to parent loop on new event; loop tick wakes immediately instead of waiting for 300s cron"
    - "Slice g: capability publish — each curator-loop at startup publishes CapabilityManifest (chump-capability-v1 schema, INFRA-1760) to NATS KV chump_capabilities bucket; 30s heartbeat refresh; TTL=5min; broadcaster reads bucket pre-fanout to filter offline curators"
    - "Slice h: crates/chump-mcp-fleet/ — new MCP server exposing 5 tools (inbox_drain, broadcast, vote, consensus_status, capabilities); supports stdio + Unix socket transport; integrates with existing chump-mcp-lifecycle crate"
    - "Slice i: chump-coord JetStream durable consumer per role — durable_name=\"chump-fleet-<role>\"; ack_policy=explicit; replay missed events on curator restart; observability dashboard panel (PWA cockpit) showing per-role consumer lag + delivery latency p50/p99"
    - "End-to-end proof: in PR closing this umbrella, broadcast a FEEDBACK kind=proposal with corr_id=META-167-E2E; assert within 30s — 5 reactors emit kind=vote (one each), consensus_result fires with verdict PASSED, voters_list contains all 5 curator session_ids"
    - Feature flag CHUMP_FLEET_WIRE_V1=1 gates all 9 slices; default OFF until E2E green; flip ON in follow-up PR after 24h soak
    - "Backwards compat: META-155 stays open until E2E proof closes it; if any slice fails E2E, file follow-up gap rather than weakening AC"

