- id: CREDIBLE-147
  domain: CREDIBLE
  title: "CREDIBLE: auth-status.sh stale-cache BROKEN false-negative caused 6-day silent fleet outage; --force does not bust cache"
  status: closed
  priority: P0
  effort: m
  description: |
    ROOT CAUSE of the 2026-06-22→28 silent 6-day fleet outage (zero merges, zero ship_landed). [CLOSED 2026-06-29 as DUPLICATE of CREDIBLE-146 — folded --force-broken + mtime-invalidate + sleep-persistence specifics into CREDIBLE-146 AC. See CREDIBLE-146.] Token was valid the whole time; recovery was rm ~/.chump/auth-status-cache.
  acceptance_criteria:
    - auth-status.sh --force (and -f) actually re-probes and overwrites the cache, never prints '(cached)'
    - cache is auto-invalidated when ~/.chump/oauth-token.json (or .env ANTHROPIC_API_KEY) mtime > cache mtime
    - a stale-but-valid scenario is covered by scripts/ci/test-auth-status*.sh (fake a BROKEN cache + valid token, assert --force flips to OK)
    - farmer does not stay in farmer_auth_dead while a live credential exists for more than one TTL window

