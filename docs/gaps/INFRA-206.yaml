- id: INFRA-206
  domain: infra
  title: per-agent gap-domain affinity — agent-1 INFRA only, agent-2 EVAL only, etc (kills hot-spot bias at 10+ agents)
  priority: P1
  effort: s
  status: open
  notes: SUPERSEDED 2026-05-02 by INFRA-314 (gap affinity tags + worker preference matching). INFRA-314 generalizes 'per-domain
    affinity' to skills_required + preferred_backend + preferred_machine — the domain case is a strict subset. Auto-skip from
    fleet pickup via SUPERSEDED prefix.

