- id: INFRA-2082
  domain: INFRA
  title: 'RESILIENT P0 trunk-RED: chump-mcp-coord build silently fails on self-hosted runner — test-mcp-coord-smoke.sh exits
    127 because target/debug/chump-mcp-coord doesn''t exist after ''cargo build -q -p chump-mcp-coord''; works locally; likely
    runner cargo registry cache miss (tauri-2.11.2 proc-macro class)'
  status: done
  priority: P0
  effort: xs
  acceptance_criteria:
  - 'Reproduce on self-hosted runner: `cargo build -q -p chump-mcp-coord` exits 0 but produces no target/debug/chump-mcp-coord
    binary; subsequent execution returns 127 (No such file or directory); works fine on local dev machine'
  - 'Diagnose: remove -q flag and capture cargo output; expect to see tauri-2.11.2 proc-macro panic OR similar registry-cache-corruption
    error class; sibling failure: clippy job in same run showed `failed to read plugin global API script /Users/jeffadkins/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tauri-2.11.2/scripts/bundle.global.js`'
  - 'Fix options (pick A or B):  (A) make scripts/ci/test-mcp-coord-smoke.sh robust: change `cargo build -q` to `cargo build`
    (no -q) so errors surface; add `cargo build --message-format=json | tee /tmp/build.log` and explicit binary existence
    check with failure message; fail loud rather than exit 127.  (B) infra-watcher curator + chump fleet-bootstrap addition:
    detect registry cache corruption at session-start, auto-`cargo clean -p chump-mcp-coord` if detected.  Option A is xs
    (just shell tweak); option B is s (new check + auto-recovery)'
  - 'Smoke test scripts/ci/test-mcp-coord-build-robust.sh: simulate broken registry by removing tauri-2.11.2 from cargo cache;
    run test-mcp-coord-smoke.sh; assert it fails LOUDLY with build-error message (not silent exit 127); option A makes this
    test pass'
  - 'Trunk-RED unwind: after fix lands, runner CI passes fast-checks step 109 (chump-mcp-coord tools/list)'
  notes: 'Filed 2026-05-27 by curator-opus-shepherd. THIRD wedge surfaced this session (INFRA-2077 dispatch, INFRA-2080 sandbox
    isolation, now this). All independent, all blocking different PRs. META-117 MAIN-CI-DEBT pattern at scale. Local repro
    confirmed builds clean — actions-runner-specific. Self-hosted-runner cache state issue.

    '
  closed_date: '2026-05-29'
  closed_pr: 2680
