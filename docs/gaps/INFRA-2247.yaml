- id: INFRA-2247
  domain: INFRA
  title: 'EFFECTIVE: lift Mission / PersistentMission / ObjectiveState type shapes into crates/chump-coord (public interface,
    derived from internal sibling repo)'
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
  - 'crates/chump-coord/src/mission/ module created with Mission trait + PersistentMission<M: Mission> struct + MissionCheckpoint
    + ObjectiveState (Pending/InProgress/Completed/Failed/Denied/Replanned) + ObjectiveDependency + Denial + FallbackMode
    + ReplanStrategy enums'
  - MissionReplanner trait + default AbortOnFailureReplanner impl that returns ReplanStrategy::Abort for every denial (conservative
    default; non-trivial replanners are caller-supplied)
  - FileBackedMissionStore — persists PersistentMission<M> to JSON under ~/.chump/missions/<id>.json; load/save/list operations;
    no network calls
  - 'All types are #[derive(Serialize, Deserialize)] and round-trip cleanly through serde_json'
  - 'Integration test crates/chump-coord/tests/mission_local.rs: define a toy concrete Mission type, persist + reload via
    FileBackedMissionStore, drive forward via simulated denials, assert AbortOnFailureReplanner aborts as expected'
  - Module-level doc comment attributes internal sibling repo as the architectural source-of-truth for the trait shape; no
    internal IP-bearing logic copied (replanner strategy table, consensus algorithms stay private)
  - Pairs with INFRA-2248 (MeshTransport) and INFRA-1804 (BandwidthBudget/MessageQueue); under umbrella INFRA-2246
  notes: '[2026-05-30T04:20:49Z] Filed by curator-opus-shepherd-2026-05-29 as part of INFRA-2246 offline-first roadmap consolidation.
    Derived from internal sibling repo crates/coord/src/mission/ module shape. Trait + serde only — robot-grade replanner
    logic, multi-robot consensus, threat-classification stay in internal.

    [2026-05-30T04:31:20Z] [2026-05-29 operator clarification] Vendoring internal code is FINE if fit-for-purpose / fast/easy
    to mod. Updated guidance: literally copy crates/coord/src/mission/{mod,persistence,execution,replanning}.rs into public
    crates/chump-coord/src/mission/ with header attribution. Strip or generic-ify robot-specific concrete types (Target, RobotId,
    BoundingBox specifics) as needed. Keep replanner logic — it''s good. Public implementation gets all the value internal
    has, public users get a working system from day one.

    '
  closed_date: '2026-05-30'
  closed_pr: 2831
