- id: INFRA-849
  domain: INFRA
  title: "CREDIBLE: Integration tests for system gaps"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "1. scripts/ci/test-system-integration.sh: end-to-end test of full claim→commit→ship pipeline using a synthetic gap in a temp state.db"
    - "2. test covers: gap-claim.sh creates worktree + lease JSON, chump-commit.sh writes commit, bot-merge.sh arms auto-merge (stubbed gh), chump gap ship marks done"
    - 3. passes with CHUMP_INTEGRATION_TEST=1 (skips real GitHub API, uses gh stub)
    - 4. emits kind=integration_test_pass or integration_test_fail to ambient.jsonl
    - 5. runs in under 60s on clean machine
    - 6. added to .github/workflows/CI.yml as integration-test job (runs on PR to main)

