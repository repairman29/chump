- id: INFRA-3458
  domain: INFRA
  title: "EFFECTIVE: the fleet actually CONSUMES comprehension — inject comprehend_repo directive into context_assembly (chump-local runtime) + worker inline prompt, so the organs stop being CLI-only shelfware"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"the fleet actually CONSUMES comprehension — inject comprehend_repo directive into context_assembly (chump-local runtime) + worker inline prompt, so the organs stop being CLI-only shelfware\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-28'
  closed_pr: 3356
  outcome_id: MISSION-010

