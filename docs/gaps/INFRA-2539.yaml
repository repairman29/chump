- id: INFRA-2539
  domain: INFRA
  title: "PR #2975 stuck [CI-RED] — CI red for 76m"
  status: closed
  priority: P1
  effort: xs
  description: |
    https://github.com/repairman29/chump/pull/2975
    
    Detected by stuck-pr-filer (2026-06-03T08:33:35Z).
    
    Trigger: CI red for 76m
    At least one required check has been failing for 76m (threshold 20m). Failing check: fast-checks.
    
    Suggested action:
      1. Check the PR — confirm whether the underlying gap landed elsewhere.
      2. If yes: gh pr close 2975 --comment 'superseded'.
      3. If no:  rebase the branch and re-arm via scripts/coord/bot-merge.sh.
    
    Original gap(s) cited in PR title/commits: INFRA-2435
    Branch: chump/infra-2435-claim
    Failing check: fast-checks
    Stuck class: CI-RED — ci-flake-rerun or human investigation required
  acceptance_criteria:
    - "TODO: what events emitted on success/failure/timeout"
    - "TODO: how cost tracked and reported to operator"
    - "TODO: failure-class taxonomy (distinguish transient vs permanent)"
    - "TODO: smoke test command to verify observability"
  notes: |
    [2026-06-05T05:32:06Z] PR reached final state (MERGED or CLOSED); stuck-PR detector never auto-closed. Sweep 2026-06-05T05:35Z (sibling cleanup to RESILIENT-097 trunk-recovery class).
  outcome_id: MISSION-010

