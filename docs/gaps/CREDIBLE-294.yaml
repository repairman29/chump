- id: CREDIBLE-294
  domain: CREDIBLE
  title: "Ship-fragility: RESILIENT-025 pre-commit gate blocks commits on STALE claims. get_active_claim_file did a blind head -1 over .chump-locks/claim-*.json, so a leftover claim from a prior ship blocked a commit for a DIFFERENT gap — every ship became a fight. Fix: match the claim to the current branch gap (chump/domain-num)."
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"RESILIENT-025 pre-commit gate blocks commits on STALE claims. get_active_claim_file did a blind head -1 over .chump-locks/claim-*.json, so a leftover claim from a prior ship blocked a commit for a DIFFERENT gap — every ship became a fight. Fix: match the claim to the current branch gap (chump/domain-num).\" is implemented in the relevant CREDIBLE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-21'
  closed_pr: 4069
