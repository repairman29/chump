- id: INFRA-3475
  domain: INFRA
  title: "EFFECTIVE: chump agent-run — chump-local agent on an ad-hoc prompt in a cwd (external-repo execution Phase-1 keystone, no gap-conflation)"
  status: shipped
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"chump agent-run — chump-local agent on an ad-hoc prompt in a cwd (external-repo execution Phase-1 keystone, no gap-conflation)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3366
  outcome_id: MISSION-010

