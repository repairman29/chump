- id: PRODUCT-303
  domain: PRODUCT
  title: "[gcp-exit] Export all live Firebase/GCP data (peakvinyl, trove-web, echeo-vid, fulcrum-41e50, smuggler) to owned storage before billing wind-down"
  status: open
  priority: P1
  effort: s
  description: |
    Step 0 of the GCP exit (workspace-docs/GCP_EXIT.md). The Firebase/GCP billing account is closed (ASK-021); Blaze projects cannot deploy and risk suspension while holding live data. Export everything to owned storage BEFORE any migration or project deletion. Firestore managed export (gcloud firestore export) requires billing enabled: operator reopens billing on the account first (see GCP_EXIT.md Sequence). Needs gcloud/firebase CLI logged in as jeffadkins1 (Mac/owned node; cannot run from a cloud session). Projects: peakvinyl (pvc members), trove-web (+Storage bucket), echeo-vid (+Storage: videos), fulcrum-41e50 (postsub), playsmuggler-b1cda / smuggler-d1b4a (smuggler sessions), workbench-tam-app, lifeos-dev, mythseekers-rpg. Per project: firestore export to a GCS bucket then gsutil cp to owned disk; firebase auth:export --format=json (keeps scrypt hashes + the project's hash config from Firebase console > Auth > password hash params); gsutil -m cp -r of Storage buckets.
  acceptance_criteria:
    - Firestore export + Auth export (with hash params) + Storage copy exist on owned storage for peakvinyl, trove-web, echeo-vid, fulcrum-41e50 and both smuggler projects
    - Per-collection document counts and auth user counts recorded in workspace-docs/GCP_EXIT.md (export ledger table)
    - Exports verified readable (one sample doc per collection re-parsed)
    - No project deleted and no data mutated by this gap
  notes: |
    DISPATCH (Jeff 2026-09-26): run ON cuphead (ubuntu@cuphead, tailnet; 116 GB free, always on). Exports land under /srv/gcp-export/<project>/ on cuphead, then a second copy is rsync'd to CJ (off Oracle). The Mac is not used. The pull picker does not enforce preferred_machine yet, so a worker on any other node that picks this must release it (chump claim --discard-wip). Operator-only prerequisites (a worker cannot do these; check first and page with the exact ask if missing): (1) reopen the Firebase/GCP billing account (ASK-021): Firestore managed export needs billing enabled; (2) authenticate the CLIs on cuphead as jeffadkins1: 'gcloud auth login --no-launch-browser' and 'firebase login --no-localhost' (browser step only Jeff can complete), or drop one service-account key per project into ~/.chump/gcp/ (chmod 600, never in git). Without billing, still do what works: firebase auth:export (no billing needed) and gsutil copies of Storage buckets, then record Firestore as blocked-on-billing in the ledger. Read-only gap: never delete or mutate a project.

    DONE 2026-09-26 (cuphead, this session): CLIs already authenticated as jeffadkins1 (prereq 2 was
    complete). Prereq 1 (billing) was still NOT done, and turned out to gate more than expected —
    Storage bucket *downloads* need billing too (`gcloud storage cp` → 403 BILLING_DISABLED), not just
    Firestore export; only metadata reads (bucket/collection listing) work billing-free. So this session
    shipped the full no-billing ceiling: `firebase auth:export` for peakvinyl/trove-web/echeo-vid/
    fulcrum-41e50 (playsmuggler-b1cda has no Auth config — CONFIGURATION_NOT_FOUND) plus
    workbench-tam-app/mythseekers-rpg/lifeos-family-os, all re-parsed and verified readable; full
    Firestore collection inventories and Storage object counts (no content) for every accessible
    project. `smuggler-d1b4a` still isn't visible under this account (confirmed again — the real
    playsmuggler-b1cda has zero Firestore/Storage/Auth-config to export anyway). Full ledger:
    workspace-docs/GCP_EXIT.md "2026-09-26 — PRODUCT-303 export ledger" section (commit d7737e0).
    Remaining work (Firestore document export + Storage object copy for trove-web/echeo-vid/
    fulcrum-41e50, and the Firebase-console-only password hash params for the 4 password-auth
    projects) is tracked in PRODUCT-310 and blocked on the same ASK-021 billing reopen. No project
    deleted or mutated.
  preferred_machine: cuphead

