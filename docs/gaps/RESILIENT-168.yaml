- id: RESILIENT-168
  domain: RESILIENT
  title: "RESILIENT: integrator-daemon dead (exit 127) ~32h → merge queue down → fleet opens PRs but can't merge them (backlog frozen, behind-PRs never auto-rebased)"
  status: open
  priority: P0
  effort: m
  acceptance_criteria:
    - "1. com.chump.integrator-daemon starts and STAYS running — currently exit=127 (ProgramArguments command/binary not found); diagnose + repair the broken path/binary (likely stale plist or missing subcommand after a rename). 2. With it up, BLOCKED/behind PRs get auto-update-branched + re-run + merged so the backlog drains (per-PR Mode-B fallback does NOT drain because behind PRs are never rebased to green). 3. A healthcheck pages when integrator exit!=0 for >15min (it was silently dead ~32h). 4. The 4 DIRTY stale-duplicate PRs (INFRA-918 #3155/3156/3157, INFRA-1526 #3163) are closed by the orphan/dup reaper."
  notes: |
    P0 promotion 2026-07-19 cycle plan: true unblocker for fleet revival / registry truth (operator-directed re-rank; see docs/ROADMAP.md current cycle).
  outcome_id: RESILIENT-000
  evidence: |
    2026-06-24: 0 fleet-worker merges in 32h (last was #3170) despite auth OK (force-fresh validated) + audit-gate fixed (#3172) + workers actively producing PRs (open count 8->9). integrator-daemon exit=127, merge-queue-monitor exit=78. Manual kickstart did not hold (127=binary-not-found). The fleet cannot self-heal: it can't merge its own merge-pipeline fixes (catch-22) — needs an out-of-band substrate repair. Siblings: RESILIENT-165 (bot-merge silent-wedge), RESILIENT-167 (keeper/autopilot won't revive pool), CREDIBLE-146 (auth-cache false-positive).
  evidence: |
    2026-06-24: 0 fleet-worker merges in 32h (last was #3170) despite auth OK (force-fresh validated) + audit-gate fixed (#3172) + workers actively producing PRs (open count 8->9). integrator-daemon exit=127, merge-queue-monitor exit=78. Manual kickstart did not hold (127=binary-not-found). The fleet cannot self-heal: it can't merge its own merge-pipeline fixes (catch-22) — needs an out-of-band substrate repair. Siblings: RESILIENT-165 (bot-merge silent-wedge), RESILIENT-167 (keeper/autopilot won't revive pool), CREDIBLE-146 (auth-cache false-positive).

