- id: META-095
  domain: META
  title: Wizard strategic backlog — docs/process/WIZARD_STRATEGIC_BACKLOG.md as durable surface for loop-slack work selection
    (replaces chat-text tracking; updated by wizard each iter; ranked by ring + leverage)
  status: done
  priority: P1
  effort: xs
  acceptance_criteria:
  - 'Background — when the wizard is in a /loop iter and has slack between PR-pulse cycles (60-300s of wait per 5min cron),
    there''s a recurring question: ''what else would drive value right now?'' Today that answer lived only in chat (operator
    caught it; one-off response). Without a durable surface, the same question gets re-asked every session and the answer
    drifts. META-089 (OPERATOR_PLAYBOOK.md) describes HOW the wizard works; this backlog describes WHAT-NEXT specifically
    when there''s loop slack'
  - 'New file docs/process/WIZARD_STRATEGIC_BACKLOG.md — 4 sections ranked by leverage: (1) Highest = Retirement work (META-088/INFRA-1898/META-090
    — work that ends the wizard role), (2) High = Command durability (playbook updates, tool promotions, curator lane briefs),
    (3) Medium = Preventer gaps + pillar inventory + registry audits, (4) Low/Anti = explicit skip list'
  - Each item must include — gap-id (if exists), leverage rationale (why this and not others), concrete next action (≤3 commands
    or equivalent), done-signal (how to know it's complete)
  - Wizard discipline — at every loop iter where pulse returns HEALTHY OR keystone-merged-waiting-on-curators, wizard reads
    the top of this file and pulls the highest-ranked item with a concrete next-action. After ship — cross item off and insert
    any new item discovered
  - Operator review hook — at end of each session, operator can grep this file for changes vs. start-of-session to see what
    strategic work landed (vs. just PR rescue noise)
  - Done definition — file exists with 4 ranked sections, ≥3 items per section, and wizard has executed top item at least
    once in subsequent loop iters
  notes: filed 14:55Z 2026-05-24 by wizard responding to operator directive 'put strategic answer into action plan, execute
    it'; turns chat-text strategy into durable artifact
  closed_date: '2026-05-24'
  closed_pr: 2496
