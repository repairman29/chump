- id: INFRA-719
  domain: INFRA
  title: "RESILIENT: eliminate manual gh-pr-create ship path — wrap or block direct PR creation for gap branches; force every shipper through bot-merge.sh so gap-ship-fatal (INFRA-678) always fires."
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - either (a) git pre-push hook detects gh-pr-create for chump/<gap-id>-* branches and refuses unless CHUMP_BYPASS_BOT_MERGE=1, OR (b) bot-merge.sh --quick subcommand that does ONLY chump-gap-ship + auto-merge-arm in <30s — making manual path strictly slower
    - "test: scripts/ci/test-no-manual-ship-bypass.sh"
    - operator + Opus stop bypassing the auto-close path
  closed_date: '2026-05-10'
  closed_pr: 1417

