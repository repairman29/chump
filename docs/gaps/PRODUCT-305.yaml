- id: PRODUCT-305
  domain: PRODUCT
  title: "[smuggler][external_repo:repairman29/smuggler][gcp-exit] Replace firebase-admin sessions/presence + token verify with Supabase"
  status: open
  priority: P2
  effort: s
  description: |
    playsmuggler.com already hosts on Vercel. server/firebaseConfig.js uses firebase-admin for the sessions and presence collections and ID-token verification (server/routes/apiSubscriptions.js, server/socket/handlers/session.js); service-account creds read in server/config.js and routes/admin.js. Projects playsmuggler-b1cda / smuggler-d1b4a. Target stack per workspace-docs/GCP_EXIT.md: Vercel (hosting + API routes/functions) + Supabase Pro (dedicated project per app: Postgres, Auth, Storage). Keep Sign in with Google (Supabase Google provider; OAuth client stays in its GCP project, no billing needed). Depends on PRODUCT-303 export. Firebase email/password users must keep working: import users from the auth:export JSON and verify Firebase scrypt hashes on first sign-in (Supabase firebase-to-supabase pattern), or document why a reset flow was required. Secrets move from GCP Secret Manager to Vercel env vars.
  acceptance_criteria:
    - sessions and presence live in Supabase tables (or Supabase Realtime presence) with parity tests
    - Socket session handler verifies Supabase JWTs; firebase-admin removed from package.json
    - No GOOGLE_APPLICATION_CREDENTIALS / service-account JSON referenced anywhere in the repo
    - playsmuggler.com login + session flow verified on a preview deploy
  depends_on: [PRODUCT-303]
  skills_required: "external_repo:repairman29/smuggler"

