- id: META-179
  domain: META
  title: "RESILIENT: trunk-red-detector daemon — 5-min poll WARN broadcast (META-177 Lane C sub-gap)"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "TODO: what events emitted on success/failure/timeout"
    - "TODO: how cost tracked and reported to operator"
    - "TODO: failure-class taxonomy (distinguish transient vs permanent)"
    - "TODO: smoke test command to verify observability"
  notes: |
    Duplicate of META-177 Lane C, already shipped as INFRA-2324 trunk-sentinel-daemon.sh (PR #2916, merged 2026-05-31). Confirmed still wired: launchd plist ~/Library/LaunchAgents/com.chump.trunk-sentinel.plist runs scripts/coord/trunk-sentinel-daemon.sh every 60s, covered by scripts/ci/test-trunk-sentinel.sh + test-trunk-loop-e2e.sh. Emits kind=trunk_state_change/trunk_red_persistent/trunk_red_dispatch/trunk_red_operator_recall/trunk_recovered/trunk_sentinel_tick per scanner-anchors in the script. No new code needed; AC placeholders were never filled in because META-177 already shipped this exact capability before META-179's sub-gap was picked up.
  closed_pr: 2916
  outcome_id: MISSION-010

