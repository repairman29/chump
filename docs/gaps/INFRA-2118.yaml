- id: INFRA-2118
  domain: INFRA
  title: "RESILIENT P1 (CI-REVIEW Lever 2): per-PR CARGO_TARGET_DIR + registry/src isolation — one PR's corruption stops poisoning the next"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "1. .github/workflows/ci.yml sets CARGO_TARGET_DIR per-job to a unique-per-PR + per-job path (e.g. /tmp/cargo-target-pr\\${PR_NUM}-\\${JOB_NAME}) instead of the shared runner-wide /Users/jeffadkins/.cache/chump-runner/cargo-target\n2. registry/src/ extraction race (Swatinem cache restore + parallel cargo invocations) mitigated via per-job cargo fetch --locked pre-step before parallel test runs\n3. After 14-day observation, `cache_drift` events drop from 618 to <100; cascade events (`pr_stuck_cluster`) drop from 56 to <10\n4. sccache+R2 hit rate maintained (per-job cache is just for incremental compile artifacts; sccache layer is per-rustc-call and unaffected)\n5. Source: docs/strategy/CI_REVIEW_2026-05-29.md Lever 2\n6. Depends-on or pairs-with INFRA-2117 (containerize) — these compose"
  closed_date: '2026-05-29'
  closed_pr: 2720
  outcome_id: MISSION-010

