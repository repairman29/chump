- id: INFRA-756
  domain: INFRA
  title: "EFFECTIVE: chump gap reserve adds 4 observability ACs by default — what events emitted, how cost tracked, failure-class taxonomy, smoke test command; chump gap show flags gaps with incomplete obs ACs so picker downranks them. Forces observability into design phase."
  status: done
  priority: P1
  effort: xs
  acceptance_criteria:
    - "src/main.rs subcommand 'chump gap reserve' adds 4 obs-AC placeholders to every new gap (template strings in --acceptance-criteria flag): obs-1 events emitted; obs-2 cost tracked; obs-3 failure-class; obs-4 smoke test"
    - new flag --skip-obs-acs for legacy/cleanup gaps
    - chump gap show prints WARN if any obs-AC contains template-placeholder text 'TODO
    - TBD
    - <fill in>'
    - picker (_pick_and_claim_gap.py) downranks gaps with incomplete obs-ACs by score offset 5 (still pickable but lower priority within same priority bucket)
  closed_date: '2026-05-08'
  closed_pr: 1345

