- id: INFRA-3459
  domain: INFRA
  title: "EFFECTIVE: the CHEAP fleet consumes comprehension — embed the comprehend summary into build_free_tier_prompt (free-tier execute_gap has system_prompt=None, so INFRA-3458's context_assembly directive never reaches it)"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"the CHEAP fleet consumes comprehension — embed the comprehend summary into build_free_tier_prompt (free-tier execute_gap has system_prompt=None, so INFRA-3458's context_assembly directive never reaches it)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-28'
  closed_pr: 3357
  outcome_id: MISSION-010

