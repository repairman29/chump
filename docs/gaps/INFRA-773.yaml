- id: INFRA-773
  domain: INFRA
  title: 'EFFECTIVE: Review-as-Handoff sub-gap 5/6 — ambient telemetry + fleet-brief integration. Five new kinds: review_handoff_initiated|applied|failed|timeout|escalated.
    Register per INFRA-754. Consumers: fleet-brief surfaces ''N PRs self-healed today''; kpi-report computes (applied)/(initiated)
    self-heal rate; waste-tally computes (failed+timeout)/(initiated) reviewer-error rate. Spec: docs/architecture/REVIEW_AS_HANDOFF.md
    §7. Depends on: INFRA-768.'
  status: done
  priority: P1
  effort: xs
  acceptance_criteria: 'Criterion 1: Five new ambient event kinds (`review_handoff_initiated`, `review_handoff_applied`, `review_handoff_failed`,
    `review_handoff_timeout`, `review_handoff_escalated`) are registered per INFRA-754 conventions

    Criterion 2: `fleet-brief` surfaces a "N PRs self-healed today" metric consuming these events

    Criterion 3: `kpi-report` computes a self-heal rate = `applied / initiated`

    Criterion 4: `waste-tally` computes a reviewer-error rate = `(failed + timeout) / initiated`

    Criterion 5: All consumers handle zero-event states without errors

    '
  closed_date: '2026-05-11'
  closed_pr: 1471
