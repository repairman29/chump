- id: EFFECTIVE-337
  domain: EFFECTIVE
  title: "chump bootstrap: create --dir if missing (unblocks CREATE bench laps + kills mkdir footgun)"
  status: done
  priority: P2
  effort: xs
  acceptance_criteria:
    - "The change described by \"create --dir if missing (unblocks CREATE bench laps + kills mkdir footgun)\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3425

