- id: INFRA-3524
  domain: INFRA
  title: "RESILIENT: pre-push clippy gate (INFRA-1390) also double-runs for bot-merge pushes → cold-ring rebuild blows 300s push budget (INFRA-3521 follow-on)"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"pre-push clippy gate (INFRA-1390) also double-runs for bot-merge pushes → cold-ring rebuild blows 300s push budget (INFRA-3521 follow-on)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3415

