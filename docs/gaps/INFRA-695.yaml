- id: INFRA-695
  domain: INFRA
  title: "ZERO-WASTE: state.sql git history bloat — 416 versions ~416MB of blobs, move to .gitignore or manage via migrations only"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - ".chump/state.sql either (a) added to .gitignore + .chump/state.sql.lz4 committed (lz4-compressed 50:1) OR (b) moved to git LFS"
    - history rewrite NOT required (forward-only)
    - state.sql still regeneratable via 'chump gap dump --out .chump/state.sql' for human review
    - repo size .git/ shrinks by >300MB after rotation policy kicks in
  closed_date: '2026-05-08'
  closed_pr: 1306

