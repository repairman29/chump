- id: INFRA-733
  domain: INFRA
  title: "EFFECTIVE: chump-local backend tool-call adapter for free-tier LLMs (Groq/Cerebras Llama 3.3 70B etc) — pilot today: 0 PRs shipped over 30min, 5 consecutive rc=1, worker_circuit_open. Agent loop's tool-call protocol assumes Anthropic-style; non-Anthropic models need adapter (parse JSON tool-call from response text, or use OpenAI-compat function-calling format properly)."
  status: open
  priority: P1
  effort: m

