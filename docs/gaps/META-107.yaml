- id: META-107
  domain: META
  title: 'WIZARD-AS-DAEMON Phase 2: steps 3+4+5 — real-fails URGENT-INBOX, pickable-gap dispatch, cascade rebase'
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
  - 'Step 3 — for BLOCKED+real-fails PRs: classify failure kind using wedge catalog; if W-NNN pattern matched emit wedge_detected
    (wedge-state-machine picks up); if unknown pattern emit URGENT-INBOX message with author login and failing check names'
  - 'Step 4 — for each pickable gap (P0/P1, no active lease, AC complete, not in stop-list): dispatch via chump --execute-gap
    <ID> in background; rate-limited to CHUMP_WIZARD_MAX_PARALLEL concurrent (default 4); emits wizard_daemon_action with
    step=step4, decision=gap_dispatched|rate_limited|no_pickable_gaps'
  - 'Step 5 — after a recovery-queue cycle resolves a cluster: for OTHER PRs that were BLOCKED+cascading on the same trunk-RED,
    trigger gh pr update-branch or re-arm auto-merge; emits wizard_daemon_action with step=step5, decision=cascade_rebase_triggered|skipped'
  - All three steps guarded by the existing Phase 1 safety checks (HOT-temp refusal, CONFLICTING-PR refusal, fleet-hold stand_down)
  - 'Audit: steps 3/4/5 each emit kind=wizard_daemon_action with correct step/target/decision/rate_limit_state fields'
  - Smoke test scripts/ci/test-wizard-daemon.sh extended with cases for steps 3, 4, 5 (stub gh + chump CLIs); 100% pass required
  - Depends on META-109 Phase 1 being merged and stable in production for at least one full recovery cycle before Phase 2
    is enabled
  depends_on:
  - - META-109
  description: "Phase 2 of the wizard-as-daemon DRIVE primitive (META-109).\nPhase 1 shipped steps 1, 2, 6 + safety guards\
    \ (default-OFF).\nThis gap adds the three deferred steps:\n  - Step 3: real-fails triage (wedge catalog lookup → URGENT-INBOX)\n\
    \  - Step 4: autonomous gap dispatch (chump --execute-gap background)\n  - Step 5: cascade rebase after a cluster resolves\n\
    Prerequisite: Phase 1 must be validated in production (at least one\nfull recovery-queue cycle observed) before Phase\
    \ 2 is enabled.\n"
  closed_date: '2026-05-25'
  closed_pr: 2630
