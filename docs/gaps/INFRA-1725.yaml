- id: INFRA-1725
  domain: INFRA
  title: "RESILIENT: enforce index-only repository reads for subagents — sandbox file-access via MCP tool budget so subagents cannot dump full worktrees into context"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "TODO: what events emitted on success/failure/timeout"
    - "TODO: how cost tracked and reported to operator"
    - "TODO: failure-class taxonomy (distinguish transient vs permanent)"
    - "TODO: smoke test command to verify observability"

