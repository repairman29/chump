- id: INFRA-2275
  domain: INFRA
  title: "EFFECTIVE: chump onboard --schedule plist installer (INFRA-2268 sub-slice 1/3) — generate + load + unload per-repo launchd plist"
  status: done
  priority: P1
  effort: xs
  description: |
    INFRA-2268 sub-slice 1/3 — plist installer. Sister: INFRA-2276 (worker loop), INFRA-2277 (observability+state+test). Filed 2026-05-30 from APPROACH_AUDIT_2026-05-30.md Angle 3 recommendation to pre-decompose INFRA-2268 (10-AC M-effort scheduler gap → 3 PRs).
  acceptance_criteria:
    - chump onboard --schedule <repo-path> generates plist from scripts/plists/com.chump.external-repo-loop.plist.template with per-repo unique label (com.chump.external-repo.<sanitized-path>); loads via launchctl on success
    - chump onboard --unschedule <repo-path> unloads plist + removes from list-scheduled output
    - chump onboard --list-scheduled [--json] shows all scheduled repos (label, last-iter-ts placeholder, plist-loaded bool)
    - "Two new ambient kinds registered in docs/observability/EVENT_REGISTRY.yaml: kind=external_repo_scheduled (fields: repo_path, plist_label) and kind=external_repo_unscheduled (fields: repo_path, reason); both effect_metric set"
    - "Smoke test scripts/ci/test-external-repo-plist-installer.sh: <30s, creates tmp repo, runs --schedule, asserts launchctl list shows plist, runs --unschedule, asserts removed; --list-scheduled output matches"
    - chump preflight runs smoke when src/onboard.rs OR scripts/plists/com.chump.external-repo-loop.plist.template touched
  closed_date: '2026-06-05'
  closed_pr: 3047
  outcome_id: MISSION-010

