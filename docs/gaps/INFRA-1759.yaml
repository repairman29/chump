- id: INFRA-1759
  domain: INFRA
  title: "EFFECTIVE: chump-coord call_rpc + serve_rpc API stubs + request_id dedup (META-061 Layer 2b slice 1/4, INFRA-1119 foundation)"
  status: open
  priority: P1
  effort: xs
  acceptance_criteria:
    1. New pub async fn call_rpc(target_session: &str, method: &str, args: serde_json::Value, timeout_ms: u64) -> Result<Response> in crates/chump-coord/src/rpc.rs (stub returns NotImplemented + emits ambient a2a_rpc_stub_called)
    2. New pub async fn serve_rpc(method: &str, handler: Box<dyn Fn(Value) -> Result<Value> + Send>) -> Result<()> stub
    3. pub struct RpcRequest {request_id: String, method, args, sent_at} + RpcResponse {request_id, result, error, latency_ms}
    4. request_id is uuid v4; server-side dedup table is a tokio Mutex<HashMap<String, Instant>> with 60s expiry — implemented as in-memory stub here
    5. Register kinds: a2a_rpc_stub_called in EVENT_REGISTRY.yaml
    6. Integration test stub at crates/chump-coord/tests/a2a_layer2b.rs
    7. Sets up Layer 2b foundation; the 5 use-case wrappers (ask-eta/ask-overlap/ask-handoff/ask-progress/ask-capability) ship in slice 2
