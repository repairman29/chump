- id: INFRA-3379
  domain: INFRA
  title: preflight-mirror gap/state-registry consistency gates (~20 scripts, META-086 cluster 1/5)
  status: open
  priority: P1
  effort: m
  description: |
    Mirror the ~20 unmirrored scripts/ci/test-*.sh gates covering gap/state-registry consistency (gap-id uniqueness/lease/fuzzy-match/reserve races, docs-delta trailer validation, ambient-schema, schema-version-assert, migration-pipeline-gates, model-registry, raw-yaml-guard, credential-pattern-guard, submodule-guard, git-identity-guard, hardcoded-date-guard) into src/preflight.rs so 'chump preflight' catches these locally instead of only in CI. Full inventory: docs/process/AUDIT_JOB_DECOMPOSITION.md cluster 'gap-state'.
  acceptance_criteria:
    - Every script listed under the gap-state cluster in docs/process/AUDIT_JOB_DECOMPOSITION.md is invoked from src/preflight.rs (same pattern as existing mirrored scripts)
    - chump preflight exits non-zero when any of these gates would fail, matching CI behavior
    - scripts/ci/preflight-ci-parity-exceptions.txt has no entries for scripts in this cluster (either mirrored or Tier-D documented in CI_GATES_INVENTORY.md)
    - cargo fmt/clippy/check pass; scripts/ci/test-preflight-ci-parity.sh passes

