- id: INFRA-2290
  domain: INFRA
  title: 'ZERO-WASTE: ci.yml dependency surface audit (META-177 lane B) — catalog all external deps, identify broken/unverifiable
    ones'
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
  - docs/audits/CI_DEPENDENCY_AUDIT_2026-05-30.md exists with a structured markdown table covering every external dependency
    found in ci.yml and the scripts it invokes
  - 'Each table row has: dependency, file-line, category (rustup/cargo-install/apt/curl-bootstrapper/binary-check/external-service),
    verified-status (works/broken/unverifiable), evidence'
  - For each broken finding an INFRA gap is filed via chump gap reserve with a fix proposal in the description
  - An ambient event kind=meta177_lane_b_done is emitted to ambient.jsonl on completion with PR number and counts
  closed_date: '2026-06-05'
  closed_pr: 2864
