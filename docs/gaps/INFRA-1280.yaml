- id: INFRA-1280
  domain: INFRA
  title: 'EFFECTIVE: PWA state-persistence + operator-toggle pass — view, filters, theme, sidecar, stream pause, budget thresholds
    across reload'
  status: done
  priority: P1
  effort: m
  description: 'Sub-gap 1 (view persistence / URL routing / history pushState) is DEFERRED to existing gap PRODUCT-097 (PWA
    client-side routing). PRODUCT-097 was filed earlier covering exactly that scope. This gap (1280) tracks the remaining
    9 sub-gaps (queue filters / events filters / theme toggle / sidecar collapse / timeline auto-collapse / cost thresholds
    / stream pause / Settings scaffold / scroll restore) plus the cross-cutting AC (storage schema doc, telemetry, a11y, mobile,
    test coverage). When PRODUCT-097 lands, Sub-gap 2 (queue filter URL params) becomes implementable.

    '
  acceptance_criteria:
  - 'TODO: what events emitted on success/failure/timeout'
  - 'TODO: how cost tracked and reported to operator'
  - 'TODO: failure-class taxonomy (distinguish transient vs permanent)'
  - 'TODO: smoke test command to verify observability'
  closed_date: '2026-05-14'
  closed_pr: 1934
