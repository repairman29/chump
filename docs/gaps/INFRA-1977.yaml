- id: INFRA-1977
  domain: INFRA
  title: "HIGH: chump binary staleness gate forces routine bypass; JIT refresh missing (critique H8)"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "Failure: INFRA-825 binary staleness gate refuses destructive `chump gap` ops when binary >N commits behind. Override is CHUMP_ALLOW_STALE_DESTRUCTIVE=1. I used the bypass 4x in 24h. Rebuild is multi-minute via `cargo install --path . --bin chump --force` — too slow for in-loop use."

