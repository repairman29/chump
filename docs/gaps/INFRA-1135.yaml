- id: INFRA-1135
  domain: INFRA
  title: "EFFECTIVE: mirror INFRA-1132 cache+criticality conventions into AGENTS.md (canonical tool-agnostic)"
  status: done
  priority: P1
  effort: xs
  description: |
    After INFRA-1132 added the three new architecture sections to CLAUDE.md (the Chump-specific overlay), AGENTS.md (the canonical tool-agnostic doctrine) is still silent on the new cache + criticality conventions. Sonnet workers spawned via opencode, Gemini reviewer invocations, and any non-Claude-Code agent read AGENTS.md but not CLAUDE.md — they won't see the new patterns. This gap propagates the same content to AGENTS.md so every agent harness gets it. xs because pure doc edits to existing files; ships fast via INFRA-1042 doc-only fastpath.
  acceptance_criteria:
    - "AGENTS.md gains the same three sections INFRA-1132 added to CLAUDE.md: Cache-first reads (INFRA-1081), Call criticality (INFRA-1080), GraphQL exhaustion handling (INFRA-1040/1079)"
    - content phrased tool-agnostically — no Claude-Code-specific references — since AGENTS.md is read by Sonnet via opencode, Gemini reviewer, ACP clients, and any other agent harness
    - scripts/ci/test-doc-cache-architecture.sh extended to assert the same sections exist in AGENTS.md (not just CLAUDE.md)
    - "insertion point: after the existing \"Hard rules\" or equivalent operational-doctrine section, mirroring CLAUDE.md placement"
  closed_date: '2026-05-14'
  closed_pr: 1798

