- id: INFRA-3517
  domain: INFRA
  title: "RESILIENT: CI test-*.sh resolve chump bin via cargo metadata (config.toml build.target-dir breaks preflight locally)"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"CI test-*.sh resolve chump bin via cargo metadata (config.toml build.target-dir breaks preflight locally)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3399

