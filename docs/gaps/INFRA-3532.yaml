- id: INFRA-3532
  domain: INFRA
  title: "Multi-slice branch-name reuse: chump claim reuses chump/<gap-id>-claim, poisoned by prior slice's merged PR (GitHub auto-deletes; gh pr create returns the old merged PR); shipping slice-2+ of a gap needs a fresh branch + manual PR"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"chump claim reuses chump/<gap-id>-claim, poisoned by prior slice's merged PR (GitHub auto-deletes; gh pr create returns the old merged PR); shipping slice-2+ of a gap needs a fresh branch + manual PR\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-07'
  closed_pr: 3466

