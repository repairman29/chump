- id: INFRA-672
  domain: INFRA
  title: "ZERO-WASTE: lint-storm auto-relax — when 3+ open PRs fail clippy on the same lint name within 1h, file an INFRA gap to relax it at workspace.lints.clippy. Eliminates the manual INFRA-664/668 wave-by-wave cycle."
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "new scripts/ops/lint-storm-detector.sh runs hourly via launchd; greps last 1h of ambient.jsonl for clippy failures, parses lint name from -D clippy::<name> messages"
    - "when same lint name appears in 3+ distinct PR runs, files INFRA gap titled 'ZERO-WASTE: relax <lint_name> at workspace.lints.clippy' P0 effort xs"
    - de-dups by checking for existing open gap with same lint_name in title
  closed_date: '2026-05-08'
  closed_pr: 1344

