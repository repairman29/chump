- id: INFRA-1273
  domain: INFRA
  title: 'A2A — post-ship retro hook: prompt agent for ''what did not fit?'' on every chump gap ship'
  status: done
  priority: P2
  effort: s
  acceptance_criteria:
  - 'scripts/coord/bot-merge.sh: at successful close, emit a one-liner prompt to stdout pointing at scripts/coord/broadcast.sh
    FEEDBACK retro <gap> "..." for retro logging'
  - 'If running under a harness with stdin-prompt capability (claude -p, opencode, etc.): emit a structured prompt the harness
    can route to the agent'
  - 'Per-session opt-out: CHUMP_NO_RETRO_PROMPT=1'
  - 'Test: run bot-merge dry-run, assert the prompt appears on stdout'
  notes: '[2026-05-14T18:13:24Z] Most useful feedback arrives right after shipping when the friction is fresh. Without a prompt
    the agent moves on and the lesson is lost.

    '
  closed_date: '2026-05-14'
  closed_pr: 1929
