- id: INFRA-740
  domain: INFRA
  title: 'RESILIENT: tool-call normalizer for weak/local LLMs that emit malformed JSON'
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
  - Normalizer layer between streaming parser (local_openai.rs) and tool_runner.rs that repairs common malformed tool-call
    patterns
  - 'Handles: JSON with trailing commas or missing closing braces; tool calls as markdown code blocks; hallucinated tool names
    mapped to nearest registered tool'
  - Normalizer fires only when raw parse fails — zero overhead on well-formed responses
  - Reduces qwen3:8b patch_file parse-loop failures from 25 iterations to <=3
  - 'Telemetry: emits kind=tool_normalize to ambient.jsonl with original vs repaired diff'
  closed_pr: 1602
