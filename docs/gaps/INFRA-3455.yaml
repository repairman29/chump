- id: INFRA-3455
  domain: INFRA
  title: "RESILIENT: bot-merge ship-path — stop EXIT-trap deleting the lease on recoverable exits + reconcile stale CHUMP_SESSION_ID (kills the operator-claim --no-verify friction)"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"bot-merge ship-path — stop EXIT-trap deleting the lease on recoverable exits + reconcile stale CHUMP_SESSION_ID (kills the operator-claim --no-verify friction)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3354
  outcome_id: MISSION-010

