- id: INFRA-1904
  domain: INFRA
  title: "CI blocker: audit failing on 10+ open PRs"
  status: closed
  priority: P1
  effort: s
  description: |
    Shared CI-blocker detected by stuck-pr-filer (2026-05-24T00:58:57Z).
    
    Failing CI step:  audit
    Script path:      unknown
    Affected PRs:     10 (#2482 #2466 #2464 #2460 #2455 #2449 #2448 #2438 #2436 #2428)
    Oldest failure:   73h
    
    Recent changes to script on origin/main:
    (script path not identified from check name: audit)
    
    Suggested action:
      1. Check whether origin/main already fixed this test (git log above).
      2. If yes: bulk-rebase affected PRs so they pick up the fix.
      3. If no: fix the script and ship a patch PR first.
      4. Once root cause resolved, re-arm affected PRs via bot-merge.sh.
  acceptance_criteria:
    - "All 10 PRs cited in the original CI-blocker report (#2482 #2466 #2464 #2460 #2455 #2449 #2448 #2438 #2436 #2428) are verified closed/merged via gh pr view"
    - "A scan of all currently open PRs' statusCheckRollup shows zero failing checks with 'audit' in the name"
    - "Gap closed with notes documenting the verification and reason (stale ghost gap, root cause self-resolved)"
  outcome_id: MISSION-010
  notes: |
    stale-ghost-gap: filed 2026-05-24 by stuck-pr-filer citing 10 open PRs
    (#2482 #2466 #2464 #2460 #2455 #2449 #2448 #2438 #2436 #2428) failing an
    'audit' CI check. Verified 2026-08-13: all 10 cited PRs are closed/merged,
    and a scan of all currently open PRs' statusCheckRollup shows zero failing
    checks with 'audit' in the name. Root cause self-resolved (or affected PRs
    were rebased/closed) long before this session; no reproducible CI blocker
    remains. Closed as not-a-bug rather than fabricating an implementation
    against a no-longer-existing failure.

