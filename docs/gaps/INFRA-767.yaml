- id: INFRA-767
  domain: INFRA
  title: "RESILIENT: mirror critical pre-commit guards to CI — every guard with a CHUMP_*_CHECK=0 bypass should ALSO run as a CI fast-checks step (no bypass). Pre-commit is local + bypassable; CI is the strict branch-protection gate. Apply to: lease-collision (agent_lease), gaps.yaml-write-discipline (CHUMP_GAPS_LOCK), credential-pattern (CHUMP_CREDENTIAL_CHECK), event-registry (INFRA-754), obs-budget (INFRA-755). The --no-verify culture means agents that don't care already bypass everything; CI catches the actual ship."
  status: open
  priority: P2
  effort: s

