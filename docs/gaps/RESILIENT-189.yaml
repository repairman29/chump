- id: RESILIENT-189
  domain: RESILIENT
  title: "provisioner: set CHUMP_REPO in env + bound the hanging validation dry-run"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"set CHUMP_REPO in env + bound the hanging validation dry-run\" is implemented in the relevant RESILIENT code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3263

