- id: RESILIENT-150
  domain: RESILIENT
  title: "RESILIENT: INFRA-2407 bypass-trailer validator matches 'bypass' substring (prose + 12 sanctioned single-line trailers) → false-blocks clean pushes; trigger only on Bypass-Tier/Class/Reason/Followup schema keys"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - commit-msg-bypass-trailers.sh + pre-push-bypass-trailers.sh trigger the 4-trailer (Bypass-Tier/Class/Reason/Followup) requirement ONLY when a ^Bypass-(Tier
    - Class
    - Reason
    - "Followup): line is present — NOT on any 'bypass' substring."
    - A commit whose body merely mentions bypass/bypasses in prose passes.
    - "A commit using a sanctioned single-line trailer (Rust-First-Bypass:/Test-Gate-Bypass:/Off-Rails-Bypass:/etc.) WITHOUT the 4-schema passes (those have their own validators)."
    - A commit that uses any Bypass-Tier/Class/Reason/Followup key but is missing one of the 4 still BLOCKS.
    - test-bypass-trailer-validator.sh updated with prose-pass + sanctioned-trailer-pass + partial-schema-block cases; all green.

