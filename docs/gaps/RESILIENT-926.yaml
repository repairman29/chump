- id: RESILIENT-926
  domain: RESILIENT
  title: "RESILIENT: Add input validation to widget intake to reject malformed rows (RESILIENT-241 slice)"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    1. All incoming rows are validated against the approved regex pattern.
    2. Malformed rows result in HTTP 400 Bad Request with an error message.
    3. Invalid rows are logged for audit purposes.
    4. Valid rows continue to the autonomous work queue unchanged.
  notes: |
    [chump harvest check 'holler']
    === primitives_index match for 'holler' ===

    === cluster keyword match for 'holler' ===
      cluster misc (28 repos): workspace-docs, almanac, games-workspace, machine-substrate, grave-dancer, jeffadkins-dev, holler, privateer, opportunity-library, posse, realm-of-shadows, upshift-cli, space-shooter, crystal-rush, inversion, roblox-game-manager, kosmos, fulcrum, okr, project-2026-case, pixi-game, jeffadkins-me, bulwark, choose, derelict, registry, project-forge, project_forge

    === extracted_primitives (per-file, line-refd) match for 'holler' ===

    === repo-description match for 'holler' ===

    === HARVEST_ROADMAP.md mention of 'holler' (deep-scan findings) ===

    === cross-pollination briefs mentioning 'holler' ===

    [2026-09-11] Shipped as repairman29/holler PR #2 (external_repo gap): https://github.com/repairman29/holler/pull/2 — adds a BEFORE INSERT trigger (feedback_events_validate_row) validating project/session_id/dedupe_hash/reporter against an approved identifier pattern (^[A-Za-z0-9._-]+$), since those fields are used as literal keys downstream (indexes, the RESILIENT-925 rate limiter's buckets, dedupe grouping). Rejected rows are logged to a new holler_malformed_row_log table (project, kind, reason, raw_session_id, raw_reporter) before the trigger raises, which PostgREST maps to HTTP 400 — same default the table's existing CHECK constraints already produce. Valid rows return unchanged. README documents the new contract.
    [2026-09-11] Closed via chump gap set (not chump gap ship): the actual code landed as repairman29/holler PR #2 (merged), a different repo entirely — chump's own proof-of-merge guard only scans chump's local main for the gap ID. closed_pr=2 refers to that external PR number, not a chump PR.
  closed_pr: 2
  skills_required: "external_repo:repairman29/holler"
  outcome_id: RESILIENT-000
