- id: INFRA-2036
  domain: INFRA
  title: "RESILIENT P1: toolchain-ratchet wedge class — main goes clean at merge but blocks subsequent PRs after rust 1.95.0 / strict-fmt / strict-clippy refresh (same class as THE FLOOR strict-mode flip)"
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - When main is clean at merge but a toolchain bump (e.g. rust 1.95) makes subsequent PRs fail, the wedge class is detected and an auto-fix gap is filed to pin/update the toolchain; subsequent PRs pass without manual intervention.
  outcome_id: MISSION-010

