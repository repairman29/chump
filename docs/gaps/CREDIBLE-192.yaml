- id: CREDIBLE-192
  domain: CREDIBLE
  title: "SPIRIT judge (CREDIBLE-191 job 4, the keystone): an LLM lens that scores 'does this diff GENUINELY achieve the intent — not a stub, not teaching-to-the-test, not a hack?' given (task/AC + the diff). Reuses chump llm-complete (shared cascade), like code-reviewer-agent but a spirit lens. Would have caught the photo-renamer stub + the false-green. First slice: advisory spirit-verdict in the bench scorecard (alongside letter/correctness); then gate on it. This is the missing half of letter-vs-spirit"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"an LLM lens that scores 'does this diff GENUINELY achieve the intent — not a stub, not teaching-to-the-test, not a hack?' given (task/AC + the diff). Reuses chump llm-complete (shared cascade), like code-reviewer-agent but a spirit lens. Would have caught the photo-renamer stub + the false-green. First slice: advisory spirit-verdict in the bench scorecard (alongside letter/correctness); then gate on it. This is the missing half of letter-vs-spirit\" is implemented in the relevant CREDIBLE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_pr: 3454

