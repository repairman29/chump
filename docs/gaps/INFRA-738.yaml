- id: INFRA-738
  domain: INFRA
  title: "EFFECTIVE: fleet default to chump-local when no ANTHROPIC_API_KEY"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - run-fleet.sh defaults FLEET_BACKEND=chump-local when ANTHROPIC_API_KEY is unset/empty
    - run-fleet.sh defaults FLEET_BACKEND=claude when ANTHROPIC_API_KEY is set (preserves current behavior)
    - "worker.sh chump-local path passes a 3-gap smoke run with Ollama (qwen2.5:7b) — at least 1 PR ships"
    - chump start (roadmap Week 1 front door) works on a machine with only Ollama installed

