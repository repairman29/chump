- id: INFRA-2754
  domain: INFRA
  title: "RESILIENT: pr-stuck-cluster — 2 PRs blocked >2h as of 2026-06-05"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - "1. pr-stuck-cluster-detector.sh emits kind=pr_stuck_cluster_detector_run ambient event on every invocation (no-op, cluster-filed, and error paths) with fields: outcome, stuck_pr_count, duration_ms.\n2. Run event includes gap_reserve_calls field so mutation cost (chump gap reserve invocations) is visible to the operator without grepping logs.\n3. Error paths distinguish failure class via the outcome field: gap_reserve_failed (permanent — chump gap reserve returned nonzero) vs gap_id_extract_failed (transient — reserve succeeded but ID unparseable from output).\n4. scripts/ci/test-pr-stuck-cluster-observability.sh smoke-tests that the ambient event fires for the no-stuck-events and cluster-detected paths; runnable standalone."
  closed_date: '2026-07-20'
  closed_pr: 3221
  outcome_id: MISSION-010

