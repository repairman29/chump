- id: INFRA-955
  domain: INFRA
  title: "ZERO-WASTE: slo_breach events carry no detail — name the breached SLO + dedupe continuous breaches"
  status: done
  priority: P2
  effort: s
  acceptance_criteria:
    - "scripts/coord/opus-curator.sh: when emitting kind=slo_breach, include a specific `slo_name` field (which SLO breached) and `value`/`threshold` fields"
    - "EVENT_REGISTRY.yaml entry for slo_breach updated with required fields_required: [ts, kind, severity, slo_name, value, threshold]"
    - "Dedupe: emit only on EDGE (transition into-breach or out-of-breach), not on every audit while in continuous breach. Track in fleet-state.json: `slo_breached_at`."
    - "Test: scripts/ci/test-slo-breach-dedupe.sh runs curator twice with synthetic in-breach state; second run should NOT emit slo_breach event."
    - "Why: META-055 #3 finding. 3 events × ~9k tokens = 27k tokens (7.7%) per 7d. Continuous-breach re-emission inflates the cost without adding info."
    - "Depends-on: INFRA-848 (curator decision logging — defines the existing slo_breach emitter site)."
  depends_on: [INFRA-951, META-055]
  closed_date: '2026-05-13'
  closed_pr: 1642

