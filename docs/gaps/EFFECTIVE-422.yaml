- id: EFFECTIVE-422
  domain: EFFECTIVE
  title: "VOICE-ADVISOR: voice talking path to ChumpOS via Siri Shortcut reusing /api/chat agent loop"
  status: done
  priority: P2
  effort: m
  description: |
    Voice advisor: talk to ChumpOS by voice (Hey Siri, Ask Chump <question>) and get a spoken answer grounded in almanac_search. Reuses Olive's proven Siri-Shortcut voice-ask shape routed through Chump's existing /api/chat agent loop (which already has almanac_search as a tool via src/almanac_tool.rs). Add voice:bool to ChatRequest (web_server.rs:62), append VOICE_ADDENDUM to chump_system_prompt when voice, factor the run-one-turn body of handle_chat into a helper, add /api/voice/ask route that drains stream_events AgentEvent::TurnComplete.full_text. Brief: docs/design/VOICE_ADVISOR.md.
  acceptance_criteria:
    - "The change described by \"voice talking path to ChumpOS via Siri Shortcut reusing /api/chat agent loop\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-09'
  closed_pr: 3535

