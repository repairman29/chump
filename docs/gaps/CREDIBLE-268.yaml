- id: CREDIBLE-268
  domain: CREDIBLE
  title: merging one PR marked THREE unrelated P1 gaps done with its closed_pr — work that was never started is now terminal
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "MECHANISM FOUND AND VERIFIED ON MAIN — no longer a hypothesis. scripts/ops/github-webhook-receiver.py: _extract_gap_ids (line 161) runs regex \\b([A-Z][A-Z-]+-\\d+)\\b over BOTH title AND body; _auto_flip_gaps_done (line 243, CREDIBLE-092) then runs `chump gap set <ID> --status done --closed-pr <N>` for EVERY id it found, on any merged PR. The receiver is RUNNING (1 process confirmed). That is the whole bug"
    - "IT EXPLAINS THE TWO THINGS THAT PREVIOUSLY DID NOT ADD UP: (1) it calls `gap set`, NOT `gap ship`, so INFRA-1392 PROOF-OF-MERGE never fires — that guard lives on the ship path only. Two routes to status=done, one guarded. (2) `gap set --status done` does not write closed_date, which is exactly why every falsely-closed gap has closed_pr set and closed_date empty"
    - "FULL DAMAGE, from the emitter own ambient trail: 30 gap_flipped_done_on_merge events across 8 PRs (#3551 flipped 7, #3544 5, #3556 5, #3554 3, #3549 3, #3553 3, #3548 2, #3550 2). Roughly 8 are legitimate (one per PR), so ~22 are collateral. It is hitting FLEET PRs, not just one session"
    - "THE SHARPEST ILLUSTRATION: PR #3556 changed exactly two files (docs/strategy/FACTORY_YIELD_2026-08-09.md and scripts/ops/first-pass-yield.sh) and closed FIVE gaps — CREDIBLE-273, CREDIBLE-175, DOC-096, CREDIBLE-251, RESILIENT-250. The document analysing why the factory loses work closed the two gaps it identified as the CAUSES of destroyed PRs, by citing them"
    - "CREDIBLE-092 INTENT WAS SOUND, do not simply revert it: auto-flip-on-merge.yml runs in CI where state.db does not exist, so merged gaps stayed open and got re-claimed (real ghost-gap waste). The receiver has filesystem access and fixes that. The EXTRACTION is what is wrong, not the idea"
    - "FIX 1 — key closure on INTENT, not prose: extract from the PR TITLE only, or require an explicit Closes: trailer. Never body text. Do NOT ask authors to stop cross-referencing; cross-references are how the registry stays navigable and are exactly what good ACs and handoff docs do"
    - FIX 2 — route it through `gap ship`, not `gap set`, so PROOF-OF-MERGE adjudicates and closed_date is written. That single change closes the guard bypass AND removes the closed_date anomaly
    - "FIX 3 — MINE BEFORE BUILD, and I failed to do this when first filing: scripts/ci/test-gap-closure-consistency.sh ALREADY exists (CREDIBLE-028/039/031) with forward mode (done gaps whose closed_pr PR actually merged) and reverse mode (open gaps with closed_pr -> stale_post_merge_gap). It would NOT have caught tonight (the PRs did merge), but the repair belongs as an EXTENSION of that gate, not a new detector. Add a file-overlap check: a gap closed by a PR that touched none of the files its ACs name"
    - "REPAIR PATH IS ALSO BROKEN AND NOW URGENT — see INFRA-3580, escalated to P1: the three gaps reopened tonight sit as status=open with a stale closed_pr, which is precisely the stale_post_merge_gap shape. Wiring a consumer for that event (as CREDIBLE-275 asks) BEFORE fixing INFRA-3580 could re-close the work we just recovered"
    - "INTERIM LEVER, documented in the code itself: there is deliberately no env escape hatch — stopping the local receiver process is the intended off switch"
  outcome_id: MISSION-010
  evidence: |
    COMMAND: chump gap show on RESILIENT-262, RESILIENT-265, RESILIENT-266 after PR #3544 merged 2026-08-09T20:15:42Z | OUTPUT: all three report status: done with closed_pr: 3544. PR #3544 is RESILIENT-263 (outbound Discord DM + chunking) and did NONE of their work — 262 is triage-vs-escalate, 265 is the approve/deny loop, 266 is the gateway daemon whose actual work is in PR #3551 and was still OPEN when they flipped. Two of the three had zero commits of any kind | THEORY: unconfirmed. auto-flip-on-merge.yml extracts a SINGLE gap id from the PR TITLE, and that title named only RESILIENT-263, so the title path alone does not explain it. The three closed gaps are all referenced in RESILIENT-263 ACs and in the #3544 body under a LINK line, so a body scan or an AC-link cascade in chump gap ship are the leading suspects — NEITHER IS PROVEN. Do not fix on this theory without confirming it | ALT: rejected the fleet did the work — RESILIENT-265 and 262 have no branch, no PR and no commits; RESILIENT-266 has real work but it lives in PR #3551, which was open at the time and is still not merged

