- id: INFRA-684
  domain: INFRA
  title: "CREDIBLE: speculative-on-speculative merge-queue guard — prevent two --speculative agents from both landing conflicting changes on same gap"
  status: done
  priority: P1
  effort: s
  acceptance_criteria:
    - scripts/coord/bot-merge.sh detects 'two armed PRs cite the same gap_id'; second arm fails with explicit error pointing to first PR
    - scripts/coord/gap-preflight.sh prints which PR holds the live spec lease
    - "test: scripts/ci/test-speculative-on-speculative-guard.sh — arm twice with --speculative on same gap, assert second exits non-zero"
  closed_date: '2026-05-10'
  closed_pr: 1414

