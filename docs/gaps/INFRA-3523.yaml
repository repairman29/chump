- id: INFRA-3523
  domain: INFRA
  title: "CREDIBLE: ChumpBench human-touch counter miscounts clone history instead of the lap's own commits (reports 50/0)"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"ChumpBench human-touch counter miscounts clone history instead of the lap's own commits (reports 50/0)\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3410

