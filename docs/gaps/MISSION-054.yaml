- id: MISSION-054
  domain: MISSION
  title: publish gap lifecycle to NATS events + node subscriber applies to state.db (real-time backlog coherence)
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"publish gap lifecycle to NATS events + node subscriber applies to state.db (real-time backlog coherence)\" is implemented in the relevant MISSION code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

