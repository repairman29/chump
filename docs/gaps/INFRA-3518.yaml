- id: INFRA-3518
  domain: INFRA
  title: "RESILIENT: remediate_held checks out origin/<branch> which narrow clones lack — use FETCH_HEAD"
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
    - "The change described by \"remediate_held checks out origin/<branch> which narrow clones lack — use FETCH_HEAD\" is implemented in the relevant INFRA code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-07-30'
  closed_pr: 3403

