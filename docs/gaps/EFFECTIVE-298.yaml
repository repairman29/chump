- id: EFFECTIVE-298
  domain: EFFECTIVE
  title: "EFFECTIVE: backfill-ac sweep — regenerate AC for the ~434 existing vague/TODO open gaps"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "1. A 'chump gap backfill-ac [--apply] [--limit N]' command finds open gaps whose acceptance_criteria are TODO/empty/<20 chars and regenerates concrete AC for them. 2. Prefer LLM-gap-specific generation (reuse the onboard/decompose parse_llm_gaps path); fall back to default_acceptance_criteria(title,domain) (EFFECTIVE-294) when offline/no-auth. 3. Default dry-run prints count + a sample; --apply writes via the canonical chump gap set path. 4. After a full --apply run, 'chump gap audit-priorities' vague-count trends to ~0. 5. Test: a synthetic open gap with TODO AC gets concrete (non-TODO) AC after backfill."
  evidence: |
    Follow-up to EFFECTIVE-294 (reserve now stamps concrete AC, fixing NEW gaps). This sweeps the ~434 EXISTING open gaps (260 P1) that still carry the old TODO obs-AC template (e.g. INFRA-3012..3024) and are therefore unclaimable. Depends on EFFECTIVE-294's default_acceptance_criteria fn.

