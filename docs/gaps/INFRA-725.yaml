- id: INFRA-725
  domain: INFRA
  title: "ZERO-WASTE: split main.rs phase 4 — extract memory/ module group (memory_db, memory_graph, memory_graph_tool, memory_graph_viz, memory_tool, belief_state, episode_db, episode_extractor)"
  status: open
  priority: P2
  effort: m
  acceptance_criteria: |
    Criterion 1: All memory-* modules (memory_db, memory_graph, memory_graph_tool, memory_graph_viz, memory_tool, belief_state, episode_db, episode_extractor) are extracted from main.rs into a `memory/` module directory
    Criterion 2: main.rs imports from the `memory` module rather than containing the implementations inline
    Criterion 3: Cargo build and `cargo test` pass with no functional changes
    Criterion 4: No public API surface change — callers of memory functions continue to work unchanged

