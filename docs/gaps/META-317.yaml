- id: META-317
  domain: META
  title: Mirror observability & schema gates cluster into chump preflight (META-086 follow-up)
  status: open
  priority: P2
  effort: m
  description: |
    META-086 cluster follow-up: mirror the Observability & schema gates cluster (12 scripts) from ci.yml fast-checks job into chump preflight. See docs/process/AUDIT_JOB_DECOMPOSITION.md for the full script list.
  acceptance_criteria:
    - "Every script in the Observability & schema gates cluster (coord-surfaces-smoke, test-ambient-schema, test-attribution-portable, test-cli-version-debug, test-fleet-brief, test-gate-promotion-no-regression, test-mcp-coord-smoke, test-model-registry, test-obs-coverage-guard, test-observability-coverage, test-orchestrate-session-summary, test-schema-version-assert) is either mirrored as a GateKind::Scripts gate in src/preflight.rs, or classified Tier-D in docs/process/CI_GATES_INVENTORY.md with a reason"
    - docs/process/AUDIT_JOB_DECOMPOSITION.md 'Mirrored?' column updated to yes for every mirrored script in this cluster
    - scripts/ci/test-preflight-ci-parity.sh passes for the newly-mirrored scripts
  depends_on: [META-086]

