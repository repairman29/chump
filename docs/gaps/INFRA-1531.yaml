- id: INFRA-1531
  domain: INFRA
  title: "RESILIENT: bot-merge stale-health-file reaper — kill .health entries on fleet up + scan periodically; today's pid=91790 health from 22h ago still triggered bot_merge_hung ALERT"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - scripts/coord/bot-merge.sh writes .chump-locks/bot-merge-<pid>.health on startup, removes it on exit (trap EXIT)
    - chump fleet up scans .chump-locks/bot-merge-*.health, removes any whose pid is not alive (kill -0 $pid 2>/dev/null fails)
    - chump-ambient-glance.sh also scans on each run and emits kind=bot_merge_health_reaped {pid, age_hours, machine} when reaping
    - "Today's signal: pid=91790 health from 22h ago (started_at 2026-05-15T20:22Z) was triggering bot_merge_hung ALERT every 30min because the process was dead but file lingered; 11 stale files cleaned manually across 4 worktrees"
    - "Smoke test scripts/ci/test-stale-bot-merge-health.sh: write fake health with pid=99999, assert reaper removes it"
    - Pair with INFRA-1516 (orphan-process reaper) — both address dead-process-leaves-state-behind class
  notes: |
    Demoted P1→P2 2026-07-19 re-rank: open >60 days with no ship activity; P1 now reserved for current-cycle critical path (docs/ROADMAP.md). Re-promote freely if picked up.
  outcome_id: MISSION-010

