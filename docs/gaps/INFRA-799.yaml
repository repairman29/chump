- id: INFRA-799
  domain: INFRA
  title: "EFFECTIVE: FTUE clean-machine CI test — brew install + chump init + chump gen on a fresh macOS runner (Week 4 demo gate)"
  status: open
  priority: P1
  effort: s
  acceptance_criteria:
    - "(a) scripts/ci/test-chump-init-clean-machine.sh: 5-check local smoke test — chump --version, chump init --no-interactive (config.toml + state.db written), chump mcp list exits 0"
    - "(b) ftue-clean-machine-2026.yml extended with chump init --no-interactive step (id=init) and chump mcp list step (id=mcp_list); both added to run summary"
    - "(c) workflow path triggers extended to include src/mcp_discovery.rs, chump-mcp.json, scripts/ci/test-chump-init-clean-machine.sh"

