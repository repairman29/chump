- id: EFFECTIVE-360
  domain: EFFECTIVE
  title: "EFFECTIVE: remove patch_file from the free-tier tool set — weak models loop on it (12 failed diffs) instead of using str_replace"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"remove patch_file from the free-tier tool set — weak models loop on it (12 failed diffs) instead of using str_replace\" is implemented in the relevant EFFECTIVE code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.
  closed_date: '2026-08-10'
  closed_pr: 3572
  outcome_id: EFFECTIVE-000

