- id: INFRA-2327
  domain: INFRA
  title: "fix-trunk: re-add pub mod nats_primary; to chump-coord/src/lib.rs — INFRA-2247 conflict-dropped"
  status: done
  priority: P0
  effort: m
  acceptance_criteria:
    - crates/chump-coord/src/lib.rs has 'pub mod nats_primary;' between 'pub mod mesh;' and 'pub mod mission;' with the 2-line doc comment from the brief
    - cargo check --workspace passes locally
    - cargo fmt --all -- --check passes
    - cargo clippy -p chump-coord --all-targets -- -D warnings passes
    - PR opened and merged via admin-squash to restore main green (META-207 T4 doctrine)
  closed_date: '2026-05-31'
  closed_pr: 2912
  outcome_id: MISSION-010

