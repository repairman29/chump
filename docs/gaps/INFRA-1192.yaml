- id: INFRA-1192
  domain: INFRA
  title: skill_hub::tests::default_registries_reads_env flake — missing serial attr on env-var-mutating test
  status: done
  priority: P2
  effort: m
  acceptance_criteria:
  - 'Add #[serial_test::serial] to skill_hub::tests::default_registries_reads_env so env-var mutations don''t race with parallel
    tests'
  - cargo test skill_hub::tests::default_registries_reads_env passes 5 consecutive runs under cargo test --tests (full parallel
    suite)
  - Remove INFRA-1192 entry from docs/process/KNOWN_FLAKES.yaml once fixed
  closed_date: '2026-05-14'
