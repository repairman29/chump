- id: INFRA-1223
  domain: INFRA
  title: "ZERO-WASTE: gh pr merge --auto path uses GraphQL — swap to REST PUT /pulls/{n}/merge to dodge GraphQL secondary rate limit"
  status: done
  priority: P1
  effort: m
  acceptance_criteria:
    - bot-merge.sh uses REST PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge instead of enablePullRequestAutoMerge GraphQL mutation
    - secondary rate limit errors on --auto path drop to 0 in a 30-min soak with 5 concurrent workers
    - fallback to GraphQL mutation if REST returns 405 (branch protection requires admin)
    - "CI test scripts/ci/test-bot-merge-rest-merge.sh: mock REST endpoint, verify correct HTTP verb + payload"
  closed_date: '2026-05-14'
  closed_pr: 1926

