- id: INFRA-1704
  domain: INFRA
  title: 'CREDIBLE: wire queue-health-monitor.plist into bootstrap-manifest — plist exists, manifest doesn''t install it (1/3
    INFRA-1546 narrow-split)'
  status: done
  priority: P1
  effort: xs
  acceptance_criteria:
  - 'Append a new installer entry to scripts/setup/bootstrap-manifest.yaml under installers: id=queue-health-monitor-launchd;
    priority=P1; install=bash scripts/setup/install-queue-health-monitor-launchd.sh; check=launchctl list 2>/dev/null '
  - ' grep -q com.chump.queue-health-monitor; depends_on=[chump-binary]'
  - chump-fleet-bootstrap.sh --check exits non-zero on a machine where queue-health-monitor isn't loaded (verify by unloading
    + re-running --check)
  - Pre-commit hook scripts/git-hooks/pre-commit-bootstrap-manifest.sh continues to pass (this PR adds the entry that should
    have been added when the plist was first created)
  - No new test scripts; existing scripts/ci/test-bootstrap-manifest.sh validates manifest schema
  closed_date: '2026-05-23'
  closed_pr: 2362
