- id: INFRA-788
  domain: INFRA
  title: "EFFECTIVE: Gemini-first cascade — enable paid-tier billing + correct rate limits in .env"
  status: done
  priority: P1
  effort: xs
  closed_pr: 1451
  acceptance_criteria: |
    The .env declares CHUMP_PROVIDER_8_RPD=1500 for gemini-2.5-flash,
    but the actual free-tier limit is 20 RPD (confirmed 2026-05-08 via
    429 error: "limit: 20, model: gemini-2.5-flash").

    Three changes:

    1. Operator action: enable billing on the Google AI Studio project
       (ai.google.dev → Settings → Billing). Paid tier removes the
       20 RPD cap entirely; cost is ~$0.15/$0.60 per Mtok for Flash.

    2. Update .env rate limits to match reality:
       - Free tier: RPD=20 per model (not 1500)
       - Paid tier: RPD=unlimited, RPM=2000, TPM=4M for Flash

    3. Reorder cascade so Gemini slots are priority 1-3 (ahead of
       Cerebras/Groq/NVIDIA). Gemini 2.5 Flash has better tool-call
       quality than Llama 3.3 70B and 1M context.

    Test: `chump cascade status` shows Gemini slots first with correct
    rate limits. A dispatch against gemini-2.5-flash completes without
    429 errors.

