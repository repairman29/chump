- id: INFRA-1621
  domain: INFRA
  title: 'PWA-INFRA: pr-hygiene runs ''node --check web/v2/app.js'' — block PR merge on JS syntax errors (would''ve caught
    INFRA-1620 in 1 PR)'
  status: done
  priority: P0
  effort: xs
  acceptance_criteria:
  - 'Add step to pr-hygiene job in .github/workflows/ci.yml: ''node --check web/v2/app.js'' — fails the job on any syntax
    error.'
  - If web/v2/ has other top-level .js files (search via find), check each.
  - 'Skill tag: pwa,ci,frontend.'
  - 'Smoke test scripts/ci/test-pwa-parse-gate.sh: stage a fixture file with a missing brace, assert the gate exits non-zero
    with offending line number.'
  - Wire into pr-hygiene required-check list once smoke is stable (~5 PRs observation).
  - 'Cross-link: 2026-05-14 commit c64ddd676 truncated 5 PWA classes; bug went unnoticed for 3 days because no gate parses
    the PWA''s main JS file. This is the durable fix.'
  skills_required: pwa,ci,javascript
  closed_date: '2026-05-22'
  closed_pr: 2321
