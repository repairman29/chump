- id: ZERO-WASTE-007
  domain: ZERO-WASTE
  title: "ZERO-WASTE: extract shared build_claude_command(model,prompt)->Command in dispatch.rs; reuse in onboard spawn_agentic_scout (#3059) + worker dispatch — the 'claude -p --dangerously-skip-permissions --model' incantation + auth-error msg is duplicated. Share the command builder (not the run flow — scout=clonedir+capture-stdout, worker=worktree+inherit-stdio differ legitimately)"
  status: open
  priority: P2
  effort: m
  acceptance_criteria:
    - "TODO: what events emitted on success/failure/timeout"
    - "TODO: how cost tracked and reported to operator"
    - "TODO: failure-class taxonomy (distinguish transient vs permanent)"
    - "TODO: smoke test command to verify observability"
  outcome_id: ZERO-WASTE-000

