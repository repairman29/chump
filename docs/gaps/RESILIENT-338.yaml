- id: RESILIENT-338
  domain: RESILIENT
  title: "OS must NOT be disconnected from the GitHub/CI plane: node-orchestrator SENSES GitHub/CI (open-PR backlog, per-PR check conclusions, merge-flow, systemic-red, farmer<->CI false-red cascade) as a first-class surface alongside cpu/ram/disk, and AUTO-HEALS — rerun false-red CI when the root cause clears (e.g. farmer back GREEN), requeue abandoned-red PRs, resolve green-stuck armed PRs (mergeable=UNKNOWN). COTG: installs via housekeeping ORGANS table on any fresh node, GitHub-wired, verified by self-test. Tonight the OS was blind to a 14-PR jam + farmer-flap false-red; the board hand-diagnosed + hand-reran (RESILIENT-337 tripwire + CREDIBLE-291 decouple are pieces; this is the sensing+healing spine)."
  status: open
  priority: P1
  effort: m
  acceptance_criteria:
    - "The change described by \"node-orchestrator SENSES GitHub/CI (open-PR backlog, per-PR check conclusions, merge-flow, systemic-red, farmer<->CI false-red cascade) as a first-class surface alongside cpu/ram/disk, and AUTO-HEALS — rerun false-red CI when the root cause clears (e.g. farmer back GREEN), requeue abandoned-red PRs, resolve green-stuck armed PRs (mergeable=UNKNOWN). COTG: installs via housekeeping ORGANS table on any fresh node, GitHub-wired, verified by self-test. Tonight the OS was blind to a 14-PR jam + farmer-flap false-red; the board hand-diagnosed + hand-reran (RESILIENT-337 tripwire + CREDIBLE-291 decouple are pieces; this is the sensing+healing spine).\" is implemented in the relevant RESILIENT code path(s)."
    - At least one test (cargo test or scripts/ci/test-*.sh) proves the new behavior and fails without the change.
    - cargo fmt + clippy --all-targets -D warnings + check pass; no regression to existing tests.

