# EVENT_REGISTRY.yaml — INFRA-754
#
# **GROUND-TRUTH CONTRACT** (INFRA-1237). This file is the contract that
# fleet-brief, waste-tally, kpi-report, watchdogs, and every other ambient
# consumer reads to know which `kind=X` values to expect from
# `.chump-locks/ambient.jsonl`. Drift in either direction silently degrades
# operator trust:
#   - emit-without-register → consumers see kinds they don't understand and
#     drop them; SLO dashboards under-report
#   - register-without-emit → consumers wait for kinds that never fire;
#     dashboards display empty rows the operator can't interpret
#
# Enforcement (post-INFRA-1237):
#   - scripts/ci/test-event-registry-coverage.sh runs on every PR in
#     fast-checks. Default mode (strict-emit) fails the build on
#     emit-without-register. Set CHUMP_REGISTRY_GATE_MODE=strict to also
#     fail on register-without-emit (after the orphan-reconciliation
#     follow-up gap lands).
#   - scripts/git-hooks/pre-commit-event-registry.sh blocks staged diffs
#     that add a kind literal without a corresponding registry entry.
#   - scripts/ci/event-registry-reserved.txt allow-lists kinds that are
#     legitimately exempt from both directions (with a required `# reason:`
#     comment per entry).
#
# Why: "observability before scale" needs mechanical enforcement. Without
# this registry, every contributor adds events with their own conventions
# and consumers silently fail on the unknown shapes.
#
# ── SCHEMA FIELD CANONICAL: 'kind' (INFRA-1159) ─────────────────────────────
# Two fields coexist in the ambient stream:
#   event: (legacy)  — written by scripts/dev/ambient-emit.sh before INFRA-1159.
#                       Still present (dual-write bridge) for backward compat.
#   kind:  (canonical) — the discriminator field this registry describes.
#                       Prefer 'kind' in ALL consumers and new emitters.
#
# Migration timeline: dual-write (event + kind) until INFRA-1159 consumers are
# fully migrated; remove 'event' field in a follow-up gap when all .event
# selectors are confirmed gone from scripts/ and src/.
# ─────────────────────────────────────────────────────────────────────────────
#
# Adding a new event:
#   1. Add an entry below with: kind, emitter, consumers, trigger, fields_required
#   2. Update the consumer code to parse the event
#   3. Stage your changes; pre-commit guard verifies the entry exists
#
# ── UNIVERSAL FIELDS (EFFECTIVE-028) ─────────────────────────────────────────
# The following fields are available on EVERY event emitted by broadcast.sh,
# regardless of kind. Consumers may rely on them being present.
#
#   corr_id        — correlation ID linking request/response pairs (INFRA-1255).
#                    Auto-derived from: gap-id > branch name > timestamp.
#                    Override with --corr <id> or CHUMP_CORR_ID env var.
#                    The deliberator (deliberator-loop.sh) tallies FEEDBACK/
#                    preference votes keyed by this field.
#
#   parent_corr_id — (optional) set when --reply-to <parent-corr-id> is used
#                    to thread a reply under a specific prior broadcast.
#                    When present, corr_id=parent_corr_id so the vote/reply
#                    lands in the correct deliberator tally bucket.
#                    Absent on events not using --reply-to (default path).
#
# ─────────────────────────────────────────────────────────────────────────────
#
# Schema:
#   kind:                (required) the literal string emitted in `"kind":"..."` JSON
#   emitter:             (required) source file or component that emits it
#   trigger:             (required) one-line description of when it fires
#   effect_metric:       (required since INFRA-1371) name of the metric that proves
#                          this event is doing its job. Use "self" when the emission
#                          count itself is the metric (most observability events).
#   consumers:           (recommended) list of downstream consumers
#   fields_required:     (recommended) list of fields the consumer expects
#   expected_min_per_day:(optional) int — if set, drift gate flags the kind as
#                          silent when fewer than this many events appear in 24h
#   docs:                (optional) link to a longer explanation
#   status:              (optional) "stable" / "deprecated" — default stable
#
# Bypass: CHUMP_EVENT_REGISTRY_CHECK=0 in commit env (with reason in
# commit body trailer "Event-Registry-Bypass: <text>"). Same discipline
# as --no-verify; sparingly.

schema_version: 2
last_audit: "2026-05-15"

events:

  # ── Fleet lifecycle ────────────────────────────────────────────────────────
  - kind: session_start
    effect_metric: self
    emitter: scripts/dev/ambient-emit.sh + claude SessionStart hook
    trigger: any chump session begins (fleet worker, operator, reaper)
    consumers: [fleet-brief, ambient-context-inject]

  - kind: farmer_drain_guard
    effect_metric: self
    emitter: scripts/dev/farmer-drain-guard.sh (via farmer-brown tick, RESILIENT-076)
    trigger: drain-guard action — ruleset strict-policy reconcile/drift or drain-daemon reload; status field carries reconciled/drift_paused/reconcile_failed/daemon_reloaded/daemon_down_paused; never emitted on the in-sync path
    consumers: [fleet-brief, observability]

  - kind: session_end
    effect_metric: self
    emitter: claude Stop hook + worker.sh cycle_end
    trigger: session terminates (clean or timeout)
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, session_id, outcome, input_tokens, output_tokens]

  - kind: cycle_end
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: fleet worker finishes a cycle (claim+ship or empty pick)
    consumers: [fleet-brief]
    fields_required: [agent_id, gap_id, elapsed_s, rc, cycle_kind]

  - kind: fleet_starved
    effect_metric: fleet_auto_widen_applied_within_5min
    emitter: scripts/dispatch/worker.sh
    trigger: N consecutive empty picks (CHUMP_STARVE_THRESHOLD)
    consumers: [fleet-brief, watchdog]

  - kind: fleet_auto_widen_applied
    effect_metric: self
    emitter: src/main.rs (INFRA-615, chump fleet auto-widen --apply)
    trigger: operator runs auto-widen with --apply after starvation detected
    consumers: [fleet-brief, waste_tally]
    fields_required: [starve_count, effort, priority_filter]

  - kind: fleet_wedge
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: claude -p produced 0 stdout for full timeout window
    consumers: [waste-tally, watchdog]

  - kind: fleet_recovery_choir_kicked
    effect_metric: self
    emitter: scripts/coord/ci-health-gate.sh
    trigger: fleet-paused sentinel auto-lifted after slo-check passed; supervisor/reaper choir kicked (RESILIENT-066)
    consumers: [watchdog, fleet-brief]

  - kind: fleet_health
    effect_metric: self
    emitter: launchd / scripts/ops/fleet-health.sh
    trigger: periodic fleet metrics summary
    consumers: [fleet-brief]

  # ── Circuit breaker (RESILIENT-011) ───────────────────────────────────────────
  - kind: circuit_breaker_opened
    effect_metric: self
    emitter: src/circuit_breaker.rs (RESILIENT-011)
    trigger: circuit breaker for a target transitions from Closed to Open (error threshold exceeded)
    consumers: [fleet_health, operator_dashboard]
    fields_required: [ts, kind, target, reason]
    docs: |
      Emitted when a target reaches its error_threshold consecutive failures.
      The circuit breaker will now fail fast (reject calls) for this target.

  - kind: circuit_breaker_state_change
    effect_metric: self
    emitter: src/circuit_breaker.rs (RESILIENT-011)
    trigger: circuit breaker for a target changes state (any transition)
    consumers: [fleet_health, operator_dashboard]
    fields_required: [ts, kind, target, from_state, to_state, reason]
    docs: |
      General-purpose state change event emitted on any circuit breaker transition.
      Includes the target, previous state, new state, and reason for the change.

  # ── Supervision trees — RESILIENT-058 (A2A L6a) ──────────────────────────────
  # Per-gap and fleet-aggregate restart-intensity escalation, mirroring
  # Erlang/OTP supervision-tree semantics. Motivating incident: 2026-06-03
  # 12-zombie audit-run wedge (25+ min, required manual operator cancel).
  # See: docs/design/A2A_MASTER_PLAN_2026-06-03.md §1.L6, §M2

  - kind: gap_supervisor_escalated
    effect_metric: self
    emitter: scripts/coord/gap-supervisor.sh
    trigger: >
      Per-gap restart count exceeds CHUMP_GAP_SUPERVISOR_MAX_RESTARTS (default 3)
      within CHUMP_GAP_SUPERVISOR_WINDOW_S (default 300s). Gap status set to blocked.
    consumers: [ops-audit, fleet-brief, fleet-supervisor]
    fields_required: [ts, kind, gap_id, restart_count, window_s, reason]
    expected_min_per_day: 0
    status: stable
    docs: |
      Per-gap restart-storm escalation. The fleet supervisor reads this event
      to aggregate cross-gap escalations and trigger fleet-level pause when
      CHUMP_FLEET_SUPERVISOR_MAX_ESCALATIONS is exceeded.
      Scanner-anchor: "kind":"gap_supervisor_escalated"

  - kind: gap_supervisor_heartbeat
    effect_metric: self
    emitter: scripts/coord/gap-supervisor.sh
    trigger: >
      Emitted on every gap-supervisor.sh tick (record or daemon tick command).
      Confirms the per-gap supervisor is alive and processing.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, source]
    expected_min_per_day: 1440
    status: stable
    docs: |
      Liveness signal for com.chump.gap-supervisor launchd daemon (RESILIENT-058).
      Absence for >2 tick intervals (>120s) indicates daemon failure.
      Scanner-anchor: "kind":"gap_supervisor_heartbeat"

  - kind: fleet_supervisor_pickup_paused
    effect_metric: self
    emitter: scripts/coord/fleet-supervisor.sh
    trigger: >
      Fleet-aggregate escalation count exceeds CHUMP_FLEET_SUPERVISOR_MAX_ESCALATIONS
      (default 2) within CHUMP_FLEET_SUPERVISOR_WINDOW_S (default 600s).
      Creates .chump-locks/.fleet-pickup-paused sentinel; new gap claims blocked.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, escalation_count, window_s, gap_ids, sentinel_file]
    expected_min_per_day: 0
    status: stable
    docs: |
      Fleet-level pickup pause triggered by cross-gap retry-storm. The sentinel
      file .chump-locks/.fleet-pickup-paused must be removed (via resume-attempt
      after fleet-doctor-strict passes) before new claims are allowed.
      Scanner-anchor: "kind":"fleet_supervisor_pickup_paused"

  - kind: fleet_pickup_resumed
    effect_metric: self
    emitter: scripts/coord/fleet-supervisor.sh
    trigger: >
      Recovery attempt succeeds: fleet-doctor-strict.sh exits 0.
      Sentinel .chump-locks/.fleet-pickup-paused is removed.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, recovery_method, source]
    expected_min_per_day: 0
    status: stable
    docs: |
      Confirms fleet pickup has resumed after a supervised pause.
      Paired with fleet_supervisor_pickup_paused. Absence after a pause event
      indicates the fleet is still blocked — check fleet-doctor-strict output.
      Scanner-anchor: "kind":"fleet_pickup_resumed"

  - kind: fleet_doctor_strict_failed
    effect_metric: self
    emitter: scripts/coord/fleet-supervisor.sh
    trigger: >
      Recovery attempt via fleet-doctor-strict.sh returned non-zero.
      Sentinel remains in place; fleet pickup stays paused.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, failing_checks, doctor_rc, sentinel_kept, source]
    expected_min_per_day: 0
    status: stable
    docs: |
      Fleet-doctor-strict.sh failed during a supervisor recovery attempt.
      The fleet remains paused. Operator must fix the health invariants listed
      in failing_checks before pickup can resume.
      Scanner-anchor: "kind":"fleet_doctor_strict_failed"

  - kind: fleet_supervisor_heartbeat
    effect_metric: self
    emitter: scripts/coord/fleet-supervisor.sh
    trigger: >
      Emitted on every fleet-supervisor.sh tick (every 300s from launchd).
      Confirms the fleet-aggregate supervisor is alive.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, paused, window_s, max_escalations, source]
    expected_min_per_day: 288
    status: stable
    docs: |
      Liveness signal for com.chump.fleet-supervisor launchd daemon (RESILIENT-058).
      Absence for >2 tick intervals (>600s) indicates daemon failure.
      Scanner-anchor: "kind":"fleet_supervisor_heartbeat"

  # ── Dispatch guardrail — RESILIENT-060 (A2A L6c) ─────────────────────────────
  # Pre-write validation that BLOCKS Sonnet dispatch before any file is written.
  # Invariants checked: lease path coverage, gap-id/branch alignment, cargo fmt.
  # Motivating incident: 2026-06-03 three "writes outside lease" Sonnet drops.
  # See: docs/design/A2A_MASTER_PLAN_2026-06-03.md §1.L6c

  - kind: agent_dispatch_guardrail_passed
    effect_metric: self
    emitter: scripts/coord/agent-dispatch-guardrail.sh
    trigger: >
      All three pre-dispatch invariants passed: (1) every proposed write path is
      in claim.paths, (2) current branch matches lease gap_id, (3) cargo fmt
      --check passes (when *.rs paths are included). Orchestrator may proceed
      with Agent tool dispatch.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, branch, attempted_paths, leased_paths, reason]
    expected_min_per_day: 0
    status: stable
    docs: |
      Emitted by scripts/coord/agent-dispatch-guardrail.sh before the orchestrator
      invokes the Agent tool. Absence of this event (when a dispatch was made)
      indicates the guardrail was bypassed — check for CHUMP_OFF_RAILS_CHECK=0.
      Scanner-anchor: "kind":"agent_dispatch_guardrail_passed"

  - kind: agent_dispatch_guardrail_blocked
    effect_metric: self
    emitter: scripts/coord/agent-dispatch-guardrail.sh
    trigger: >
      One or more pre-dispatch invariants failed: out-of-lease path, branch/gap-id
      mismatch, missing lease, or cargo fmt --check failure. The orchestrator must
      NOT invoke the Agent tool. The reason field identifies the specific violation.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, branch, attempted_paths, leased_paths, reason]
    expected_min_per_day: 0
    status: stable
    docs: |
      Emitted when scripts/coord/agent-dispatch-guardrail.sh exits 1 (BLOCKED).
      The ops-audit consumer uses this to detect scope-creep patterns across
      dispatches. A cluster of these events from the same gap_id indicates the
      lease paths need expansion (re-claim with --paths) or the agent brief needs
      tighter scope definition.
      Scanner-anchor: "kind":"agent_dispatch_guardrail_blocked"

  - kind: fleet_snapshot
    effect_metric: self
    emitter: scripts/ops/fleet-snapshot.sh
    trigger: scheduled fleet state capture
    consumers: [chump fleet status]

  - kind: fleet_queue_depth
    effect_metric: self
    emitter: picker / fleet status
    trigger: queue depth metric capture
    consumers: [fleet-brief]

  - kind: fleet_restore
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh
    trigger: fleet restart event
    consumers: [audit-log]

  - kind: fleet_scale_request
    effect_metric: self
    emitter: operator or auto-scaler
    trigger: request to change fleet size
    consumers: [audit-log]

  - kind: fleet_version_skew
    effect_metric: self
    emitter: scripts/ops/fleet-version-check.sh
    trigger: workers running different chump binary versions
    consumers: [watchdog]

  - kind: fleet_pid_invariant
    effect_metric: self
    emitter: scripts/ops/fleet-audit-pids.sh
    trigger: PID/lease consistency check
    consumers: [watchdog]

  - kind: fleet_worker_silent
    effect_metric: self
    emitter: scripts/ops/fleet-worker-watchdog.sh (FLEET-042)
    trigger: worker heartbeat > N min stale
    consumers: [operator-recall]

  - kind: fleet_timeout_checkpoint
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-525)
    trigger: T-30s before claude -p timeout, WIP commit pushed
    consumers: [fleet-brief]

  - kind: wip_sigterm_checkpoint
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (_sigterm_wip_checkpoint — INFRA-686)
    trigger: SIGTERM/SIGINT received mid-gap; WIP commit created before exit
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, agent_id, gap_id, branch]
  # ── FLEET-035: speculative race loss tracking ─────────────────────────────
  - kind: speculative_race_loss
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (FLEET-035, INFRA-193)
    trigger: a speculative loser PR is closed as superseded — gap was picked by
      two agents in parallel; one won and the other's PR is auto-closed
    consumers: [fleet-status --pane race-loss, waste-tally]
    fields_required: [ts, kind, session, gap_id, loser_pr, winner_pr, loser_branch]

  - kind: worker_exit
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: worker exits cycle (rc + classified exit_class)
    consumers: [waste-tally, kpi-report]
    fields_required: [agent_id, gap_id, rc, exit_class]

  # ── INFRA-740: tool-call normalizer ──────────────────────────────────────
  - kind: tool_normalize
    effect_metric: self
    emitter: src/tool_normalize.rs (INFRA-740)
    trigger: tool-call JSON argument was malformed on first parse; normalizer
      repaired it (trailing comma, missing brace, code fence, single quotes)
    consumers: [fleet-brief, waste_tally]
    fields_required: [ts, kind, session, tool, repair]

  - kind: cycle_stall_killed
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-705)
    trigger: no new output bytes for CHUMP_STALL_THRESHOLD_S (default 120s) during an active cycle
    consumers: [fleet-brief, waste_tally, watchdog]
    fields_required: [gap_id, agent_id, idle_s]

  - kind: worker_cooldown
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (FLEET-051)
    trigger: single worker failure on a gap; per-worker cooldown written
    consumers: [fleet-brief]
    fields_required: [gap_id, worker_id, until, cooldown_kind]

  - kind: worker_cooldown_cluster_wide
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (FLEET-051)
    trigger: distinct-worker failure count reaches FLEET_COOLDOWN_THRESHOLD (default 3)
    consumers: [fleet-brief, watchdog]
    fields_required: [gap_id, worker_count, until]

  - kind: worker_circuit_open
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (FLEET-043)
    trigger: N consecutive dispatch failures, worker pauses
    consumers: [fleet-brief, watchdog]

  - kind: worker_stand_down
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-613)
    trigger: starvation threshold hit, worker exits cleanly
    consumers: [fleet-brief]

  - kind: worker_wedge_detected
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-823)
    trigger: claude -p produced near-empty log after timeout (rc=124 or rc=0 AND log < 100B)
    consumers: [fleet-brief, watchdog]
    fields_required: [agent_id, gap_id, rc, cycle_log_size, wedge_count]

  - kind: fleet_wedge_storm
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-823)
    trigger: N wedge events in rolling 1h window (CHUMP_WEDGE_STORM_THRESHOLD, default 5)
    consumers: [fleet-brief, watchdog]
    fields_required: [agent_id, recent_wedges, threshold, window_h]

  - kind: fleet_wedge_escalated
    effect_metric: self
    emitter: scripts/coord/fleet-wedge-handler.sh (INFRA-845)
    trigger: wedge persisted past CHUMP_WEDGE_ESCALATE_S (default 1800s) since wedge_start
    consumers: [fleet-brief, watchdog, oncall]
    fields_required: [ts, kind, wedge_age_s, threshold_s, handler]

  - kind: execution_timeout
    effect_metric: self
    emitter: src/blocker_detect.rs (chump fleet blocker-detect)
    trigger: subprocess or operation exceeded its timeout during blocking-operation detection
    consumers: [fleet-brief, watchdog]

  - kind: hang_detector
    effect_metric: self
    emitter: src/dispatch.rs (chump dispatch)
    trigger: subprocess hung past deadline; process details and name logged
    consumers: [fleet-brief, watchdog, operator-recall]

  - kind: claim_aborted_disk_full
    effect_metric: self
    emitter: scripts/lib/disk-check.sh via gap-claim.sh (INFRA-975)
    trigger: chump claim attempted but free disk < CHUMP_DISK_LOW_GB (default 5 GB)
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, free_gb, threshold_gb, path]

  - kind: gap_claimed
    effect_metric: gap_shipped_within_deadline_for_same_id
    emitter: src/atomic_claim.rs run_claim (INFRA-1240)
    trigger: successful gap claim via the Rust atomic path (post-INFRA-987 shell deletion)
    consumers: [fleet-brief, silent_agent-watchdog, lease-overlap-watchdog]
    fields_required: [ts, kind, gap_id, session_id, worktree, paths]
    # INFRA-1240: mirrors the event previously emitted only by gap-claim.sh.
    # Without this, silent_agent and lease_overlap watchdogs missed Rust-path claims.

  - kind: chump_claim_force_recover
    effect_metric: self
    emitter: src/atomic_claim.rs emit_force_recover_event (INFRA-1439)
    trigger: chump claim --force-recover auto-removed a stale worktree dir and/or
      local branch before the atomic-claim step; actions field lists each recovery
      step taken (worktree_remove_force, rm_rf, branch_delete)
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap_id, branch, actions]

  - kind: force_recover_wip_loss
    effect_metric: waste_prevented
    emitter: src/atomic_claim.rs emit_force_recover_wip_loss (INFRA-2235)
    trigger: chump claim --force-recover detected uncommitted changes in the target
      worktree and REFUSED to wipe it (--discard-wip not passed). No data was lost;
      the operator must commit+push or re-run with --discard-wip. files_lost_count
      and first_paths name the dirty files that would have been destroyed.
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap_id, files_lost_count, first_paths]

  - kind: force_recover_wip_discarded
    effect_metric: self
    emitter: src/atomic_claim.rs emit_force_recover_wip_discarded (INFRA-2235)
    trigger: chump claim --force-recover --discard-wip intentionally wiped a worktree
      that had uncommitted changes. This is the explicit-consent path; files_lost_count
      and first_paths document what was destroyed for retro audit.
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap_id, files_lost_count, first_paths]

  - kind: claim_aborted_pr_in_flight
    effect_metric: waste_prevented
    emitter: src/atomic_claim.rs emit_claim_aborted_pr_in_flight_event (INFRA-1503)
    trigger: chump claim refused because a non-draft OPEN PR already exists on
      the canonical claim branch `chump/<gap>-claim` — prevents the "2 sessions
      wasted creating duplicate worktrees" failure mode observed 2026-05-16.
      Overrides logged separately:  --allow-duplicate-pr / CHUMP_CLAIM_ALLOW_OPEN_PR
      (rescue), --resume (continue same work), CHUMP_ALLOW_STOMP (abandoned PR
      takeover).
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap_id, existing_pr, existing_author]

  - kind: claim_open_pr_dup_blocked
    effect_metric: waste_prevented
    emitter: src/atomic_claim.rs emit_claim_open_pr_dup_blocked (INFRA-1982)
    trigger: chump claim refused because an open PR already covers this gap — either
      the PR title contains the gap ID (case-insensitive) or the head branch starts
      with chump/<gap-id-lowercase>. This catches the duplicate-WORK failure mode
      (two agents file different gap IDs for the same problem, both claim+push).
      Complement to claim_duplicate_gap_blocked (INFRA-1970) which catches same gap ID.
      Override: CHUMP_CLAIM_ALLOW_DUPLICATE_PR=1 or --allow-duplicate-pr.
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap, open_pr]

  - kind: claim_duplicate_gap_blocked
    effect_metric: waste_prevented
    emitter: src/atomic_claim.rs emit_claim_duplicate_gap_event (INFRA-1970)
    trigger: chump claim refused because a live lease from a DIFFERENT session already
      holds the same gap_id — prevents the duplicate-PR race documented in META-105
      (PRs #2539 + #2540 both claimed INFRA-1950 on 2026-05-24 with different --paths).
      Primary key is now (gap_id, session_id); path-overlap remains a secondary gate.
      Override: CHUMP_CLAIM_ALLOW_DUPLICATE_GAP=1 (emits claim_duplicate_gap_bypassed).
    consumers: [fleet-brief, waste-tally, audit-log]
    fields_required: [ts, kind, gap_id, this_session, detail]

  - kind: claim_duplicate_gap_bypassed
    effect_metric: waste_risk
    emitter: src/atomic_claim.rs emit_claim_duplicate_gap_event (INFRA-1970)
    trigger: operator used CHUMP_CLAIM_ALLOW_DUPLICATE_GAP=1 to proceed with a
      claim despite another live session already holding the same gap_id.
      Emitted for audit so the operator can correlate duplicate PRs back to the
      bypass event. Same emit site as claim_duplicate_gap_blocked.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, this_session, detail]

  - kind: lease_broad_dir_claim
    effect_metric: self
    emitter: src/atomic_claim.rs emit_lease_broad_dir_claim (INFRA-1885)
    trigger: operator used CHUMP_LEASE_ALLOW_BROAD_DIRS=1 to bypass the
      lease-breadth cap and claim a top-level dir (src, scripts/ci,
      docs/gaps, src/lib, app). Emitted for audit; operator must add
      Broad-Lease-Reason commit trailer.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, session_id, gap, paths, reason]

  - kind: gap_claim_nats_conflict
    effect_metric: self
    emitter: src/atomic_claim.rs nats_dual_write (INFRA-986)
    trigger: NATS KV CAS claim returned CONFLICT — another session on the
      fleet (possibly cross-machine) holds the atomic claim on this gap
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, gap_id, session_id]

  # ── INFRA-2398/INFRA-2428: main-health-gate events ───────────────────────────
  - kind: claim_main_health_stale
    effect_metric: resilient
    emitter: src/atomic_claim.rs emit_main_health_event (INFRA-2398)
    trigger: >
      Emitted when `chump claim` detects that the main-preflight-watchdog state
      file (.chump/main-preflight-state.json) is older than 30 minutes
      (STALE_THRESHOLD_SECS=1800). Indicates the watchdog daemon may be down.
      Claim proceeds with a stderr warning. age_secs reflects how old the state
      file is at the moment of the claim attempt.
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, gap_id, status, failing_gates, age_secs, trunk_fix_gap_id]
    expected_volume: "rare — only when watchdog daemon is stopped or crashed"
    status: stable
    # payload: {gap_id: str, status: str, failing_gates: [str], age_secs: int, trunk_fix_gap_id: str}

  - kind: claim_main_health_missing
    effect_metric: resilient
    emitter: src/atomic_claim.rs emit_main_health_event (INFRA-2398)
    trigger: >
      Emitted (debug-level, once per claim) when `chump claim` cannot find
      .chump/main-preflight-state.json. This is expected on fresh installs or
      when the watchdog daemon (INFRA-2397) has not been installed yet. Claim
      proceeds without blocking.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, status, failing_gates, age_secs, trunk_fix_gap_id]
    expected_volume: "once per claim on fleets without the watchdog installed"
    status: stable
    # payload: {gap_id: str, status: "" (empty), failing_gates: [], age_secs: 0, trunk_fix_gap_id: ""}

  - kind: claim_main_health_bypass
    effect_metric: resilient
    emitter: deleted (INFRA-2428)
    deprecated: true
    trigger: >
      DEPRECATED (INFRA-2428): CHUMP_CLAIM_IGNORE_MAIN_HEALTH env var and this
      event kind have been deleted. The gate now redirects to the trunk-fix gap
      instead of allowing bypass. Historical records may contain this kind.
      Replaced by claim_main_health_redirect.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, status, failing_gates, age_secs]
    expected_volume: "zero (deleted)"
    status: deprecated

  - kind: claim_main_health_redirect
    effect_metric: resilient
    emitter: src/atomic_claim.rs emit_main_health_event (INFRA-2428)
    trigger: >
      Emitted when `chump claim` is blocked because main is RED and the claimer
      is routed to the trunk-fix gap filed by the watchdog daemon. Replaces the
      deleted claim_main_health_bypass kind (INFRA-2428 zero-bypass).
      failing_gates contains the gates that are red; trunk_fix_gap_id is the
      most recent gap auto-filed by the preflight watchdog for this red window.
      Setting CHUMP_CLAIM_IGNORE_MAIN_HEALTH has no effect — the gate always
      redirects (bypass env var deleted).
    consumers: [fleet-brief, audit-log, waste-tally]
    fields_required: [ts, kind, gap_id, status, failing_gates, age_secs, trunk_fix_gap_id]
    expected_volume: "once per claim attempt during a red-main window"
    status: stable
    # payload: {gap_id: str, status: "red", failing_gates: [str], age_secs: int, trunk_fix_gap_id: str}

  - kind: claim_main_health_garbled
    effect_metric: resilient
    emitter: src/atomic_claim.rs emit_main_health_event (INFRA-2524)
    trigger: >
      Emitted when `chump claim` sees main RED but ALL failing-gate names are
      garbled (empty, punctuation-only, or a stray em-dash — the 2026-06-03
      watchdog parser bug, INFRA-2458). The claim proceeds (fail-OPEN) rather
      than halting the whole fleet on an untrustworthy signal (A2A_ROADMAP
      principle 7 fail-open-over-deadlock). A real gate name matches
      [A-Za-z][A-Za-z0-9_.-]*; if none match, the RED is treated as noise.
    consumers: [fleet-brief, audit-log, waste-tally]
    fields_required: [ts, kind, gap_id, status, failing_gates, age_secs]
    expected_volume: "rare — only when the watchdog emits a malformed RED"
    status: stable
    # payload: {gap_id: str, status: "red", failing_gates: [str], age_secs: int}

  - kind: curator_paused
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (META-065)
    trigger: operator set CHUMP_CURATOR_PAUSE=1 to short-circuit the
      curator run; logged so audit shows when mutations were disabled
      and for how long.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, reason]

  - kind: curator_auto_exec_armed
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (META-065)
    trigger: first curator run after launchd install (sentinel absent at
      .chump-locks/curator-armed.sentinel) — captures the moment
      automated registry mutations went live for this operator.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, note]
  - kind: planner_rank_ran
    effect_metric: self
    emitter: scripts/setup/install-chump-planner-launchd.sh --once (INFRA-1257)
    trigger: hourly launchd run wrote .chump-locks/gap-priority.json — fleet
      picker consumes this file (INFRA-1258) to pick best-scored gap.
    consumers: [picker, fleet-brief, audit-log]
    fields_required: [ts, kind, out_file]

  - kind: fleet_bootstrap_ran
    effect_metric: self
    emitter: scripts/setup/chump-fleet-bootstrap.sh (META-066)
    trigger: operator (or CI/pre-flight) ran the bootstrap orchestrator.
      Captures installed_count, skipped_healthy_count, failed_count,
      missing_at_check_count so the audit log shows when fleet-discipline
      was actually activated on this machine vs. just present-on-disk.
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, mode, installed, skipped_healthy, failed, missing_count]

  - kind: fleet_bootstrap_incomplete
    effect_metric: self
    emitter: scripts/setup/chump-fleet-bootstrap.sh (INFRA-1594)
    trigger: --check mode found one or more REQUIRED_DAEMONS unregistered
      in launchd (paramedic, bot-merge-watchdog, etc.). Names the missing
      daemons so paramedic on peer hosts can flag the bootstrap gap on
      machines that joined the fleet without a full install.
      2026-05-16 M4 incident: host ran runner plists but no paramedic;
      PRs got stuck DIRTY for hours because the self-healing daemon
      literally wasn't installed.
    consumers: [paramedic, fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, missing_daemons, missing_count, host]

  - kind: picker_used_priority
    effect_metric: self
    emitter: scripts/dispatch/_pick_gap.py + _pick_and_claim_gap.py (INFRA-1258)
    trigger: picker successfully selected a gap from the planner-ranked list
      at .chump-locks/gap-priority.json. Includes file path, age, scored rank,
      and whether the layer gate filtered any candidates (layer_enforced).
    emitter: scripts/dispatch/_pick_gap.py (INFRA-1258)
    trigger: picker successfully selected a gap from the planner-ranked
      list at .chump-locks/gap-priority.json. Captures planner_rank so
      we can measure how much delta vs. legacy first-fit.
    consumers: [fleet-brief, audit-log, kpi-report]
    fields_required: [ts, kind, gap_id, planner_rank, worker_id]

  - kind: picker_priority_stale
    effect_metric: self
    emitter: scripts/dispatch/_pick_gap.py (INFRA-1258)
    trigger: picker tried to load .chump-locks/gap-priority.json but it
      was absent / stale / invalid; picker fell back to legacy ordering.
      Surfacing the staleness so fleet-brief can alert when the planner
      launchd job has been down.
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, reason, worker_id]

  - kind: picker_mission_boost
    effect_metric: self
    emitter: scripts/dispatch/_pick_gap.py (MISSION-011)
    trigger: picker selected a gap linked to the active mission outcome AND
      there was at least one equal-priority substrate gap in the same
      planner-rank bucket that the mission gap outranked via the
      mission_rank tiebreaker. Confirms the mission-directed picker is
      actually influencing fleet behavior (not just configured).
    consumers: [fleet-brief, audit-log, mission-grade]
    fields_required: [ts, kind, gap_id, active_mission, worker_id]

  - kind: rust_first_bypass_used
    effect_metric: self
    emitter: scripts/git-hooks/pre-commit-rust-first.sh (META-064)
    trigger: pre-commit gate matched Rust-first criteria on a new shell
      file but commit body had 'Rust-First-Bypass:' trailer — bypass
      logged for audit.
  - kind: rust_first_blocked
    effect_metric: self
    emitter: scripts/git-hooks/pre-commit-rust-first.sh (INFRA-1448)
    trigger: pre-commit gate matched Rust-first criteria on a new shell
      file AND no 'Rust-First-Bypass:' trailer was present — commit
      rejected. Fields. files=comma-separated paths, reasons=pipe-separated
      per-file reasons (hot-path|state-mutator|>200 LOC|long-lived).
    fields_required: [ts, kind, files, reasons]
    consumers: [fleet-brief, audit-log, META-064-effectiveness]
  - kind: rust_first_strict_blocked
    effect_metric: self
    emitter: scripts/git-hooks/pre-commit-rust-first.sh (INFRA-1580)
    trigger: pre-commit STRICT gate (INFRA-1580) saw a 'Rust-First-Bypass:'
      trailer but the file failed 2+ machine-checkable criteria (loc/state/
      hot/test) AND the commit lacked a 'Rust-First-Bypass-Accept:' CSV
      covering them. Commit rejected; narrative-only bypass insufficient.
    fields_required: [ts, kind, files, unacknowledged]
    consumers: [fleet-brief, audit-log, META-064-effectiveness]
  - kind: rust_first_bypass_audit
    effect_metric: self
    emitter: scripts/dev/rust-first-bypass-audit.sh (INFRA-1580)
    trigger: read-only audit walks scripts/coord/*.sh + scripts/setup/*.sh,
      applies the strict 4-criteria check, and emits one event per violating
      file with violations CSV (loc,state,hot,test), bypass-present bool,
      and acknowledgment-present bool. Used for port-to-Rust prioritization.
    fields_required: [ts, kind, path, violations, has_bypass, has_acknowledgment, loc]
    consumers: [fleet-brief, audit-log, META-064-effectiveness]
  - kind: redundancy_bypass_used
    effect_metric: self
    emitter: scripts/git-hooks/pre-commit-redundancy.sh (META-063)
    trigger: pre-commit gate detected high function-name overlap between
      a new shell file and an existing sibling, but commit body had
      'Redundancy-OK:' trailer — bypass logged for audit.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, files, reason]

  - kind: fleet_paused_disk_critical
    effect_metric: self
    emitter: scripts/lib/disk-check.sh via worker.sh (INFRA-975)
    trigger: worker pre-cycle disk check found free < CHUMP_DISK_CRITICAL_GB (default 1 GB)
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, free_gb, threshold_gb, path]

  - kind: fleet_wedge_resolved
    effect_metric: self
    emitter: scripts/coord/fleet-wedge-handler.sh (INFRA-845)
    trigger: no fleet_wedge events for CHUMP_WEDGE_CLEAR_S (default 1800s) while wedged=true
    consumers: [fleet-brief, watchdog]
    fields_required: [ts, kind, quiet_window_s, handler]

  - kind: slo_breach
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-848 / INFRA-955)
    trigger: curator audit detected an SLO violation (chump health --slo-check non-zero)
    consumers: [fleet-brief, watchdog, waste-tally]
    fields_required: [ts, kind, severity, slo_name, detail]
    docs: |
      Edge-only emission per INFRA-955: fires when the active breach set
      CHANGES (transitions into breach or between different breach sets),
      not on every audit tick while in continuous breach. slo_name is a
      sorted comma-separated key of breached SLO identifiers (e.g.
      "L1-SLO-3,L2-SLO-4"). detail carries the full BREACH line(s) for
      forensic context.

  - kind: slo_recovered
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-955)
    trigger: curator audit transitions from breach → healthy (pairs with slo_breach edge)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, previously_breached, handler]

  - kind: fleet_scale_change
    effect_metric: self
    emitter: scripts/coord/fleet-wedge-handler.sh (INFRA-845) + manual scaling per CLAUDE.md
    trigger: fleet size adjusted up or down by handler or operator
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, from, to, rationale]

  - kind: fleet_scale_changed
    effect_metric: self
    emitter: src/main.rs fleet-up disk-plan gate + fleet auto-scale (INFRA-2198)
    trigger: fleet size downsized by disk-plan REFUSE/WAIT gate or adjusted by auto-scale tick
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, from, to, reason]

  - kind: fleet_restart
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh (INFRA-844 --restart flag)
    trigger: operator runs run-fleet.sh --restart; existing fleet torn down and relaunched
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, from_size, to_size, reason]

  # ── FLEET-054: waste-spike auto-pause ─────────────────────────────────────
  - kind: waste_spike_detected
    effect_metric: self
    emitter: scripts/coord/waste-spike-detector.sh (FLEET-054)
    trigger: waste rate (total_incidents / total_events × 100) exceeded
      CHUMP_WASTE_SPIKE_THRESHOLD (default 30%) in the last CHUMP_WASTE_WINDOW
      (default 2h); fleet-paused sentinel created to halt new claims
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, rate, threshold, window, total_events, total_incidents]

  - kind: fleet_resumed
    effect_metric: self
    emitter: scripts/coord/waste-spike-detector.sh (FLEET-054)
    trigger: waste rate fell below CHUMP_WASTE_RECOVERY_THRESHOLD (default 20%)
      for two consecutive waste-spike-detector checks; fleet-paused sentinel
      removed and workers allowed to resume claiming
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, rate, recovery_threshold, window]

  - kind: fleet_stopped_kill_switch
    effect_metric: resilient
    emitter: scripts/dispatch/worker.sh + scripts/coord/bot-merge.sh (RESILIENT-073)
    trigger: AUTONOMY_LEVEL file at ~/.chump/AUTONOMY_LEVEL was 0, missing,
      unreadable, or non-numeric at the start of a work cycle or bot-merge
      invocation. Fail-closed — any read failure is treated as STOP.
      Worker sleeps and retries; bot-merge exits 10.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, autonomy_level]
    # payload: {source: "worker"|"bot-merge", agent_id?: str, autonomy_level: int}
    status: stable

  - kind: worker_paused_waste_spike
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (FLEET-054)
    trigger: worker found .chump/fleet-paused sentinel at the top of a claim
      cycle, indicating an active waste spike; worker sleeps before retrying
      instead of claiming a new gap
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, agent_id, pause_file]

  - kind: worker_paused_waste_spike_bypass
    effect_metric: self
    emitter: scripts/coord/waste-spike-detector.sh (FLEET-054)
    trigger: CHUMP_IGNORE_WASTE_PAUSE=1 was set; waste-spike check was bypassed
      and no fleet-paused sentinel was created or checked regardless of rate
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, session]

  - kind: pipeline_health_throttle
    effect_metric: self
    emitter: scripts/coord/ci-health-gate.sh (INFRA-1607)
    trigger: ci-health-gate.sh transitions fleet-paused state — either writing
      the sentinel on a SLO breach or pipeline jam (>= CHUMP_CI_HEALTH_JAM_THRESHOLD
      % BLOCKED PRs), or removing it after 2 consecutive clean runs
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, state, reason, blocked_pct, slo_rc]
    docs: |
      state="paused" when the sentinel is written; state="resumed" when cleared.
      reason is one of: slo_breach, pipeline_jam.
      blocked_pct is the integer % of open PRs in BLOCKED/BEHIND state at check time.
      slo_rc is the exit code from `chump health --slo-check` (0=pass, 1=breach).

  - kind: pager_notified
    effect_metric: self
    emitter: scripts/coord/fleet-wedge-handler.sh (INFRA-845)
    trigger: CHUMP_PAGER_WEBHOOK set + escalation fires + POST attempted
    consumers: [audit-log]
    fields_required: [ts, kind, webhook, event]

  # ── Auth ───────────────────────────────────────────────────────────────────
  - kind: fleet_auth_storm
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: N consecutive auth failures
    consumers: [fleet-brief, watchdog]

  - kind: fleet_auth_mode
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh
    trigger: auth path detected at fleet launch
    consumers: [fleet-brief]

  - kind: fleet_auth_verified
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh (INFRA-621)
    trigger: launch-time auth probe succeeded
    consumers: [audit-log]

  - kind: fleet_auth_misconfigured
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh (INFRA-621)
    trigger: launch-time auth probe failed
    consumers: [operator-recall, audit-log]

  - kind: fleet_auth_refresh
    effect_metric: self
    emitter: scripts/dispatch/worker.sh refresh_oauth_token
    trigger: OAUTH token refreshed mid-cycle
    consumers: [audit-log]

  - kind: fleet_auth_unrecoverable
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: auth failed beyond circuit-breaker threshold
    consumers: [operator-recall, fleet-brief]

  - kind: fleet_oauth_token_refreshed
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: oauth token file re-read with new value
    consumers: [audit-log]

  - kind: oauth_token_refreshed
    effect_metric: self
    emitter: scripts/coord/oauth-token-refresh.sh (INFRA-2124)
    trigger: keychain extraction + atomic write to ~/.chump/oauth-token.json succeeded
    consumers: [audit-log, infra-watcher]
    fields_required: [ts, kind, source, prev_age_seconds, new_age_seconds, token_len]
    docs: |
      Standalone refresh daemon independent of run-fleet.sh; fills the
      CLAUDE.md INFRA-622 5-min refresh promise. Drives subscription-mode
      headless `claude -p` subprocesses (Oracle, JIT scheduler) so they
      inherit a fresh CLAUDE_CODE_OAUTH_TOKEN. Plist:
      launchd/com.chump.oauth-refresh.plist; installer:
      scripts/setup/install-oauth-refresh-launchd.sh.

  - kind: oauth_token_refresh_failed
    effect_metric: self
    emitter: scripts/coord/oauth-token-refresh.sh (INFRA-2124)
    trigger: keychain miss / JSON parse fail / empty token / write fail
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, reason, prev_age_seconds]
    docs: |
      reason ∈ {keychain_miss, json_parse_or_missing_field, empty_access_token}.
      Distinguishes "operator hasn't installed plist" (no event ever) from
      "daemon ran and failed" (event fires every 5min). Loud signal that
      auth chain is broken; pairs with oauth_token_stale_despite_daemon.

  - kind: oauth_token_stale_despite_daemon
    effect_metric: self
    emitter: scripts/coord/infra-watcher-loop.sh (INFRA-2124)
    trigger: ~/.chump/oauth-token.json age > CHUMP_OAUTH_STALE_S (default 900s) AND com.chump.oauth-refresh plist loaded
    consumers: [audit-log, operator-recall, infra-watcher]
    fields_required: [ts, kind, category, severity, detail]
    docs: |
      Catch-net for wedged refresher: launchd thinks the plist is alive but
      the file hasn't been touched in 15+ min. Common causes: keychain
      permission revoked after macOS update, python3 removed from refresher
      PATH, refresh-once subprocess hangs without timeout. Critical-severity
      infra_watcher_finding shape (re-emitted through the same category/severity/detail
      contract for dashboard compatibility).

  - kind: credential_scrub_run
    effect_metric: self
    emitter: scripts/coord/scrub-credential-logs.sh (INFRA-871)
    trigger: scrubber invoked with --report; summary of any leaks found
    consumers: [audit-log, ci-gate]
    fields_required: [ts, kind, summary]
    docs: |
      summary is a JSON object with {total_hits, by_path_kind}.
      Emitted only when --report flag passed (CI gates pass --report;
      bare invocations stay quiet to avoid noise).

  - kind: fleet_auto_restart_decision
    effect_metric: self
    emitter: scripts/dispatch/fleet-autorestart-daemon.sh (INFRA-611)
    trigger: daemon decides to restart fleet
    consumers: [audit-log, fleet-brief]

  - kind: fleet_auto_restart_cancel
    effect_metric: self
    emitter: scripts/dispatch/fleet-autorestart-daemon.sh
    trigger: pending restart canceled
    consumers: [audit-log]

  - kind: fleet_auto_restart
    effect_metric: self
    emitter: scripts/dispatch/fleet-autorestart-daemon.sh
    trigger: fleet auto-restart actually executed (follows fleet_auto_restart_decision)
    consumers: [audit-log, fleet-brief]

  - kind: fleet_restart_snapshot
    effect_metric: self
    emitter: src/main.rs (chump fleet restart-snapshot)
    trigger: fleet state snapshot recorded before a coordinated restart
    consumers: [audit-log]

  # ── Reapers / cleanup daemons ──────────────────────────────────────────────
  - kind: reaper_run
    effect_metric: self
    emitter: scripts/ops/stale-*-reaper.sh
    trigger: reaper completes a sweep
    consumers: [reaper-watchdog]
    fields_required: [reaper_name, status]

  - kind: reaper_silent
    effect_metric: self
    emitter: scripts/ops/reaper-heartbeat-watchdog.sh (INFRA-120)
    trigger: reaper missed expected cadence
    consumers: [fleet-brief, operator-recall]

  - kind: stale_gap_lock_reaped
    effect_metric: self
    emitter: scripts/ops/stale-gap-lock-reaper.sh (INFRA-676)
    trigger: zombie gap-lock removed
    consumers: [fleet-brief]
    fields_required: [lock, session]

  - kind: orphan_subprocess_reaped
    effect_metric: self
    emitter: scripts/ops/reap-orphan-claude-procs.sh (INFRA-1662)
    trigger: >
      launchd watchdog swept the process table for `claude` binary subprocesses
      whose ppid chain does not reach the foreground Claude.app PID and whose
      etime exceeds REAP_AGE (default 3600s). Each sweep emits exactly one
      event, including count=0 to prove the watchdog is alive (steady state).
      Prevents the 60h cron-loop leak that pinned 8.5GB RAM with 354 wedged
      subprocesses on 2026-05-22.
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, count, oldest_etime_secs, total_rss_kb, reap_age_secs, fg_pid, dry_run]

  - kind: stale_process_reaped
    effect_metric: self
    emitter: scripts/ops/stale-process-watchdog.sh (INFRA-1663)
    trigger: >
      launchd watchdog (every 30 min) swept the process table for long-tail
      fleet leaks — rustc (10m), cargo (15m), `chump health` (2m), worker.sh
      (4h), bot-merge.sh (20m), run-fleet.sh (24h). Any matching process whose
      etime exceeds its class's expected lifetime is SIGTERMed with 30s grace,
      then SIGKILLed if still alive. One per-kill event is emitted (action =
      sigterm or sigkill) plus one heartbeat sweep record per invocation
      (sweep=true, count=N) so the watchdog's liveness is always visible.
      Generalizes INFRA-1662 (claude-only) to the rest of the fleet.
      Discovery: 2026-05-22 cleanup found 5 rustc procs alive 1.5+ days and
      2 `chump health --slo-check` alive 11+ h.
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, pid, comm, class, etime_secs, expected_secs, action, dry_run]
  - kind: main_red_detected
    effect_metric: self
    emitter: scripts/ops/main-health-watchdog.sh (INFRA-1656)
    trigger: >
      Daily launchd watchdog at 02:00 local queries the latest CI run on main.
      Each sweep emits exactly one event: on success it is a heartbeat
      (status=success, gap_id=none) proving the watchdog is alive; on failure
      it carries status=filed plus the freshly-reserved gap_id (or
      status=deduped if a P0 INFRA-NEW-MAIN-RED-* gap already references the
      same head_sha). Closes the 2026-05-20 24h-red-main feedback gap where
      every PR's CI burned silently until an operator noticed.
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, gate, sha, run_url, head_sha, gap_id, dry_run, status]

  - kind: target_artifact_reaped
    effect_metric: self
    emitter: scripts/coord/target-dir-reaper.sh (INFRA-1349)
    trigger: cargo target/ directory deleted from idle worktree under disk pressure;
      normal mode (idle threshold respected)
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, path, freed_gb, worktree_age_h]

  - kind: target_artifact_critical_reap
    effect_metric: self
    emitter: scripts/coord/target-dir-reaper.sh (INFRA-1431)
    trigger: cargo target/ directory deleted in critical-pressure mode (free disk <
      CHUMP_REAPER_CRITICAL_GB); idle threshold bypassed; active lease still protected
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, path, freed_gb, worktree_age_h]

  - kind: stale_bot_merge_killed
    effect_metric: self
    emitter: scripts/ops/stale-bot-merge-reaper.sh (INFRA-673)
    trigger: bot-merge.sh process > 1h killed
    consumers: [fleet-brief]
    fields_required: [pid, age_s]

  - kind: ghost_status_closed
    effect_metric: self
    emitter: scripts/ops/stale-pr-reaper.sh (INFRA-674 extension)
    trigger: gap auto-closed because PR merged but state was open
    consumers: [audit-log]
    fields_required: [gap_id, pr_number]

  - kind: curator_skip_active_rebase
    effect_metric: self
    emitter: scripts/ops/stale-pr-reaper.sh (INFRA-1195)
    trigger: reaper skipped closing a stale PR because it was updated within the freshness window (active rebase/force-push detected)
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, pr, gap, age_minutes, reason]

  - kind: daemon_silent
    effect_metric: self
    emitter: scripts/ops/reaper-heartbeat-watchdog.sh (INFRA-683)
    trigger: pr-watch.sh / distill-pr-skills.sh missed heartbeat
    consumers: [fleet-brief, operator-recall]

  - kind: watchdog_silent
    effect_metric: self
    emitter: scripts/ops/meta-watchdog.sh
    trigger: a watchdog itself missed its tick
    consumers: [operator-recall]

  - kind: silent_agent
    effect_metric: self
    emitter: chump-coord
    trigger: live session stopped heartbeating
    consumers: [waste-tally, fleet-brief]

  - kind: edit_burst
    effect_metric: self
    emitter: chump-coord
    trigger: rapid file mutations (rebase stomp risk)
    consumers: [fleet-brief]

  - kind: lease_expired_server
    effect_metric: self
    emitter: scripts/coord/lease-reaper.sh
    trigger: server-side lease TTL expired
    consumers: [waste-tally]

  - kind: lease_overlap
    effect_metric: self
    emitter: chump-coord
    trigger: two sessions claim same files
    consumers: [fleet-brief]

  - kind: fixture_commit_dropped
    effect_metric: self
    emitter: scripts/git-hooks/pre-push (INFRA-1408, Guard 0d)
    trigger: ghost commits authored by Test<test@test.local> or matching CHUMP_FIXTURE_AUTHOR_REGEX detected; --auto-drop or CHUMP_AUTODROP_FIXTURE_COMMITS=1 rewrote local branch
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, branch, dropped_count]
  - kind: lease_orphaned_by_sibling_merge
    effect_metric: self
    emitter: scripts/ops/github-webhook-receiver.py (INFRA-1444)
    trigger: a pull_request.closed+merged=true webhook references a gap_id whose claim lease exists from a different session; the lease file is auto-deleted to release the orphaned worktree
    consumers: [fleet-brief, stale-worktree-reaper, audit-log]
    fields_required: [ts, kind, gap_id, released_session_id, merged_pr, lease_file]

  # ── PR / gap lifecycle ─────────────────────────────────────────────────────
  - kind: gap_reserved
    effect_metric: self
    emitter: chump gap reserve
    trigger: new gap added to state.db
    consumers: [audit-log]
    fields_required: [gap_id, domain]

  - kind: gap_reserve_blocked
    effect_metric: self
    emitter: src/main.rs — chump gap reserve (INFRA-1607)
    trigger: chump gap reserve was called while .chump/fleet-paused sentinel
      exists and CHUMP_IGNORE_WASTE_PAUSE is not set to 1; the reserve is
      refused before any state.db write
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, pause_reason, domain, title]
    docs: |
      pause_reason is the "reason" field from the fleet-paused JSON payload
      (e.g. pipeline_jam, slo_breach). Bypass: set CHUMP_IGNORE_WASTE_PAUSE=1.

  - kind: gap_cooldown_cleared
    effect_metric: self
    emitter: scripts/coord/lib/gap-cooldown.sh via gap_cooldown_clear() (INFRA-1220)
    trigger: operator clears a post-close cooldown via the shell library
    consumers: [audit-log]
    fields_required: [ts, kind, gap, reason]
    docs: |
      Emitted when gap_cooldown_clear() removes the cooldown stamp for a gap.
      reason is operator-provided and mandatory for audit trail.

  - kind: gap_cooldown_cleared_cli
    effect_metric: self
    emitter: src/main.rs — chump gap clear-cooldown (INFRA-1220)
    trigger: operator runs 'chump gap clear-cooldown <ID> --reason ...'
    consumers: [audit-log]
    fields_required: [ts, kind, gap_id, reason]
    docs: |
      Rust-side ambient event emitted by chump gap clear-cooldown after the
      shell script completes. Pairs with gap_cooldown_cleared (shell side).
      Both events appear together on a successful clear.

  - kind: gap_shipped
    effect_metric: self
    emitter: "chump gap ship; src/main.rs --execute-gap arm (INFRA-2055)"
    trigger: "gap status flipped to done, OR chump --execute-gap exits cleanly after agent reply"
    consumers: [audit-log, kpi-report, wizard-daemon]
    fields_required: [gap_id, pr_number, commit_sha]
    docs: |
      Emitted by two paths: (1) chump gap ship on manual ship, (2) the
      chump --execute-gap arm in src/main.rs on every clean Ok(reply) exit
      (INFRA-2055 terminal-outcome guarantee). pr_number is parsed from the
      agent reply (may be empty if agent did not print one); commit_sha is
      the HEAD SHA at ship time. wizard-daemon reads this kind to confirm
      a dispatched gap reached a terminal state without heuristic guessing.

  - kind: gap_blocked
    effect_metric: self
    emitter: "src/main.rs --execute-gap arm (INFRA-2055)"
    trigger: "chump --execute-gap exits with any error, timeout, panic, or signal"
    consumers: [wizard-daemon, fleet-brief, audit-log]
    fields_required: [gap_id, reason, recoverable_by]
    docs: |
      Emitted on EVERY non-clean exit from chump --execute-gap: uncaught
      error, provider exhaustion, timeout, unexpected exit. reason is the
      formatted anyhow error chain (truncated); recoverable_by is a hint
      tag (fix_billing | restart_daemon | manual_rescue | signal_term).
      wizard-daemon reads this to stop re-dispatching a blocked gap.
      INFRA-2055: authoritative fix for the wheel-spin class observed in
      the 2026-05-27 trial run.

  - kind: gap_deferred
    effect_metric: self
    emitter: "src/main.rs --execute-gap arm (INFRA-2055)"
    trigger: "chump --execute-gap agent explicitly punts to wait for an external event"
    consumers: [wizard-daemon, fleet-brief]
    fields_required: [gap_id, reason, defer_until_event]
    docs: |
      Emitted when the agent decides it cannot make progress without an
      external event (e.g. upstream_pr_landed, ci_green, billing_resolved).
      defer_until_event is a short tag naming the event; wizard-daemon uses
      it to schedule a re-dispatch check rather than immediately retrying.

  - kind: gap_workflow_phase
    effect_metric: self
    emitter: src/web_server.rs (PWA autonomous workflows)
    trigger: autonomous gap workflow transitions between phases
    consumers: [fleet-brief, ambient-context-inject]
    fields_required: [gap_id, phase, status]
    docs: "Phase values: preflight, claim, execute-gap, ship. Status: started, success, failed, error, partial"

  - kind: gap_drift_orphan
    effect_metric: self
    emitter: scripts/coord/gap-doctor.py
    trigger: gap in state.db without YAML mirror
    consumers: [fleet-brief]

  - kind: gap_drift_yaml_only
    effect_metric: self
    emitter: scripts/coord/gap-doctor.py
    trigger: YAML file without state.db row
    consumers: [fleet-brief]

  - kind: pr_classified
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh (META-183, META-185)
    trigger: >
      emitted once per open PR per tick of the PR-shepherd daemon; classifies each PR based on
      mergeStateStatus, autoMergeRequest, and (META-185) statusCheckRollup.
      classification enum: BEHIND (needs rebase), MERGEABLE (CLEAN, no auto-merge),
      ARMED (CLEAN, auto-merge set), DIRTY (merge conflict),
      BLOCKED_GREEN (BLOCKED + all checks pass + no auto-merge armed — auto-rearm target),
      BLOCKED_REAL_FAIL (BLOCKED + at least one check FAILURE — file-gap target),
      BLOCKED (BLOCKED + checks still running or inconclusive — catch-all),
      UNKNOWN (GitHub still computing mergeability).
    consumers: [fleet-brief, pr-shepherd-daemon, pr-auto-rearm-daemon]
    fields_required: [ts, kind, pr, classification, gap_id, age_minutes, dry_run]

  - kind: pr_shepherd_tick
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh
    trigger: emitted once per tick of the PR-shepherd daemon (META-181/META-180); counts open PRs and drives classification + rebase-action phase (META-183/META-184)
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, open_pr_count, dry_run]

  - kind: pr_queue_auto_action
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh (INFRA-2346)
    purpose: >
      Audit trail for the pr-queue auto-processor tiers. Every CLEAN_GREEN
      admin-merge, BLOCKED_FLAKE rerun, and their skip/failure variants
      emit one of these. Operator can grep ambient and reconstruct every
      autonomous merge action the fleet took.
    trigger: |
      emitted once per CLEAN_GREEN / BLOCKED_FLAKE action attempt; action enum:
        admin_merge              — CLEAN_GREEN PR squash-admin-merged
        admin_merge_skipped      — skipped; reason=trunk_red|capped
        admin_merge_failed       — gh exit non-zero; reason=gh_exit_<N>
        flake_rerun              — gh run rerun --failed fired; reason=known_flake
        flake_rerun_skipped      — skipped; reason=trunk_red|capped|no_run_id
        flake_rerun_failed       — gh exit non-zero; reason=gh_exit_<N>
    consumers: [fleet-brief, waste-tally, kpi-report, operator-audit]
    fields_required: [ts, kind, pr, action, reason, author, mergeStateStatus, dry_run]
    volume_estimate: "~10-30/h during active queue catch-up; ~0-5/h steady-state"

  - kind: pr_wedged
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh (INFRA-2346)
    purpose: >
      Signal that a PR is open >24h with no commits in the last 12h and no
      auto-merge armed — a human steward decision is needed. Debounced to
      once per PR per 24h via .chump-locks/pr-wedged-signaled.json.
    trigger: emitted when PR meets WEDGED_24H criteria and not signaled within last 24h
    consumers: [fleet-brief, operator-recall, pr-author-DM]
    fields_required: [ts, kind, pr, author, age_hours, dry_run]
    volume_estimate: "~0-3/h; debounced per PR per 24h"

  - kind: pr_queue_skipped_trunk_red
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh (INFRA-2346)
    purpose: >
      Single-line rollup emitted at end of tick when one or more CLEAN_GREEN
      admin-merges were blocked by the trunk-red gate. Operator can grep this
      to confirm the safety gate is firing as intended during incidents.
    trigger: emitted once per tick when admin_merge_skipped_trunk_red > 0
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, skipped_count, dry_run]
    volume_estimate: "~0-1/tick during trunk-red incidents; 0 otherwise"

  - kind: pr_action_taken
    effect_metric: self
    emitter: scripts/coord/pr-shepherd-daemon.sh (META-184/META-186)
    trigger: |
      emitted once per actioned PR per tick; action enum (META-186 extended):
        rebase                    — BEHIND PR rebased via gh pr update-branch
        rebase_failed             — gh error on rebase
        rebase_skipped            — rebase guard fired; reason=trunk_red|claim|debounce|throttle|conflict
        arm_auto_merge            — BLOCKED_GREEN PR armed via gh pr merge --auto --squash
        arm_auto_merge_failed     — gh error on arm
        arm_auto_merge_skipped    — arm guard fired; reason=trunk_red|throttle
        file_followup_gap         — BLOCKED_REAL_FAIL gap filed via chump gap reserve; new_gap_id field present
        file_followup_gap_skipped — gap filing skipped; reason=trunk_red|throttle|dedup
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, pr_number, action, reason, gap_id, dry_run]
    fields_optional: [new_gap_id]

  - kind: pr_merged
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh; GitHub webhook / PR close hook
    trigger: a PR was merged to main (squash merge via auto-merge)
    consumers: [fleet-brief, fleet_resize, kpi-report, waste-tally]
    fields_required: [ts, kind]

  - kind: pr_fmt_auto_fixed
    effect_metric: self
    emitter: scripts/ops/pr-fmt-shepherd.sh (INFRA-759)
    trigger: open PR had a failing cargo fmt check; shepherd ran cargo fmt,
      committed, and pushed the fix
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, pr, branch, commit_sha]

  - kind: pr_fmt_shepherd_run
    effect_metric: self
    emitter: scripts/ops/pr-fmt-shepherd.sh (INFRA-759)
    trigger: pr-fmt-shepherd completed a scan pass (emitted once per run)
    consumers: [fleet-brief]
    fields_required: [ts, kind, scanned, fixed, skipped, errors, status]

  - kind: pr_auto_closed_superseded
    effect_metric: self
    emitter: scripts/coord/close-superseded-prs.sh (INFRA-994)
    trigger: chump gap ship detects an open PR whose gap is already done; PR has
      no unique commits not in main (git cherry check passes)
    consumers: [audit-log, waste-tally]
    fields_required: [ts, kind, gap_id, pr, title, merged_via]

  - kind: ci_retrigger_pushed
    effect_metric: self
    emitter: scripts/coord/chump-ci-retrigger.sh (INFRA-1407)
    trigger: operator pushed an empty "ci: retrigger" commit to a PR branch to force
      a fresh CI checkout; `gh run rerun --failed` replays the OLD snapshot so this
      shim is needed when new commits must be picked up by CI
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, pr, branch, sha, reason]

  - kind: pr_stuck
    effect_metric: pr_rescue_action_within_4h
    emitter: scripts/ops/stuck-pr-filer.sh (INFRA-307)
    trigger: PR BLOCKED > 4h with no progress
    consumers: [audit-log, fleet]

  - kind: pr_stuck_cycle_1_rebase_attempted
    effect_metric: pr_auto_closed_for_respawn_within_30m
    emitter: scripts/ops/stale-pr-reaper.sh (INFRA-1410)
    trigger: PR has been mergeStateStatus=BLOCKED for ≥ CHUMP_PR_STUCK_SLO_HRS
      (default 2h); reaper records state and invokes chump-rebase-and-push.sh
      as the first rescue attempt before any close action.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, pr, branch, age_hrs]

  - kind: pr_auto_closed_for_respawn
    effect_metric: gap_reopened_within_1h
    emitter: scripts/ops/stale-pr-reaper.sh (INFRA-1410)
    trigger: PR remained BLOCKED for ≥ CHUMP_PR_STUCK_RECLOSE_MINS (default 30m)
      AFTER a pr_stuck_cycle_1_rebase_attempted; reaper closes the PR with a
      "gap re-claimable" comment. stuck-pr-filer.sh reads this event and
      re-opens the cited gap for the next picker.
    consumers: [stuck-pr-filer, fleet-brief, audit-log, operator-recall]
    fields_required: [ts, kind, pr, branch, gap_ids, title]

  - kind: pr_stuck_exempt
    effect_metric: self
    emitter: scripts/ops/stale-pr-reaper.sh (INFRA-1410)
    trigger: PR carries the `do-not-respawn` label; reaper skips all
      auto-respawn logic for the PR and clears any pending state.
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, pr, reason]

  - kind: stuck_pr_filing_dedup_hit
    effect_metric: self
    emitter: scripts/ops/stuck-pr-filer.sh (INFRA-855)
    trigger: INDIVIDUAL path finds already_filed() returns true for a PR; gap
      filing suppressed to prevent duplicate stuck-pr gaps per PR per failing check
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, pr_number, check_name]

  - kind: pr_blocked_long
    effect_metric: self
    emitter: scripts/ops/stuck-pr-filer.sh
    trigger: pr_stuck variant for very long blocks
    consumers: [operator-recall]

  - kind: pr_resolved
    effect_metric: self
    emitter: scripts/ops/queue-health-monitor.sh
    trigger: a previously stuck or blocked PR is now resolved; paired with pr_stuck
    consumers: [audit-log, fleet-brief]

  - kind: serializing_blocking_parallel
    effect_metric: self
    emitter: scripts/ops/queue-health-monitor.sh
    trigger: bot-merge queue is serializing PRs that could safely run in parallel;
      emitted when serializing_count > parallel_safe_count threshold
    consumers: [fleet-brief, operator-recall]

  - kind: fat_worktree
    effect_metric: self
    emitter: scripts/ops/queue-health-monitor.sh
    trigger: a linked worktree exceeds CHUMP_FAT_WT_GB (default 4 GB); likely due
      to target/ cargo build artifacts
    consumers: [operator-recall]

  - kind: pr_repair
    effect_metric: self
    emitter: scripts/ops/pr-repair-loop.sh (INFRA-727)
    trigger: PR rebase repair attempted
    consumers: [audit-log]

  - kind: pr_title_drift
    effect_metric: self
    emitter: scripts/ci/pr-title-check.sh
    trigger: PR title diverged from gap title
    consumers: [audit-log]

  - kind: pr_watch
    effect_metric: self
    emitter: scripts/coord/pr-watch.sh
    trigger: pr-watch loop iteration
    consumers: [reaper-watchdog]

  # ── Auto-fix / triage bots ─────────────────────────────────────────────────
  - kind: auto_fix_lint
    effect_metric: self
    emitter: .github/workflows/pr-triage-bot.yml (INFRA-624)
    trigger: lint-class CI failure → cargo clippy --fix + push
    consumers: [audit-log]

  - kind: ci_flake_rerun
    effect_metric: self
    emitter: scripts/ops/ci-flake-rerun.sh + pr-triage-bot
    trigger: flake-class CI failure → gh run rerun --failed
    consumers: [audit-log, fleet-brief]

  - kind: flake_catalog_orphan
    effect_metric: self
    emitter: scripts/ops/audit-flake-catalog.sh (INFRA-1866 / parent INFRA-1861 slice a)
    trigger: daily audit finds a KNOWN_FLAKES.yaml entry with no
      tracking_gap: INFRA-NNNN. Entry is masking failures forever
      without a fix-owner. One emit per orphan per day.
    consumers: [operator-recall, audit-log, fleet-brief]
    fields_required: [ts, kind, test, last_observed, added]

  - kind: audit_flake_catalog_bypassed
    effect_metric: self
    emitter: scripts/ops/audit-flake-catalog.sh (INFRA-1866)
    trigger: CHUMP_AUDIT_FLAKE_CATALOG=0 silences the daily audit
  - kind: ci_qa_score
    effect_metric: self
    emitter: scripts/ops/ci-qa-score.sh (INFRA-1872 / parent INFRA-1861 slice g)
    trigger: daily aggregate — pct of last-N merged PRs landed clean (no --no-verify, no flake-rerun, no post-CI rebase)
    consumers: [audit-log, fleet-brief, dashboard]

  - kind: ci_qa_score_bypassed
    effect_metric: self
    emitter: scripts/ops/ci-qa-score.sh (INFRA-1872)
    trigger: CHUMP_CI_QA_SCORE=0 silences the daily score
    consumers: [audit-log]

  - kind: flake_rerun
    effect_metric: self
    emitter: pr-triage-bot
    trigger: rerun action explicitly fired
    consumers: [audit-log]

  - kind: flake_budget_exceeded
    effect_metric: self
    emitter: pr-triage-bot
    trigger: rerun cap (3 attempts) hit
    consumers: [operator-recall]

  - kind: cascade_rebase_triggered
    effect_metric: self
    emitter: .github/workflows/post-merge-cascade-rebase.yml (INFRA-670)
    trigger: workspace-file landing → open-PR rebase sweep
    consumers: [audit-log]

  - kind: cascade_rebase_skipped_duplicate
    effect_metric: self
    emitter: scripts/coord/queue-driver.sh (INFRA-1310)
    trigger: worker detected hot-file commit but another worker already holds the per-SHA debounce lock
    consumers: [audit-log]
    fields_required: [ts, kind, sha, triggered_by]

  - kind: dirty_pr_auto_resolved
    effect_metric: self
    emitter: scripts/coord/queue-driver.sh (INFRA-1137)
    trigger: DIRTY PR rescued by rebase + .gitattributes merge driver resolution
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, pr, conflict_files]

  - kind: dirty_pr_unresolvable
    effect_metric: self
    emitter: scripts/coord/queue-driver.sh (INFRA-1137)
    trigger: DIRTY PR has conflicts in files with no configured merge driver — needs human or new driver
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, kind, pr, conflict_files]

  - kind: dirty_pr_push_failed
    effect_metric: self
    emitter: scripts/coord/queue-driver.sh (INFRA-1141)
    trigger: force-with-lease push failed after clean rebase or dirty auto-resolve — stale lease or remote changed
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, kind, pr, phase, error]

  # ── Cost / token tracking ─────────────────────────────────────────────────
  - kind: token_usage_partial
    effect_metric: self
    emitter: scripts/dispatch/_parse_token_usage.py (INFRA-639)
    trigger: claude streaming response usage event
    consumers: [waste-tally, kpi-report]
    fields_required: [session_id, input, output]

  - kind: token_anomaly
    effect_metric: self
    emitter: chump waste-tally
    trigger: token-burn outlier detected
    consumers: [fleet-brief]

  - kind: cost_cap_exceeded
    effect_metric: self
    emitter: scripts/dispatch/cost-cap-monitor.sh
    trigger: daily LLM spend exceeded threshold
    consumers: [operator-recall]

  - kind: cascade_decision
    effect_metric: self
    emitter: src/provider_cascade.rs
    trigger: bandit/priority chose a slot for a request
    consumers: [audit-log]

  - kind: cascade_routed
    effect_metric: self
    emitter: src/provider_cascade.rs (INFRA-1004)
    trigger: every successful LLM call — records which slot was chosen, its tier, and the active cascade_mode
    fields_required: [slot, tier, cascade_mode]
    consumers: [audit-log, fleet-brief]

  - kind: cascade_report
    effect_metric: self
    emitter: scripts/overnight/99-cascade-consumption-report.sh
    trigger: daily provider RPD utilization summary
    consumers: [fleet-brief]

  - kind: pricing_drift
    effect_metric: self
    emitter: scripts/dev/refresh-model-prices.sh (INFRA-731)
    trigger: model_rates.yaml drifted > 5% from LiteLLM upstream
    consumers: [fleet-brief, audit-log]

  # ── Mission / pillar metrics ──────────────────────────────────────────────
  - kind: mission_grade
    effect_metric: operator_action_or_gap_filed_within_60s
    emitter: scripts/ops/mission-auto-grader.sh (INFRA-599)
    trigger: per-cycle pillar grade computed
    consumers: [fleet-brief]

  - kind: audit_finding
    effect_metric: operator_action_within_60s_for_same_feature_id
    emitter: src/audit.rs (INFRA-1370, `chump audit aha-sweep`)
    trigger: ambient-kind sweep finds a registered kind whose recent emit count diverges from its expected_min_per_day floor (or whose effect_metric=self yields 0 emits when --flag-silent-self is set)
    consumers: [fleet-brief, audit-log, PWA <chump-view-audit-findings>]
    fields_required: [ts, kind, feature_id, feature_class, plumbing_ok, heartbeat_ok, effect_ok, effect_metric_name, effect_observed, effect_expected_min, severity, note]

  - kind: pillar_imbalance
    effect_metric: self
    emitter: scripts/dispatch/_pick_and_claim_gap.py (INFRA-720)
    trigger: one pillar > 60% of recent ships for 8h
    consumers: [fleet-brief, operator-recall]

  - kind: rebalance_active
    effect_metric: self
    emitter: picker
    trigger: pillar bias kicks in
    consumers: [fleet-brief]

  - kind: curator_decision
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-848)
    trigger: curator audit run produces a structured decision on fleet/gap health
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, decision_type, reasoning, action_taken]
    docs: |
      decision_type is one of: p0_demotion | gap_ac_filled | gap_filed |
      pr_unstick | waste_investigation | balance_restock | auto_decompose.
      action_taken is either a description of what was done or
      "identified_only" when the decision flags an issue for operator action.

  - kind: curator_auto_decompose
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-943)
    trigger: curator Decision 3b fires — a pillar has 0 pickable xs/s/m gaps
             AND an l/xl gap with no depends_on exists in that pillar
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, pillar, gap_id, sub_gaps_filed]
    docs: |
      Emitted when the curator calls 'chump gap decompose <gap_id> --apply'
      to break a large gap into smaller pickable units rather than only filing
      a fresh MISSION-* tracking gap. sub_gaps_filed is a comma-separated list
      of newly created gap IDs. At most one per curator run (guard).

  - kind: decompose_sliced
    effect_metric: self
    emitter: scripts/coord/decompose-loop.sh (INFRA-1924)
    trigger: curator-opus-decompose slice subcommand reviewed and (when --auto-accept)
             filed sub-gaps for an umbrella via 'chump gap decompose'
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, parent_id, slice_count]
    docs: |
      Emitted by the productized decompose curator loop (INFRA-1924). parent_id is
      the umbrella gap; slice_count is the number of sub-gaps proposed (capped at
      --max-slices, default 5); child_ids is a JSON array of newly filed sub-gap
      IDs when --auto-accept was passed (empty in dry-run mode). AC source: INFERRED
      from chump gap decompose CLI semantics + CLAUDE.md two-phase decomposition
      doctrine — confirm-or-refactor when curator-opus-decompose wakes up.

  - kind: decompose_audit
    effect_metric: self
    emitter: scripts/coord/decompose-loop.sh (INFRA-1924)
    trigger: curator-opus-decompose audit-pending subcommand scanned open gaps for
             stale umbrellas (open >7d with "Rough shape:" / "decompose at claim
             time" / "umbrella" / "sub-slice" doctrine phrasing)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, candidate_count]
    docs: |
      Emitted each audit-pending tick. candidate_count is the number of umbrellas
      flagged for decomposition. Stop condition: when candidate_count is 0 (and
      inbox empty), the loop exits 0 so cron doesn't spin.

  - kind: decompose_heartbeat
    effect_metric: self
    emitter: scripts/coord/decompose-loop.sh (INFRA-1924)
    trigger: curator-opus-decompose heartbeat subcommand fires each cron tick
             (every 30 min via scripts/launchd/com.chump.decompose-loop.plist)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, role]
    docs: |
      Liveness signal for the decompose curator role. role is always
      "curator-opus-decompose". Pairs with kind=decompose_audit and
      kind=decompose_sliced to verify the role is active when expected.

  - kind: system_gap_tick
    effect_metric: self
    emitter: scripts/coord/system-gap-tick.sh (INFRA-841)
    trigger: a scheduled task (curator, fast-path, gardener) begins a run
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, task, run_id]
    docs: |
      Heartbeat emitted by scripts/coord/{opus-curator,emergency-fast-path}.sh
      on each scheduled invocation. interval_s is included when the task is
      declared in scripts/coord/system-gap-frequencies.yaml. task identifies
      the scheduled-task name (opus-curator | emergency-fast-path | gap-gardener).

  - kind: daemon_started
    effect_metric: self
    emitter: src/main.rs `chump fleet daemon` (INFRA-964)
    trigger: chump fleet daemon process starts and parses its task config
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, tasks, mode]
    docs: |
      mode is "once" (--once dev/test invocation) or "loop" (long-lived
      production daemon). tasks is a comma-separated list of declared task
      names from scripts/coord/system-gap-frequencies.yaml. Absence of this
      event for > 1 launchd KeepAlive cycle means the daemon failed to
      start; investigate /tmp/chump-fleet-daemon.err.log.

  - kind: daemon_tick
    effect_metric: self
    emitter: src/main.rs `chump fleet daemon` (INFRA-964)
    trigger: each scheduled task invocation by the daemon, after it returns
    consumers: [fleet-brief, audit-log, watchdog]
    fields_required: [ts, kind, task, interval_s, run_id, exit_code, elapsed_ms]
    docs: |
      Replaces the Claude-Code-hosted scheduled-tasks MCP path. exit_code is
      the script's exit status; non-zero means the task failed (visible in
      ambient AND in /tmp/chump-fleet-daemon.err.log). elapsed_ms is wall-clock
      run time. Missed daemon_tick events for a known interval_s mean the
      daemon itself is dead — check `launchctl list | grep fleet-daemon`.

  - kind: opus_roadmap_published
    effect_metric: self
    emitter: opus curator (META-054, ad-hoc)
    trigger: opus curator publishes docs/ROADMAP_OPUS_SEQUENCING.md
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap, path]
    docs: |
      One-shot event marking that the opus-curator wave-sequencing roadmap
      doc was (re-)published. Useful for after-the-fact "when did sequencing
      change?" reconstruction.

  - kind: fleet_resize_decision
    effect_metric: self
    emitter: src/main.rs (chump fleet auto-resize, INFRA-650)
    trigger: fleet auto-resize evaluated 4 conditions (queue-empty, cost-cap,
      flat-ship-rate, operator-absent) and produced a resize recommendation
    consumers: [fleet-brief, operator-recall, opus-curator]
    fields_required: [ts, kind, trigger, current_size, recommended_size, rationale]

  - kind: gap_failed
    effect_metric: self
    emitter: scripts/coord/detect-gap-failure.sh (INFRA-872)
    trigger: detector classifies a stuck lease or failing PR as a gap failure
    consumers: [fleet-brief, audit-log, auto-rollback]
    fields_required: [ts, kind, gap_id, class, reason, recovery_action, age_s, detector]
    docs: |
      Failure-class taxonomy:
        - transient    — network / rate-limit; safe to auto-retry
        - code_quality — failing checks; needs human review
        - stalled      — no progress on lease for >2h
        - infra        — bot-merge or CI infra failure
      recovery_action ∈ {auto_rollback, manual_review}. Auto-execute is
      consumed by INFRA-889; INFRA-872 only emits.

  - kind: ship_grade
    effect_metric: self
    emitter: scripts/ops/ship-quality-grader.sh
    trigger: per-PR ship-quality grade computed
    consumers: [kpi-report]

  - kind: weekly_health_digest
    effect_metric: self
    emitter: scripts/overnight/weekly-health-digest.sh (INFRA-646)
    trigger: weekly summary cron
    consumers: [operator-recall]

  - kind: ci_health
    effect_metric: self
    emitter: scripts/ops/ci-health.sh
    trigger: CI health metric
    consumers: [fleet-brief]

  - kind: shared_ci_blocker
    effect_metric: self
    emitter: scripts/ops/shared-ci-blocker.sh
    trigger: cross-PR CI blocker detected
    consumers: [operator-recall]

  - kind: botmerge_wedged
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh stage_start watchdog + stage_done (INFRA-1422)
    trigger: >
      A bot-merge stage exceeded CHUMP_BOT_MERGE_STAGE_BUDGET_S (default 300s);
      watchdog fires, emits this event, and sends SIGTERM to the bot-merge process.
    consumers: [fleet-brief, waste-tally, operator-recall]
    fields_required: [ts, stage, elapsed_s, budget_s, gap]

  - kind: botmerge_recovery_start
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1422 CHUMP_BOT_MERGE_RECOVERY_MODE=1)
    trigger: operator retried bot-merge with RECOVERY_MODE=1 after a wedge event; fast-path push+PR+merge starts
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, gap, branch]

  - kind: botmerge_recovery_done
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1422 CHUMP_BOT_MERGE_RECOVERY_MODE=1)
    trigger: recovery fast-path completed; PR created/updated and auto-merge armed
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, gap, pr, branch]

  - kind: bot_merge_hang
    effect_metric: self
    emitter: scripts/ops/stuck-bot-merge.sh
    trigger: bot-merge.sh hanging > threshold
    consumers: [operator-recall]

  - kind: bot_merge_hung
    effect_metric: self
    emitter: scripts/ops/stale-bot-merge-reaper.sh
    trigger: bot-merge.sh hung beyond reaper threshold
    consumers: [audit-log]

  - kind: bot_merge_stall_detected
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1399)
    trigger: pre-push hook stalled longer than CHUMP_BOT_MERGE_PHASE_TIMEOUT_S (default 300s) during the git push step; typically caused by cargo fmt or another slow pre-push gate under sibling contention
    consumers: [fleet-brief, waste-tally, operator-recall]
    fields_required: [ts, kind, phase, timeout_s, gap_id, branch, note]

  - kind: bot_merge_test_gate_skipped
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1399)
    trigger: bot-merge delegated the cargo test gate to CI by setting CHUMP_TEST_GATE=0 before push; CI on the PR runs the same tests, making the pre-push gate redundant and the source of sibling contention
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, branch, note]

  - kind: bot_merge_hot_file
    effect_metric: self
    emitter: scripts/ops/bot-merge-watch.sh
    trigger: many bot-merges touching same file
    consumers: [audit-log]

  # ── INFRA-2119: webhook-cache MERGED wait ─────────────────────────────────
  - kind: bot_merge_webhook_hit
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-2119 webhook-cache MERGED wait)
    trigger: bot-merge observed the PR's merged_at field via cache_lookup_pr
      (webhook-populated github_cache.db) while waiting for MERGED transition
      under CHUMP_BOT_MERGE_WAIT_MERGED=1. Used to measure the webhook-hit vs
      poll-fallback ratio that motivated killing the bot_merge_hung wedge class.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, pr, gap, elapsed_s, polls, merged_at, note]

  - kind: bot_merge_timeout
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-2119 webhook-cache MERGED wait)
    trigger: bot-merge's CHUMP_BOT_MERGE_WAIT_MERGED=1 wait loop hit
      CHUMP_BOT_MERGE_WAIT_TIMEOUT_S (default 900s = 15 min) without
      observing the PR's merged_at transition. The bash caller (typically a
      sub-agent dispatch wrapper) should switch to manual recovery per the
      SUBAGENT_DISPATCH.md STOP-block contract. Replaces silent bot_merge_hung
      wedges with an explicit non-zero exit.
    consumers: [fleet-brief, waste-tally, operator-recall]
    fields_required: [ts, kind, pr, gap, elapsed_s, timeout_s, polls, source, note]

  - kind: bot_merge_contention_avoided
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-860 mutex)
    trigger: flock wait > 5s acquiring bot-merge.lock; another bot-merge was active
    consumers: [waste-tally, fleet-brief]
    fields_required: [ts, kind, branch, wait_s]

  - kind: scratch_commit_blocked
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-993)
    trigger: pre-push diff vs origin/main has > CHUMP_SCRATCH_GUARD_MAX_DELETIONS (default 50000) deletions OR deletions > CHUMP_SCRATCH_GUARD_RATIO (default 100) × additions
    consumers: [audit-log, watchdog, oncall]
    fields_required: [ts, kind, gap_id, additions, deletions, files, threshold_abs, threshold_ratio, branch]
    docs: |
      The repo-wipe protection guard. Fires when a PR would delete an
      unreasonable fraction of the repo. Near-miss observed 2026-05-11
      (PRs #1441, #1452 each proposing -378K lines). bot-merge.sh exits
      with code 15 on this event.

  - kind: scratch_commit_override_used
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-993)
    trigger: --allow-mass-delete flag set on a push that would otherwise trip the scratch-commit guard
    consumers: [audit-log, watchdog, oncall]
    fields_required: [ts, kind, gap_id, additions, deletions, files, threshold_abs, threshold_ratio, branch]
    docs: |
      Louder than scratch_commit_blocked because it records that an
      operator deliberately overrode the safety guard. Audit-log
      consumers should keep these as long as bot_merge_blocked events.

  # INFRA-950: register kinds that the system already emits but were never
  # captured here. waste-tally classifies these as of this gap.
  - kind: pr_stuck_cluster
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-848)
    trigger: 3+ stuck PRs detected in same audit cycle; correlated stall
    consumers: [waste-tally, fleet-brief, watchdog]
    fields_required: [ts, kind, count]

  - kind: fleet_auth_fallback
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-622)
    trigger: primary auth path failed; fallback path used for the spawn
    consumers: [waste-tally, audit-log]
    fields_required: [ts, kind, failed_mode, fallback_mode]

  # ── INFRA-1118: A2A Layer 1a NATS-primary delivery ────────────────────────
  - kind: fleet_a2a_degraded
    effect_metric: self
    emitter: crates/chump-coord/src/events.rs (INFRA-1118)
    trigger: >
      NATS broker unreachable at session start or dropped mid-session while
      CHUMP_A2A_LAYER=1 is active. Worker falls back to ambient.jsonl polling
      within 5s. Pairs with fleet_a2a_recovered when NATS comes back.
    consumers: [fleet-brief, ops-audit, watchdog]
    fields_required: [ts, kind, session_id, reason]
    docs: "Layer 1a fallback path — INFRA-1118 A2A ROADMAP §Layer 1a"

  - kind: fleet_a2a_recovered
    effect_metric: self
    emitter: crates/chump-coord/src/events.rs (INFRA-1118)
    trigger: >
      NATS broker reconnected after a fleet_a2a_degraded event. JetStream
      durable consumer resumed from the last-acked offset so no events are
      missed during the gap. Pairs with fleet_a2a_degraded.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, session_id, durable_name, resume_sequence]
    docs: "Layer 1a recovery path — INFRA-1118 A2A ROADMAP §Layer 1a"

  - kind: fleet_a2a_backpressure
    effect_metric: self
    emitter: crates/chump-coord/src/events.rs (INFRA-1118)
    trigger: >
      JetStream push consumer breached max_ack_pending; slow consumer detected.
      Pending count logged; consumer is paused until ack drain completes. Visible
      in `chump fleet-status` A2A section.
    consumers: [fleet-brief, ops-audit, watchdog]
    fields_required: [ts, kind, session_id, pending_count, max_ack_pending]
    docs: "Layer 1a backpressure — INFRA-1118 A2A ROADMAP §Layer 1a"

  - kind: slo_breach
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-848)
    trigger: curator audit detected an SLO violation (chump health --slo-check non-zero)
    consumers: [waste-tally, fleet-brief, watchdog]
    fields_required: [ts, kind, severity]

  # ── Subagent / cognition ──────────────────────────────────────────────────
  - kind: subagent_budget_exceeded
    effect_metric: self
    emitter: scripts/dispatch/subagent-budget-monitor.sh
    trigger: dispatched subagent exceeded token/time cap
    consumers: [audit-log]

  - kind: subagent_killed_at_budget
    effect_metric: self
    emitter: src/dispatch.rs (chump dispatch — wait_with_hang_detection, INFRA-1972)
    trigger: parent-enforced subagent budget exceeded; SIGTERM sent at
      CHUMP_SUBAGENT_BUDGET_S (default 900s), then SIGKILL after 30s grace.
      Distinct from subagent_budget_exceeded (script-side soft signal) and
      from hang_detector (longer fall-through 3600s wall-clock guard).
    consumers: [fleet-brief, watchdog, operator-recall, audit-log]
    fields_required: [ts, session, kind, gap, process, pid, budget_secs]

  - kind: subagent_heartbeat
    effect_metric: self
    emitter: src/execute_gap.rs (INFRA-2056)
    trigger: subagent (chump --execute-gap) emits periodic liveness signal every
      CHUMP_SUBAGENT_HEARTBEAT_SECS (default 60s) while agent.run() is in flight;
      wizard-daemon infers agent dying/dead if heartbeat >120s stale (INFRA-2056)
    consumers: [subagent-watchdog, fleet-brief, wizard-daemon]
    fields_required: [ts, session, kind, gap_id, pid, last_action, iter_count]

  - kind: subagent_silent
    effect_metric: self
    emitter: scripts/ops/subagent-watchdog.sh (INFRA-334)
    trigger: subagent heartbeat not seen within SUBAGENT_SILENT_TIMEOUT_S
      (default 900s) — subagent likely hung, crashed, or detached
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, gap_id, age_seconds, subagent_silent_timeout_s]

  - kind: lessons_shown
    effect_metric: self
    emitter: src/lessons_engine.rs (COG family)
    trigger: lessons block injected into prompt
    consumers: [audit-log]

  - kind: lessons_audit_run
    effect_metric: self
    emitter: scripts/ops/lessons-audit.sh
    trigger: periodic lessons quality audit
    consumers: [fleet-brief]

  - kind: lessons_injection_active
    effect_metric: self
    emitter: src/lessons_engine.rs
    trigger: lessons system actively shaping prompts
    consumers: [audit-log]

  - kind: lessons_pruned
    effect_metric: self
    emitter: scripts/ops/lessons-pruner.sh
    trigger: stale lessons removed
    consumers: [audit-log]

  - kind: lesson_applied
    effect_metric: self
    emitter: src/lessons_engine.rs
    trigger: a specific lesson influenced an agent decision
    consumers: [audit-log]

  - kind: lesson_not_applied
    effect_metric: self
    emitter: src/lessons_engine.rs
    trigger: lesson available but agent did not apply it
    consumers: [audit-log]

  - kind: asks_clarification
    effect_metric: self
    emitter: scripts/dispatch/clarifying-question-detector.sh
    trigger: dispatched agent asked a question instead of acting
    consumers: [waste-tally, fleet-brief]

  - kind: assertion_failure
    effect_metric: self
    emitter: src/assertion.rs (CREDIBLE-065, runtime assertion framework)
    trigger: gap claim or ship operation detects data invariant violation
    consumers: [audit-log, fleet-brief, operator-dashboard]
    fields_required: [ts, gap_id, assertion_type, field_name, expected, actual, context]

  - kind: avoids_tool
    effect_metric: self
    emitter: scripts/dispatch/tool-avoidance-detector.sh
    trigger: agent skipped a recommended tool
    consumers: [audit-log]

  - kind: autonomous_mode_entered
    effect_metric: self
    emitter: scripts/dispatch/autonomous-mode-monitor.sh
    trigger: agent entered fully-autonomous decision path
    consumers: [audit-log]

  - kind: autonomous_mode_exited
    effect_metric: self
    emitter: scripts/dispatch/autonomous-mode-monitor.sh
    trigger: agent left autonomous decision path
    consumers: [audit-log]

  # ── Operator / paging ─────────────────────────────────────────────────────
  - kind: operator_recall
    effect_metric: self
    emitter: scripts/dispatch/operator-recall.sh (INFRA-665)
    trigger: an event needs operator attention
    consumers: [discord, telegram messaging adapter]

  - kind: escalation
    effect_metric: self
    emitter: operator-agent (INFRA-737, future)
    trigger: Layer-2 Sonnet escalates to Layer-3 Opus
    consumers: [audit-log, operator-recall]

  - kind: operator_escalation_unjustified
    effect_metric: self
    emitter: future detector (not yet wired); manually emitted by orchestrator on self-audit until detector lands
    trigger: >
      Emitted when an agent invokes operator-escalation (AskUserQuestion / operator-recall.sh)
      outside the 4 legitimate triggers (T1 irreversible third-party / T2 credential-rotation /
      T3 operator-explicit-domain / T4 halt-class fleet condition). Reviewed via
      scripts/dev/operator-escalation-leaderboard.sh. See AGENTS.md → No-operator-escalation discipline.
    consumers: [scripts/dev/operator-escalation-leaderboard.sh]
    fields_required:
      - role        # the agent role that escalated
      - escalation_kind  # ask_user_question | operator_recall | direct_message
      - trigger_matched  # T1 | T2 | T3 | T4 | none
    registered_by: META-207

  - kind: manual_rescue
    effect_metric: self
    emitter: scripts/dispatch/manual-rescue-emit.sh
    trigger: operator manually intervened (logged for ledger)
    consumers: [fleet-brief]

  # ── Misc ──────────────────────────────────────────────────────────────────
  - kind: file_edit
    effect_metric: self
    emitter: PostToolUse hook (Edit / Write)
    trigger: any agent edits a file
    consumers: [ambient-context-inject]

  - kind: commit
    effect_metric: self
    emitter: PostToolUse hook (Bash git commit)
    trigger: any commit made by an agent
    consumers: [ambient-context-inject, audit-log]

  - kind: intent
    effect_metric: self
    emitter: scripts/dispatch/intent-emit.sh
    trigger: agent declares pending intent (rebase, ship, etc.)
    consumers: [coord]

  - kind: notable_finding
    effect_metric: self
    emitter: scripts/ops/notable-finding-emit.sh
    trigger: agent flagged something worth operator review
    consumers: [operator-recall]

  - kind: decomposition_hint
    effect_metric: self
    emitter: scripts/dispatch/decomposition-detector.sh
    trigger: agent suggested a gap should be split
    consumers: [audit-log]

  - kind: recurring_gap_pattern
    effect_metric: self
    emitter: scripts/ops/pattern-detector.sh
    trigger: same failure shape filed multiple times
    consumers: [operator-recall]

  - kind: roadmap_status
    effect_metric: self
    emitter: scripts/ops/roadmap-status.sh
    trigger: periodic roadmap progress snapshot
    consumers: [fleet-brief]

  - kind: roadmap_inject_applied
    effect_metric: self
    emitter: scripts/coord/ambient-context-inject.sh (INFRA-1146)
    trigger: chump roadmap-status drift summary injected into SessionStart context
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, starved_count, untraced_p0_count]

  # ── Log health (INFRA-1468) ───────────────────────────────────────────────────
  - kind: ambient_rotation_lagging
    effect_metric: self
    emitter: src/ambient_emit.rs maybe_warn_and_rotate (INFRA-1468)
    trigger: >
      ambient.jsonl exceeded CHUMP_AMBIENT_MAX_MB threshold (default 10 MB).
      Written immediately before the in-process rotation so it lands in the
      archived .1 copy. Repeat events indicate rotation is failing.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, size_bytes, threshold_bytes]

  - kind: ambient_rotated
    effect_metric: self
    emitter: scripts/ops/ambient-rotate-now.sh (INFRA-1468)
    trigger: >
      scripts/ops/ambient-rotate-now.sh completed a force-rotation.
      Written to the fresh ambient.jsonl immediately after the rename so
      every rotation is traceable.
    consumers: [ops-audit]
    fields_required: [ts, kind, size_bytes_before, lines_before, archive]

  # (product_drought removed by INFRA-1287: emitter scripts/ops/pillar-drought-detector.sh
  #  never shipped; drought detection done via chump gap list + pillar counts)

  - kind: disk_critical
    effect_metric: self
    emitter: scripts/ops/disk-health-monitor.sh + disk-watchdog.sh
    trigger: disk free < 5% (CHUMP_DISK_CRITICAL_PCT); fleet pauses
    consumers: [operator-recall, fleet-brief]

  - kind: disk_critical_auto_remediated
    effect_metric: self
    emitter: scripts/ops/disk-health-monitor.sh (INFRA-1440)
    trigger: disk_critical fired and target-dir-reaper --critical completed; freed_gb
      captures bytes reclaimed; emitted even if 0 bytes freed
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, freed_gb, timeout_s]

  - kind: disk_low
    effect_metric: self
    emitter: scripts/ops/disk-health-monitor.sh + disk-pressure-watchdog.sh
    trigger: disk free < 10% (CHUMP_DISK_LOW_PCT); warning-level alert
    consumers: [operator-recall, fleet-brief]

  - kind: disk_audit_report
    effect_metric: self
    emitter: scripts/coord/disk-audit.sh (INFRA-1469)
    trigger: disk-audit.sh run manually or on a schedule; reports top-N consumers
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, kind, free_gb, used_gb, top_paths]

  - kind: disk_recovered
    effect_metric: self
    emitter: scripts/ops/disk-pressure-watchdog.sh
    trigger: disk free rose above the low threshold after a disk_low event
    consumers: [fleet-brief]

  - kind: delta_recorded
    effect_metric: self
    emitter: scripts/coord/reflect-delta.sh
    trigger: agent reflection delta captured
    consumers: [chump_improvement_targets]

  - kind: alert
    effect_metric: self
    emitter: generic ALERT wrapper (event=ALERT,kind=...)
    trigger: meta-kind for the event=ALERT envelope
    consumers: [fleet-brief, ambient-context-inject]
    notes: "Always paired with a specific sub-kind. Use the specific kind in registry, not 'alert'."

  - kind: affinity_starved
    effect_metric: self
    emitter: scripts/dispatch/_pick_and_claim_gap.py (INFRA-314)
    trigger: worker has skill constraints but no matching gaps
    consumers: [fleet-brief]

  # ── Activation telemetry (chump-init) ─────────────────────────────────────
  - kind: ftue_init_smoke_passed
    effect_metric: self
    emitter: scripts/ci/test-chump-init-clean-machine.sh
    trigger: all 5 FTUE init smoke checks passed (chump --version, init, config.toml, state.db, mcp list)
    consumers: [fleet-brief, kpi-report]

  - kind: mcp_server_installed
    effect_metric: self
    emitter: src/mcp_discovery.rs::install_mcp_server
    trigger: chump mcp install <name> completes (any outcome — already_installed, cargo_installed, config_only)
    consumers: [fleet-brief, audit-log]

  # ── Bot-autonomous CI assertion (CREDIBLE-002) ───────────────────────────
  - kind: bot_autonomous_check_passed
    effect_metric: self
    emitter: scripts/ci/test-bot-autonomous.sh
    trigger: pr-triage-bot committed an auto-fix to the fixture PR within the timeout
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, outcome, fixture_pr]

  - kind: bot_autonomous_check_failed
    effect_metric: self
    emitter: scripts/ci/test-bot-autonomous.sh
    trigger: pr-triage-bot did NOT commit within the timeout (bot may be broken)
    consumers: [fleet-brief, watchdog, operator-paging]
    fields_required: [ts, outcome, fixture_pr]

  - kind: activation_install
    effect_metric: self
    emitter: scripts/setup/install.sh
    trigger: fresh chump install on a machine
    consumers: [audit-log]

  - kind: activation_first_task
    effect_metric: self
    emitter: chump init / first-run hook
    trigger: first agent task on a new install
    consumers: [audit-log]

  - kind: activation_return_d2
    effect_metric: self
    emitter: scripts/ops/activation-tracker.sh
    trigger: operator returned to chump on day 2
    consumers: [audit-log]

  # ── Adversary / safety ────────────────────────────────────────────────────
  # (adversary_alert removed by INFRA-1287: emitter scripts/ops/adversary-detector.sh
  #  never shipped; no planned gap; concept superseded by general RESILIENT monitoring)

  # ── Aggregations ──────────────────────────────────────────────────────────
  # (aggregate removed by INFRA-1287: generic placeholder, no concrete emit site;
  #  real aggregations emit domain-specific kinds like fleet_metrics_snapshot)

  # ── Bounced-PR detector (INFRA-781) ───────────────────────────────────────
  - kind: pr_bounced_unfinished
    effect_metric: self
    emitter: scripts/coord/bounced-pr-detector.sh
    trigger: PR closed without merging AND no equivalent file content has
      landed in main since close — work bounced into the void; auto-files
      a recovery gap when CHUMP_BOUNCED_PR_AUTO_FILE_GAP=1 (default on)
    consumers: [fleet-brief, watchdog, operator-paging]
    fields_required: [ts, source, pr, note]

  - kind: pr_bounced_relanded
    effect_metric: self
    emitter: scripts/coord/bounced-pr-detector.sh
    trigger: PR closed unmerged but >=50% of touched files have post-close
      merged commits — work re-routed, informational only
    consumers: [fleet-brief]
    fields_required: [ts, source, pr, note]

  # ── Orphan-PR closer (INFRA-1139) ─────────────────────────────────────────
  - kind: orphan_pr_candidate
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh (dry-run mode)
    trigger: open non-draft PR whose gap is status=done — eligible to close as superseded
    consumers: [fleet-brief]
    fields_required: [ts, kind, pr, gap, note]

  - kind: orphan_pr_closed
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh --apply
    trigger: orphan PR auto-closed because its gap is already done
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, pr, gap, note]

  - kind: orphan_pr_closed_at_ship
    effect_metric: self
    emitter: src/main.rs (INFRA-1144, chump gap ship)
    trigger: orphan PR closed atomically when gap is marked done via chump gap ship
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, pr, gap, ship_pr]
    notes: structural orphan closing at write time; complements orphan-pr-closer.sh sweeper (INFRA-1139)

  - kind: orphan_pr_close_failed
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh --apply
    trigger: orphan PR detected but gh PATCH /pulls/N state=closed failed — operator surface
    consumers: [operator-recall]
    fields_required: [ts, kind, pr, gap, note]

  - kind: orphan_pr_close_evidence_missing
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh, scripts/coord/close-superseded-prs.sh
    trigger: INFRA-1289 — gap is status=done but git log origin/main --grep=<gap-id> returns no commits; close is blocked until operator investigates
    consumers: [operator-recall, audit-log]
    fields_required: [ts, kind, pr, gap, note]
    fields_optional: [gap_id, reason, source]
    notes: replaces the false-positive close pattern; operator should verify if chump gap ship was called prematurely

  - kind: pr_close_skipped_auto_merge_armed
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh (INFRA-1326)
    trigger: orphan-closer found a done-gap PR but auto_merge is non-null — PR is queued
      for GitHub to merge itself; closing now would race the merge
    consumers: [fleet-brief, operator-recall, audit-log]
    fields_required: [ts, kind, pr, gap, note]

  - kind: pr_close_skipped_ci_running
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh (INFRA-1326)
    trigger: orphan-closer found a done-gap PR but at least one check_run has
      status=queued or status=in_progress — closing mid-CI races the merge path
    consumers: [fleet-brief, operator-recall, audit-log]
    fields_required: [ts, kind, pr, gap, note]

  - kind: orphan_close_skipped
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh (INFRA-1423)
    trigger: orphan-closer found that the gap yaml (docs/gaps/<id>.yaml) exists in
      the PR's own commit diff — gap is being registered by this PR; closing would
      be a false positive caused by state.db drift
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, pr, gap, note]

  # ── Review-as-Handoff telemetry (INFRA-773) ──────────────────────────────
  - kind: review_handoff_initiated
    effect_metric: self
    emitter: src/review_handoff.rs (chump review --serve daemon)
    trigger: reviewer-agent posts a [handoff:apply] comment on a PR
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, reviewer_session, failure_surface]

  - kind: review_handoff_applied
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (author re-engagement loop)
    trigger: author-agent push lands and CI flips green after applying handoff diff
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, author_session, handoff_comment_id]

  - kind: review_handoff_failed
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (author re-engagement loop)
    trigger: author-agent push lands but CI is still red after applying handoff diff
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, pr, author_session, failure_detail]

  - kind: review_handoff_timeout
    effect_metric: self
    emitter: src/review_handoff.rs (chump review --serve daemon)
    trigger: no author push within 15 min of handoff comment; escalation queued
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, pr, reviewer_session, elapsed_seconds]

  - kind: review_handoff_escalated
    effect_metric: self
    emitter: src/review_handoff.rs (chump review --serve daemon)
    trigger: failure or timeout triggers operator notification via configured channel
    consumers: [fleet-brief, operator-paging]
    fields_required: [ts, pr, reason, channel]

  - kind: review_handoff_branch_diverged
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-778 branch-diverged guard)
    trigger: handoff comment found on PR whose branch file overlap vs merge-base is < 50%;
      indicates branch was recycled for unrelated work; auto-apply skipped
    consumers: [fleet-brief, operator-recall, kpi-report]
    fields_required: [ts, kind, pr_number, overlap_pct, agent_id, gap_id]

  # ── PR-stale auto-rebase (RESILIENT-006) ─────────────────────────────────
  - kind: pr_rescue_triggered
    effect_metric: self
    emitter: scripts/coord/pr-rescue.sh
    trigger: PR identified as rescue candidate — has auto-merge armed, CI fails on PR but passes on main HEAD, age >= PR_RESCUE_STALE_HOURS
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, detail]

  - kind: pr_rescue_completed
    effect_metric: self
    emitter: scripts/coord/pr-rescue.sh
    trigger: PR branch successfully rebased onto main HEAD, force-pushed, and auto-merge re-armed
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, detail]

  - kind: pr_rescue_failed
    effect_metric: self
    emitter: scripts/coord/pr-rescue.sh
    trigger: rebase failed (merge conflict or push rejected); PR left unchanged for human intervention
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, pr, detail]

  - kind: pr_rescue_timeout
    effect_metric: self
    emitter: scripts/coord/pr-rescue.sh (INFRA-1153)
    trigger: wall-clock timeout (CHUMP_PR_RESCUE_TIMEOUT_S, default 300s) exceeded before all PRs processed; script exits early to avoid secondary rate-limit abuse
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, detail]

  # ── INFRA-2169: Mode D external-ship audit receipt ────────────────────────
  - kind: external_repo_ship
    effect_metric: effective
    emitter: scripts/coord/pr-rescue.sh (INFRA-2169)
    trigger: >
      A cross-repository (fork) PR was successfully rebased and merged
      by the fork-aware shepherd (INFRA-2114). Emitted once per merged
      fork PR so operators can audit Mode D (external-repo) throughput.
      Also fires broadcast.sh DONE <gap_id> <head_sha> for A2A routing.
    consumers: [fleet-brief, kpi-report, ops-audit]
    fields_required: [ts, kind, gap_id, external_repo, pr_url, head_sha, files_touched_count, shipper_session]
    related_kinds: [pr_rescue_completed]
    status: stable
    # payload: {ts: string, gap_id: string, external_repo: "owner/repo", pr_url: string, head_sha: string, files_touched_count: number, shipper_session: string}

  # ── Chump paramedic rule-engine rescue daemon (INFRA-1375) ───────────────
  - kind: paramedic_action
    emitter: src/paramedic.rs (INFRA-1375)
    trigger: paramedic daemon determined an action to take on an open PR (REBASE_DIRTY, RERUN_FLAKE, ALLOWLIST_EMIT_NO_REG, SQUASH_INIT_LEAK, FILE_CLUSTER_RESCUE); emitted once per action per PR per triage cycle
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, kind, pr_number, action, reason, dry_run]
    fields_optional: [gap_id]
    effect_metric: self

  - kind: paramedic_heartbeat
    emitter: src/paramedic.rs (INFRA-1397)
    trigger: paramedic daemon leader completes one triage+execute cycle; emitted every --interval-secs (default 600s); absence for 15min triggers L4-SLO-1 breach (chump health --slo-check)
    consumers: [fleet-brief, chump health --slo-check]
    fields_required: [ts, kind, machine, pid, cycle_count, pr_count, last_action_count]
    fields_optional: [dry_run]
    effect_metric: self

  - kind: paramedic_standby
    effect_metric: self
    emitter: src/paramedic.rs (INFRA-1397)
    trigger: paramedic daemon process lost leader election and is waiting for leader expiry; emitted every 60s from the standby process
    consumers: [fleet-brief]
    fields_required: [ts, kind, machine, pid]
    fields_optional: [dry_run]

  # ── INFRA-1713: CI-failure rescue action ──────────────────────────────────
  - kind: ci_rescue_attempt
    effect_metric: self
    emitter: src/paramedic.rs (INFRA-1713)
    trigger: >
      paramedic RESCUE_CI_FAILURE action fires on a BLOCKED PR with ≥1 required
      check FAILURE and no author push in the last 30 min; emitted once per
      dispatch attempt with the outcome (dispatched | gave_up | dry_run).
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, kind, pr, check_name, failure_class, outcome]
    fields_optional: [dry_run]

  - kind: ci_rescue_exhausted
    effect_metric: self
    emitter: src/paramedic.rs (INFRA-1713)
    trigger: >
      3 consecutive RESCUE_CI_FAILURE attempts on the same PR all failed;
      paramedic posts a "recommend manual review" comment and stops attempting.
      Signals the operator that the PR needs human attention.
    consumers: [fleet-brief, kpi-report, operator-recall]
    fields_required: [ts, kind, pr, check_name, attempt_count]

  # ── Centralized auto-merge armer (INFRA-1113) ─────────────────────────────
  - kind: auto_merge_armed
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1113)
    trigger: gh pr merge --auto succeeded for a PR; spacing enforced to avoid secondary rate limits
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, detail]

  - kind: auto_merge_arm_failed
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1113)
    trigger: gh pr merge --auto failed after retries; PR left unarmed
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, pr, detail]

  - kind: auto_merge_arm_verified
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1438)
    trigger: post-arm gh pr view confirmed autoMergeRequest is non-null; attempt_count
      records whether the 30s retry was needed
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, pr, detail]

  - kind: auto_merge_arm_verify_failed
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1438)
    trigger: gh pr merge --auto returned 0 but autoMergeRequest was null after 2s +
      30s retry; PR left unarmed (silent GraphQL exhaustion)
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, pr, detail]

  - kind: auto_merge_rest_direct
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1113)
    trigger: auto-merge armer fell back to direct REST merge (GraphQL unavailable);
      PR merged via REST API rather than auto-merge queue
    consumers: [fleet-brief, audit-log]

  # ── Auto-merge rearm daemon (INFRA-2309) ──────────────────────────────────
  - kind: auto_merge_rearmed
    effect_metric: effective
    emitter: scripts/coord/auto-merge-rearm-daemon.sh (INFRA-2309)
    trigger: >
      CLEAN PR had autoMergeRequest=null (disarmed by force-push/rebase);
      daemon re-armed auto-merge via gh pr merge --auto --squash.
      dry_run=true when CHUMP_AUTO_MERGE_REARM_DRY_RUN is set.
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, pr, reason, dry_run]

  - kind: auto_merge_rearm_skipped
    effect_metric: self
    emitter: scripts/coord/auto-merge-rearm-daemon.sh (INFRA-2309)
    trigger: >
      CLEAN+unarmed PR was found but its title prefix is not in
      scripts/coord/lib/fix-class-allowlist.txt; operator must either
      set CHUMP_AUTO_MERGE_REARM_OPEN=1 or add the prefix to the allowlist.
    consumers: [fleet-brief]
    fields_required: [ts, kind, pr, reason, dry_run]

  - kind: auto_merge_rearm_failed
    effect_metric: self
    emitter: scripts/coord/auto-merge-rearm-daemon.sh (INFRA-2309)
    trigger: gh pr merge --auto --squash returned non-zero for a CLEAN+allowlisted PR;
      PR remains unarmed. Investigate gh auth / branch protection settings.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, pr, reason, dry_run]
  # ── ARM-drop watcher (INFRA-2289) ─────────────────────────────────────────
  # Detects PRs whose auto-merge ARM was silently cleared after a conflict-rebase
  # race (e.g. two PRs both touching src/main.rs rebased in quick succession).
  # scanner-anchor: scripts/coord/auto-merge-arm-watcher.sh

  - kind: auto_merge_arm_dropped
    effect_metric: resilient
    emitter: scripts/coord/auto-merge-arm-watcher.sh (INFRA-2289)
    trigger: >
      Emitted when the watcher detects an OPEN PR with autoMergeRequest=null
      whose head_sha matches a successful auto-rebase event in the last 30 min.
      Precedes an immediate re-arm attempt. Carries pr, sha (12-char prefix).
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, pr, sha]
    expected_volume: "0-N; non-zero indicates rebase/ARM race; should be rare (<5/day)"
    status: stable
    # payload: {pr: number, sha: string (12-char prefix), ts: string}

  - kind: auto_merge_arm_restored
    effect_metric: resilient
    emitter: scripts/coord/auto-merge-arm-watcher.sh (INFRA-2289)
    trigger: >
      Emitted after a successful gh pr merge --auto --squash following an
      auto_merge_arm_dropped detection. Confirms the PR is re-armed.
      Carries pr, sha (12-char prefix). Pairs with auto_merge_arm_dropped.
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, pr, sha]
    expected_volume: "matches auto_merge_arm_dropped volume on successful re-arm"
    status: stable
    # payload: {pr: number, sha: string (12-char prefix), ts: string}

  - kind: auto_merge_arm_skipped
    effect_metric: resilient
    emitter: scripts/coord/auto-merge-arm-watcher.sh (INFRA-2289)
    trigger: >
      Emitted when a safety guard prevents re-arming. Reason field indicates
      which guard fired: chump_hold_label (CHUMP_HOLD label present),
      operator_recent_commit (human pushed to the branch in last 30 min),
      rate_limit_reached (CHUMP_AM_WATCHER_RATE_PER_HOUR cap exceeded).
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, pr, reason]
    expected_volume: "0-N; rate_limit_reached expected only on high-volume rebase days"
    status: stable
    # payload: {pr: number (optional), reason: string, ts: string}

  # ── Free-tier dispatch testing ─────────────────────────────────────────
  - kind: free_tier_e2e
    effect_metric: self
    emitter: scripts/dispatch/free-tier-e2e-test.sh
    trigger: free-tier e2e test completes (pass, partial, timeout, or fail)
    consumers: [fleet-brief]
    fields_required: [provider, model, result, detail]

  # ── Flake auto-rerun harness (INFRA-764) ──────────────────────────────────
  - kind: flake_autorerun_initiated
    effect_metric: self
    emitter: scripts/ci/cargo-test-with-rerun.sh
    trigger: cargo test failed AND every failing test is in
      docs/process/KNOWN_FLAKES.yaml; about to rerun once
    consumers: [fleet-brief, kpi-report, waste-tally]
    fields_required: [ts, source, note]

  - kind: flake_autorerun_recovered
    effect_metric: self
    emitter: scripts/ci/cargo-test-with-rerun.sh
    trigger: rerun-after-flake-detection went green; CI treats as transient
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, source, note]

  - kind: flake_autorerun_persisted
    effect_metric: self
    emitter: scripts/ci/cargo-test-with-rerun.sh
    trigger: rerun-after-flake-detection still red; surface to CI as real failure
    consumers: [fleet-brief, waste-tally, watchdog]
    fields_required: [ts, source, note]

  - kind: flake_autorerun_skipped
    effect_metric: self
    emitter: scripts/ci/cargo-test-with-rerun.sh
    trigger: at least one failing test is NOT in KNOWN_FLAKES.yaml, or no
      parseable failure shape detected; auto-rerun declined
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, source, note]

  - kind: feature_silent_failure
    effect_metric: self
    emitter: scripts/ci/run-feature-smokes.sh
    trigger: a load-bearing feature path (chump --briefing, bot-merge.sh) produced
      empty or structurally incomplete output for a known input — cascading-silent-
      failure class detected (INFRA-396)
    consumers: [fleet-brief, watchdog, kpi-report]
    fields_required: [ts, kind, feature, reason]

  # ── Orchestrate conversational loop (INFRA-796) ──────────────────────────
  - kind: orchestrate_intent
    effect_metric: orchestrate_action_within_60s_for_same_session
    emitter: src/orchestrate.rs (chump orchestrate interactive loop)
    trigger: operator types a natural-language intent; emitted once per loop iteration
    consumers: [fleet-brief, kpi-report, cost-watch]
    fields_required: [ts, kind, intent, status, tool_count, est_input_tokens, est_output_tokens, elapsed_ms, failure_class]
    docs: |
      status: "success" when all tool calls exit 0; "failure" when any tool call
        returns an error string.
      failure_class: "transient" (network/rate-limit/timeout) | "permanent"
        (syntax/unknown-command) | "none" (no failures).
      tool_count: number of TOOL: lines dispatched this iteration.
      est_input_tokens / est_output_tokens: rough 1-token-per-4-chars estimate.

  - kind: orchestrate_session_end
    effect_metric: self
    emitter: src/orchestrate.rs (chump orchestrate — "exit" or EOF)
    trigger: operator types "exit"/"quit" or closes stdin
    consumers: [fleet-brief]
    fields_required: [ts, kind, intent, status]

  - kind: orchestrate_llm_timeout
    effect_metric: self
    emitter: src/orchestrate.rs (chump orchestrate — INFRA-1364)
    trigger: LLM call exceeds CHUMP_ORCHESTRATE_LLM_TIMEOUT_S (default 60s); emitted once
      per timed-out attempt (max 2 per intent before session abort)
    consumers: [fleet-brief, kpi-report, ops-audit]
    fields_required: [ts, kind, session_id, attempt_number, call_kind, elapsed_s]
    docs: |
      attempt_number: 1 (first timeout, retry follows) or 2 (second timeout, session aborted).
      call_kind: "intent_parse" — the orchestrator currently only wraps intent LLM calls.
        "tool_select" and "summary" are reserved for future routing layers.
      elapsed_s: actual wall-clock seconds elapsed before the timeout fired; should
        be approximately CHUMP_ORCHESTRATE_LLM_TIMEOUT_S.
      session_id: matches CHUMP_ORCHESTRATE_SESSION_ID or the synthesized session identity.
      On attempt_number=2, a companion orchestrate_session_end{status="timeout"} is also
        emitted, and INFRA-1363 session_summary will carry exit_reason="timeout".
  - kind: orchestrate_session_summary
    effect_metric: self
    emitter: src/orchestrate.rs (chump orchestrate — emitted on each session exit, INFRA-1363)
    trigger: session exits for any reason (clean, user_quit, error)
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, session_id, exit_reason, intents_routed]

  - kind: orchestrate_session_resumed
    emitter: src/orchestrate.rs (chump orchestrate --resume <session-id>)
    trigger: operator invokes --resume for a non-clean-exited session within the max-resume limit
    effect_metric: self
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, session_id, events_replayed, resume_attempts, prior_exit_reason]
    docs: |
      events_replayed: number of ambient events loaded for this session_id (up to 200).
      resume_attempts: cumulative count including this invocation (1-based).
      prior_exit_reason: exit_reason from the last orchestrate_session_summary event,
        "unknown" if no summary event exists yet (pre-INFRA-1363 sessions).
      Emitted before the interactive loop resumes so the event is always recorded
      even if the resumed session immediately crashes again.
      Refusal conditions (exit non-zero, no event emitted):
        - exit_reason=clean → "session already completed"
        - resume_attempts > CHUMP_ORCHESTRATE_MAX_RESUMES (default 3) → loop guard
  - kind: orchestrate_session_summary
    emitter: src/orchestrate.rs (chump orchestrate — emitted once at session end, INFRA-1363)
    trigger: every chump orchestrate session ends, regardless of exit path (clean, user_quit, crash, timeout)
    consumers: [fleet-brief, kpi-report, cost-watch, ops-audit]
    effect_metric: self
    fields_required: [ts, kind, session_id, intents_routed, intents_failed, tool_calls, cost_usd, wall_time_s, exit_reason]
    docs: |
      session_id: matches CHUMP_ORCHESTRATE_SESSION_ID env var if set, else
        synthesized as "orchestrate-<pid>-<epoch>".
      intents_routed: total operator intents processed this session (int).
      intents_failed: intents where any tool call returned an error string (int).
      tool_calls: total TOOL: lines dispatched across all intents (int).
      cost_usd: approximate dollar cost based on estimated token usage, 4 decimal
        places (float). Uses opus pricing ($15/1M input, $75/1M output) as the
        orchestrator defaults to opus.
      wall_time_s: elapsed seconds from session start to summary emit (int).
      exit_reason: one of "clean" (exit/quit typed), "user_quit" (EOF/Ctrl-D),
        "crash" (unhandled error), "timeout" (future INFRA-1364 hang detection).

  # ── Intent parser (INFRA-798) ─────────────────────────────────────────────
  - kind: intent_parse_ok
    effect_metric: self
    emitter: src/intent_parser.rs (chump orchestrate <text> single-shot mode)
    trigger: natural-language intent resolved to a known IntentOp variant
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, command]

  - kind: intent_parse_unknown
    effect_metric: self
    emitter: src/intent_parser.rs (chump orchestrate <text> single-shot mode)
    trigger: pattern matching returned IntentOp::Unknown — intent not recognized
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, command]

  - kind: intent_parse_llm
    effect_metric: self
    emitter: src/main.rs (chump orchestrate <text> single-shot LLM fallback — INFRA-1452)
    trigger: >
      pattern matching returned Unknown AND a configured LLM provider was available;
      LLM resolved the intent to a chump command via TOOL: response line.
    consumers: [fleet-brief, kpi-report, cost-ledger]
    fields_required: [ts, raw, command, provider]

  # ── Pre-registered eval gate (META-043) ──────────────────────────────────
  - kind: prereg_blocked
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh §4c
    trigger: commit modifies cognition/routing src but docs/eval/preregistered/<GAP-ID>.md is absent; bot-merge aborts
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, gap, missing, session]

  - kind: prereg_ok
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh §4c
    trigger: cognition/routing src changed and preregistered eval doc confirmed present; ship proceeds
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, gap, doc, session]

  # ── CREDIBLE-037: harness attribution ─────────────────────────────────────
  - kind: missing_attribution
    effect_metric: self
    emitter: scripts/dev/ambient-emit.sh (CREDIBLE-037)
    trigger: CHUMP_AGENT_HARNESS not set and no --harness flag passed; ambient
      event emitted with harness=unknown; one-shot ALERT per session
    consumers: [fleet-brief]
    fields_required: [ts, session, harness, hint]

  # ── CREDIBLE-039: closure consistency ─────────────────────────────────────
  - kind: stale_post_merge_gap
    effect_metric: self
    emitter: scripts/ci/test-gap-closure-consistency.sh (CREDIBLE-039 reverse mode)
    trigger: state.db has status=open + closed_pr set, but the referenced PR is
      already merged on GitHub — gap should have been flipped to done
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, ids, note]

  - kind: premature_closure_auto_fixed
    effect_metric: self
    emitter: scripts/ci/test-gap-closure-consistency.sh --auto-fix (CREDIBLE-039)
    trigger: gap with status=done + closed_pr points to BLOCKED/DIRTY PR;
      auto-fix flipped status back to in_progress
    consumers: [fleet-brief]
    fields_required: [ts, gap_id, pr, old_status, new_status, mergeable]

  # ── META-033: system-invariants monitor ─────────────────────────────────
  - kind: invariant_violation
    effect_metric: self
    emitter: scripts/ops/system-invariants-monitor.sh (META-033)
    trigger: one of INV-1 through INV-7 detected a violation (PR pile-up,
      domain leak, stale reaper heartbeat, low disk, duplicate worktree
      paths, CI red on main, green-test regression)
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, inv, details]

  - kind: invariant_recovered
    effect_metric: self
    emitter: scripts/ops/system-invariants-monitor.sh (META-033)
    trigger: an invariant that was previously violating is now passing
    consumers: [fleet-brief]
    fields_required: [ts, inv]

  - kind: model_selected
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-843)
    trigger: worker picks a gap; emitted once per cycle to record which model was
      selected and why (gap.required_model override vs. routing.yaml vs. default)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, requested, actual, reason]
    docs: |
      reason values: gap_required_model (gap.required_model set and used),
      routing_yaml (INFRA-471 resolved a model from routing.yaml),
      fleet_model_default (FLEET_MODEL env with no override).
      requested = gap.required_model or '' if not set.
      actual = FLEET_MODEL after all overrides.

  # ── INFRA-906: per-gap performance profiling ──────────────────────────────
  - kind: gap_perf_sample
    effect_metric: self
    emitter: scripts/ci/emit-gap-timing.sh (INFRA-906)
    trigger: emitted by emit-gap-timing.sh wrapper after any timed CI phase
      completes; records wall-clock duration and exit code for p50/p95 analysis
    consumers: [fleet-brief, gap-perf-report]
    fields_required: [ts, kind, gap_id, phase, duration_ms, exit_code, host]
    docs: |
      phase values: lint, test, build, e2e (or any custom label)
      duration_ms: wall-clock milliseconds for the wrapped command
      exit_code: exit status of the wrapped command (0 = success)
      host: short hostname for cross-machine comparison
      Consumers: scripts/ops/gap-perf-report.sh computes p50/p95 per (gap_id,phase)

  # ── INFRA-879: credential lifecycle ───────────────────────────────────────
  - kind: credential_rotation_due
    effect_metric: self
    emitter: scripts/ops/credential-lifecycle.sh (INFRA-879)
    trigger: a tracked credential (ANTHROPIC_API_KEY, GH_TOKEN) has exceeded the
      rotation policy age (CHUMP_CRED_MAX_AGE_DAYS, default 90d); emitted once
      per over-age credential per invocation of credential-lifecycle.sh
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, cred_name, age_days, max_age_days]

  # ── INFRA-895: manual gap run-now trigger ─────────────────────────────────
  - kind: gap_run_now_triggered
    effect_metric: self
    emitter: scripts/ops/gap-run-now.sh (INFRA-895)
    trigger: operator manually invoked gap-run-now.sh to dispatch a gap outside
      the normal scheduler cycle; emitted before worker invocation (or instead
      of it in --dry-run mode)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, model, timeout_s, dry_run]

  - kind: worker_timeout_scaled
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-1160)
    trigger: worker scaled FLEET_TIMEOUT_S by the gap's effort estimate before
      spawning the claude subprocess. xs=0.5×, s=1.0×, m=1.5×, l=2.0×, xl=3.0×,
      capped at CHUMP_WORKER_TIMEOUT_MAX_S (default 7200s).
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, gap_id, effort, base_timeout_s, scaled_timeout_s]

  - kind: worker_timeout_no_commit
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-831)
    trigger: FLEET_TIMEOUT_S expired (rc=124) and claude produced no git commit
      during the cycle; worker attempted a WIP rescue commit via chump-commit.sh
      before releasing the lease. rescue_committed=1 means the rescue succeeded.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, agent_id, gap_id, timeout_s, rescue_committed]

  - kind: worker_first_output_timeout
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-828)
    trigger: claude subprocess produced no stdout within CHUMP_FIRST_OUTPUT_TIMEOUT_S
      (default 120s) and all retries (CHUMP_RETRY_TIMEOUT_S, max 2) were also
      timed out — worker killed subprocess and is moving to next cycle
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, agent_id, gap_id, elapsed_s, retries]

  # ── INFRA-765: stacked PR auto-rebase ─────────────────────────────────────
  - kind: stacked_pr_rebase_scan
    effect_metric: self
    emitter: scripts/coord/rebase-stacked-prs.sh (INFRA-765)
    trigger: base PR merged — rebase-stacked-prs.sh scanned for open PRs with
      base=merged_branch. stacked_count=0 means no stacked PRs found.
    consumers: [fleet-brief]
    fields_required: [ts, kind, merged_branch, stacked_count]

  - kind: stacked_pr_rebased
    effect_metric: self
    emitter: scripts/coord/rebase-stacked-prs.sh (INFRA-765)
    trigger: a PR stacked on a just-merged base branch was rebased onto
      origin/main and auto-merge was re-armed. status=ok or status=failed.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, merged_branch, stacked_pr, stacked_branch, status]

  # ── INFRA-539: gh API probe ────────────────────────────────────────────────
  - kind: github_unreachable
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh, scripts/dispatch/run-fleet.sh (INFRA-539, CREDIBLE-032)
    trigger: gh installed but api /rate_limit probe fails; backward-compat alias emitted
      alongside gh_errored — consumers may watch either kind
    consumers: [fleet-brief, operator-recall, watchdogs]
    fields_required: [ts, kind, source, exit_code, note]

  # ── CREDIBLE-032: gh failure-class taxonomy ───────────────────────────────
  - kind: gh_missing
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh, scripts/dispatch/run-fleet.sh (CREDIBLE-032)
    trigger: gh CLI binary not found in PATH at probe time
    consumers: [fleet-brief, watchdogs]
    fields_required: [ts, kind, source, note]

  - kind: gh_errored
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh, scripts/dispatch/run-fleet.sh (CREDIBLE-032)
    trigger: gh binary present but api /rate_limit call failed (auth error, rate-limit,
      network timeout, or any non-zero exit)
    consumers: [fleet-brief, operator-recall, watchdogs]
    fields_required: [ts, kind, source, exit_code, note]

  # ── INFRA-999: per-call GitHub API cost telemetry ─────────────────────────
  - kind: github_api_call
    effect_metric: self
    emitter: scripts/coord/lib/github.sh (chump_gh, chump_gh_record)
    trigger: any wrapped gh invocation. Records script name, 1-2 token api tag,
      remaining_core + remaining_graphql at time of call, elapsed ms, rc.
      Used to rank which scripts burn which rate-limit bucket (INFRA-1000).
    consumers: [api-cost-report]
    fields_required: [ts, kind, script, api, remaining_core, remaining_graphql, used_ms, rc]

  # ── INFRA-995: pre-push staleness gate ────────────────────────────────────
  - kind: stale_branch_blocked
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-995); src/main.rs chump gap ship (INFRA-1007)
    trigger: branch is >threshold (default 15) commits behind origin/main. phase=pre-push
      from bot-merge.sh; phase=gap-ship from the Rust manual-ship path.
    consumers: [waste-tally, fleet-brief]
    fields_required: [ts, kind, branch, behind, threshold, phase]

  # ── INFRA-1042: doc-only fast-path attribution ─────────────────────────────
  - kind: bot_merge_doc_only_fastpath
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1042, builds on INFRA-920)
    trigger: bot-merge auto-detects shell/doc-only diff (no .rs files) and skips
      cargo test + cargo clippy entirely. Each emission represents ~2-3 min saved
      vs the full pipeline (DOC-036 baseline 2m36s clippy for 1-line README).
    consumers: [waste-tally, fleet-brief]
    fields_required: [ts, kind, branch, files_changed, saved_steps]

  # ── META-059: gap-closure drift detector ──────────────────────────────────
  - kind: gap_closure_drift
    effect_metric: self
    emitter: scripts/coord/gap-doctor-reconcile.py --check-closure-drift (META-059)
    trigger: open gap has closed_pr set AND that PR is state=merged — the
      "OPEN-BUT-LANDED" antipattern bot-merge.sh:243 warns about but doesn't
      catch on its own. Surfaces silent auto-close failures.
    consumers: [fleet-brief, operator-recall, audit-priorities]
    fields_required: [ts, kind, gap_id, pr_number, merged_at, source]
  # ── INFRA-1040: graphql exhaustion fleet-wide signal ──────────────────────
  - kind: graphql_exhausted
    effect_metric: self
    emitter: scripts/coord/lib/github.sh (INFRA-1040, piggyback on every chump_gh call)
    trigger: GitHub GraphQL bucket remaining_graphql <= CHUMP_GH_EXHAUSTED_THRESHOLD
      (default 100). Debounced to once per reset window via
      .chump-locks/.graphql-exhausted-since. Lets every fleet agent pivot to
      REST-only paths (INFRA-1016 / `--rest-only`) without each having to
      discover exhaustion by hitting its own failed gh call first.
    consumers: [chump-ambient-glance, fleet-brief, operator-recall]
    fields_required: [ts, kind, threshold_seen, resets_at, source]

  # ── INFRA-1034: bot-merge per-PID log discoverability ─────────────────────
  - kind: bot_merge_log_started
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1034)
    trigger: bot-merge.sh sets up tee redirection to .chump-locks/bot-merge-<pid>.log.
      One emit per script invocation; lets operator-recall / chump-ambient-glance
      surface "bot-merge currently running, tail -f <path>" without scanning ps.
    consumers: [chump-ambient-glance, fleet-brief, operator-recall]
    fields_required: [ts, kind, pid, log_path, branch]

  # ── INFRA-1079: chump_gh secondary rate-limit self-throttle ──────────────
  - kind: gh_self_throttled
    effect_metric: graphql_call_rate_drop_after_emit
    emitter: scripts/coord/lib/github.sh chump_gh (INFRA-1079, INFRA-1112)
    trigger: class-specific token-bucket window detected >= cap calls in the last
      60s. INFRA-1112 splits into mutation bucket (CHUMP_GH_MUTATION_MAX=15/min)
      and query bucket (CHUMP_GH_QUERY_MAX=60/min). chump_gh sleeps 1s and retries;
      after 30s of waiting (fail-safe) it lets the call through with fail_safe=true
      so the fleet does not deadlock on a stuck counter. Each delay emits one event.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, script, api_class, waited_ms, calls_in_window]
  # ── INFRA-1077: daily API cost leaderboard digest ─────────────────────────
  - kind: api_cost_digest_emitted
    effect_metric: self
    emitter: scripts/dev/api-cost-leaderboard.sh --emit-ambient (INFRA-1077)
    trigger: launchd com.chump.api-cost-digest daily at 09:00 local — analyzes
      24h of kind=github_api_call events (INFRA-999 telemetry), emits a single
      digest with top burner identified. Lets fleet-brief surface "today's top
      script + api" without re-scanning ambient on every consumer call.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, window_hours, total_calls, top_script, top_api, total_estimated_points]

  # ── INFRA-1080: chump_gh pre-emptive backoff for low-GraphQL background calls
  - kind: gh_preempted
    effect_metric: self
    emitter: scripts/coord/lib/github.sh chump_gh (INFRA-1080)
    trigger: a background call (CHUMP_GH_CALL_CRITICALITY=background) hit
      chump_gh when remaining_graphql is below CHUMP_GH_BACKOFF_THRESHOLD %
      of the limit (default 10 %). The helper sleeps min(60s, time_to_reset)
      then re-checks. Critical calls are never preempted. Logs before / after
      remaining-percent so consumers see how much the wait helped.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, script, api, waited_s, remaining_percent_before, remaining_percent_after]

  # ── INFRA-2484: debug breadcrumb for skipped exhausted-emit on failed gh call
  - kind: gh_recording_skipped_rc_nonzero
    effect_metric: self
    emitter: scripts/coord/lib/github.sh chump_gh_record (INFRA-2484)
    trigger: chump_gh_record received rc != 0 from the recorded gh call. The
      rate-limit data fetched alongside a failed call is unreliable (may return
      sentinel -1 values), so the graphql_exhausted emit path is skipped to
      prevent false alarms. This breadcrumb lets operators audit how often
      cascade triggers originated from failing gh calls vs genuine exhaustion.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, script, api, rc, used_ms]

  # ── INFRA-1115: a2a mailbox primitive ─────────────────────────────────────
  - kind: inbox_advance
    effect_metric: self
    emitter: scripts/coord/chump-inbox.sh (INFRA-1115)
    trigger: a session ran `chump-inbox.sh read` and the cursor was advanced
      past one or more new messages. Emitted once per read; the count tells
      auditors how many messages the recipient consumed in this read.
    consumers: [fleet-brief, operator-recall, waste-tally]
    fields_required: [ts, kind, session, messages_read, new_offset]

  - kind: inbox_archived
    effect_metric: self
    emitter: scripts/coord/inbox-reap.sh (INFRA-1115)
    trigger: reaper detected a dead session (lease expired past grace
      window) with an unread inbox. The inbox was gzip-archived to
      .chump-locks/inbox-archive/<session>/<yyyy-mm>.jsonl.gz and the live
      inbox file deleted. Operator-recall surfaces this in case the
      original recipient's work was time-sensitive.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, session, unread_messages, archive]

  # ── INFRA-1117: chump pr nudge auto-diagnosis + structured comment ────────
  - kind: pr_nudged
    effect_metric: self
    emitter: scripts/coord/chump-pr-nudge.sh (INFRA-1117)
    trigger: scripts/coord/chump-pr-nudge.sh classified a stuck PR
      (dirty / blocked-ci / orphan-disarmed / base-modified / clean-not-merged),
      rendered the matching template, and posted a structured comment via
      REST. Cooldown: same (pr,sha,class) within
      CHUMP_NUDGE_COOLDOWN_HOURS (default 24h) skipped with NOTE.
    consumers: [fleet-brief, operator-recall, waste-tally]
    fields_required: [ts, kind, pr, sha, class]

  # ── INFRA-1116: INTENT-overlap gate at claim time ─────────────────────────
  - kind: intent_overlap_detected
    effect_metric: self
    emitter: scripts/coord/intent-overlap-check.sh (INFRA-1116)
    trigger: gap-preflight detected another live session's INTENT with
      overlapping path scope to the claiming session's CHUMP_CLAIM_PATHS.
      Preflight refuses with exit 14; operator coordinates or sets
      CHUMP_CLAIM_FORCE_OVERLAP=1 with a reason. Surfaces silent claim
      races (INFRA-779 family) that would otherwise produce concurrent
      shipping of the same gap.
    consumers: [fleet-brief, operator-recall, waste-tally]
    fields_required: [ts, kind, session, gap, my_paths, conflicting]

  - kind: intent_overlap_overridden
    effect_metric: self
    emitter: scripts/coord/intent-overlap-check.sh (INFRA-1116)
    trigger: caller set CHUMP_CLAIM_FORCE_OVERLAP=1 to bypass an
      overlap-detected refusal. CHUMP_CLAIM_OVERRIDE_REASON='<text>' is
      recorded for audit. Operator-recall surfaces these so repeat-bypass
      patterns are visible.
    consumers: [fleet-brief, operator-recall, audit-priorities]
    fields_required: [ts, kind, session, gap, paths, reason]

  - kind: intent_announced
    effect_metric: self
    emitter: src/atomic_claim.rs emit_intent_announced() (INFRA-1116)
    trigger: chump claim succeeds and emits this event so subsequent
      claiming sessions' INTENT gates can detect path overlap. Emitted with
      paths (from --paths CSV) and expires_at (= claim time + 4h TTL).
      Sessions without --paths emit nothing (undeclared scope = no overlap
      detection, not a refusal).
    consumers: [check_intent_overlap, fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, session_id, paths, expires_at]

  - kind: intent_retracted
    effect_metric: self
    emitter: src/atomic_claim.rs emit_intent_retracted() via chump --release (INFRA-1116)
    trigger: chump --release emits this event so other sessions' overlap
      gates immediately treat this session as inactive even if expires_at
      has not passed. check_intent_overlap filters retracted sessions from
      the live set before checking overlap.
    consumers: [check_intent_overlap, fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, session_id]

  - kind: intent_refreshed
    effect_metric: self
    emitter: scripts/dispatch/worker.sh heartbeat loop (INFRA-1116)
    trigger: worker heartbeat loop emits this every 30s while claude is
      running to confirm the INTENT is still active. Consumers can infer
      that a session with no recent intent_refreshed (and a stale lease)
      is inactive even before expires_at passes.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, session_id]

  # ── INFRA-1081: GitHub webhook cache ─────────────────────────────────────
  - kind: webhook_event_received
    effect_metric: self
    emitter: scripts/ops/github-webhook-receiver.py (INFRA-1081)
    trigger: a validated GitHub webhook (HMAC-SHA256 pass) was processed.
      Receiver upserts pr_state in .chump/github_cache.db. Consumers
      (queue-driver, etc.) read from cache instead of polling.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, event_type, action, pr_number]

  - kind: webhook_event_rejected
    effect_metric: self
    emitter: scripts/ops/github-webhook-receiver.py (INFRA-1081)
    trigger: webhook delivery failed HMAC-SHA256 validation or arrived
      malformed. Receiver returned 401/400. Either operator's secret is wrong,
      smee/webhook misconfigured, or an unauthorized POST hit the local port.
    consumers: [fleet-brief, operator-recall, watchdogs]
    fields_required: [ts, kind, reason, source_ip]

  - kind: cache_drift
    effect_metric: self
    emitter: scripts/ops/github-cache-reconcile.sh (INFRA-1081)
    trigger: periodic reconcile (every 5 min) found a pr_state row whose
      columns differ from a single gh api pulls?state=open call. Catches
      missed webhook deliveries.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, pr_number, columns]

  - kind: cache_hit
    effect_metric: gh_rest_call_count_24h_drop
    emitter: scripts/coord/lib/github_cache.sh (CREDIBLE-064)
    trigger: cache_lookup_pr / cache_query_behind_prs / cache_lookup_checks
      returns data from sqlite without a REST API call.
    consumers: [scripts/dev/cache-hit-rate.sh, fleet-brief]
    fields_required: [ts, kind, helper, target, age_s]

  - kind: cache_miss
    effect_metric: cache_hit_rate_24h
    emitter: scripts/coord/lib/github_cache.sh (INFRA-1081, CREDIBLE-064)
    trigger: a cache helper found no row, a stale row (TTL expired), or
      DB missing. Helper falls back to REST API and writes the result back.
    consumers: [fleet-brief, operator-recall, scripts/dev/cache-hit-rate.sh]
    fields_required: [ts, kind, helper, target, reason]

  - kind: cache_refilled
    effect_metric: self
    emitter: scripts/coord/lib/github_cache.sh (INFRA-1275)
    trigger: cache_refresh_open_prs fetched the /pulls?state=open list via
      REST (not GraphQL) and upserted rows into github_cache.db. Emitted once
      per refresh cycle with the row count inserted/updated.
    consumers: [fleet-brief, scripts/dev/cache-hit-rate.sh]
    fields_required: [ts, kind, helper, rows, source]

  - kind: cache_warmed
    effect_metric: self
    emitter: scripts/ops/github-cache-reconcile.sh (INFRA-1106)
    trigger: reconcile detected pr_state rows with empty mergeable_state (the
      bulk /pulls?state=open list omits it; GitHub computes asynchronously).
      Did a bounded per-PR REST fetch (max CHUMP_CACHE_RECONCILE_MAX_FETCH=20
      per cycle) and filled the column. One event per warmed row.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, pr_number, mergeable_state]
    fields_required: [ts, kind, script, api_class, waited_ms, calls_in_window]

  - kind: check_runs_warmed
    effect_metric: self
    emitter: scripts/ops/github-cache-reconcile.sh (INFRA-1129)
    trigger: reconcile detected pr_state rows whose head_sha had no check_runs
      entries (cold start — webhook not yet fired). Did a bounded per-SHA REST
      fetch (GET /commits/<sha>/check-runs, max CHUMP_CACHE_RECONCILE_MAX_FETCH=20
      per cycle) and inserted rows into the check_runs table. One event per
      backfilled SHA.
    consumers: [fleet-brief, operator-recall, bot-merge]
    fields_required: [ts, kind, head_sha, count]

  # ── INFRA-1065: chump binary auto-refresh ────────────────────────────────
  - kind: chump_binary_refreshed
    effect_metric: self
    emitter: scripts/ops/refresh-chump-binary.sh (INFRA-1065)
    trigger: hourly LaunchAgent detected origin/main has gap-store-affecting
      commits ahead of ~/.cargo/bin/chump's baked SHA; ran cargo install --path
      and the rebuild succeeded. Removes the daily friction of needing
      CHUMP_ALLOW_STALE_DESTRUCTIVE=1 on every gap-mutation call.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, baked_sha_before, commits_ahead, elapsed_s]

  - kind: chump_binary_refresh_failed
    effect_metric: self
    emitter: scripts/ops/refresh-chump-binary.sh (INFRA-1065)
    trigger: cargo install --path . --bin chump --force exited non-zero. Operator
      action needed — INFRA-825 staleness warnings will continue until the next
      successful refresh.
    consumers: [fleet-brief, operator-recall, watchdogs]
    fields_required: [ts, kind, baked_sha, commits_ahead, elapsed_s, rc]

  # ── INFRA-1600 follow-up: which target dir resolved for CI test scripts ──
  - kind: chump_bin_resolved
    effect_metric: self
    emitter: scripts/ci/lib/discover-chump-bin.sh (INFRA-1600 follow-up)
    trigger: CI test script sourced discover-chump-bin.sh and found a chump
      binary. source=cargo_target_dir on self-hosted runners using INFRA-1540
      shared cache; source=repo_root_target on GitHub-hosted runners.
    consumers: [fleet-brief, diagnostic-tooling]
    fields_required: [ts, kind, source, path]

  # ── INFRA-847: fleet-state.json mutex ─────────────────────────────────────
  - kind: fleet_state_lock_timeout
    effect_metric: self
    emitter: scripts/coord/emergency-fast-path.sh, scripts/dispatch/opus-curator.sh (INFRA-847)
    trigger: flock(1) wait on fleet-state.lock exceeded CHUMP_FLEET_STATE_LOCK_TIMEOUT_S
      (default 5s); script proceeds without lock (fail-open) to avoid deadlock
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, source, timeout_s, note]

  # ── INFRA-1068: fleet-state.json batch-write telemetry ────────────────────
  - kind: fleet_state_lock_contention
    effect_metric: self
    emitter: scripts/coord/emergency-fast-path.sh (INFRA-1068)
    trigger: flock(1) acquisition on fleet-state.lock took more than 1000ms,
      indicating write contention between concurrent workers. Emitted INSIDE
      the lock after acquisition succeeds (not on timeout), so the write
      still proceeds. Carry wait_ms so operators can track contention trends.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, wait_ms, note]

  - kind: fleet_state_batch_flush
    effect_metric: self
    emitter: scripts/coord/lib/fleet-state-writer.sh fleet_state_flush (INFRA-1068)
    trigger: fleet_state_flush applied one or more queued field updates to
      fleet-state.json in a single flock acquisition (batch mode). Emitted
      after every successful flush that had at least one queued write.
      fields_updated = number of distinct keys flushed (not raw queue lines —
      last-write-wins deduplication is applied before the flock grab).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, fields_updated, wait_ms]

  - kind: curator_decision
    effect_metric: self
    emitter: scripts/coord/opus-curator.sh (INFRA-848)
    trigger: curator completed a proactive health audit cycle and made a decision
      (p0_demotion, gap_ac_filled, gap_filed, pr_unstick, waste_investigation,
      or balance_restock). Emitted once per decision made.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, decision_type, reasoning, action_taken]

  - kind: change_approved
    effect_metric: self
    emitter: scripts/ops/change-approval.sh (INFRA-912)
    trigger: operator approves a high-risk fleet change (scale-up, model-override,
      routing-yaml-edit); approval token written to .chump-locks/change-approvals/
    consumers: [operator-recall, audit-log]
    fields_required: [ts, kind, change_id, approver, rationale]

  - kind: change_rolled_back
    effect_metric: self
    emitter: scripts/ops/change-approval.sh (INFRA-912)
    trigger: operator reverts a fleet change; fleet-state.json restored from
      pre-change snapshot in .chump-locks/fleet-state-snapshots/
    consumers: [operator-recall, audit-log]
    fields_required: [ts, kind, change_id, snapshot]

  - kind: fleet_metrics_snapshot
    effect_metric: self
    emitter: scripts/ops/fleet-metrics-snapshot.sh (INFRA-900)
    trigger: operator runs fleet metrics snapshot (manually or via cron); reads
      ambient.jsonl + state.db to compute ship_rate, waste_rate, cycle_time_p50,
      active_gaps, p0_count
    consumers: [fleet-brief, kpi-report, opus-curator]
    fields_required: [ts, kind, ship_rate_24h, waste_rate_24h, cycle_time_p50_h, active_gaps, p0_count]
  - kind: cascade_near_cap
    effect_metric: self
    emitter: scripts/ops/obs-alerting.sh (INFRA-679)
    trigger: ≥3 provider cascade slots have consumed >80% of their daily RPD limit;
      routed to ambient.jsonl and CHUMP_ALERT_WEBHOOK if set
    consumers: [operator-recall, watchdog]
    fields_required: [ts, kind, near_cap_slots, detail, threshold_pct]

  - kind: cost_budget_breach
    effect_metric: self
    emitter: scripts/ops/obs-alerting.sh (INFRA-679)
    trigger: daily Anthropic spend exceeds CHUMP_DAILY_COST_CAP_USD (or the budget_usd
      field in .chump/daily-cost.json); routed to ambient.jsonl and CHUMP_ALERT_WEBHOOK
    consumers: [operator-recall, watchdog]
    fields_required: [ts, kind, spent_usd, cap_usd, pct]

  # ── INFRA-1486: per-gap execution budgets (Marcus trust gate) ────────────
  - kind: gap_budget_warn
    effect_metric: marcus-trust
    emitter: src/budget_tracker.rs (INFRA-1486)
    trigger: per-gap budget tracker crossed 75% threshold on one dimension
      (file_touches, wallclock_minutes, dep_adds, or llm_cost_usd). Agent
      keeps going; operator can extend via `chump nudge --extend-budget`.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, dimension, used, max, pct]

  - kind: gap_budget_breach
    effect_metric: marcus-trust
    emitter: src/budget_tracker.rs (INFRA-1486)
    trigger: per-gap budget tracker reached 100% on one dimension. Returns
      BudgetAction::SoftPause to caller — agent harness must pause. The
      structural prevention for Marcus's "Frankenstein monster" compounding
      failure mode (2026-05-15 interview).
    consumers: [fleet-brief, operator-recall, watchdog]
    fields_required: [ts, kind, gap_id, dimension, used, max, pct]

  # ── INFRA-1488: merge-conflict-resolution agent (Marcus M-C) ─────────────
  - kind: conflict_resolve_start
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: rebase landed in conflict state and the resolver is dispatching an agent
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_success
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: agent resolved + preservation guard passed + rebase continued
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_dropped
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: preservation guard caught >2 dropped lines unique to one side
    consumers: [fleet-brief, operator-recall, watchdog]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_handoff
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: agent failed after CHUMP_CONFLICT_RETRIES attempts; operator-action-needed.json written
    consumers: [fleet-brief, operator-recall, watchdog]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_skipped
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: caller invoked the resolver but no conflicts present, or feature disabled
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_attempt_failed
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: single agent dispatch attempt returned non-zero
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_validated
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: preservation guard passed; about to git add + rebase --continue
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_continue_failed
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: git rebase --continue failed after agent resolution (e.g. test gate)
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap_id, body]

  - kind: conflict_resolve_failed
    effect_metric: marcus-daily-tax
    emitter: scripts/coord/conflict-resolver-agent.sh (INFRA-1488)
    trigger: structural prerequisites missing (no gap_id, etc)
    consumers: [fleet-brief, watchdog]
    fields_required: [ts, kind, gap_id, body]

  # ── INFRA-1673: local-CI discipline audit (preflight bypass) ─────────────
  - kind: preflight_bypassed
    deprecated: true
    effect_metric: self
    emitter: scripts/coord/chump-commit.sh (INFRA-1673)
    trigger: commit fires with CHUMP_PREFLIGHT_SKIP=1 while staged paths
      touch Rust or scripts (where local CI catches the bulk of failures).
      Each emit is one bypass — retrospectives sum these to surface
      patterns that warrant tooling or process changes.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, session, files]
    deprecation_note: >
      CHUMP_PREFLIGHT_SKIP deleted by INFRA-2422 (main-red-aware gate auto-skip).
      Event no longer emitted; kept here so ambient log replay doesn't trigger
      emit-without-register failures. Use preflight_main_red_skip going forward.

  - kind: no_bypass_violation
    effect_metric: self
    emitter: scripts/coord/lib/chump-strict-mode.sh (INFRA-1836 phase 1)
    trigger: CHUMP_NO_BYPASS=1 strict mode is active AND a script
      attempted to honor a bypass env (CHUMP_FMT_CHECK=0,
      CHUMP_PREFLIGHT_SKIP=1, --no-verify, etc.). Helper emits this
      audit line + exits 1. Call-site wires (chump-commit.sh,
      bot-merge.sh, etc.) land in follow-up gaps.
    consumers: [operator-recall, audit-log, fleet-brief]
    fields_required: [ts, kind, bypass_kind, would_have_skipped, session]
  # ── INFRA-1828 / META-061: A2A Layer 2b RPC v0 (bash wrappers) ───────────
  - kind: a2a_rpc_sent
    effect_metric: self
    emitter: scripts/coord/rpc/_rpc_lib.sh (INFRA-1828)
    trigger: an ask-* bash wrapper sent an RPC request to a peer session
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, method, target, request_id]

  - kind: a2a_rpc_timeout
    effect_metric: self
    emitter: scripts/coord/rpc/_rpc_lib.sh (INFRA-1828)
    trigger: client deadline (default 10s, CHUMP_RPC_TIMEOUT_S override)
      expired before a reply with corr_id=<request_id> landed in inbox.
      Per INFRA-1759 / INFRA-1119 design, no blocking RPC call ever
      exceeds its deadline; this kind announces the dropped call.
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, request_id, timeout_s]

  - kind: a2a_rpc_send_failed
    effect_metric: self
    emitter: scripts/coord/rpc/_rpc_lib.sh (INFRA-1828); crates/chump-coord/src/rpc.rs (INFRA-1119)
    trigger: broadcast.sh invocation rc != 0 when sending an RPC request; or NATS
      publish failed in call_rpc_with_nats
    consumers: [audit-log]
    fields_required: [ts, kind, method, target, request_id]
  # scanner-anchor: a2a_rpc_handler_crash
  - kind: a2a_rpc_handler_crash  # scanner-anchor
    effect_metric: self
    emitter: crates/chump-coord/src/rpc.rs::serve_rpc_with_nats (INFRA-1119)
    trigger: >
      Server-side RPC handler returned Err(...) or panicked. Emitted by the
      serve_rpc background task before returning error="handler_crash: <reason>"
      in the RpcResponse. Distinct from a2a_rpc_timeout (client-side deadline)
      and a2a_rpc_send_failed (transport failure). Client detects the
      "handler_crash:" prefix in the response error field and surfaces
      RpcError::HandlerCrash.
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, method, session, request_id, reason]
    docs: "docs/design/A2A_ROADMAP.md (META-061 Layer 2b)"
    status: stable
  # scanner-anchor: a2a_rpc_registered
  - kind: a2a_rpc_registered  # scanner-anchor
    effect_metric: self
    emitter: crates/chump-coord/src/rpc.rs::serve_rpc_with_nats (INFRA-1119)
    trigger: >
      serve_rpc_with_nats successfully subscribed to chump.rpc.<session>.<method>
      and spawned the background handler task. Emitted once per method at worker
      startup. Signals that the session is ready to answer that RPC method.
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, method, session, subject]
    docs: "docs/design/A2A_ROADMAP.md (META-061 Layer 2b)"
    status: stable
  # ── INFRA-1758 / META-061: A2A Layer 1a subscribe_events stub ────────────
  - kind: a2a_subscribe_stub_invoked
    effect_metric: self
    emitter: crates/chump-coord/src/events.rs::subscribe_events (INFRA-1758)
    trigger: >
      subscribe_events() stub called before the real NATS JetStream consumer
      (slice 2/4, INFRA-1118) is implemented. Signals which call-sites need
      wiring once the real delivery layer ships.
    consumers: [audit-log, fleet-brief]
    fields_required: [ts, kind, filter]
    docs: "docs/design/A2A_ROADMAP.md (META-061 Layer 1a)"
    status: stable
  - kind: bypass_threshold_breach
    effect_metric: self
    emitter: scripts/ops/audit-bypass-frequency.sh (INFRA-1837)
    trigger: daily auditor finds a session whose 24h bypass count
      (audit_no_verify + preflight_bypassed) exceeds CHUMP_BYPASS_DAILY_THRESHOLD
      (default 5). Pairs with INFRA-1872 (ci_qa_score) — that's the aggregate
      rear-view; this is the per-session shame-loop signal.
    consumers: [operator-recall, fleet-brief, audit-log]
    fields_required: [ts, kind, session, count, threshold, week_avg]

  - kind: audit_bypass_freq_bypassed
    effect_metric: self
    emitter: scripts/ops/audit-bypass-frequency.sh (INFRA-1837)
    trigger: CHUMP_AUDIT_BYPASS_FREQ=0 silences the daily auditor
    consumers: [audit-log]

  # ── RESILIENT-001: stale binary detection ────────────────────────────────
  - kind: stale_binary_detected
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (RESILIENT-001)
    trigger: worker startup detects target/debug/chump mtime > CHUMP_BINARY_MAX_AGE_SECS behind src/main.rs
    consumers: [fleet-brief, watchdog]
    fields_required: [ts, kind, source, age_secs, max_age_secs, note]

  - kind: stale_binary_destructive_override
    effect_metric: self
    emitter: src/version.rs (chump fleet version-check)
    trigger: operator forces a destructive action despite stale binary detection warning
    consumers: [audit-log, operator-recall]

  - kind: binary_refresh_started
    effect_metric: self
    emitter: src/version.rs (INFRA-1977 JIT binary refresh)
    trigger: stale binary detected + destructive op attempted; background
      `cargo install --path . --bin chump --force` spawned. Replaces the
      manual rebuild step that turned CHUMP_ALLOW_STALE_DESTRUCTIVE=1 into
      a routine bypass. State recorded at .chump/binary-refresh-state.json.
    consumers: [audit-log, fleet-brief]

  - kind: binary_refresh_completed
    effect_metric: self
    emitter: src/version.rs (INFRA-1977 JIT binary refresh — wrapper subshell)
    trigger: background `cargo install` succeeded; new chump binary on PATH.
      Next chump invocation finds Fresh and proceeds. Current invocation is
      still the stale binary and cannot self-replace.
    consumers: [audit-log, fleet-brief, watchdog]

  - kind: binary_refresh_failed
    effect_metric: self
    emitter: src/version.rs (INFRA-1977 JIT binary refresh — wrapper subshell)
    trigger: background `cargo install` failed (e.g. compile error, locked
      cargo registry, disk full). State marker records reason (last log
      line, truncated). Operator must rebuild manually or use
      CHUMP_ALLOW_STALE_DESTRUCTIVE=1 override.
    consumers: [audit-log, operator-recall]

  # ── Disk hygiene ─────────────────────────────────────────────────────────
  - kind: tmp_cleanup
    effect_metric: self
    emitter: scripts/dev/cleanup-claude-tmp.sh
    trigger: daily launchd job (04:00) or manual run; prunes stale /private/tmp/claude-*/ dirs
    consumers: [fleet-brief, opus-curator]
    fields_required: [ts, session, kind, dirs_pruned, files_removed, bytes_freed]

  - kind: gap_rollback_executed
    effect_metric: self
    emitter: scripts/ops/rollback-gap.sh (INFRA-899)
    trigger: operator (or curator) rolls back a stalled/failed gap; lease released,
      worktree removed, branch deleted, gap status reset to open
    consumers: [fleet-brief, operator-recall, opus-curator]
    fields_required: [ts, kind, gap_id, worktree_removed, branch_deleted, lease_released]

  # ── INFRA-634: cross-repo fleet startup ──────────────────────────────────
  - kind: fleet_cross_repo_start
    effect_metric: self
    emitter: scripts/dispatch/run-fleet.sh (INFRA-634)
    trigger: run-fleet.sh launched with --repo, --locks-dir, or --tmux-session
      override flags targeting a non-Chump repo
    consumers: [fleet-brief, fleet-status, opus-curator]
    fields_required: [ts, kind, repo, locks_dir, session]

  # ── Cognition-stack A/B experiment (META-045) ───────────────────────────────
  - kind: cognition_ab_run_start
    effect_metric: self
    emitter: scripts/experiments/cognition-ab-setup.sh
    trigger: one cell of the cognition-stack A/B experiment is launched; records cell config
    consumers: [cognition-ab-report]
    fields_required: [ts, kind, cell, run_tag, lessons_at_spawn_n, embedding_enabled, fleet_size]

  - kind: cognition_ab_comparison
    effect_metric: self
    emitter: scripts/experiments/cognition-ab-report.sh
    trigger: comparison report generated after both cells have run; points to source log files
    consumers: [kpi-report, meta-retrospective]
    fields_required: [ts, kind, log_a, log_b, run_tag]

  # ── RESILIENT-010/RESILIENT-011: bot-merge.sh step-specific failure events ──
  - kind: bot_merge_phase_failure
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (_bm_fail helper, RESILIENT-011)
    trigger: any named phase in bot-merge.sh fails; emitted immediately before
      the script exits with the step-specific exit code (10=preflight, 11=rebase,
      12=fmt, 13=clippy, 14=test, 15=push, 16=pr-create)
    consumers: [fleet-brief, opus-curator, emergency-fast-path, recovery-automation]
    fields_required: [ts, kind, step, exit_code, gap_id, branch]
  - kind: bot_merge_crashed
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (_bm_cleanup trap, INFRA-1035)
    trigger: bot-merge exits with an open step transition (SIGTERM, OOM, or
      set-e failure) — the step's stage_start had no matching stage_done
    consumers: [fleet-brief, emergency-fast-path]
    fields_required: [ts, kind, step, pid, steps_file]

  - kind: bot_merge_mode_failopen
    effect_metric: resilient
    emitter: scripts/coord/bot-merge.sh (_bm_emit_mode_failopen, INFRA-2523)
    trigger: bot-merge would route a gap to the batched integrator (Mode A) but
      the integrator daemon is unhealthy (binary missing at the plist path, or
      launchd last-exit != 0). Fails OPEN to per-PR auto-merge (Mode B) so the
      gap cannot land in dead-queue limbo (the INFRA-2455 / INFRA-1120/2188 class).
    consumers: [fleet-brief, audit-log, waste-tally]
    fields_required: [ts, kind, gap_id, from_mode, to_mode, reason]
    expected_volume: "once per ship while the integrator daemon is down"
  # ── FLEET-034 work-routing mesh consumer (INFRA-2545) ─────────────────────
  - kind: mesh_worker_routed
    effect_metric: effective
    emitter: scripts/coord/mesh-worker-loop.sh (INFRA-2545, route-only mode)
    trigger: the mesh-worker consumer picked a capability-matched, mesh-routed
      gap via chump-coord worker but is in ROUTE-ONLY mode (default) — it logs
      the routing + releases the NATS-KV claim without executing. Proves the
      FLEET-034 work-routing is live (coordinated, not coincidental, convergence).
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, session, mode]
    expected_volume: "once per mesh-worker tick that matched a gap (route-only)"
    status: stable
  - kind: mesh_worker_executing
    effect_metric: effective
    emitter: scripts/coord/mesh-worker-loop.sh (INFRA-2545, CHUMP_MESH_WORKER_EXECUTE=1)
    trigger: the mesh-worker picked a capability-matched gap and handed it to
      chump --execute-gap (execute mode). The NATS-KV routing-claim is released
      first; the executor takes the authoritative file-lease (coexists with the
      pull fleet — the file-lease arbitrates).
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, gap_id, session]
    expected_volume: "once per mesh-worker tick that executed a gap"
    status: stable
  # ── Gap store curation ───────────────────────────────────────────────────
  - kind: gap_store_curated
    effect_metric: self
    emitter: scripts/ops/gap-curate.sh
    trigger: nightly curation run (04:00 local) completes rebalance + consolidate + retention-sweep
    consumers: [fleet-brief, opus-curator, kpi-report]
    fields_required: [ts, kind, session, rebalanced, consolidated, retained, errors]
    trigger: operator runs fleet metrics snapshot; reads ambient.jsonl + state.db
      to compute ship_rate, waste_rate, cycle_time_p50, active_gaps, p0_count
    consumers: [fleet-brief, kpi-report, opus-curator, velocity-trending.sh]
    fields_required: [ts, kind, ship_rate_24h, waste_rate_24h, cycle_time_p50_h, active_gaps, p0_count]

  - kind: velocity_trend_computed
    effect_metric: self
    emitter: scripts/ops/velocity-trending.sh (INFRA-901)
    trigger: operator runs velocity-trending.sh to analyse fleet ship_rate and
      waste_rate trends over the last N days; compares last 3d vs prior 4d to
      classify trend as improving, stable, or degrading
    consumers: [fleet-brief, opus-curator, kpi-report]
    fields_required: [ts, kind, window_days, ship_rate_7d, waste_rate_7d, trend, days_sampled]
  - kind: cost_quota_warning
    effect_metric: self
    emitter: src/cost_ledger.rs (INFRA-877)
    trigger: daily spend reaches 80% of CHUMP_DAILY_BUDGET_USD; emitted before
      each new claude -p spawn when the warning threshold is breached
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap_id, model, cost_so_far_usd, limit_usd, budget_used_pct]

  - kind: cost_quota_exceeded
    effect_metric: self
    emitter: src/cost_ledger.rs (INFRA-877)
    trigger: daily spend reaches 100% of CHUMP_DAILY_BUDGET_USD; new spawns are
      hard-capped until UTC midnight reset
    consumers: [fleet-brief, operator-recall, emergency-fast-path]
    fields_required: [ts, kind, gap_id, model, cost_so_far_usd, limit_usd, budget_used_pct]

  # ── INFRA-890: network availability events ───────────────────────────────
  - kind: network_unavailable
    effect_metric: self
    emitter: scripts/ops/net-probe.sh
    trigger: api.anthropic.com unreachable after CHUMP_NET_PROBE_RETRIES attempts
      (default 3, 2s apart); fleet should pause dispatch until resolved
    consumers: [fleet-brief, opus-curator, emergency-fast-path]
    fields_required: [ts, kind, session, host, retries]

  - kind: network_restored
    effect_metric: self
    emitter: scripts/ops/net-probe.sh
    trigger: api.anthropic.com reachable again after a prior network_unavailable
      event; emitted exactly once on restoration
    consumers: [fleet-brief, opus-curator]
    fields_required: [ts, kind, session, host]
  # ── FLEET-048: Operator impact ratings ───────────────────────────────────
  - kind: gap_impact_rated
    status: active
    effect_metric: self
    emitter: src/main.rs (FLEET-048, chump gap rate)
    emitter_paths: [src/main.rs]
    trigger: operator rates a shipped gap 1-5 via 'chump gap rate <ID> <1-5>'
    consumers: [kpi-report --impact, atomic_claim::load_class_ratings (INFRA-1555 picker tie-break)]
    fields_required: [ts, kind, gap_id, rating, comment, pr_number]
  - kind: gap_abandoned
    effect_metric: self
    emitter: scripts/ops/gap-lifecycle-manager.sh (INFRA-870)
    trigger: gap has been open for more than CHUMP_LIFECYCLE_DAYS (default 90)
      without any commit activity; emitted once per stale gap per run
    consumers: [fleet-brief, operator-recall, opus-curator]
    fields_required: [ts, kind, gap_id, age_days, title]
  - kind: worktree_contaminated
    effect_metric: self
    emitter: scripts/ops/worktree-contamination-check.sh (INFRA-931)
    trigger: untracked files from a foreign gap-claim branch detected in the
      current worktree (docs/gaps/*.yaml or scripts/ paths belonging to another
      active gap session)
    consumers: [fleet-brief, operator-recall, opus-curator]
    fields_required: [ts, kind, gap_id, worktree_path, contaminated_count, example_file]


  # ── Test fixtures (synthetic kinds for unit-test scaffolding only) ────────
  - kind: obs_coverage_test_fixture
    effect_metric: self
    emitter: scripts/ci/test-obs-coverage-guard.sh
    trigger: synthetic event written by INFRA-757 test fixture; never emitted
      by production code paths
    consumers: []
    status: test-fixture

  # ── INFRA-827: stale worktree pruner ─────────────────────────────────────
  - kind: worktree_pruned
    effect_metric: self
    emitter: src/main.rs (chump fleet prune-worktrees --apply, INFRA-827)
    trigger: a linked worktree was removed because it was older than CHUMP_WT_MAX_AGE_H
      (default 48h) and its branch had no open GitHub PR and no uncommitted changes
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, path, branch, age_h]

  - kind: worktree_prune_summary
    effect_metric: self
    emitter: src/main.rs (chump fleet prune-worktrees --apply, INFRA-827)
    trigger: emitted once per prune-worktrees --apply run summarising total
      pruned and skipped counts
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, pruned, skipped_active_pr, skipped_uncommitted, skipped_young]

  # ── RESILIENT-013: orphaned /tmp worktree reaper ─────────────────────────
  - kind: worktree_orphan_pruned
    effect_metric: self
    emitter: scripts/ops/prune-worktrees.sh (RESILIENT-013, orphan mode)
    trigger: a /tmp/chump-* worktree had no active lease in .chump-locks/
      AND no open PR for its branch — safely removed
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, path, branch]

  - kind: worktree_orphan_skipped
    effect_metric: self
    emitter: scripts/ops/prune-worktrees.sh (RESILIENT-013, orphan mode)
    trigger: a /tmp/chump-* worktree was skipped because it had an active lease,
      an open PR, or uncommitted changes
    consumers: [fleet-brief]
    fields_required: [ts, kind, path, reason, branch]

  - kind: worktree_reaper_skipped_active
    emitter: >
      scripts/ops/stale-worktree-reaper.sh (_emit_reaper_skipped, INFRA-1074);
      scripts/coord/worktree-prune.sh (emit_reaper_event, INFRA-1347)
    trigger: >
      Reaper skips a worktree because in-flight work was detected. Reasons:
      active_lease (heartbeat_at within 15 min) — INFRA-1074;
      git_index_fresh (.git/index mtime within 30 min) — INFRA-1074/1347;
      uncommitted_changes (git status --porcelain non-empty, tracked files) — INFRA-1347;
      untracked_unignored (new files not yet `git add`'d, non-gitignored) — INFRA-1347;
      unpushed_commits (HEAD ahead of origin/<branch>) — INFRA-1347.
      Reaping in any of these states would destroy work — protection is mandatory.
    effect_metric: self
    emitter: scripts/ops/stale-worktree-reaper.sh (_emit_reaper_skipped, INFRA-1074)
    trigger: stale-worktree-reaper skips a worktree because an active lease
      (heartbeat_at within 15 min) or fresh .git/index mtime (within 5 min)
      indicates in-flight work — reaping would destroy uncommitted work
    consumers: [fleet-brief, emergency-fast-path]
    fields_required: [ts, kind, worktree, reason]

  - kind: worktree_reap_protected
    effect_metric: self
    emitter: >
      scripts/ops/stale-worktree-reaper.sh (_emit_worktree_reap_protected, INFRA-1291);
      scripts/ops/active-target-reaper.sh (_emit_worktree_reap_protected, INFRA-1291)
    trigger: >
      Reaper would have removed a worktree (or its target/) but spared it
      because an active lease's heartbeat_at is within CHUMP_LEASE_HEARTBEAT_TTL_S
      (default 600s). Distinct from worktree_reaper_skipped_active — this event
      specifically marks the heartbeat-freshness protection path, and includes
      the protecting lease name and the configured TTL.
    consumers: [fleet-brief, reaper-heartbeat-watchdog]
    fields_required: [ts, kind, worktree, lease, ttl_s]

  # ── RESILIENT-029: wip-stash before reap ────────────────────────────────
  - kind: worktree_work_stashed_before_reap
    effect_metric: self
    emitter: scripts/ops/stale-worktree-reaper.sh (_wip_stash_work, RESILIENT-029)
    trigger: >
      A reapable worktree had uncommitted changes or unpushed commits at reap
      time. The reaper committed any loose changes (git add -A + commit
      --no-verify) and pushed them to a wip/<gap_id>-<ts> branch on origin
      before calling git worktree remove --force. Emitted only when the push
      succeeds; push failure is logged to /tmp/stale-worktree-reaper.log with
      a WARN banner but does not block the reap.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap_id, branch, uncommitted_lines, unpushed_commits, original_worktree]

  # ── ZERO-WASTE-001: read-only stale-worktree audit ───────────────────────
  - kind: worktree_stale_detected
    effect_metric: disk_used_bytes{component="worktree"} (fleet-brief surfaces stale count; drop to 0 = all cleaned)
    emitter: scripts/ops/audit-stale-worktrees.sh (ZERO-WASTE-001)
    trigger: >
      Read-only audit found a worktree whose directory mtime exceeds
      --age-hours (default 24h) AND either (a) has no matching lease in
      .chump-locks/ ("no_lease_ever_existed"), or (b) has a lease whose
      heartbeat_at is older than the same threshold ("orphaned_lease").
      Distinct from worktree_reaper_skipped_active and worktree_orphan_pruned —
      this event is an early-warning surface BEFORE the reaper trips; no
      deletion happens here. Pruning belongs to INFRA-1347 worktree-prune.sh.
    consumers: [fleet-brief, disk-health-monitor]
    fields_required: [ts, kind, worktree, age_hours, reason, branch, lease]

  # ── CREDIBLE-038: file-count-blast rule ──────────────────────────────────
  - kind: pr_scope_violation
    effect_metric: self
    emitter: scripts/ci/check-mass-deletion.sh (CREDIBLE-038, rule C)
    trigger: PR title prefix (chore(gaps): or docs:) promises narrow scope
      but the diff includes files outside the expected directory for that prefix
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, rule, title_prefix, out_of_scope_count]

  # ── CREDIBLE-041: no-bundle-PR detector ──────────────────────────────────
  - kind: pr_bundle_blocked
    effect_metric: self
    emitter: scripts/ci/check-pr-scope.sh (CREDIBLE-041, Rule C)
    trigger: PR title lists multiple gap IDs (e.g. INFRA-1,INFRA-2) that are
      not depends_on-linked in .chump/state.db — indicates bundled unrelated gaps
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, gap_ids, depends_on_check_result, branch]

  # ── INFRA-944: gap dep-clean ──────────────────────────────────────────────
  - kind: dep_cleaned
    effect_metric: self
    emitter: src/main.rs (chump gap dep-clean --apply, INFRA-944)
    trigger: a stale depends_on entry pointing at a done gap was stripped
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap_id, stripped_deps]

  # ── CREDIBLE-048: per-gate fire-rate telemetry ───────────────────────────
  - kind: gate_check_start
    effect_metric: self
    emitter: scripts/ci/lib/gate-emit.sh (sourced by each CI gate — CREDIBLE-048)
    trigger: any CI gate begins execution
    consumers: [gate-fire-rate, fleet-brief]
    fields_required: [ts, kind, gate, pr_or_branch, args]

  - kind: gate_check_result
    effect_metric: self
    emitter: scripts/ci/lib/gate-emit.sh (sourced by each CI gate — CREDIBLE-048)
    trigger: any CI gate completes (pass / fail / bypassed / skipped)
    consumers: [gate-fire-rate, fleet-brief, curator]
    fields_required: [ts, kind, gate, outcome, rule_fired, evidence, pr_or_branch]
  # ── INFRA-1003: auto-rerun out-of-scope PR check ─────────────────────────
  - kind: auto_rerun_out_of_scope
    effect_metric: self
    emitter: scripts/coord/auto-rerun-out-of-scope.sh (INFRA-1003)
    trigger: PR check failed on tests with zero overlap with the PR diff; rerun
      triggered once to distinguish environmental flake from real regression
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, pr_number, run_id, diff_paths, failing_tests]

  # ── RESILIENT-014: test-gate bypass audit ─────────────────────────────────
  - kind: merge_preview_dirty
    effect_metric: self
    emitter: scripts/git-hooks/pre-push Guard 0e (INFRA-1391)
    trigger: git merge-tree detected a DIRTY semantic conflict between HEAD and
      origin/main before the push; push was blocked to prevent a 20-min CI thrash
    consumers: [fleet-brief, waste-tally, curator]
    fields_required: [ts, kind, branch, conflict_files]

  - kind: merge_preview_skipped
    effect_metric: self
    emitter: scripts/git-hooks/pre-push Guard 0e (INFRA-1391)
    trigger: CHUMP_MERGE_PREVIEW=0 bypassed the merge-tree pre-check; logged for
      waste-tally and operator audit
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, gap_id, branch, bypassed_by]

  - kind: test_gate_bypassed
    effect_metric: self
    emitter: scripts/git-hooks/pre-push (RESILIENT-014)
    trigger: CHUMP_TEST_GATE=0 used to skip the cargo test gate on push
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, gap_id, reason, branch, bypassed_by]

  # ── INFRA-1372: pre-push HEAD-drift detection ─────────────────────────────
  - kind: prepush_head_drift
    emitter: scripts/git-hooks/pre-push (INFRA-1372)
    trigger: HEAD moved during cargo test run (Rust test leaked a git commit via GIT_DIR inheritance)
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, pre_head, post_head, branch]
    docs: |
      Emitted immediately before blocking the push. pre_head and post_head are
      the full 40-char SHA of HEAD before and after the cargo test run. The delta
      indicates that at least one Rust test called Command::new("git commit") (or
      equivalent) without clearing GIT_DIR, causing the commit to land on the
      real worktree branch rather than the test's temporary repo fixture.
      Fix: add env_remove("GIT_DIR")/env_remove("GIT_WORK_TREE")/
      env_remove("GIT_COMMON_DIR")/env_remove("GIT_INDEX_FILE") to all
      Command::new("git") invocations in the offending test.

  # ── INFRA-1006: bot-merge watchdog ────────────────────────────────────────
  - kind: bot_merge_watchdog_killed
    effect_metric: self
    emitter: scripts/coord/bot-merge-watchdog.sh (INFRA-1006)
    trigger: bot-merge.sh process older than CHUMP_BOT_MERGE_MAX_AGE_S with done/merged gap killed
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, pid, gap, age_s, gap_status, pr_status]

  - kind: bot_merge_watchdog_stuck
    effect_metric: self
    emitter: scripts/coord/bot-merge-watchdog.sh (INFRA-1006)
    trigger: bot-merge.sh process over age limit but gap still open — operator review
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, pid, gap, age_s, gap_status, pr_status]
  # ── INFRA-1346: shadow ship-plan advisory ─────────────────────────────────
  - kind: ship_plan_advisory
    emitter: scripts/coord/bot-merge.sh _bm_shadow_plan() (INFRA-1346)
    trigger: emitted once per bot-merge.sh invocation at main-flow entry; calls `chump ship plan` best-effort and records the planner's recommended action for INFRA-1229 divergence analysis
    consumers: [fleet-brief, curator, operator]
    effect_metric: self
    fields_required: [ts, kind, source, branch, gap, plan_action, plan_json_truncated_to_2kb]
  # ── INFRA-1033: worktree gitdir auto-repair (INFRA-779 family) ────────────
  - kind: worktree_gitdir_repaired
    effect_metric: self
    emitter: src/atomic_claim.rs (INFRA-1033)
    trigger: chump claim detected that .git/worktrees/<wt>/gitdir pointed at the wrong path (INFRA-779 concurrent-sibling clobber) and rewrote it to the correct canonical path
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, wt_name, was, now]

  # ── CREDIBLE-047: autonomous ship-rate regression ──────────────────────────
  - kind: autonomous_ship_rate_regression
    effect_metric: self
    emitter: scripts/dispatch/autonomous-ship-rate.sh (CREDIBLE-047)
    trigger: day-over-day autonomous rate drops > 10 percentage points
    consumers: [fleet-brief, curator, PWA-daily-brief]
    fields_required: [ts, kind, prev_rate, curr_rate, drop_pp]

  # ── CREDIBLE-052: cross-session gap-ID collision prevention ───────────────
  - kind: gap_id_allocator_offline
    effect_metric: self
    emitter: src/gap_store.rs (reserve → external_pending_ids — CREDIBLE-052)
    trigger: open-PR scan failed (network error or gh unavailable); reserve continues
      with lease+DB coverage only; slight residual race risk remains
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, domain, reason]

  - kind: gap_id_allocator_collision_avoided
    effect_metric: self
    emitter: src/gap_store.rs (reserve_with_external — CREDIBLE-052)
    trigger: naive next ID (existing_max+1) was already claimed by an open PR or
      sibling lease; allocator skipped to the next safe ID
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, domain, skipped_id]

  - kind: gap_id_allocator_collision
    effect_metric: self
    emitter: crates/chump-gap-store/src/lib.rs
    trigger: gap ID collision detected after reservation (sibling claimed the same ID
      concurrently); emitted as ALERT; caller must retry with a new ID
    consumers: [fleet-brief, curator, operator-recall]

  # ── INFRA-1020: opencode identity residue audit ───────────────────────────
  - kind: identity_residue_swept
    effect_metric: self
    emitter: scripts/ops/audit-opencode-identity-residue.sh (INFRA-1020)
    trigger: one-shot audit finds (or confirms clean) opencode t@t.t placeholder identity in worktree configs
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, worktree, removed_email, removed_name, count]

  # ── INFRA-997: staging-only PR guard ────────────────────────────────────
  - kind: staging_only_pr_blocked
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-997)
    trigger: branch has commits since base but ALL are INFRA-472 auto-staging — refuses pr create
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, branch, commits]

  # ── INFRA-1219: PR create dedup gate ───────────────────────────────────────
  - kind: pr_dedup_blocked
    effect_metric: self
    emitter: scripts/coord/pr-create-gate.sh (INFRA-1219)
    trigger: an open PR already exists for the same gap-ID; gh pr create refused
    consumers: [fleet-brief, waste-tally, operator-recall]
    fields_required: [ts, kind, source, gap, dup_kind, dup_pr]
  - kind: pr_dedup_bypassed
    effect_metric: self
    emitter: scripts/coord/pr-create-gate.sh (INFRA-1219)
    trigger: CHUMP_PR_DEDUP_BYPASS=1 with --justification; PR creation proceeds
      despite an existing duplicate (audit-logged with justification)
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, source, gap, dup_kind, dup_pr, justification]
  - kind: pr_dedup_bypass_rejected
    effect_metric: self
    emitter: scripts/coord/pr-create-gate.sh (INFRA-1219)
    trigger: bypass env var set but no --justification supplied; gate still refuses
    consumers: [operator-recall]
    fields_required: [ts, kind, source, gap, dup_kind, dup_pr, reason]
  # ── INFRA-1063: cargo build-dir lock contention telemetry ─────────────────
  - kind: cargo_lock_wait
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (_run_cargo_with_lock_detect — INFRA-1063)
    trigger: cargo emits "Blocking waiting for file lock on build directory" during
      a bot-merge phase (clippy --fix, clippy, or test), indicating two parallel
      bot-merge runs are sharing a cargo target/ directory
    consumers: [fleet-brief, waste-tally, curator]
    fields_required: [ts, kind, session, phase, gap_id, target_dir]
  # ── INFRA-1031: GraphQL exhaustion fallback ────────────────────────────────
  - kind: graphql_exhausted
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1031)
    trigger: gh pr create attempted while GraphQL quota = 0; REST fallback taken or failed
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, source, note]
  # ── INFRA-1043: chump --release observability ──────────────────────────────
  - kind: session_released
    effect_metric: self
    emitter: src/main.rs --release handler (INFRA-1043)
    trigger: operator or agent releases a lease via chump --release (with or without --lease)
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, session_id, gap_id, source]

  # ── CREDIBLE-049: operator-leverage ratio ─────────────────────────────────
  - kind: leverage_regression
    effect_metric: self
    emitter: scripts/dispatch/operator-leverage.sh (CREDIBLE-049)
    trigger: weekly aggregate leverage_ratio dropped >20% compared to prior week.
      Signals that the fleet is requiring more operator attention per unit of
      fleet-active time — compounding is going backwards.
    consumers: [fleet-brief, curator, operator-recall]
    fields_required: [ts, kind, week, prev_mean_leverage, curr_mean_leverage, drop_pct]
  # ── PRODUCT-079: needs-judgment panel ─────────────────────────────────────
  - kind: operator_acknowledged
    effect_metric: self
    emitter: src/web_server.rs handle_needs_judgment_ack (PRODUCT-079)
    trigger: operator marks a gap/event as handled via the Judgment panel
    consumers: [fleet-brief, needs-judgment-panel]
    fields_required: [ts, kind, item_type, item_id]

  # ── INFRA-1056: worktree gitdir back-ref repair ──────────────────────
  - kind: worktree_gitdir_repair_fired
    effect_metric: self
    emitter: src/atomic_claim.rs verify_and_repair_gitdir (INFRA-1056)
    trigger: chump claim detected a wrong gitdir back-ref after git worktree add and rewrote it
    consumers: [fleet-brief, claim-race-monitor]
    fields_required: [ts, kind, worktree, was, now, attempt]

  # ── INFRA-1166: REST-direct merge fast path ───────────────────────────────
  - kind: bot_merge_rest_direct
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh INFRA-1166 REST-direct fast path
    trigger: bot-merge.sh confirmed all required CI checks already green and merged
      PR via REST PUT /pulls/N/merge instead of GraphQL enablePullRequestAutoMerge.
      Eliminates GraphQL secondary rate limit errors in green-CI fast path.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, pr, gap, sha, checks_verified]

  - kind: branch_deleted_fallback
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh INFRA-1413 REST-direct branch cleanup
    trigger: After a successful REST-direct merge, bot-merge.sh detected that the
      source branch was not yet deleted by the repo-level delete-branch-on-merge
      setting and deleted it manually via gh api -X DELETE. Common when the setting
      lags a newly-provisioned fork or there is a race between merge and cleanup.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, pr, branch]

  - kind: bot_merge_auto_armed
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh INFRA-1166 auto-merge fallback
    trigger: bot-merge.sh could not use the REST-direct path (checks still running
      or REST PUT failed) and fell back to arming GitHub auto-merge via GraphQL
      enablePullRequestAutoMerge.
    consumers: [fleet-brief]
    fields_required: [ts, kind, pr, gap]
  # ── INFRA-1029: pre-claim duplicate sweep ────────────────────────────────
  - kind: preflight_dupe_worktree
    effect_metric: self
    emitter: scripts/coord/gap-preflight.sh (INFRA-1029)
    trigger: gap-preflight finds an existing /tmp/chump-* worktree directory for the gap being claimed
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, gap, path]

  - kind: preflight_dupe_pr
    effect_metric: self
    emitter: scripts/coord/gap-preflight.sh (INFRA-1029)
    trigger: gap-preflight finds an open PR whose title contains the gap ID
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, gap, prs]
  # ── INFRA-1165: pre-push gap-check false positive suppressed ─────────────
  - kind: gap_check_false_positive
    effect_metric: self
    emitter: scripts/coord/gap-preflight.sh (INFRA-1165)
    trigger: gap ID is done on origin/main but current session holds an active lease
      for it — push allowed with warning instead of rejected
    consumers: [operator-recall, fleet-brief]
    fields_required: [ts, kind, gap_id, session]

  # ── INFRA-1165: pre-push gap-check false positive detection ──────────────
  - kind: gap_check_false_positive
    effect_metric: self
    emitter: scripts/git-hooks/pre-push INFRA-1165
    trigger: pre-push gap-check detected that gap-preflight rejected a push even
      though the push session holds a valid lease for the gap — indicating a
      session-ID mismatch false positive rather than a real conflict.
    consumers: [fleet-brief, ambient-context-inject]
    fields_required: [ts, kind, gap_ids, session]

  # ── INFRA-1169: bot-merge.sh fast-fail on missing worktree ───────────────
  - kind: bot_merge_aborted_no_worktree
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh INFRA-1169
    trigger: bot-merge.sh detected that the target worktree directory no longer
      exists (reaped by stale-worktree-reaper or pruned by operator). Exits 17
      immediately before writing any health files to prevent No-such-file
      error spam from _bm_health_init.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, gap, worktree_path]
  # ── CREDIBLE-058: branch-protection / workflow-job alignment ─────────────
  - kind: branch_protection_drift
    effect_metric: self
    emitter: scripts/ci/audit-branch-protection.sh (CREDIBLE-058)
    trigger: a required status-check context has no matching workflow job name in
      .github/workflows/ — means auto-merge will stall on every PR until fixed
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, missing, required_count]

  # ── INFRA-1186: gh-shim installer worktree-safety events ─────────────────
  - kind: gh_shim_worktree_install_blocked
    effect_metric: self
    emitter: scripts/setup/install-gh-shim.sh (INFRA-1186)
    trigger: installer detected that the resolved REPO_ROOT is an ephemeral path
      (under /tmp, /private/tmp, or /var/folders) and refused to embed it in the
      wrapper to prevent breakage when the worktree is reaped.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, ephemeral_path, script_path]

  - kind: gh_shim_worktree_path_resolved
    effect_metric: self
    emitter: scripts/setup/install-gh-shim.sh (INFRA-1186)
    trigger: installer was called from a linked worktree but resolved the canonical
      main-checkout path via git worktree list and used that in the wrapper instead.
    consumers: [fleet-brief]
    fields_required: [ts, kind, worktree_path, canonical_path]
  # ── INFRA-1145: roadmap-status drift detection ─────────────────────────────
  - kind: roadmap_drift_detected
    effect_metric: self
    emitter: src/main.rs roadmap-status --exit-on-drift handler (INFRA-1145)
    trigger: chump roadmap-status --exit-on-drift detects starved outcomes or
      untraced P0/P1 gaps not referenced in any ROADMAP week.
    consumers: [fleet-brief, ci-failure-digest, operator-recall]
    fields_required: [ts, kind, starved_outcomes, untraced_p0]

  # ── INFRA-1147: weekly roadmap-update LLM agent ────────────────────────────
  - kind: roadmap_update_proposal_cost
    effect_metric: self
    emitter: scripts/auto-docs/roadmap-update-agent.py (INFRA-1147)
    trigger: every LLM call (haiku by default, sonnet --high-fidelity)
    consumers: [api-cost-leaderboard, fleet-brief]
    fields_required: [ts, kind, model, input_tokens, output_tokens, usd]
  - kind: roadmap_update_proposal_opened
    effect_metric: self
    emitter: scripts/auto-docs/roadmap-update-agent.py (INFRA-1147)
    trigger: successful LLM proposal → PR opened (never auto-merged)
    consumers: [audit-log, operator-recall]
    fields_required: [ts, kind, branch, url, model, usd]
  - kind: roadmap_update_proposal_failed
    effect_metric: self
    emitter: scripts/auto-docs/roadmap-update-agent.py (INFRA-1147)
    trigger: LLM/parse/PR failure — no half-baked PR opened
    consumers: [operator-recall]
    fields_required: [ts, kind, reason]
  - kind: roadmap_update_proposal_skipped
    effect_metric: self
    emitter: scripts/auto-docs/roadmap-update-agent.py (INFRA-1147)
    trigger: idempotency hit (same-week branch exists) or LLM returned NO_CHANGE
    consumers: [audit-log]
    fields_required: [ts, kind, reason]
  # ── INFRA-1150: a2a-mailbox coord wiring for roadmap stack ─────────────────
  - kind: a2a_coord_broadcast_sent
    effect_metric: self
    emitter: scripts/coord/roadmap-status-broadcast.sh (INFRA-1150)
    trigger: roadmap-status detected drift and a WARN broadcast was sent --to all
      so peer-agent inboxes pick it up at next SessionStart inject (INFRA-1146/1150)
    consumers: [fleet-brief, audit-log]
    fields_required: [ts, kind, source]
  - kind: a2a_coord_inbox_consumed
    effect_metric: self
    emitter: scripts/coord/ambient-context-inject.sh (INFRA-1150)
    trigger: SessionStart inject surfaced pending broadcasts from the agent's
      inbox (dedup by kind+from+gap, capped at 10 messages)
    consumers: [audit-log]
    fields_required: [ts, kind, source, messages]
  # ── INFRA-1200: write-ahead log (WAL) events ──────────────────────────
  - kind: chump_plan_patch_saved
    effect_metric: self
    emitter: scripts/coord/chump-edit-wrap.sh (INFRA-1200)
    trigger: agent writes a substantive file edit to .chump-plans/<gap-id>/ before
      applying to the /tmp worktree; provides replay-on-reap durability.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, gap, seq, relpath]

  - kind: chump_plans_gc
    effect_metric: self
    emitter: src/main.rs gap ship handler (INFRA-1200)
    trigger: chump gap ship cleans up .chump-plans/<gap-id>/ directories whose
      SHIPPED_AT marker is older than the 7-day grace period.
    consumers: [fleet-brief]
    fields_required: [ts, kind, gap, removed_count]

  # ── INFRA-998: PR-closure-reason categorization ───────────────────────
  - kind: waste_category_report
    effect_metric: self
    emitter: src/waste_tally.rs build_close_reason_report (chump waste-tally --by-close-reason --emit-ambient)
    trigger: weekly cron tallies closed-not-merged PRs by closure pattern
    consumers: [fleet-brief, waste-tally consumers]
    fields_required: [ts, kind, window_secs, total, by_category]
  # ── INFRA-1152: pillar-balance guard at reserve time ─────────────────────
  - kind: pillar_balance_warn
    effect_metric: self
    emitter: src/main.rs gap reserve (INFRA-1152)
    trigger: proposed gap's pillar would exceed CHUMP_PILLAR_BALANCE_WARN threshold
      (default 35%) of open-pickable gaps; reserve proceeds with warning
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, pillar, ratio, total_pickable]

  - kind: pillar_balance_block
    effect_metric: self
    emitter: src/main.rs gap reserve (INFRA-1152)
    trigger: proposed gap's pillar would exceed CHUMP_PILLAR_BALANCE_BLOCK threshold
      (default 50%) of open-pickable gaps; reserve blocked unless --force-pillar used
    consumers: [fleet-brief, curator, waste-tally]
    fields_required: [ts, kind, pillar, ratio, total_pickable]

  # ── INFRA-988: PWA settings panel ────────────────────────────────────────
  - kind: pwa_setting_changed
    effect_metric: self
    emitter: src/web_server.rs handle_settings_post (INFRA-988)
    trigger: operator persists a non-secret config value via PWA POST /api/settings/{key}
    consumers: [fleet-brief, audit-trail]
    fields_required: [ts, kind, key, value, source_before]
  # ── INFRA-990: PWA first-run doctor banner ───────────────────────────────
  - kind: pwa_doctor_check
    effect_metric: self
    emitter: src/web_server.rs handle_doctor_health (INFRA-990)
    trigger: PWA banner polls GET /api/health/doctor; emitted on each cache miss
      (5s tumbling-window) so observers see config-health transitions without
      the message bodies (which can leak env paths). Counts only.
    consumers: [fleet-brief, audit-trail]
    fields_required: [ts, kind, ok, failure_count, warning_count]
  # ── PRODUCT-116: strategic-redirect composer ─────────────────────────────────
  - kind: operator_hint
    effect_metric: self
    emitter: src/web_server.rs handle_inject_hint (PRODUCT-116)
    trigger: operator POSTs to /api/inject-hint via PWA strategic-redirect composer;
      emitted after the hint is written to the blackboard so the history list
      (/api/ambient/recent?kind=operator_hint) can surface recent directives.
    consumers: [fleet-brief, ambient-viewer, hint-composer-history]
    fields_required: [ts, kind, hint, ttl_minutes, blackboard_id]
  # ── INFRA-1030: bot-merge arm-ship ordering fix ────────────────────────────
  - kind: gap_ship_post_arm_failed
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1030)
    trigger: chump gap ship fails AFTER gh pr merge --auto already succeeded; auto-merge is armed, so bot-merge continues rather than aborting — this event signals the operator to manually run chump gap ship
    consumers: [fleet-brief, curator, opus-curator]
    fields_required: [ts, kind, gap_id, pr, rc]

  # ── INFRA-1028: operator-recovery umbrella bypass audit ───────────────────
  - kind: guard_bypassed
    effect_metric: self
    emitter: scripts/git-hooks/pre-commit (INFRA-1028), scripts/git-hooks/pre-push (INFRA-1028)
    trigger: CHUMP_OPERATOR_RECOVERY=1 activated the operator-recovery bypass umbrella
      in pre-commit or pre-push; all recovery-appropriate guards silenced for this
      invocation; operator=true distinguishes human recovery from agent shortcuts
    consumers: [fleet-brief, curator, ops-audit]
    fields_required: [ts, kind, guard_name, bypasses, operator]

  # ── INFRA-1014: PWA auth middleware ──────────────────────────────────────
  - kind: web_auth_unauthorized
    effect_metric: self
    emitter: src/web_server.rs auth_middleware (INFRA-1014)
    trigger: a request to /api/* arrived without (or with a wrong) Authorization Bearer token when CHUMP_WEB_TOKEN is set
    consumers: [fleet-brief, audit-trail]
    fields_required: [ts, kind, path, reason]
  # ── INFRA-1867: preflight-vs-CI parity drift ──────────────────────────────
  - kind: ci_parity_drift
    effect_metric: self
    emitter: scripts/ci/test-preflight-ci-parity.sh (INFRA-1867)
    trigger: a CI gate in .github/workflows/ci.yml is not mirrored in src/preflight.rs
      and is not listed as Tier-D in CI_GATES_INVENTORY.md or allowlisted in
      preflight-ci-parity-exceptions.txt.
    consumers: [fleet-brief, operator-recall, ops-audit]
    fields_required: [ts, kind, gate_name, ci_path]
  # ── INFRA-1199: ci-yml merge driver abort ─────────────────────────────────
  - kind: ci_yml_merge_driver_abort
    effect_metric: self
    emitter: scripts/git/merge-driver-ci-yml-add-row.sh (INFRA-1199)
    trigger: merge driver detected an orphan '- name:' step without 'run:'/'uses:'
      body in theirs_tail; aborts instead of writing a corrupt workflow file.
    consumers: [fleet-brief, operator-recall]
    fields_required: [ts, kind, ours, theirs]
  # ── INFRA-1149: reserve-time title similarity check ───────────────────────
  - kind: gap_reserve_similarity_warn
    effect_metric: self
    emitter: src/main.rs gap reserve handler (INFRA-1149)
    trigger: chump gap reserve detected a proposed title with Jaccard similarity
      >= CHUMP_GAP_RESERVE_SIMILARITY_WARN (default 0.65) against an existing
      open or recently-closed gap. Operator is prompted y/N to continue.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, proposed_title, top_match_id, top_match_score]

  - kind: target_artifact_reaped
    effect_metric: self
    emitter: scripts/coord/target-dir-reaper.sh (INFRA-1349)
    trigger: target-dir-reaper.sh cleaned a build-artifact directory under disk
      pressure. Already allowlisted in scripts/ci/event-registry-reserved.txt
      but the precommit-strict-replay hook reads the registry YAML directly —
      this explicit entry unblocks rebased PRs that picked up the emit.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, path, freed_gb, worktree_age_h]

  - kind: gap_reserve_similarity_block
    effect_metric: self
    emitter: src/main.rs gap reserve handler (INFRA-1149)
    deprecated: true
    deprecated_reason: "INFRA-1982 demoted similarity gate from BLOCK to WARN-only;
      this kind is no longer emitted. Both thresholds now emit gap_reserve_similarity_warn.
      Entry retained for historical query compatibility — ambient scans from before
      2026-05-25 may still reference this kind."
    trigger: "[DEPRECATED] chump gap reserve detected a proposed title with Jaccard
      similarity >= CHUMP_GAP_RESERVE_SIMILARITY_BLOCK (default 0.85) against an
      existing open or recently-closed gap. Was blocked; now warns only (INFRA-1982)."
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, proposed_title, top_match_id, top_match_score]

  - kind: gap_import_similarity_block
    effect_metric: self
    emitter: crates/chump-gap-store/src/lib.rs import_from_yaml_with_similarity (INFRA-1434)
    trigger: chump gap import detected a net-new gap whose title has Jaccard
      similarity >= CHUMP_GAP_IMPORT_SIMILARITY_BLOCK (default 0.85) against an
      existing open or recently-closed gap. Row is skipped from import (treated
      as duplicate); import exits 1 if any row was blocked.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, proposed_id, top_match_id, top_match_title,
      top_match_score]

  - kind: gap_offline_check_block
    effect_metric: self
    emitter: src/main.rs gap reserve handler (INFRA-1418)
    trigger: chump gap reserve detected one or more forbidden-without-fallback
      patterns from docs/strategy/OFFLINE_COMPLIANCE_RUBRIC.md §2 in the proposed
      title/description (e.g. "webhook ONLY", "gh pr X ONLY"). Reserve is
      blocked; override with --force-anti-offline + --offline-bypass-reason.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, title, hits]

  - kind: gap_offline_bypass
    effect_metric: self
    emitter: src/main.rs gap reserve handler (INFRA-1418)
    trigger: operator deliberately bypassed the offline-compliance lint via
      --force-anti-offline + --offline-bypass-reason. Audit row also written
      to gap_offline_bypass_audit. See OFFLINE_COMPLIANCE_RUBRIC.md §4 for the
      playbook.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, title, reason, hits]

  - kind: gap_dup_archived
    effect_metric: self
    emitter: src/main.rs gap consolidate --apply handler (INFRA-1435)
    trigger: chump gap consolidate --apply detected a near-duplicate pair above
      threshold (default 90% Jaccard with --apply), archived the higher-ID gap
      as duplicate of the lower-ID gap, rewrote any depends_on backlinks across
      open gaps, and wrote an audit row to gap_dup_archive_audit.
    consumers: [fleet-brief, waste-tally, kpi-report]
    fields_required: [ts, kind, kept_id, archived_id, similarity_pct,
      depends_on_rewrites, reason]

  # ── INFRA-1247: alert classifier false-positive suppression ───────────────
  - kind: alert_classifier_suppressed
    effect_metric: self
    emitter: scripts/dev/ambient-watch.sh (INFRA-1247)
    trigger: |
      an alert was detected but suppressed because it matched a known false-positive
      pattern. Current suppression rules:
        operator_session — silent_agent skipped for chump-Chump-* IDE sessions
        pr_closed        — pr_stuck skipped for PRs whose state is closed/merged
      Allows measuring suppression rate and validating classifier precision.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, original_kind, reason, target_id]
  - kind: cargo_target_reaped
    effect_metric: self
    emitter: >
      scripts/ops/cargo-target-reaper.sh (INFRA-1250 + INFRA-1170)
    trigger: >
      cargo-target-reaper.sh reaped a stale fingerprint dir, rlib, fleet
      target directory, or an orphaned /tmp/chump-*/target/ whose originating
      git worktree is no longer registered. Failure events (cargo lock active,
      rm failure) also use this kind with an error + failure_class field.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, path]
    fields_optional: [bytes_freed, age_days, dry_run, worktree_gone, error, failure_class]

  - kind: cargo_target_reaper_summary
    effect_metric: self
    emitter: scripts/ops/cargo-target-reaper.sh (INFRA-1250 + INFRA-1170 + INFRA-2188)
    trigger: cargo-target-reaper.sh completed a full run (dry or execute).
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, reaped_count, bytes_freed, execute, worktree_orphan_count]
    fields_optional: [cross_build_count, cargo_test_target_count, lease_auto_merge_count, runner_scope_count, aggressive_mode]

  - kind: cargo_target_reaper_event_listener_started
    effect_metric: self
    emitter: scripts/ops/cargo-target-reaper.sh (INFRA-2181)
    trigger: cargo-target-reaper.sh started in --event-listen daemon mode.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, poll_interval_s, execute]

  - kind: cargo_target_reaper_event_listener_stopped
    effect_metric: self
    emitter: scripts/ops/cargo-target-reaper.sh (INFRA-2181)
    trigger: cargo-target-reaper.sh event-listen daemon received SIGTERM/SIGINT and exited cleanly.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind]

  - kind: cargo_target_reaper_event_triggered
    effect_metric: self
    emitter: scripts/ops/cargo-target-reaper.sh (INFRA-2181)
    trigger: cargo-target-reaper.sh detected kind=integration_cycle_shipped in ambient.jsonl and fired a reap.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, trigger_event_ts, gap_id, execute]

  # ── INFRA-2188: disk-pressure structural fix (2026-06-02) ────────────────
  # cargo_reaper_aggressive_mode_engaged — emitted by cargo-target-reaper.sh
  # when free disk on $HOME drops below CHUMP_DISK_CRITICAL_GB (default 20GB).
  # Signals that age thresholds were tightened (FINGERPRINT_AGE_D→1, FLEET_AGE_D→2)
  # so fleet observers can correlate cold-build spikes with disk events.
  - kind: cargo_reaper_aggressive_mode_engaged
    effect_metric: disk_free_gb
    emitter: scripts/ops/cargo-target-reaper.sh (INFRA-2188)
    trigger: >
      cargo-target-reaper.sh detected free disk on $HOME below CHUMP_DISK_CRITICAL_GB
      (default 20GB) and escalated to aggressive pruning: FINGERPRINT_AGE_D forced to
      1d and FLEET_AGE_D to 2d for this run. Emitted once per run when escalation fires.
    consumers: [fleet-brief, disk-health-monitor, ops-audit]
    fields_required: [ts, kind, free_gb, disk_critical_gb, fingerprint_age_d, fleet_age_d]

  # ── INFRA-1237: emit-without-register reconciliation (2026-05-14) ────────
  # 19 kinds that were fired in production but missing from the registry.
  # Discovered by scripts/ci/test-event-registry-coverage.sh, registered here
  # with {emitter, trigger, consumers, fields_required} derived from the actual
  # emit-site source.

  - kind: branch_reaper_pruned
    effect_metric: self
    emitter: scripts/coord/branch-reaper.sh
    trigger: branch-reaper.sh deleted a stale remote branch (no open PR, age past threshold).
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, branch, pr, age_days, dry_run]

  - kind: branch_reaped
    effect_metric: self
    emitter: scripts/coord/branch-reaper.sh (INFRA-1453)
    trigger: branch-reaper.sh deleted a stale remote branch. Per-deletion event
      with reaper_run_id so operator can audit which branches were cleaned without
      grepping /tmp logs. Emitted in --act mode; also emitted in --dry-run with
      --emit-anyway for CI + observability tests. Supersedes branch_reaper_pruned
      (legacy compat kind kept until fleet-brief migrates).
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, branch_name, age_days, repo, reaper_run_id]

  - kind: branch_reaper_run_summary
    effect_metric: self
    emitter: scripts/coord/branch-reaper.sh (INFRA-1453)
    trigger: branch-reaper.sh completed a full scan. Always emitted (dry-run and
      act). Use reaped_count to track waste removed; dry_run field to distinguish.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, reaped_count, skipped_open_pr_count, skipped_fresh_count, duration_s, dry_run, reaper_run_id]
    fields_required: [ts, kind, branch, age_days, reaper_run_id]
  - kind: orphan_branch_deleted
    effect_metric: self
    emitter: scripts/coord/orphan-branch-sweeper.sh INFRA-1450
    trigger: orphan-branch-sweeper.sh deleted a remote branch that had no open PR
      and whose last commit exceeded the age threshold (default 14d). Fires in
      --apply mode only; dry-run runs produce no event.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, branch, last_commit_age_days, sweep_run_id]

  - kind: orphan_branch_sweep_run
    effect_metric: self
    emitter: scripts/coord/orphan-branch-sweeper.sh INFRA-1450
    trigger: orphan-branch-sweeper.sh completed a full scan. Emitted in both
      dry-run and apply modes; use dry_run field to distinguish.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, nwo, candidates, deleted, skipped_open_pr, skipped_recent, dry_run, sweep_run_id]

  - kind: dup_pr_blocked
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh
    trigger: bot-merge.sh refused to create a duplicate PR for a gap that
      already has an open PR; current branch HEAD was not used.
    consumers: [fleet-brief, curator, waste-tally]
    fields_required: [ts, kind, gap_id, existing_pr_numbers, current_branch]

  - kind: duplicate_pr_closed
    effect_metric: self
    emitter: scripts/coord/duplicate-pr-detector.sh
    trigger: detector found two open PRs claiming the same gap and closed the
      loser (older or smaller) while leaving the winner intact.
    consumers: [fleet-brief, curator, ops-audit]
    fields_required: [ts, kind, gap, loser, winner, source]

  - kind: feedback_curated
    effect_metric: self
    emitter: scripts/coord/feedback-curator.sh
    trigger: feedback-curator.sh classified a FEEDBACK event into a curated
      bucket (proposal / retro / opinion) per INFRA-1271.
    consumers: [fleet-brief, feedback-report]
    fields_required: [ts, kind, gap, bucket, source]

  - kind: gap_db_auto_seeded
    effect_metric: self
    emitter: src/main.rs (chump init / first-run state.db bootstrap)
    trigger: chump auto-created an empty .chump/state.db when a command
      needed it but the file was missing (fresh clone or wiped state).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, path]

  - kind: gate_skipped
    effect_metric: self
    emitter: scripts/coord/api-rate-limit-gate.sh
    trigger: api-rate-limit-gate.sh let an api call proceed because remaining
      quota was above threshold (no throttle needed). Inverse of rate_limit_*.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, source, phase, reason, rest_remaining, gql_remaining]

  - kind: gh_secondary_limit_hit
    effect_metric: self
    emitter: scripts/coord/lib/github.sh (chump_gh wrapper)
    trigger: chump_gh wrapper observed a `secondary rate limit` error and
      backed off; emitted after retries succeed or after final failure.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, script, api, retries, total_waited_ms]

  - kind: ghost_lease_handoff
    effect_metric: self
    emitter: scripts/ops/stale-gap-lock-reaper.sh
    trigger: reaper detected a stale gap lock whose owner had committed work
      and was about to hand the lease off to a fresh session (after ack
      window expired).
    consumers: [fleet-brief, ops-audit, curator]
    fields_required: [ts, kind, gap, session, commits_ahead, branch, ack_window_s]

  - kind: ghost_lease_handoff_pending
    effect_metric: self
    emitter: scripts/ops/stale-gap-lock-reaper.sh
    trigger: reaper detected a candidate lease for handoff but is waiting on
      the ack window before releasing.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap, session, age_s, window_s]

  - kind: lint_storm
    effect_metric: self
    emitter: scripts/ops/lint-storm-detector.sh
    trigger: detector saw >=N open PRs failing on the same clippy/eslint
      lint within a short window; filed a follow-up gap to fix the lint
      class once.
    consumers: [fleet-brief, curator]
    fields_required: [ts, kind, event, lint, pr_count, prs, filed_gap]

  - kind: meta_filed
    effect_metric: self
    emitter: scripts/coord/gap-reserve.sh
    trigger: gap-reserve.sh filed a META-* gap (PM/curation domain rather
      than INFRA/CREDIBLE/etc). Distinct ambient channel so curator can
      surface PM work separately.
    consumers: [fleet-brief, curator, opus-curator]
    fields_required: [ts, kind, session, event, gap_id, title, note]

  - kind: pr_stuck_announced
    effect_metric: self
    emitter: scripts/coord/pr-stuck-announcer.sh
    trigger: announcer detected an open PR stuck past its SLO (age>threshold
      with at least one failing required check) and emitted an alert.
    consumers: [fleet-brief, ops-audit, curator]
    fields_required: [ts, kind, pr, gap, target, age_h, failing_check]

  - kind: pre_restart
    effect_metric: self
    emitter: src/main.rs (chump fleet restart prep)
    trigger: chump emitted a pre-restart marker before tearing down workers,
      so observers can correlate the restart with subsequent fresh leases.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind]

  - kind: rate_limit_approaching
    effect_metric: self
    emitter: scripts/coord/api-rate-limit-gate.sh
    trigger: gate observed REST or GraphQL bucket below warning threshold
      (default 25% remaining). Background callers should pivot to cache or
      defer per INFRA-1080.
    consumers: [fleet-brief, ops-audit, waste-tally]
    fields_required: [ts, kind, source, phase, api, remaining, limit, pct]

  - kind: rate_limit_exhausted
    effect_metric: self
    emitter: scripts/coord/api-rate-limit-gate.sh
    trigger: bucket at 0; non-critical callers blocked until reset window.
    consumers: [fleet-brief, ops-audit, waste-tally]
    fields_required: [ts, kind, source, phase, api, remaining, limit]

  - kind: session_rescue
    effect_metric: self
    emitter: src/rescue_tally.rs (INFRA-rescue-tally)
    trigger: rescue-tally detected a commit that bears a session-rescue
      marker (e.g. `wip_checkpoint`, `auto-recovered`). Tracks operator
      recovery debt vs autonomous output.
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, commit, subject, author]

  - kind: stale_gap_lock_protected
    effect_metric: self
    emitter: scripts/ops/stale-gap-lock-reaper.sh
    trigger: reaper considered a stale lock but kept it alive (heartbeat
      still fresh per TTL). Records the "no-op decision" so observers can
      distinguish silent reaper from protective reaper.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, event, lock, session, gap, heartbeat_age_s, heartbeat_ttl_s, reason]

  - kind: stale_gap_lock_skipped
    effect_metric: self
    emitter: scripts/ops/stale-gap-lock-reaper.sh
    trigger: reaper skipped a stale lock because an open PR already exists
      for the gap (handoff would lose work).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason, gap, prs, lock]

  - kind: timeout_rescue
    effect_metric: self
    emitter: scripts/dispatch/worker.sh
    trigger: worker hit its run-timeout and committed a WIP checkpoint so
      the next session can resume. Distinct from session_rescue (which
      indexes off the commit, not the worker run).
    consumers: [fleet-brief, waste-tally]
    fields_required: [ts, kind, event, session, worktree, agent_id, gap_id]
  - kind: bot_merge_phase_duration
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh::stage_done (INFRA-1067)
    trigger: emitted by stage_done() after each named bot-merge phase
      completes. Allows the fleet to accumulate a distribution of phase
      wall-clock times and tune timeouts data-drivenly (AC 1 + AC 2 of
      INFRA-1067). One event per stage_done() call — covers git fetch,
      rebase, cargo fmt, cargo clippy, cargo test, push, PR create, etc.
    consumers: [fleet-brief, ops-audit, infra-1067-timeout-tuning]
    fields_required: [ts, kind, phase, elapsed_s, gap, branch]
  - kind: rebase_and_push_invoked
    emitter: scripts/coord/chump-rebase-and-push.sh (INFRA-1404)
    trigger: operator ran chump-rebase-and-push.sh; emitted on every successful
      fetch+rebase+push cycle. On conflict or double-push-rejection the kind is
      rebase_and_push_failed instead (same emitter, different exit path).
    effect_metric: manual_rebase_ops_count{operator=<session>} — tracks how often
      operators need to manually rebase; should trend to 0 as auto-rebase daemon
      (INFRA-1406) matures
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, branch, base, remote, retries]
  - kind: rebase_and_push_failed
    emitter: scripts/coord/chump-rebase-and-push.sh (INFRA-1404)
    trigger: rebase+push encountered a conflict (exit 2) or push-lease rejection
      after retry (exit 3). Includes stage field to distinguish conflict vs. push.
    effect_metric: rebase_conflict_count{branch=<branch>} — persistent conflicts
      indicate hot-file contention; should be addressed by merge drivers
    consumers: [fleet-brief, ops-audit, paramedic-daemon]
    fields_required: [ts, kind, branch, base, stage]
  - kind: gap_revert_pr_opened
    effect_metric: self
    emitter: src/revert_pr.rs via chump revert (INFRA-1443)
    trigger: operator ran `chump revert <PR-or-commit> --reason <text>`;
      emitted after the revert PR is created on GitHub and the
      original PR has been commented with the revert link.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, merge_sha, orig_pr, revert_pr_url]
  - kind: lease_auto_extended
    effect_metric: self
    emitter: scripts/coord/lease-auto-extend.sh (INFRA-1327)
    trigger: a near-expiry claim lease (remaining < CHUMP_LEASE_EXTEND_THRESHOLD_S,
      default 1800s) has an open PR with armed auto-merge; the lease expires_at is
      rewritten to now + CHUMP_GAP_CLAIM_TTL_SECS to prevent a stomp race where a
      second worker re-claims the gap while GitHub merge is still processing.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, gap_id, pr_number, new_expires, reason]
    docs: |
      reason is always "auto_merge_armed". pr_number is the integer GitHub PR number.
      new_expires is an ISO-8601 UTC timestamp. Emitted once per lease per extension
      cycle; no dedup — operators can count events to see how many times a lease was
      extended before merge completed.

  - kind: lease_release_failed
    effect_metric: self
    emitter: src/dispatch.rs::release_with_retry (INFRA-1243)
    trigger: chump --release subprocess failed twice in the dispatch step-5 cleanup
      path. The lease will auto-expire via its TTL, but the failure is recorded
      for operator visibility.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, session, gap, error]
  # ── INFRA-989: PWA secret-input flow ─────────────────────────────────────
  - kind: pwa_secret_changed
    effect_metric: self
    emitter: src/web_server.rs handle_secret_post (INFRA-989)
    trigger: operator persists a secret credential (Anthropic key, OAUTH token, GH token) via PWA POST /api/settings/secret/{name}, after a successful probe (or with CHUMP_SKIP_PROBE=1)
    consumers: [fleet-brief, audit-trail]
    fields_required: [ts, kind, key, last4, probe_skipped]
  - kind: gh_auth_token_cache_hit
    effect_metric: self
    emitter: scripts/coord/lib/gh-shim/gh (INFRA-1283)
    trigger: gh shim intercepted 'gh auth token' and returned a cached token
      (within CHUMP_GH_TOKEN_CACHE_TTL_S, default 300s). Skips the real gh
      auth token call entirely — avoids ~27 redundant calls/hr.
    consumers: [fleet-brief, api-cost-leaderboard]
    fields_required: [ts, kind, age_s, ttl_s]
  # ── INFRA-1311: bot-merge exponential backoff events ─────────────────────
  - kind: bot_merge_backoff_written
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1311)
    trigger: auto-merge-armer failed to arm a PR and wrote a backoff record with
      next-retry delay. Backoff grows exponentially per-PR (30s/60s/120s/300s).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, delay_s, until]
  - kind: bot_merge_backoff_skipped
    effect_metric: self
    emitter: scripts/coord/auto-merge-armer.sh (INFRA-1311)
    trigger: auto-merge-armer skipped a PR because its per-PR backoff has not yet
      expired. No merge attempt is made until the backoff period elapses.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, remaining_s, until]

  # ── INFRA-849: system integration test pipeline ───────────────────────────
  - kind: integration_test_pass
    effect_metric: self
    emitter: scripts/ci/test-system-integration.sh (INFRA-849)
    trigger: all assertions in the claim→commit→ship integration test passed
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pass_count, elapsed_s]

  - kind: integration_test_fail
    effect_metric: self
    emitter: scripts/ci/test-system-integration.sh (INFRA-849)
    trigger: one or more assertions in the claim→commit→ship integration test failed
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, fail_count, pass_count, elapsed_s]

  # ── INFRA-1076: lane-routed GitHub App tokens (scaffold) ──────────────────
  - kind: github_app_fallback
    effect_metric: self
    emitter: scripts/coord/lib/github.sh _chump_gh_lane_token (INFRA-1076)
    trigger: chump_gh requested a lane-specific token (CHUMP_GH_CALL_CRITICALITY=critical|background)
      but the corresponding ~/.chump/oauth-token-<lane>.json file was absent. Falls back to the
      legacy GH_TOKEN / gh-CLI keyring path. Emitted once per process so the operator sees the
      degraded mode without log spam.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, criticality, reason, expected_file]

  # ── INFRA-1441: bot-merge bypass audit trail ─────────────────────────────
  - kind: bot_merge_bypassed
    emitter: scripts/git-hooks/pre-push (INFRA-1441)
    trigger: >
      Operator pushed a chump/<gap-id> branch with CHUMP_BYPASS_BOT_MERGE=1
      and an accompanying Bot-Merge-Bypass: <reason> trailer on the HEAD
      commit body. The trailer is now mandatory for the bypass (mirrors
      Rust-First-Bypass / Obs-Bypass-Reason conventions) so the audit trail
      captures the reason on permanent record. Surfaces every bypass to
      fleet-brief + ops-audit so the bypass RATE is visible.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, branch, gap, reason]
  # ── PRODUCT-078: PWA Daily Brief ─────────────────────────────────────────
  - kind: pwa_brief_loaded
    emitter: web/v2/daily-brief.js ChumpViewBrief._load (PRODUCT-078)
    trigger: operator opens the Daily Brief tab or returns to a tab after absence;
      sendBeacon POST /api/ambient/emit fires after the /api/brief response is
      rendered, recording since_ago_secs and per-bucket counts for usage analytics
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, source, since_ago_secs, done, needs_judgment, alerts]
  - kind: reaper_fetch_fallback
    effect_metric: self
    emitter: scripts/ops/stale-worktree-reaper.sh (INFRA-1472)
    trigger: git fetch origin/main failed (network down, rate-limited, or disk pressure)
      but a cached local origin/main ref exists — reaper continues in offline mode
      rather than aborting. When no local ref exists, the reaper still aborts (exit 1).
    consumers: [fleet-brief, reaper-watchdog]
    fields_required: [ts, kind, remote, base, reason]
  # ── INFRA-1361: GitHub App installation-token rotator ────────────────────
  - kind: gh_token_rotate_noop
    effect_metric: self
    emitter: src/gh_token.rs chump gh-token rotate (INFRA-1361)
    trigger: chump gh-token rotate was invoked but ~/.chump/github_apps.toml is
      absent or empty. No tokens are fetched; exit 0. Emitted once per invocation
      so operators can confirm the cron is running even before App configuration.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason]
    fields_optional: [session, worktree, harness]

  - kind: gh_token_rotated
    effect_metric: self
    emitter: src/gh_token.rs chump gh-token rotate (INFRA-1361)
    trigger: a fresh GitHub App installation token was successfully fetched for
      one lane and written to ~/.chump/oauth-token-<lane>.json (chmod 600).
      Emitted once per lane per invocation.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, lane, expires_at]
    fields_optional: [session, worktree, harness]
  # ── INFRA-1421: CI regression guard ──────────────────────────────────────
  - kind: ci_regression_guard_blocked
    effect_metric: self
    emitter: scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421)
    trigger: push blocked because a CI-Regression-Guard trailer references a
      test script that does not exist in the repo at push time
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, note]

  - kind: ci_regression_guard_missing
    effect_metric: self
    emitter: scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421)
    trigger: warning (non-blocking) emitted when a fix( commit touches
      .github/workflows/ci.yml but carries no CI-Regression-Guard trailer;
      prompts operator to add a regression test
    consumers: [fleet-brief]
    fields_required: [ts, kind, source, note]

  - kind: ci_regression_guard_suite_failed
    effect_metric: self
    emitter: scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421)
    trigger: push blocked because a regression-guard test script returned
      non-zero — the CI regression it was guarding has been re-introduced
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, note]
  # ── INFRA-1427: fleet doctor ──────────────────────────────────────────────
  - kind: fleet_doctor_run
    effect_metric: fail_count
    emitter: scripts/coord/fleet-doctor-strict.sh (INFRA-1427)
    trigger: operator runs fleet-doctor-strict.sh; always emitted regardless
      of pass/fail so fleet-brief can track health-check cadence
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, pass_count, fail_count]
  # ── INFRA-1595: fleet self-doctor --heal (Wave 0b autonomy outer loop) ────
  - kind: self_doctor_tick
    effect_metric: self
    emitter: src/fleet_self_doctor.rs run_heal_cycle (INFRA-1595)
    trigger: >
      `chump fleet doctor` ran a cycle and found nothing to heal (all
      registered daemons loaded, no stuck PRs over the 30-min SLO). Emitted
      either from --heal idle path or diagnose-only mode. Lets operators see
      cadence even when the loop is quiet.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, status]
  - kind: self_doctor_healed
    effect_metric: self
    emitter: src/fleet_self_doctor.rs run_heal_cycle (INFRA-1595)
    trigger: >
      Self-doctor took a remediating action this cycle. The `action` field is
      one of {daemon_installed, pr_dispatched}. For daemon_installed the
      `daemon` field is the launchctl label; for pr_dispatched the `pr` field
      is the PR number and `gap_id` is the gap routed to `chump --execute-gap`.
    consumers: [fleet-brief, ops-audit, kpi-report]
    fields_required: [ts, kind, source, action]
  - kind: self_doctor_failed
    effect_metric: self
    emitter: src/fleet_self_doctor.rs run_heal_cycle (INFRA-1595)
    trigger: >
      Self-doctor attempted a remediating action (daemon install or
      execute-gap dispatch) and it failed. `error` field carries the truncated
      stderr. Operator investigates if this fires repeatedly for the same
      target.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, action, error]
  - kind: self_doctor_budget_exceeded
    effect_metric: self
    emitter: src/fleet_self_doctor.rs run_heal_cycle (INFRA-1595)
    trigger: >
      Self-doctor's circuit breaker tripped — > CHUMP_SELF_DOCTOR_BUDGET
      (default 3) subagent dispatches in the last 10-min window. Doctor
      stops dispatching and writes `.chump-locks/operator-action-needed.json`
      pointing the operator at the dispatch log and pending PR list.
    consumers: [fleet-brief, ops-audit, operator-page]
    fields_required: [ts, kind, source, budget, recent, pending]
  # ── PRODUCT-081: PWA Outcome Dashboard ───────────────────────────────────
  - kind: pwa_impact_viewed
    emitter: web/v2/impact.js ChumpViewImpact._load (PRODUCT-081)
    trigger: operator opens the Impact tab or switches time window;
      sendBeacon POST /api/ambient/emit fires after /api/impact renders,
      recording the selected window and top-line metric counts
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, source, window, prs_merged, gaps_closed, operator_hours_saved]
  # ── INFRA-1600: PR auto-rescue daemon ─────────────────────────────────────
  - kind: pr_auto_rescue_invoked
    emitter: scripts/coord/pr-failure-auto-rescue.sh (INFRA-1600)
    trigger: >
      The auto-rescue daemon matched a known CI-failure pattern on an open
      PR and applied (or attempted to apply) the corresponding handler.
      Linchpin instrumentation: tonight's manual cascade-fix work, productized.
    consumers: [fleet-brief, cos-weekly-digest, ops-audit]
    fields_required: [ts, kind, pr, handler, outcome]

  # ── INFRA-1490: ci.yml merge driver mid-file fallback ────────────────────
  - kind: ci_yml_row_add_merged
    emitter: scripts/git/merge-driver-ci-yml-add-row.sh (INFRA-1490)
    trigger: >
      Custom merge driver was unable to use the original pure-append path
      (both sides modified the shared prefix) but successfully auto-merged
      via the patch-fuzz3 or git-merge-file --union fallback. Surfaces the
      otherwise-invisible "10 of 12 PRs all conflicted on ci.yml" pattern
      from 2026-05-16T04:30 in the audit trail.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, ours, theirs, mode, adds]
  - kind: pre_commit_ac_test_missing
    emitter: scripts/git-hooks/pre-commit-ac-completeness.sh (INFRA-1401)
    trigger: A commit referencing a gap ID has acceptance_criteria that name a
      scripts/ci|coord|ops/*.sh file but that file is neither staged nor present
      in HEAD. The commit is blocked until the test ships or an AC-Backfill-Reason
      trailer is provided.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gaps, missing_files]
  # ── PRODUCT-102: PWA gap list browser ────────────────────────────────────
  - kind: pwa_gap_list_filtered
    emitter: web/v2/gap-list.js ChumpViewGaps._applyFilters (PRODUCT-102)
    trigger: operator applies status/priority/domain/pillar filters or a search
      query in the PWA gap list browser; sent via navigator.sendBeacon to
      POST /api/ambient/emit so filter usage is visible in the ambient stream
      without blocking the UI thread
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, source, status_filter, priority_filter, domain_filter, pillar_filter, search, result_count]
  # ── INFRA-1568: broad-canary skip (operator override) ────────────────────
  - kind: runner_canary_skipped
    emitter: scripts/setup/install-self-hosted-runner.sh (INFRA-1568)
    trigger: >
      install-self-hosted-runner.sh ran --check or completed install but the
      operator set CHUMP_SKIP_CANARY=1 / passed --skip-canary OR the broad
      canary script was missing. The runner is reported as ready WITHOUT the
      full production-step validation the broad canary gate normally enforces.
      Reasons: operator_override, install_skip_canary_flag, canary_script_missing.
    consumers: [fleet-brief, ops-audit]
    effect_metric: self
    fields_required: [ts, kind, reason, host]
  # ── INFRA-1535: self-hosted runner migration pipeline ─────────────────────
  - kind: runner_migration_step
    emitter: scripts/coord/chump-runner-migration-pipeline.sh (INFRA-1535)
    trigger: Each stage of the self-hosted runner migration pipeline records
      its progress. Fields include stage name, outcome (started/pr_armed/done),
      and a note. Used to track the staged rollout of CI jobs to M4 runners.
    consumers: [fleet-brief, ops-audit]
    effect_metric: self
    fields_required: [ts, kind, stage, outcome, note]
  # ── INFRA-1394: hot-file collision detection at claim time ────────────────
  - kind: claim_hot_file_overlap
    effect_metric: self
    emitter: src/atomic_claim.rs emit_claim_hot_file_overlap_event (INFRA-1394)
    trigger: >
      chump claim detected that the gap's acceptance_criteria references one or
      more hot shared files (from scripts/coord/lib/hot-files.yaml) AND a sibling
      lease already declares the same file in its paths[] field. Fires before the
      worktree is created — claim is blocked unless --force-overlap is passed.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, claim_gap, sibling_gap, sibling_session, overlap_paths]
    docs: |
      Hot files are those that frequently cause merge conflicts when touched by
      multiple concurrent agents: ci.yml, EVENT_REGISTRY.yaml, web_server.rs,
      app.js, main.rs. The list is tunable in scripts/coord/lib/hot-files.yaml.
      Pairs with INFRA-1389 merge drivers (same region → no conflict) but the
      proactive block here saves the rebase cycle entirely for non-merge-driver paths.
  # ── INFRA-1437: disk_critical auto-action ─────────────────────────────────
  - kind: disk_critical_auto_remediated
    effect_metric: self
    emitter: scripts/ops/disk-health-monitor.sh (INFRA-1437)
    trigger: >
      disk-health-monitor.sh detected disk_critical (free < CRITICAL_PCT or
      BLOCKING_PCT) and auto-invoked scripts/coord/target-dir-reaper.sh
      --execute --critical, bounded by a 60s budget. Closes the
      alert→action loop so disk crises don't require operator intervention.
      Operator override: CHUMP_DISK_AUTO_REMEDIATE=0 disables the auto-action.
    consumers: [fleet-brief, disk-health-monitor, ops-audit]
    fields_required: [ts, kind, bytes_freed_mb, duration_s]
  # ── INFRA-1409: pr triage ─────────────────────────────────────────────────
  - kind: pr_triage_run
    effect_metric: self
    emitter: scripts/coord/chump-pr-triage.sh (INFRA-1409)
    trigger: operator runs chump-pr-triage.sh to get next-action table for open PRs
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, scope, pr_count, output]
  # ── INFRA-1406: orphan-pr-closer per-PR immunity ─────────────────────────
  - kind: orphan_pr_close_immunity_honored
    effect_metric: self
    emitter: scripts/coord/orphan-pr-closer.sh (INFRA-1406)
    trigger: >
      Orphan-pr-closer was about to close a PR but found
      'Orphan-Closer-Immunity: <reason>' trailer in one of the PR's
      commit messages — skipped the close. Companion to the existing
      title-marker 'orphan-pr-closer-skip' escape hatch.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, gap, reason]

  - kind: orphan_pr_rescued
    effect_metric: self
    emitter: scripts/coord/pr-rescue-false-close.sh (INFRA-1406)
    trigger: >
      Operator (or automation) invoked the rescue script after a PR was
      falsely closed by an auto-closer. The PR is reopened + auto-merge
      re-armed + an audit comment posted. The rate of this kind over
      time is the false-close-rate signal — drives whether the closer's
      classification needs tightening.
    consumers: [fleet-brief, ops-audit, operator-attention]
    fields_required: [ts, kind, pr, branch, gap]

  # ── INFRA-1541: pre-merge AC coverage gate ────────────────────────────────
  - kind: ac_coverage_miss
    effect_metric: self
    emitter: chump pr ac-coverage (INFRA-1541)
    trigger: >
      Pre-merge gate found an acceptance-criteria bullet in the gap
      referenced by the PR title that is not covered by the PR diff
      (no matching file path, symbol, Closes-AC trailer, or test
      reference). One event per uncovered bullet. Advisory-only by
      default; flips to merge-blocking when CHUMP_AC_GATE_ADVISORY!=true
      after the 50-PR observation window.
    consumers: [fleet-brief, kpi-report, ac-coverage-dashboard, pr-hygiene-gate]
    fields_required: [ts, kind, pr_number, gap_id, bullet_index, bullet_text_prefix]

  - kind: ac_coverage_waived
    effect_metric: self
    emitter: chump pr ac-coverage (INFRA-1541)
    trigger: >
      PR carries an `AC-Coverage-Waive: <bullet-index>: <reason>` trailer
      that bypasses the coverage check for that bullet. The reason is
      recorded for audit; rising waiver rate is a fleet-discipline signal.
    consumers: [fleet-brief, kpi-report, ops-audit, audit, pr-hygiene-gate]
    fields_required: [ts, kind, pr_number, gap_id, bullet_index, reason]

  - kind: ac_coverage_disabled
    effect_metric: self
    emitter: chump pr ac-coverage (INFRA-1541)
    trigger: >
      Operator override `CHUMP_AC_GATE_ENABLED=false` short-circuited the
      gate. Audit-only emit so operators can see when coverage enforcement
      was bypassed wholesale.
    consumers: [fleet-brief, ops-audit, pr-hygiene-gate]
    fields_required: [ts, kind, pr_number]
  # ── INFRA-1581 / INFRA-1538: migration pipeline gate-query telemetry ──
  - kind: migration_gate_query
    effect_metric: ci_throughput_runs_per_hour
    emitter: scripts/coord/chump-runner-migration-pipeline.sh (INFRA-1538/1581)
    trigger: each invocation of _check_job_succeeded() helper that polls the
      check-runs API to see if a stage gate (fast-checks/clippy/cargo-test/ACP)
      has been met. Lets fleet-doctor surface gate-poll rate + which jobs we
      watch most often.
    consumers: [fleet-brief, fleet-doctor]
    fields_required: [ts, kind, job, limit]

  # ── INFRA-1554 / INFRA-1686: synthesis-pass atomic-filing telemetry ──
  - kind: synthesis_gap_filed
    effect_metric: self
    emitter: scripts/dev/synthesis-pass.sh (INFRA-1554 AC #1 — atomic filing arm)
    trigger: >
      Synthesis daemon ran, identified a recommendation, invoked `chump gap
      reserve` (capturing the returned ID), and wrote the synthesis markdown
      with the actual filed gap ID inline. Without this telemetry path, the
      synthesis-truth validator (scripts/ci/test-synthesis-truth.sh, from
      INFRA-1684) can detect orphan references but cannot distinguish
      "synthesis hallucinated the ID" from "gap was reaped after filing."
    consumers: [synthesis-truth-validator, fleet-brief, ops-audit]
    fields_required: [ts, kind, synthesis_path, gap_id, title, source_note]

  # ── INFRA-1695 / META-066: Content Bots Suite dispatch telemetry ──
  - kind: content_bot_invoked
    effect_metric: self
    emitter: scripts/content-bots/run-bot.sh (INFRA-1695)
    trigger: >
      Content-bot dispatcher invoked for a specific bot_id after the toggle
      resolver (INFRA-1700) confirmed it is enabled per CHUMP_CONTENT_BOTS
      env or .chump-config.toml. Emitted before the LLM call so partial
      failures (LLM timeout, claude-cli missing) still leave a trace.
    consumers: [fleet-brief, ops-audit, content-bots-pipeline-orchestrator]
    fields_required: [ts, kind, bot_id, run_id, model_tier]

  - kind: content_bot_output
    effect_metric: self
    emitter: scripts/content-bots/run-bot.sh (INFRA-1695)
    trigger: >
      Content-bot LLM call completed. Status may be "ok" with output bytes
      written or "failed" with reason (claude-cli-missing | llm-nonzero +
      exit_code). Pairs with content_bot_invoked via shared run_id.
    consumers: [fleet-brief, ops-audit, content-bots-pipeline-orchestrator]
    fields_required: [ts, kind, bot_id, run_id, status]

  - kind: content_bot_pipeline_step
    effect_metric: self
    emitter: scripts/content-bots/run-bot.sh (INFRA-1695, when --predecessor-output is passed)
    trigger: >
      A downstream bot is consuming an upstream bot's output. Records the
      from_bot → to_bot edge so the pipeline-orchestrator (INFRA-1698) can
      reconstruct the DAG execution from ambient.jsonl. Pairs with
      content_bot_invoked via shared run_id.
    consumers: [content-bots-pipeline-orchestrator, fleet-brief, ops-audit]
    fields_required: [ts, kind, from_bot, to_bot, output_path, run_id]


  - kind: gap_yaml_written
    effect_metric: self
    emitter: src/main.rs (chump gap reserve handler — INFRA-1428)
    trigger: >
      `chump gap reserve` successfully wrote a docs/gaps/<id>.yaml mirror.
      Fields capture where it landed (main repo vs fallback worktree) so
      fleet-brief and the orphan-PR-closer can distinguish "yaml visible
      to all workers" from "yaml stuck in one linked worktree" (#2063
      cascade pattern).
    consumers: [fleet-brief, orphan-pr-closer, ops-audit]
    fields_required: [ts, kind, gap_id, path, target]
  # ── INFRA-1698 / INFRA-1702: pipeline-abort terminus event ──
  - kind: content_bot_pipeline_aborted
    effect_metric: self
    emitter: scripts/content-bots/orchestrate-pipeline.sh (INFRA-1698 — abort_pipeline helper)
    trigger: >
      Any stage failure in the Content Bots pipeline DAG (PMM → DocuBot
      || Evangelist → CopyBot). Downstream-of-failed bots are marked
      skipped in state.json before this event fires, so the run_id can
      be resumed via orchestrate-pipeline.sh --resume <run_id> once the
      operator addresses the root cause (LLM timeout, claude-cli auth,
      registry-state drift). Closes the bypass-debt from PR #2348.
    consumers: [content-bots-pipeline-orchestrator, fleet-brief, ops-audit]
    fields_required: [ts, kind, run_id, reason]
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr_number, mode]

  # ── CREDIBLE-068: Merge Queue Health Stream ──────────────────────────────────

  - kind: merge_queue_health
    effect_metric: queue_saturation_pct
    emitter: scripts/coord/monitor-merge-queue.sh (CREDIBLE-068)
    trigger: >
      Periodic (every 10 s) poll of queued GitHub Actions runs and open PRs
      with autoMergeRequest enabled. Provides real-time merge queue depth to
      fleet_health and agent_backpressure consumers so workers can self-throttle
      before saturating the CI runner pool.
    consumers: [fleet_health, agent_backpressure]
    fields_required: [ts, kind, queued_workflows, auto_merge_prs, queue_saturation_pct, backpressure_recommended]
    thresholds:
      queue_alert: 50     # saturation denominator; >70% → backpressure_recommended=true
      queue_critical: 100 # advisory log when queued_workflows ≥ this value

  - kind: queue_health_check_failed
    effect_metric: self
    emitter: scripts/coord/monitor-merge-queue.sh (CREDIBLE-068)
    trigger: >
      gh API call for merge-queue metrics timed out or returned an error.
      Advisory-only; fleet assumes queue is healthy and continues. Rising
      frequency of this event indicates GitHub API degradation.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, note]

  # ── INFRA-1340: per-tool auto-approve policies + escalation ─────────────────

  - kind: tool_auto_approved
    effect_metric: self
    emitter: src/task_executor.rs (INFRA-1340)
    trigger: >
      An incoming tool call matched an active per-tool auto-approve policy
      (PWA dropdown choice persisted in .chump/tool-policies.json keyed by
      {tool_name, scope}). The approval gate skipped the human-wait and let
      the tool execute. Distinct from the env-var-based
      auto_approved_tools_env / auto_approved_static_low_risk paths — the
      `source` field disambiguates ("persistent_policy" for this kind).
    consumers: [fleet-brief, ops-audit, approval-audit-dashboard]
    fields_required: [ts, kind, tool_name, scope, expires_at_unix, risk_level, source]

  - kind: tool_approval_escalated
    effect_metric: self
    emitter: src/task_executor.rs (INFRA-1340)
    trigger: >
      An approval request has been pending for longer than
      CHUMP_APPROVAL_ESCALATION_SECS (default 60s) without an operator
      decision. The server dispatches a Web Push notification to all
      subscribed endpoints and records the (ok, fail) counts here.
      Disabled when CHUMP_APPROVAL_ESCALATION=0 or there are no
      subscriptions / VAPID key configured.
    consumers: [fleet-brief, ops-audit, operator-attention]
    fields_required: [ts, kind, request_id, tool_name, risk_level, idle_secs, push_ok, push_fail]

  - kind: tool_approval_policy_changed
    effect_metric: self
    emitter: src/web_server.rs (INFRA-1340)
    trigger: >
      Operator created, refreshed, or deleted a per-tool auto-approve policy
      via POST/DELETE /api/tool-policy (PWA dropdown in the approval tray).
      The `action` field is "upsert" or "delete".
    consumers: [fleet-brief, ops-audit, approval-audit-dashboard]
    fields_required: [ts, kind, action, tool_name]
  - kind: preflight_registry_bypassed
    effect_metric: self
    emitter: src/preflight.rs (INFRA-1731 event-registry-audit gate bypass)
    trigger: >
      Operator ran `chump preflight` with CHUMP_PREFLIGHT_SKIP_REGISTRY=1
      set, skipping the local event-registry-audit gate (INFRA-1731). The
      bypass is intentional but recorded so post-hoc audits can see which
      PRs skipped the local registry check before push.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason]
  - kind: preflight_docsdelta_bypassed
    effect_metric: self
    emitter: src/preflight.rs (INFRA-1788 docs-delta-trailer gate bypass)
    trigger: >
      Operator ran `chump preflight --pre-commit` with
      CHUMP_PREFLIGHT_SKIP_DOCSDELTA=1 set, skipping the local
      docs-delta-trailer gate (INFRA-1788, mirrors INFRA-124). The bypass
      is intentional but recorded so post-hoc audits can see which PRs
      shipped without local validation of the Net-new-docs trailer.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason]
  - kind: preflight_main_red_skip
    effect_metric: self
    emitter: src/preflight.rs (INFRA-2422 main-red-aware gate auto-skip)
    trigger: >
      preflight auto-skipped a gate because origin/main is already failing
      it (main-preflight-watchdog reported state=RED for that gate). Emitted
      once per gate skipped per preflight run. Replaces the deleted
      CHUMP_PREFLIGHT_SKIP env bypass — instead of blanket-skipping all gates,
      only the specific trunk-failing gate is skipped. Consumers can join on
      gate + trunk_fix_gap_id to see which trunk fix resolves the skip.
    consumers: [fleet-brief, ops-audit, trunk-sentinel]
    fields_required: [ts, kind, gate, trunk_fix_gap_id]
  - kind: preflight_baseline_diff
    effect_metric: self
    emitter: src/preflight.rs (META-153 --vs diff-scoped failure attribution)
    trigger: >
      Operator ran `chump preflight --vs <ref>` and the baseline diff
      completed. Emitted once per --vs run (cache hit or miss) with counts
      of new vs pre-existing failures. new_fail_count > 0 means the current
      diff introduced regressions that should be fixed before push.
      preexisting_fail_count > 0 means the ref already had failures — those
      are listed as warnings but do NOT block the operator.
    consumers: [fleet-brief, ops-audit, ci-audit]
    fields_required: [ts, kind, new_fail_count, preexisting_fail_count, baseline_sha]
  - kind: liaison_elected
    effect_metric: github_api_calls_24h
    emitter: scripts/ops/github-liaison.sh (INFRA-1317)
    trigger: >
      Process acquired the .chump-locks/github-liaison.lock lockdir on a
      fresh start. Marks the start of a liaison reign — this process is
      now the sole reader of the GitHub API for the fleet (Phase 1 of
      docs/design/GITHUB_LIAISON.md).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, stale_takeover]

  - kind: liaison_heartbeat
    effect_metric: github_api_calls_24h
    emitter: scripts/ops/github-liaison.sh (INFRA-1317)
    trigger: >
      Per-cycle heartbeat from the elected liaison after each refresh
      pass (default 60s). Indicates the cache reconcile loop is alive and
      reconcile_ok=true means the underlying github-cache-reconcile.sh
      one-REST-call refresh succeeded.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, interval_s, reconcile_ok]

  - kind: liaison_takeover
    effect_metric: github_api_calls_24h
    emitter: scripts/ops/github-liaison.sh (INFRA-1317)
    trigger: >
      Liaison lock heartbeat was stale (older than CHUMP_LIAISON_STALE_S,
      default 90s) so this process forcibly reclaimed it. Indicates the
      prior liaison died without releasing — rising frequency is a warning
      that liaison processes are crashing rather than yielding cleanly.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, prev_holder, stale_age_s]

  - kind: liaison_yielded
    effect_metric: github_api_calls_24h
    emitter: scripts/ops/github-liaison.sh (INFRA-1317)
    trigger: >
      Liaison voluntarily released the lock via signal (SIGTERM/INT/HUP),
      EXIT trap on daemon shutdown, or operator-invoked `--release`. Pair
      with the next liaison_elected to confirm clean handoff.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, reason]

  - kind: opus_message_sent
    effect_metric: opus_messages_sent_24h
    emitter: scripts/coord/opus-message.sh (INFRA-1796)
    trigger: >
      A v0 addressed-async DM was sent between Opus sessions via the
      .chump-locks/opus-inbox/ inbox channel. Bridge primitive until
      INFRA-1759 (full A2A Layer 2b RPC) lands. Recipient can be
      session:<id>, gap:<GAP-ID> (routed to current lease holder), or
      all-opus (broadcast). Operator can inspect via opus-message.sh list.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, to, from, ref, msg_id]
  - kind: handoff_contract_violation
    effect_metric: subagent_contract_failures_24h
    emitter: crates/chump-handoff/src/lib.rs (INFRA-1720)
    trigger: >
      Subagent handoff failed schema validation, semantic validation, or
      response parsing. Indicates prompt refinement or contract definition
      needed. Routes violations by `contract_name` so authors can identify
      broken patterns per contract.
    consumers: [fleet-brief, subagent-prompt-feedback]
  - kind: ast_crawler_unsupported_language
    effect_metric: self
    emitter: crates/ast-crawler/src/lib.rs (INFRA-1719 tree-sitter AST crawler)
    trigger: >
      `chump_ast_crawler::crawl_paths` encountered a file with an extension
      not in the day-1 supported set (rust, python, javascript, typescript,
      go, bash, yaml). Debounced per-extension per invocation so a large asset
      tree doesn't flood the stream. Helps the fleet identify languages that
      should be promoted to first-class support next.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, path, ext]
  # INFRA-1720: chump-handoff typed-contract dispatch pipeline. Emitted at
  # every error boundary so dashboards can route violations back to the
  # owning contract for prompt refinement.
  - kind: handoff_contract_violation
    effect_metric: self
    emitter: crates/chump-handoff/src/lib.rs (HandoffContract::dispatch)
    trigger: >
      Subagent returned a payload that failed the contract's typed boundary —
      JSON schema mismatch (missing/extra field, parse error), Output::validate()
      rejection (semantic rule violated), or transport-level dispatch error.
      Parent rejects the payload and emits this rather than proceeding with
      malformed work.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, contract_name, error, raw_output_first_100]
  # INFRA-1722: ARCHITECTURE.md auto-generator. Emitted on each generation so
  # operators (and the fleet-brief consumer) see when a repo's architecture
  # snapshot was refreshed and how much the AST-derived shape changed.
  - kind: architecture_doc_regenerated
    effect_metric: self
    emitter: scripts/ops/generate-architecture-doc.sh (INFRA-1722)
    trigger: >
      The ARCHITECTURE.md generator finished a run against a repo (the chump
      repo itself or a takeover-demo target like echeo). Fires whether or not
      the regenerated output differed from the previous file — operators want
      both the "ran-but-unchanged" and "ran-and-changed-N-lines" signals to
      detect silent regressions in the AST crawler.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, repo, lines_changed, sections_touched]
  - kind: chump_gap_set_legacy_delim
    effect_metric: self
    emitter: src/main.rs `chump gap set --acceptance-criteria` (INFRA-1799)
    trigger: >
      Operator invoked `chump gap set --acceptance-criteria "a|b|c"` with a
      single flag whose value contains '|' — the legacy pipe-delimiter
      form. The preferred path is repeated `--acceptance-criteria BULLET`
      flags, which avoids splitting AC values that legitimately contain
      pipes (Rust generics, recipient syntax <gap-id|session-id|all-opus>,
      regex alternation). Emit lets the curator migrate callers gradually
      before removing the delimiter path.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap_id, bullet_count, note]

  # INFRA-1729: Quartermaster artifact regen — fires after every successful
  # capabilities-registry rebuild. `delta_count > 0` means a new primitive
  # surfaced; downstream dashboards can re-validate consumers' tool catalogues.
  - kind: capabilities_registry_refreshed
    effect_metric: self
    emitter: scripts/dev/build-capabilities-registry.sh (INFRA-1729)
    trigger: >
      docs/CAPABILITIES_REGISTRY.json was regenerated by the Quartermaster
      builder (or the Column A `chump ingest` wrapper at
      scripts/ops/generate-capabilities-registry.sh). Fires once per
      invocation regardless of delta; consumers that only care about
      meaningful changes filter on delta_count > 0.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, repo, items_count, delta_count]
  # INFRA-1851: PTY-pressure urgent mode for the claude-reaper. Fires when
  # the count of allocated /dev/ttys??? device files crosses 80% of
  # kern.tty.ptmx_max — the threshold past which new shells/tmux panes/SSH
  # sessions start to fail. The reaper response is to lower REAP_AGE to
  # 600s (10 min) for that sweep so a fresh leak drains in 10 min instead
  # of the 60-min default.
  - kind: reaper_pty_pressure
    effect_metric: self
    emitter: scripts/ops/reap-orphan-claude-procs.sh (INFRA-1851)
    trigger: >
      Pre-reap PTY-allocation probe found ≥80% of kern.tty.ptmx_max
      consumed (default threshold; tunable via CHUMP_REAPER_PRESSURE_THRESHOLD).
      Reaper lowers its effective age floor for this sweep only; the next
      sweep re-evaluates pressure from scratch.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, allocated, limit, pct, threshold, new_reap_age]

  # INFRA-1875: GitHub Liaison webhook-health probe + polling fallback.
  # Closes INFRA-1318 Phase 2 AC #5/#6.
  - kind: liaison_webhook_unhealthy
    effect_metric: self
    emitter: scripts/ops/github-liaison.sh (INFRA-1875)
    trigger: >
      Webhook-receiver health probe (default http://127.0.0.1:8765/health)
      failed CHUMP_LIAISON_WEBHOOK_HEALTH_MAX_FAILS times in a row (default 3).
      Liaison drops poll interval to fallback value and emits
      liaison_polling_fallback_active on the same cycle.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, consecutive_failures, url, last_error]

  - kind: liaison_polling_fallback_active
    effect_metric: self
    emitter: scripts/ops/github-liaison.sh (INFRA-1875)
    trigger: >
      Companion to liaison_webhook_unhealthy — emitted on the cycle the
      liaison switches from normal interval to fallback (default 30s). Pairs
      with liaison_webhook_recovered which fires on the next successful probe.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, poll_interval_s, reason]

  - kind: liaison_webhook_recovered
    effect_metric: self
    emitter: scripts/ops/github-liaison.sh (INFRA-1875)
    trigger: >
      Successful webhook-health probe after the liaison had been in
      polling-fallback mode. Original poll interval restored. Pairs with
      liaison_polling_fallback_active.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pid, poll_interval_s, url]
  # INFRA-1892: curator-jit-scheduler daemon. Replaces the
  # orchestrator-as-pebble-scheduler antipattern by auto-broadcasting
  # next-gap assignments to curator-opus-* sessions on DONE events.
  - kind: curator_jit_scheduled
    effect_metric: self
    emitter: scripts/coord/curator-jit-scheduler.sh (INFRA-1892)
    trigger: >
      A curator-opus-* session emitted a DONE event (or kind=gap_shipped).
      The daemon called the existing _pick_gap.py with priority filter
      P0,P1 (default), selected the top candidate, and broadcast a JIT
      ASSIGNMENT to that curator's inbox via broadcast.sh.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, curator, gap_id, priority]

  - kind: curator_jit_skipped
    effect_metric: self
    emitter: scripts/coord/curator-jit-scheduler.sh (INFRA-1892)
    trigger: >
      Skipped a JIT broadcast — either dedup-window hit (same curator+gap
      within CHUMP_JIT_DEDUP_WINDOW_S, default 3600s) or the broadcast.sh
      call itself failed. Reason field discriminates.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, curator, gap_id, reason]

  - kind: curator_jit_no_gap
    effect_metric: self
    emitter: scripts/coord/curator-jit-scheduler.sh (INFRA-1892)
    trigger: >
      Curator finished a gap but _pick_gap.py returned no candidate
      matching the priority filter (P0/P1 default). The daemon logs and
      stands by; the curator will idle until a new pickable gap appears.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, curator]
  # META-104: external-collab curator (operator-facing surface + Marcus arc + partnership pipeline)
  - kind: external_collab_finding
    effect_metric: self
    emitter: scripts/coord/external-collab-loop.sh (META-104)
    trigger: >
      curator-opus-external-collab detected a drift condition in one of
      four categories: voice_drift (banned term found in PITCH.md /
      HIDDEN_GEMS.md / DEMO_5MIN.md), surface_stale (doc >14d untouched),
      marcus_at_risk (Marcus milestone stalled >7d with no gap progress),
      partnership_stalled (INFRA-1501/1506/1511 approaching or past
      decision deadline). Curator audits only; does not edit docs directly.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, category, surface, detail]
  - kind: external_collab_scope_override
    effect_metric: self
    emitter: scripts/coord/external-collab-loop.sh (META-104)
    trigger: >
      CHUMP_EXTERNAL_COLLAB_SCOPE_OVERRIDE=1 was set, allowing the
      external-collab curator to claim work outside its defined lane
      (OPERATOR-FACING + EXTERNAL-FACING surfaces). Emitted for audit
      accountability.
    consumers: [ops-audit]
    fields_required: [ts, kind]
  # INFRA-1886: soft pre-claim suggestion in `chump gap preflight <ID>`.
  - kind: preflight_priority_hint_shown
    effect_metric: self
    emitter: src/main.rs print_priority_hint() (INFRA-1886)
    trigger: >
      `chump gap preflight <ID>` returned OK for a target gap whose
      priority is below P0; print_priority_hint surfaced up to 3
      higher-priority unclaimed gaps as advisory. Operator + curator
      see the note; choice to abandon the intended claim remains theirs.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, target_gap, suggested_gaps_count, target_priority]

  # INFRA-1899: transient-retrigger daemon. Auto-recognizes known-transient
  # CI failure patterns on open PRs and pushes an empty commit to force a
  # fresh CI run, capped at 2 retries per PR per 6 hours. Retires the
  # operator hand-empty-commit-retrigger workaround (today's session had
  # 10+ manual retriggers for the audit-cancel pattern alone).
  - kind: transient_auto_retriggered
    effect_metric: self
    emitter: scripts/coord/transient-retrigger.sh (INFRA-1899)
    trigger: >
      Daemon classified an open PR's failing required check against the
      catalog at scripts/coord/transient-classes.json (audit_cancelled,
      checkout_auth_race, flake_rerun_exhausted, network_blip), confirmed
      the per-PR retry cap (default 2/6h) was not yet exceeded, and
      pushed an empty commit to the PR's head branch to force a fresh
      CI run. The attempt_number field discriminates first vs. second
      retry within the window.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, failure_class, attempt_number]

  # META-100: RUNNER_GHOST_ONLINE detection — operator-recall condition (e).
  # Fired by scripts/dispatch/operator-recall.sh when queued workflow_runs
  # older than CHUMP_RUNNER_QUEUE_THRESHOLD_S exist AND ≥1 self-hosted runner
  # is online+idle (the contradiction that caused 14 PRs to wait 17h on
  # 2026-05-24). This event fires before the operator_recall page so consumers
  # can track detection frequency independently of cooldown-gated paging.
  - kind: runner_ghost_online_detected
    effect_metric: self
    emitter: scripts/dispatch/operator-recall.sh (META-100)
    trigger: >
      operator-recall.sh detected ≥1 queued workflow_run older than
      CHUMP_RUNNER_QUEUE_THRESHOLD_S (default 300s) while at least one
      self-hosted runner reports status=online and busy=false. Emitted
      unconditionally (not cooldown-gated) so detection frequency is
      observable even when paging is suppressed by the cooldown window.
    consumers: [operator-recall, fleet-brief, ops-audit]
    fields_required: [ts, kind, queued_count, oldest_age_s, idle_runners, threshold_s]

  # META-100: recovery script ran (scripts/ops/restart-actions-runners.sh).
  # Emitted after launchctl unload+load cycle for all configured runner plists.
  - kind: actions_runners_restarted
    effect_metric: self
    emitter: scripts/ops/restart-actions-runners.sh (META-100)
    trigger: >
      Operator (or automated recovery) ran restart-actions-runners.sh to
      cycle the launchd plists for all self-hosted GitHub Actions runner
      agents. prior_queued_count is the workflow run queue depth sampled
      immediately before the restart so the operator can confirm the queue
      drained after recovery.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, runners_restarted_count, prior_queued_count]

  # META-088: Oracle refresh cron. Periodic Opus burst (every 4h via launchd)
  # re-contemplates docs/process/THE_PATH.md against current state.db + recent
  # ambient. Emits 4 ambient kinds for observability.
  - kind: oracle_refresh_drift
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (META-088)
    trigger: >
      Oracle burst returned content with a different shasum than the existing
      docs/process/THE_PATH.md — the refresh produced real drift. THE_PATH.md
      has been overwritten and an auto-merge PR opened. before_hash + after_hash
      let consumers see the change magnitude.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, before_hash, after_hash]
  - kind: oracle_refresh_noop
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (META-088)
    trigger: >
      Oracle burst returned content with the same shasum as the existing
      THE_PATH.md — idempotent no-op. No PR opened. Useful signal for
      "Oracle ran but nothing changed" telemetry.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, hash]
  - kind: oracle_refresh_skipped
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (META-088)
    trigger: >
      Oracle daemon ran but could not complete a burst — either the claude
      CLI is missing OR the burst returned empty/too-short output. reason
      field discriminates causes (claude_cli_missing, empty_or_short_output).
      Fail-soft path so the daemon stays cron-able even when LLM access
      is down.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason]
  - kind: oracle_refresh_pr_opened
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (META-088)
    trigger: >
      After a drift event, the daemon successfully created an auto-merge
      PR with the refreshed THE_PATH.md. branch field is the PR's head
      branch name (chump/oracle-refresh-<stamp>).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, branch]
  # INFRA-2122: oracle-refresh silent-failure visibility. Replaces the
  # stderr-swallowing 2>/dev/null path that masked INFRA-2124 OAuth chain
  # breakage and let THE_PATH.md sit 4d stale despite a 4h cadence claim.
  - kind: oracle_refresh_failed
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (INFRA-2122)
    trigger: >
      claude -p exited non-zero (auth failures like "Not logged in", OAuth
      chain breakage, transport errors). Carries exit_code + elapsed_s plus
      truncated stderr (500 chars) so the real cause surfaces in ambient
      instead of being swallowed by 2>/dev/null. Distinct from
      oracle_refresh_empty which is exit-0-but-no-output.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, exit_code, elapsed_s, stderr]
  - kind: oracle_refresh_empty
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (INFRA-2122)
    trigger: >
      claude -p exited 0 but returned empty/too-short output (<200 chars).
      Discriminates "model returned empty" from "auth failed" — the legacy
      oracle_refresh_skipped conflated both. Carries body_len + elapsed_s
      + stderr so operators can tell if the model just refused vs. tripped
      a transient error.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, exit_code, elapsed_s, body_len, stderr]
  - kind: oracle_stale_despite_heartbeat
    effect_metric: self
    emitter: scripts/coord/oracle-refresh.sh (INFRA-2122)
    trigger: >
      META-alarm — detector for the detector failing. THE_PATH.md mtime is
      older than CHUMP_ORACLE_STALE_THRESHOLD_S (default 12h) but the
      oracle-refresh state file was touched within CHUMP_ORACLE_HEARTBEAT_WINDOW_S
      (default 6h) — i.e., daemon is alive but its output is stale. Same
      class as INFRA-2101 binary-refresh silent-staleness. Use to alert
      operators before another 4d quiet-failure window opens.
    consumers: [fleet-brief, ops-audit, slo-watchdog]
    fields_required: [ts, kind, path_age_s, state_age_s, stale_threshold_s]
  # META-098: daily self-productization wave. Once per day a launchd cron
  # sends 6 A2A broadcast.sh DMs (one per curator role) asking the curator
  # to contemplate what changed in their lane and add delta-AC to their
  # agent.md / SKILL.md (or file a follow-up gap). Productizes the manual
  # A2A wave that spawned META-097.
  - kind: daily_self_productize_wave
    effect_metric: self
    emitter: scripts/coord/daily-self-productize.sh (META-098)
    trigger: >
      Daemon fired once per day (idempotent on same-date guard). Sends
      one broadcast.sh DM per curator role (6 total). roles_paged is the
      count of successful pages; failed is the count of failed pages.
      Consumers can compute the wave-cadence over time to verify the
      ritual runs daily.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, date, roles_paged, failed]
  # META-103: curator-opus-observability telemetry-tuning findings.
  # Emitted by scripts/coord/observability-loop.sh via printf in _emit_finding
  # helper; structured payload not grep-scannable as bare literal.
  - kind: observability_finding
    effect_metric: self
    emitter: scripts/coord/observability-loop.sh (META-103)
    trigger: >
      Emitted on each tick cycle when audit-event-registry, reaper-cadence-audit,
      cost-leaderboard-rollup, or detector-noise-rank detects an anomaly.
      category is one of: zero_emit_kind, high_volume_kind, reaper_cadence_drift,
      api_burner. severity is one of: proposal, warn, alert, info.
    consumers: [fleet-brief, ops-audit, observability-curator]
    fields_required: [ts, kind, category, severity, kind_or_subject, detail]
  # META-102: curator-opus-infra-watcher SRE-lane substrate health finding.
  # Emitted by scripts/coord/infra-watcher-loop.sh on each tick cycle when a
  # substrate health check fires above threshold. category distinguishes the
  # four check types; severity is critical|warning. Would have caught the
  # 2026-05-24 incidents (17h runner ghost-online, StartInterval-missing-plist,
  # 154 claude procs, disk_critical) within minutes of first tick.
  - kind: infra_watcher_finding
    effect_metric: self
    emitter: scripts/coord/infra-watcher-loop.sh (META-102)
    trigger: >
      One finding per substrate health violation detected during a tick cycle.
      category is one of: daemon_plist_missing_interval, runner_ghost_online,
      disk_pressure, process_bloat. severity is critical or warning.
      detail is a human-readable description of the finding with threshold
      and observed values. Emitted at most once per category per tick to
      avoid flooding. Consumers watch for critical findings to page the
      operator via operator-recall.sh.
    consumers: [fleet-brief, ops-audit, operator-recall]
    fields_required: [ts, kind, category, severity, detail]
  # INFRA-1976: repo variable drift detection — post-incident stale variable audit.
  # Emitted by scripts/coord/infra-watcher-loop.sh check-repo-vars when a repo
  # variable deviates from its expected steady-state value in
  # scripts/setup/expected-repo-vars.yaml AND the divergence has persisted >2h.
  # Root cause: CHUMP_SELF_HOSTED_ENABLED=false sat unrecovered for 4 days
  # (2026-05-20 to 2026-05-24) causing silent CI starvation. This kind surfaces
  # post-incident variable drift before it becomes a multi-day outage.
  - kind: repo_var_stale_after_incident
    effect_metric: self
    emitter: scripts/coord/infra-watcher-loop.sh (INFRA-1976)
    trigger: >
      Emitted when gh variable list shows a repo variable value that differs
      from the expected value in scripts/setup/expected-repo-vars.yaml AND
      the divergence has persisted for more than 2 hours (tracked via
      .chump-locks/repo-var-divergence-state.json). At most one event per
      variable name per infra-watcher tick to avoid flooding. The var_name and
      actual_value fields identify which variable is stale and what it currently
      holds. Consumers should page the operator to restore the expected value.
    consumers: [fleet-brief, ops-audit, operator-recall]
    fields_required: [ts, kind, var_name, expected_value, actual_value, diverged_since]
  # RESEARCH-001: phantom doc-ref detection (chump gap audit-priorities)
  - kind: phantom_doc_ref_detected
    effect_metric: self
    emitter: src/main.rs (RESEARCH-001, chump gap audit-priorities)
    trigger: >
      RESEARCH_INTEGRITY.md cites a gap ID (e.g. EVAL-094) but no matching
      docs/gaps/<ID>.yaml exists in the registry. Emitted once per phantom
      reference detected during chump gap audit-priorities --research-check.
      Indicates a stale citation that should be removed or the gap filed.
    consumers: [audit-log]
    fields_required: [ts, kind, doc, gap_id]
  # INFRA-1922: curator-opus-handoff productization. The handoff curator role
  # ships as .claude/agents/handoff.md + .claude/skills/handoff/SKILL.md +
  # scripts/coord/handoff-loop.sh (harness-neutral CLI). The CLI emits the
  # three kinds below.
  - kind: sub_agent_dispatched
    effect_metric: self
    emitter: scripts/coord/handoff-loop.sh (INFRA-1922) — also expected from other curators per META-069 / CREDIBLE-074
    trigger: >
      An Opus curator (any role) dispatched a Sonnet sub-agent via the Agent
      tool to ship a gap. Emitted per dispatch so the operator can audit the
      Opus-vs-Sonnet ratio (CREDIBLE-074). Fields: gap (target gap id), role
      (dispatching curator role), target_model (always "sonnet" today).
    consumers: [fleet-brief, ops-audit, dispatch-hygiene]
    fields_required: [ts, kind, session, gap, role, target_model]
  - kind: handoff_heartbeat
    effect_metric: self
    emitter: scripts/coord/handoff-loop.sh (INFRA-1922) heartbeat subcommand
    trigger: >
      curator-opus-handoff role tick — emitted periodically so the
      orchestrator can audit liveness. Cron-driven (launchd plist filed
      separately) or invoked manually via /handoff heartbeat.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, session, role]
  - kind: handoff_lane_override
    effect_metric: self
    emitter: scripts/coord/handoff-loop.sh (INFRA-1922) when CHUMP_HANDOFF_LANE_OVERRIDE=1
    trigger: >
      curator-opus-handoff role accepted a claim outside its lane scope
      because the operator set CHUMP_HANDOFF_LANE_OVERRIDE=1. Audit emit
      so cross-lane work is operator-visible, not silent.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, session, role]
  # META-099: opus-shepherd migrated from cron to event-driven Monitor loop.
  - kind: opus_shepherd_migrated_to_event_driven
    effect_metric: self
    emitter: scripts/coord/opus-shepherd-migrate-to-event-driven.sh (META-099)
    trigger: >
      Operator ran the migration script; it deleted the cron job and printed
      the Monitor + ScheduleWakeup stanza for the running session.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, cron_ids_deleted]

  # INFRA-1927: preflight↔CI agreement measurement loop. Two events per push
  # let a weekly rollup compute how often the <60s local preflight gate agrees
  # with the 10-12min CI gate — surfaces preflight overconfidence (false-negatives)
  # and overcaution (false-positives).
  - kind: preflight_ci_agreement
    effect_metric: credible
    emitter: scripts/git-hooks/pre-push (INFRA-1927)
    trigger: >
      Emitted after the chump preflight guard runs in the pre-push hook
      (Guard 0a, INFRA-1671). Records the local preflight outcome (pass/fail)
      and elapsed seconds for later join with the CI outcome. sha + branch
      are the join keys. Bypass: CHUMP_PREFLIGHT_CI_AGREEMENT=0.
    consumers: [preflight-ci-agreement-report, ops-audit]
    fields_required: [ts, kind, sha, branch, gap_id, preflight_pass, preflight_secs]

  - kind: preflight_ci_agreement_resolved
    effect_metric: credible
    emitter: scripts/ops/github-liaison.sh _resolve_preflight_agreements (INFRA-1927)
    trigger: >
      Emitted by the github-liaison daemon each refresh cycle when a
      preflight_ci_agreement event (by sha) can be joined with completed
      CI check-run data from github_cache.db. ci_pass reflects whether all
      completed check-runs concluded success/skipped/neutral. mismatch=true
      when preflight_pass != ci_pass — the primary signal for the weekly
      agreement-rate report.
    consumers: [preflight-ci-agreement-report, ops-audit]
    fields_required: [ts, kind, sha, gap_id, ci_pass, ci_required_pass, mismatch]
  # INFRA-1728: voice ban-list CI lint
  - kind: voice_lint_violation
    effect_metric: self
    emitter: scripts/ci/test-voice-banlist.sh (INFRA-1728)
    trigger: >
      A changed docs/ file in a PR contains a word from the ban-list
      (synergy, revolutionary, disruptive, etc.) outside a code fence or
      inline backtick. One event per violation per PR run. Retro pattern
      detection uses word field to identify which terms slip through most often.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, doc_path, word, line_no]

  - kind: voice_lint_bypassed
    effect_metric: self
    emitter: scripts/ci/test-voice-banlist.sh (INFRA-1728)
    trigger: >
      Voice lint was skipped either via a Voice-Lint-Bypass commit trailer
      (per-PR documented bypass) or CHUMP_VOICE_LINT_DISABLE=1 operator
      override. The reason field captures the trailer text or "operator_override".
      Aggregated in retro to surface bypass-pattern abuse.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, reason, pr]
  # INFRA-1888: detect-gap-failure bypass mode audit trail
  - kind: detect_gap_failure_lax
    effect_metric: self
    emitter: scripts/coord/detect-gap-failure.sh (INFRA-1888)
    trigger: >
      CHUMP_DETECT_GAP_FAILURE_INCLUDE_DONE=1 bypass env is active — the
      gap-failure detector is running in lax mode where status=done gaps are
      included in the scan. Emitted once per detector invocation when bypass
      is in effect, to surface operator use of the debug override in the
      ambient stream.
    consumers: [audit-log]
    fields_required: [ts, kind, reason]
  # INFRA-1933: shared Cargo target dir. Emitted once per worker.sh launch
  # when CARGO_TARGET_DIR is set to the shared cache path.
  - kind: cargo_target_dir_shared
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-1933)
    trigger: >
      worker.sh found CARGO_TARGET_DIR unset and exported it to the shared
      cache path (~/.cargo/chump-shared-target). Prevents each worktree from
      building its own 5-11 GB target/ tree.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, source, target_dir]

  - kind: cargo_target_dir_external
    effect_metric: self
    emitter: scripts/dispatch/worker.sh (INFRA-1933)
    trigger: >
      CARGO_TARGET_DIR was already set by the caller before worker.sh ran;
      worker.sh respected the external value without overriding it.
    consumers: [fleet-brief]
    fields_required: [ts, kind, source, target_dir]

  # RESILIENT-031: admin-merge discipline audit trail
  - kind: admin_merge_forced
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (RESILIENT-031)
    trigger: >
      Operator used --force-admin --reason to bypass the noise-class discipline
      gate and run an admin-merge cycle without a matching known-noise-classes.yaml
      entry. Emitted so the operator can audit the frequency and justification of
      emergency admin-merge bypasses. Each event carries the PR number, reason
      text, and operator identity (USER). High frequency signals the class list
      needs updating.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, pr, reason, operator]

  - kind: admin_merge_cycle_ok
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2041 + RESILIENT-031)
    trigger: >
      Admin-merge cycle completed successfully: required_status_checks dropped,
      PR merged with --admin --squash, ruleset restored. Emitted at the end of
      a successful cycle as the primary success signal for the ops-audit log.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, pr, ruleset_id]

  - kind: admin_merge_cycle_merge_failed
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2041 + RESILIENT-031)
    trigger: >
      gh pr merge exited non-zero during an admin-merge cycle. The ruleset
      was still restored (so branch protection is intact), but the PR was not
      merged. Operator must investigate the merge failure and retry.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, pr, ruleset_id, merge_exit]

  - kind: admin_merge_cycle_restore_failed
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2041 + RESILIENT-031)
    trigger: >
      CRITICAL: the ruleset PUT to restore required_status_checks failed after
      the merge step. Branch protection is now in a degraded state (rules
      dropped). Operator must manually restore using the snapshot file logged
      in the event. Exit code 2 from the script signals this condition.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, pr, ruleset_id, restore_json, severity]

  # INFRA-2071: admin-merge burst circuit-breaker
  - kind: admin_merge_cycle_run
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2071)
    trigger: >
      Emitted once per successful admin-merge cycle immediately before
      admin_merge_cycle_ok. This is the counter event the burst circuit-breaker
      counts: when CHUMP_ADMIN_MERGE_BURST_THRESHOLD or more of these appear
      within CHUMP_ADMIN_MERGE_BURST_WINDOW_S seconds, the next invocation
      trips the circuit-breaker and emits admin_merge_burst instead of
      proceeding. Fields capture the PR, the justification (noise-class id or
      force-admin reason), and the operator session id for retrospectives.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, pr, reason, operator_session_id]

  - kind: admin_merge_burst
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2071)
    trigger: >
      Circuit-breaker tripped: admin_merge_cycle_run count in the past
      CHUMP_ADMIN_MERGE_BURST_WINDOW_S seconds reached or exceeded
      CHUMP_ADMIN_MERGE_BURST_THRESHOLD. The script exits non-zero (exit 3)
      and refuses to proceed. Operator must triage the root cause of the
      repeated admin-merge need OR re-run with --force-admin / CHUMP_ADMIN_MERGE_FORCE=1
      to override (which emits admin_merge_burst_overridden instead).
      operator_action_needed=true signals operator-recall consumers.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, count, window_s, threshold, operator_action_needed]

  - kind: admin_merge_burst_overridden
    effect_metric: self
    emitter: scripts/ops/admin-merge-cycle.sh (INFRA-2071)
    trigger: >
      Operator explicitly overrode the burst circuit-breaker via --force-admin
      or CHUMP_ADMIN_MERGE_FORCE=1 after it tripped (admin_merge_burst was
      emitted first). The merge proceeds but this separate kind is logged so
      the override pattern is visible in retrospectives and can be audited
      independently from the burst itself. override_method distinguishes the
      two override paths.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, pr, count, window_s, threshold, override_method]

  # INFRA-1979: inbox jsonl rotation — emitted after each prune run
  - kind: inbox_pruned
    effect_metric: self
    emitter: scripts/coord/chump-inbox-prune.sh (INFRA-1979)
    trigger: >
      chump-inbox-prune.sh prune completed a rotation sweep of
      .chump-locks/inbox/*.jsonl files. Emitted once per successful
      prune run, whether or not any files were actually modified (pruned_count
      may be 0 for a no-op run). Not emitted in --dry-run mode.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pruned_count, bytes_freed]

  # INFRA-2007: W-002 binary-cache-lag permanent fix — event-driven refresh
  - kind: binary_main_updated
    effect_metric: resilient
    emitter: scripts/coord/bot-merge.sh (INFRA-2007)
    trigger: >
      Emitted by bot-merge.sh immediately after a successful chump gap ship
      (auto-close rc=0). Signals that origin/main has a new commit and the
      installed runner binary may now be stale. The binary-refresh-event-watcher
      daemon subscribes to this kind and triggers refresh-runner-binary.sh
      within CHUMP_BINARY_EVENT_RATE_LIMIT_S (default 60s). This is the event
      source that eliminates the W-002 binary-cache-lag class structurally.
    consumers: [binary-refresh-event-watcher]
    fields_required: [ts, kind, gap_id, pr]

  - kind: binary_refresh_triggered_event
    effect_metric: resilient
    emitter: scripts/coord/binary-refresh-event-watcher.sh (INFRA-2007)
    trigger: >
      Emitted by binary-refresh-event-watcher.sh when a binary_main_updated
      event triggers an immediate rebuild via the event-driven path. Confirms
      the event-driven refresh fired (as opposed to the 5-min cron fallback).
      Carries since_last_s field so operators can verify zero-lag behavior.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, trigger, since_last_s]

  - kind: binary_event_watcher_rate_limited
    effect_metric: resilient
    emitter: scripts/coord/binary-refresh-event-watcher.sh (INFRA-2007)
    trigger: >
      Emitted when a binary_main_updated event arrives within
      CHUMP_BINARY_EVENT_RATE_LIMIT_S (default 60s) of the last rebuild.
      The rebuild is skipped for this event; the cron fallback will catch
      it within 5 minutes. Prevents rebuild storms during merge bursts.
    consumers: [fleet-brief]
    fields_required: [ts, kind, since_last_s, rate_limit_s]

  - kind: binary_event_watcher_no_tool
    effect_metric: resilient
    emitter: scripts/coord/binary-refresh-event-watcher.sh (INFRA-2007)
    trigger: >
      Emitted once at daemon startup when fswatch is not found on PATH.
      Signals that the watcher is using the portable tail -F polling fallback
      instead of zero-overhead FSEvents. Install fswatch via brew install
      fswatch to eliminate this signal and get sub-second event latency.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, note]

  # INFRA-1971: force-push intent signal — pr-auto-rearm deferred for branch
  - kind: pr_auto_rearm_deferred_for_force_push_intent
    effect_metric: resilient
    emitter: scripts/coord/pr-auto-rearm.sh (INFRA-1971)
    trigger: >
      pr-auto-rearm daemon found a fresh .chump-locks/force-push-intent-<branch_safe>.json
      marker file (mtime within last 60s) before re-evaluating a PR's auto-merge
      state. Skips re-arm for this sweep cycle to avoid the spurious CLOSED flash
      observed in PRs #2561 and #2566 during legitimate operator force-pushes.
      After the 60s TTL expires, normal re-arm logic resumes. Intent file is
      written by scripts/dev/force-push-intent.sh before a manual force-push.
      Bypass: CHUMP_PR_AUTO_REARM_NO_INTENT_CHECK=1.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, branch]

  # INFRA-2013: fleet-brief leading-indicator stall detection
  - kind: fleet_stalled
    effect_metric: credible
    emitter: scripts/dispatch/fleet-brief.sh (INFRA-2013)
    trigger: >
      Emitted by fleet-brief.sh when ships_1h==0 AND there are 2+ open
      BLOCKED PRs. Signals that the fleet has gone quiet — no ships in the
      last hour despite PRs waiting. Consumed by wizard-daemon.sh (Step 6)
      to broadcast a CRIT alert to URGENT-INBOX so any active
      operator/agent sees it within one tool call.
    consumers: [wizard-daemon, ops-audit, fleet-brief]
    fields_required: [ts, kind, ships_1h, blocked_prs]

  # INFRA-2029: worker exit-without-ship (THE FLOOR Phase 2)
  - kind: worker_stuck
    effect_metric: resilient
    emitter: scripts/dispatch/worker.sh (INFRA-2029)
    trigger: >
      Emitted by worker.sh in every exit-without-ship code path: stand_down
      (fleet-hold), preflight_fail, gap_already_done, worktree_create_fail,
      no_pickable_gap. Carries a reason field identifying which exit path
      fired. Consumed by wizard-daemon.sh (Step 6) to broadcast a CRIT
      alert when multiple stuck events appear within the lookback window.
    consumers: [wizard-daemon, ops-audit, fleet-brief]
    fields_required: [ts, kind, reason]

  # META-109 Phase 1: wizard-daemon autonomous DRIVE primitive
  - kind: wizard_daemon_action
    effect_metric: effective
    emitter: scripts/coord/wizard-daemon.sh (META-109)
    trigger: >
      Emitted by wizard-daemon.sh on every PR classification and orchestration
      decision in the 5-minute cycle. Carries step (step1/step2/step6/
      cycle_complete), target (PR#N or fleet), decision (classified,
      recovery_queue_emitted, rate_limited, broadcast_crit, etc.), and
      rate_limit_state. Primary audit trail for the autonomous DRIVE loop.
      Default-OFF: only emitted when CHUMP_WIZARD_DAEMON_ENABLED=1.
    consumers: [ops-audit, fleet-brief, wizard-daemon-dedup-check]
    fields_required: [ts, kind, source, step, target, decision, rate_limit_state]

  - kind: wizard_daemon_paused
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh (META-109)
    trigger: >
      Emitted at daemon startup when the CHUMP_WIZARD_DAEMON_PAUSE=1
      kill-switch is active. Operator kill-switch confirmation signal —
      confirms the daemon saw the pause flag and did nothing this cycle.
      Monitor for absence: if plist is loaded but this event stops appearing,
      the daemon may have crashed or the env var was cleared unintentionally.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, reason]

  - kind: wizard_daemon_safety_refusal
    effect_metric: credible
    emitter: scripts/coord/wizard-daemon.sh (META-109)
    trigger: >
      Emitted when a mandatory safety guard fires: floor_temp_HOT (chump
      health --temp reports HOT, cascade preventer) or pr_conflicting
      (mergeStateStatus=CONFLICTING, real conflict needs human judgment).
      Safety audit trail — never suppressed. Frequent occurrences indicate
      fleet instability (HOT) or PRs with genuine merge conflicts (CONFLICTING).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, source, reason, target]

  - kind: silent_fleet_death
    effect_metric: resilient
    emitter: scripts/coord/fleet-doctor-strict.sh + scripts/dispatch/fleet-brief.sh (INFRA-2040)
    trigger: >
      Emitted when BOTH of the following are true: (1) last merge into
      origin/main is >= SILENT_DEATH_MERGE_HOURS (default 12h) old, AND
      (2) at least one com/dev.chump.* launchd daemon has last exit code != 0.
      Catches the 2026-05-26 class of 24h dead-floor where daemons exited 127
      (stale branch — Sprint scripts existed in origin/main but not in the
      working tree daemons pointed at) while fleet-brief still reported healthy.
      Either condition alone is advisory only; both together indicate the floor
      is likely dead. Triggers ALERT banner in fleet-brief output.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, source, merge_age_h, dead_daemon_count, dead_daemons]

  - kind: silent_fleet_death_autohealed
    effect_metric: resilient
    emitter: scripts/coord/fleet-doctor-strict.sh (INFRA-2040)
    trigger: >
      Emitted when CHUMP_DOCTOR_AUTOHEAL=1 and a daemon with exit=127
      (command not found) is healed by restoring its script from origin/main
      via `git checkout origin/main -- <path>` + `launchctl kickstart`.
      Opt-in auto-heal audit trail — only fires when operator explicitly
      enables autoheal mode. Absence is expected in normal (CHUMP_DOCTOR_AUTOHEAL=0)
      operation.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, label, script_path]

  - kind: wedge_detected
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step3 (META-107)
    trigger: >
      Emitted when a BLOCKED+real-fails PR's failing check names match a known
      W-NNN signature pattern from WEDGE_CLASS_CATALOG.md. The wedge-state-machine
      consumes this event and runs the corresponding W-NNN remediation playbook.
      Carries wedge_class (e.g. W-004), pr_number, pr_title, failing_checks.
    consumers: [wedge-state-machine, ops-audit]
    fields_required: [ts, kind, source, wedge_class, pr_number, failing_checks]

  - kind: wizard_dispatch_executed
    effect_metric: effective
    emitter: scripts/coord/wizard-daemon.sh step4 (META-107)
    trigger: >
      Emitted when wizard-daemon step4 successfully spawns chump --execute-gap
      for a pickable P0/P1 gap in background. Carries gap_id and the background
      pid. Rate-limited by CHUMP_WIZARD_MAX_PARALLEL (default 4 concurrent).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, source, gap_id, pid]

  - kind: wizard_dispatch_rate_limited
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step4 (META-107)
    trigger: >
      Emitted when wizard-daemon step4 cannot dispatch a new gap because the
      active concurrent dispatch count has already reached CHUMP_WIZARD_MAX_PARALLEL.
      Carries active_count and max_parallel for rate-limit visibility.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, active_count, max_parallel]

  - kind: wizard_gap_skipped
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step4 (META-107)
    trigger: >
      Emitted when wizard-daemon step4 skips a gap that has wizard_skip:true
      in its YAML notes field. This is a deliberate operator stop-list mechanism
      to prevent the wizard from auto-dispatching gaps that require human oversight.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, gap_id, reason]

  - kind: wizard_cascade_rebase_triggered
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step5 (META-107)
    trigger: >
      Emitted when wizard-daemon step5 successfully triggers gh pr update-branch
      for a BEHIND sibling PR after a gap_shipped event resolves a cluster.
      Safety guard: only fires for PRs authored by CHUMP_WIZARD_ALLOWED_REBASE_AUTHOR
      (default repairman29) to prevent fork takeover.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, source, pr_number, author, head_ref]

  - kind: off_rails_bypassed
    effect_metric: resilient
    emitter: scripts/git-hooks/pre-commit (RESILIENT-025)
    trigger: >
      Emitted when a commit bypasses the off-rails gap-ID check via an
      'Off-Rails-Bypass: <reason>' trailer. The check normally blocks commits
      whose subject does not mention the claimed gap ID, preventing sub-agents
      from committing work for a different gap than they were dispatched on.
      This event records intentional bypasses (e.g. integrating a prerequisite
      from a sibling PR) for audit visibility.
    consumers: [ops-audit]
    fields_required: [ts, kind, claimed_gap, reason]

  - kind: prepush_lease_mismatch
    effect_metric: resilient
    emitter: scripts/git-hooks/pre-push Guard 5 (INFRA-2446)
    trigger: >
      Emitted by the RESILIENT-026 branch-name claim guard when one or more
      claim-*.json files are present in .chump-locks/ but NONE of them match
      the current branch (chump/<gap-id>-claim). This indicates peer sessions
      are running concurrently with active leases for other gaps. The guard
      proceeds without blocking — a peer's lease must not block unrelated
      work — but emits this event so operators can audit parallelism patterns
      and detect orphaned or unclaimed leases.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, branch, available_claim_files, available_gap_ids]
    # payload: {branch: str (current branch lowercased),
    #           available_claim_files: int (count of claim-*.json found),
    #           available_gap_ids: str (comma-separated gap_ids from all claim files)}

  - kind: wizard_classify_deferred
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step1 (INFRA-2042)
    trigger: >
      Emitted when wizard-daemon step1 encounters a PR whose mergeStateStatus is
      UNKNOWN (GitHub API hasn't computed mergeability yet — typically: checks
      haven't started, or PR was just pushed). The PR is skipped this iteration
      and will be re-evaluated on the next wizard-daemon cycle. This prevents
      misclassification as DIRTY which would incorrectly suppress recovery-queue
      routing and keep the DRIVE primitive dormant.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, pr, reason]

  # RESILIENT-033: register 5 orphan event kinds blocking the audit gate
  # All 5 are pre-existing emit sites that never had registry entries.
  # META-117/A: admin_merge_cycle_restore_failed duplicate removed (also at line ~5059
  # via RESILIENT-031 earlier); keeping only the canonical entry above.

  # META-117 Wave 1: dispatch_hung_hook_detected — orphan from META-116 ship
  - kind: dispatch_hung_hook_detected
    effect_metric: resilient
    emitter: scripts/coord/dispatch-health-check.sh (META-116)
    trigger: >
      Emitted when dispatch-health-check.sh detects a Sonnet sub-agent process
      stuck on a hook for longer than the configured threshold. Pattern 13
      hang-detection — distinct from heartbeat-stale (INFRA-2056). Includes
      session id, pid, age in seconds, and command for diagnostic context.
    consumers: [ops-audit, wizard-daemon]
    fields_required: [ts, kind, source, session, pid, age_s, cmd]

  - kind: md_links_heartbeat
    effect_metric: credible
    emitter: scripts/coord/md-links-loop.sh cmd_heartbeat (INFRA-1925)
    trigger: >
      Emitted on each cron-driven md-links-loop heartbeat tick to prove the
      curator-opus-md-links role is alive. No-op otherwise; used by heartbeat
      watchdogs to detect stalled md-links scanners.
    consumers: [heartbeat-shepherd, ops-audit]
    fields_required: [ts, kind]

  - kind: md_links_scan_done
    effect_metric: credible
    emitter: scripts/coord/md-links-loop.sh cmd_tick (INFRA-1925)
    trigger: >
      Emitted at the end of an md-links scan with summary fields. Used to
      track broken-link discovery rate over time and to flag when a scan
      finds zero broken links (which may indicate the scanner itself is
      broken rather than the docs being clean).
    consumers: [ops-audit, doc-health]
    fields_required: [ts, kind, scan_path, files_checked, broken_found]

  - kind: md_links_lane_override
    effect_metric: credible
    emitter: scripts/coord/md-links-loop.sh (INFRA-1925)
    trigger: >
      Emitted when the md-links curator detects an override to the standard
      scan lane (e.g. operator-initiated targeted scan with an explicit scan
      path argument). Used for audit visibility into non-default invocations.
    consumers: [ops-audit]
    fields_required: [ts, kind]

  - kind: fresh_eyes_heartbeat
    effect_metric: credible
    emitter: scripts/coord/fresh-eyes-loop.sh cmd_heartbeat (META-132)
    trigger: >
      Emitted on each cron-driven fresh-eyes-loop heartbeat to prove the
      curator-opus-fresh-eyes (mirror) role is alive. Watched by the
      daemon-silent meta-monitor (INFRA-2352) to detect a stalled
      self-consistency auditor.
    consumers: [heartbeat-shepherd, ops-audit]
    fields_required: [ts, kind]

  - kind: fresh_eyes_tick
    effect_metric: credible
    emitter: scripts/coord/fresh-eyes-loop.sh _run_cycle (META-132)
    trigger: >
      Emitted once per fresh-eyes cycle (tick/audit) regardless of outcome,
      recording the comparator window. Distinguishes "ran and found nothing"
      from "did not run" and tracks cycle cadence.
    consumers: [ops-audit]
    fields_required: [ts, kind, window_min]

  - kind: fresh_eyes_disagreement
    effect_metric: credible
    emitter: scripts/coord/fresh-eyes-loop.sh comparators 1/4/5 (META-132)
    trigger: >
      Emitted (rank-1, anti-noise) when a fleet self-report contradicts ground
      truth: fleet-brief says healthy while the stream shows fire (comparator 1),
      brief healthy while chump health --slo-check breaches (comparator 4), or the
      ROADMAP bottleneck pillar is starved by the last-7d ship distribution
      (comparator 5). comparator_id names which check fired.
    consumers: [ops-audit, wizard-daemon, operator]
    fields_required: [ts, kind, comparator_id, severity, detail]

  - kind: fresh_eyes_coverage_gap
    effect_metric: credible
    emitter: scripts/coord/fresh-eyes-loop.sh comparator_2 (META-132)
    trigger: >
      Emitted when registered ambient event kinds have zero curator-loop
      coverage (no scripts/coord/*-loop.sh greps or emits them) —
      register-without-watch drift, the inverse of the emit-without-register
      case the registry coverage gate already catches.
    consumers: [ops-audit, observability]
    fields_required: [ts, kind, comparator_id, severity, detail]

  - kind: fresh_eyes_silent_curator
    effect_metric: resilient
    emitter: scripts/coord/fresh-eyes-loop.sh comparator_3 (META-132)
    trigger: >
      Emitted when a curator session emitted heartbeats but zero actions
      (sub_agent_dispatched / DONE / ship_landed) over the comparator window —
      the silent_agent root cause (heartbeat-alive but doing-nothing) that the
      existing heartbeat watchdogs miss because a heartbeat looks like life.
    consumers: [ops-audit, heartbeat-shepherd]
    fields_required: [ts, kind, comparator_id, severity, detail]

  - kind: worktree_reaped
    effect_metric: resilient
    emitter: scripts/ops/stale-worktree-reaper.sh (INFRA-2020, INFRA-2339)
    trigger: >
      Emitted when stale-worktree-reaper removes a worktree that met all reap
      criteria (idle past threshold + no active lease + branch pushed-or-deletable).
      Includes source tag distinguishing operator-manual worktrees from
      agent-created ones for audit differentiation.
      INFRA-2339 adds scan_source field distinguishing how the worktree was
      discovered: git-list (any /tmp/* git-registered worktree regardless of name),
      tmp_chump (existing /tmp/chump-* glob), rescue-pattern (standalone /tmp/ dir
      matching infra-NNN-*, ship-NNN, fix-*, *-rescue, *-fix patterns), or
      tmp-age (standalone /tmp/ dir matched by age alone).
    consumers: [ops-audit]
    fields_required: [ts, kind, source, scan_source, branch, dry_run]
  - kind: wizard_dispatch_cooldown
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step4 (INFRA-2051)
    trigger: >
      Emitted when wizard-daemon step4 skips dispatching a gap because that gap
      had a FAILED dispatch outcome recorded in dispatch history within the
      CHUMP_WIZARD_DISPATCH_GAP_COOLDOWN_S window (default 1800s = 30min).
      Prevents infinite wheel-spin on gaps that consistently fail to ship after
      chump --execute-gap spawns. The gap will be reconsidered after the cooldown
      window expires.
    consumers: [ops-audit]
    fields_required: [ts, kind, source, gap_id, last_fail_age_s, cooldown_s]

  - kind: wizard_dispatch_giveup
    effect_metric: resilient
    emitter: scripts/coord/wizard-daemon.sh step4 (INFRA-2051)
    trigger: >
      Emitted when wizard-daemon step4 abandons dispatching a gap after recording
      >= CHUMP_WIZARD_MAX_DISPATCH_ATTEMPTS (default 3) FAILED dispatch outcomes
      in the last 24 hours. The gap is tagged with wizard_skip:true via
      chump gap set --add-note so future dispatch attempts are blocked until an
      operator manually reviews and clears the skip flag. This prevents
      autonomous systems from thrashing on gaps that need human intervention.
    consumers: [ops-audit, operator-pager]
    fields_required: [ts, kind, source, gap_id, failed_attempts, max_attempts]
  # ── RESILIENT-026: orphan worktree watchdog ────────────────────────────────
  - kind: orphan_worktree_detected
    effect_metric: resilient
    emitter: scripts/coord/orphan-worktree-watchdog.sh (RESILIENT-026)
    trigger: >
      A /tmp/chump-* linked worktree has uncommitted or unpushed work, its
      owning process is no longer running (dead session_id), it has been
      filesystem-idle for >15 min, and it has no recent push to origin.
      Fired every 5 min by launchd (ai.chump.orphan-worktree-watchdog).
      Indicates a sub-agent crashed mid-task (e.g. API socket error) or was
      stopped by the operator, leaving work-on-disk that is not on origin.
      Operator action: inspect the worktree, commit + push, or delete if stale.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, worktree_path, branch, last_commit_sha, uncommitted_line_count, age_minutes, claim_gap_id]

  # ── RESILIENT-035 (INFRA-1395): required-check grace window ────────────────
  - kind: required_check_added
    effect_metric: self
    emitter: scripts/coord/required-check-monitor.sh (INFRA-1395)
    trigger: >
      A new required CI check appears in branch-protection required-contexts
      that was not in the prior snapshot (.chump-locks/required-check-snapshot.json).
      Fires each time required-check-monitor.sh detects a new check name.
      A 30-minute grace window is opened (.chump-locks/required-check-grace.json)
      so PRs opened before grace_until are not blocked by the new check.
    consumers: [ops-audit, fleet-brief, required-check-monitor]
    fields_required: [ts, kind, check_name, added_at, grace_until, grace_window_mins]
    docs: docs/process/REQUIRED_CHECK_ADDITION.md

  # ── INFRA-1939 wedge guard (bot-merge graphql exhaustion) ────────────────
  # Emitted by scripts/coord/bot-merge.sh at script top when the last 200
  # ambient events show a `graphql_exhausted` within CHUMP_GRAPHQL_WEDGE_LOOKBACK_S
  # (default 1800s). Aborts the ship to prevent piling on an already-throttled
  # GraphQL bucket. Backlog drains once the secondary rate-limit window resets.
  # INFRA-2096 follow-up: registered here (was emit-without-register prior, kept
  # the audit's emit-without-register count at 1 and blocked the keystone PR).
  - kind: bot_merge_graphql_wedge_aborted
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-1939)
    trigger: |
      bot-merge.sh detected a recent graphql_exhausted event in ambient.jsonl
      within the wedge-lookback window, and aborted to avoid worsening GraphQL
      starvation. The aborted ship can be retried after CHUMP_GRAPHQL_WEDGE_LOOKBACK_S
      seconds (default 1800s = 30min).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, last_graphql_exhausted, lookback_s]
    docs: scripts/coord/bot-merge.sh (INFRA-1939 wedge-guard comment block at top)

  - kind: bot_merge_graphql_wedge_cleared
    effect_metric: self
    emitter: scripts/coord/bot-merge.sh (INFRA-2426 recovery path)
    trigger: |
      bot-merge.sh detected a recent graphql_exhausted AND a subsequent
      graphql_recovered event AFTER the exhaustion timestamp. Bypasses the
      INFRA-1939 wedge guard and proceeds with the ship, since the GraphQL
      surface has demonstrably recovered. Recovery-bypass closes the
      previously-undocumented exit-144 silent-fail path (INFRA-2426).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, last_graphql_exhausted, last_graphql_recovered]
    docs: scripts/coord/bot-merge.sh (INFRA-2426 recovery-bypass comment block)

  # ── RESILIENT-trunk-RED guard (INFRA-2096): test SKIP because runner-side ──
  # capability missing (cargo, binary build, etc.). Audit-only; SKIPs are
  # transient unblocks while the underlying runner-side issue (INFRA-2086
  # silent-build-failure class) is being resolved.
  - kind: trunk_red_skip
    effect_metric: self
    emitter: scripts/ci/test-mcp-coord-smoke.sh (INFRA-2096); future similar SKIP guards.
    trigger: |
      A CI smoke test that requires a built binary (or other runtime capability)
      detected the capability missing AND chose to SKIP rather than fail. This
      lets PRs through despite runner-side trunk-RED, but each SKIP is logged
      here so the operator can audit which PRs were unblocked by SKIP vs. real
      PASS — and flip back to blocking once the runner state is healthy.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, script, gap, reason]
    docs: docs/process/CLAUDE_GOTCHAS.md (see "Runner-side cargo silent-build-failure")
  # ── INFRA-2086: chump_cargo_build loud-fail wrapper events ─────────────────
  - kind: cargo_build_failed
    effect_metric: resilient
    emitter: scripts/coord/lib/cargo-helpers.sh chump_cargo_build (INFRA-2086)
    trigger: >
      Emitted by chump_cargo_build() when a cargo build invocation fails for
      any of 4 classes: timeout (rc=124), registry_corruption (tauri/proc-macro
      pattern in stderr), compile_error (any non-zero rc), or missing_binary
      (rc=0 but expected binary path doesn't exist after build — the silent-
      failure class that surfaced as INFRA-2082).
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, package, binary, exit_code, duration_s, suspected_class]

  - kind: cargo_registry_corruption
    effect_metric: resilient
    emitter: scripts/coord/lib/cargo-helpers.sh chump_cargo_build (INFRA-2086)
    trigger: >
      Emitted when cargo build stderr matches the tauri/proc-macro registry-
      cache-corruption pattern. Carries the suspect crate name + suggested
      fix command (cargo clean -p <crate>) so the operator can recover
      without grep-archaeology.
    consumers: [ops-audit, fleet-doctor, infra-watcher]
    fields_required: [ts, kind, crate, suggested_fix]

  - kind: auto_merge_policy_bypassed
    effect_metric: resilient
    emitter: scripts/coord/bot-merge.sh (INFRA-2155)
    trigger: >
      Emitted by bot-merge.sh when CHUMP_BYPASS_AUTO_MERGE_POLICY=1
      skips the chump-policy check before arming auto-merge. Used for
      genuine recovery scenarios where the operator needs to ship past
      a policy gate; every bypass is auditable so the operator can
      review patterns (frequent bypasses on a given operator-repo =
      signal that the policy is wrong, not that the bypass is wrong).
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, pr, session]

  - kind: gap_auto_flipped
    effect_metric: effective
    emitter: .github/workflows/auto-flip-on-merge.yml (INFRA-2121)
    trigger: >
      Emitted when the auto-flip-on-merge workflow successfully runs
      chump gap ship <ID> --closed-pr <PR#> on a merged PR whose title
      contains a gap-ID. Closes the gap-registry-drift loop CI-REVIEW
      Lever 5 identified.
    consumers: [ops-audit, fleet-brief, pr-watch]
    fields_required: [ts, kind, gap, pr, outcome]

  - kind: gap_auto_flip_failed
    effect_metric: resilient
    emitter: .github/workflows/auto-flip-on-merge.yml (INFRA-2121)
    trigger: >
      Emitted when chump gap ship exits non-zero from the auto-flip
      workflow (gap unknown, YAML malformed, etc.). Non-fatal — the PR
      already merged; this only signals that the registry-side update
      did not land for operator follow-up.
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, gap, pr, reason]

  - kind: gap_backfill_flipped
    effect_metric: effective
    emitter: scripts/ops/backfill-shipped-gaps.sh (INFRA-2121)
    trigger: >
      Emitted by the one-time backfill script for each gap whose status
      it successfully flipped from open → done. Lets the operator audit
      which gaps the backfill touched without re-running it in dry-run.
    consumers: [ops-audit]
    fields_required: [ts, kind, gap, pr, source]
  - kind: reviewer_routing_computed
    effect_metric: effective
    emitter: crates/chump-reviewer-routing/src/bin/chump-reviewer-routing.rs (INFRA-1491)
    trigger: >
      Emitted by `chump-reviewer-routing show` and `route` whenever a
      reviewer set is computed. Discloses the count + suggested logins
      so an operator audit trail exists even when `route` exits without
      actually requesting (dry-run, empty set, error mid-way). Emitted
      on stderr so stdout stays parseable JSON for callers consuming
      `show --json`.
    consumers: [ops-audit, pr-watch]
    fields_required: [ts, kind, reviewer_count]

  - kind: reviewer_routing_requested
    effect_metric: effective
    emitter: crates/chump-reviewer-routing/src/bin/chump-reviewer-routing.rs (INFRA-1491)
    trigger: >
      Emitted by `chump-reviewer-routing route` only after a successful
      `gh pr edit --add-reviewer ...` call. Disambiguates "we computed a
      set" (reviewer_routing_computed) from "we actually asked GitHub to
      request these humans" (reviewer_routing_requested). Pairs cleanly
      with the auto_merge_policy_evaluated event from INFRA-1489 — both
      are the audit trail behind Marcus M-E.
    consumers: [ops-audit, pr-watch]
    fields_required: [ts, kind, pr, reviewers]
  - kind: auto_merge_policy_evaluated
    effect_metric: effective
    emitter: crates/chump-policy/src/bin/chump-policy.rs (INFRA-1489)
    trigger: >
      Emitted by `chump-policy check` when the layered policy chain
      (fleet + operator + repo, most-restrictive wins) allows auto-merge
      under the current configuration. Operator-facing audit trail for
      Marcus M-E trust-cliff knob.
    consumers: [ops-audit, bot-merge.sh integration]
    fields_required: [ts, kind, outcome]

  - kind: auto_merge_policy_blocked
    effect_metric: resilient
    emitter: crates/chump-policy/src/bin/chump-policy.rs (INFRA-1489)
    trigger: >
      Emitted by `chump-policy check` when the layered policy chain
      blocks auto-merge. Carries the contributing scope(s) so the
      operator can see WHICH scope (fleet / operator / repo) drove the
      block — kills the "silent auto-merge refused, why?" debugging tax.
    consumers: [ops-audit, bot-merge.sh integration]
    fields_required: [ts, kind, reason, contributing]

  # ── Integration-cycle pipeline (META-124 / INFRA-2130) ────────────────────
  - kind: integration_cycle_started
    effect_metric: effective
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted at the very beginning of an integration-cycle run, before
      candidate selection. Operator can correlate this ts with
      integration_cycle_shipped to measure wall-clock cycle latency.
    consumers: [fleet-brief, kpi-report, ops-audit]
    fields_required: [ts, kind, cycle_id, trigger_reason, candidate_count]
    expected_volume: "1 per integration cycle run"
    # payload: {cycle_id: string, trigger_reason: string, candidate_count: number, ts: string}

  - kind: integration_candidates_selected
    effect_metric: effective
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted after the cycle selects its candidate set but before any
      merging begins. The manifest array lets post-mortem queries
      reconstruct exactly which branches were in-flight for a given cycle.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, cycle_id, manifest]
    expected_volume: "1 per integration cycle run"
    # payload: {cycle_id: string, manifest: Array<{gap_id: string, branch: string, loc: number, priority: string, queue_age_s: number}>, ts: string}

  - kind: integration_cycle_merges
    effect_metric: effective
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Per-cycle AGGREGATE emitted once after all branch merges in a
      cycle complete (replaces per-merge fanout). Carries a merges array
      summarising every branch merged in the cycle — lower ambient noise
      while preserving full audit trail. Observability-curator amendment
      per INFRA-2132: prefer aggregate over per-event fanout for
      high-frequency in-cycle events.
    consumers: [fleet-brief, kpi-report, ops-audit]
    fields_required: [ts, kind, cycle_id, merges]
    expected_volume: "1 per integration cycle run"
    # payload: {cycle_id: string, merges: Array<{gap_id: string, branch: string, parent_sha: string, merge_sha: string}>, ts: string}

  - kind: integration_preflight_started
    effect_metric: resilient
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted when the cycle begins running the preflight checks on the
      assembled integration branch. Pairs with integration_preflight_failed
      or integration_cycle_shipped to bound preflight duration.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, cycle_id, branch, expected_duration_s]
    expected_volume: "1 per integration cycle run"
    # payload: {cycle_id: string, branch: string, expected_duration_s: number, ts: string}

  - kind: integration_preflight_failed
    effect_metric: resilient
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted when the integration-branch preflight fails before a ship
      attempt. Carries the failure_class so the CI-audit loop can bucket
      this as flake vs. logic-bug without re-reading logs manually.
    consumers: [fleet-brief, ci-audit, ops-audit]
    fields_required: [ts, kind, cycle_id, branch, failure_class, log_path]
    expected_volume: "0-N per integration cycle run; 0 on green cycles"
    # payload: {cycle_id: string, branch: string, failure_class: string, log_path: string, ts: string}

  - kind: ship_bisect_root_cause
    effect_metric: resilient
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted when a post-ship bisect identifies the offending gap
      (commit) that caused a cycle failure. Carries bisect_steps_taken
      so tooling can monitor bisect efficiency over time.
    consumers: [ci-audit, ops-audit, fleet-brief]
    fields_required: [ts, kind, cycle_id, offending_gap_id, failure_signature, bisect_steps_taken]
    expected_volume: "0-1 per failed cycle that runs bisect"
    # payload: {cycle_id: string, offending_gap_id: string, failure_signature: string, bisect_steps_taken: number, ts: string}

  - kind: bisect_quarantine
    effect_metric: resilient
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted when the bisect result is used to quarantine a gap branch,
      pulling it out of the integration queue. requeue_status indicates
      whether the gap was re-opened for rework ("requeued") or held
      ("held") pending operator review.
    consumers: [ci-audit, ops-audit, waste-tally]
    fields_required: [ts, kind, cycle_id, quarantined_gap_id, reason, requeue_status]
    expected_volume: "0-1 per ship_bisect_root_cause event"
    # payload: {cycle_id: string, quarantined_gap_id: string, reason: string, requeue_status: "requeued"|"held", ts: string}

  - kind: integration_cycle_shipped
    effect_metric: effective
    emitter: scripts/coord/integration-cycle.sh (INFRA-2130)
    trigger: >
      Emitted once after a full integration cycle lands successfully on
      main. Carries the final manifest, PR URL, and wall_time_s so
      kpi-report can compute throughput and cycle latency without log
      parsing. Cross-reference: pr_merged events for individual GAP PRs
      within the cycle.
    consumers: [fleet-brief, kpi-report, ops-audit, waste-tally]
    fields_required: [ts, kind, cycle_id, final_manifest, pr_url, merge_sha, total_loc, wall_time_s]
    related_kinds: [pr_merged]
    expected_volume: "1 per successful integration cycle"
    # payload: {cycle_id: string, final_manifest: Array<{gap_id: string, branch: string}>, pr_url: string, merge_sha: string, total_loc: number, wall_time_s: number, ts: string}

  - kind: cycle_sampling_decision
    effect_metric: credible
    emitter: crates/chump-integrator/src/daemon.rs (INFRA-2139)
    trigger: >
      Phase 2 gate. Emitted once per cycle immediately after CLAIM, before
      SELECT. A deterministic FNV-1a hash of cycle_id mod 100 + 1 gives a
      roll in [1, 100]; if roll <= threshold the cycle runs LIVE, otherwise
      it stays DRY-RUN. Enables post-hoc analysis of sampling rate and
      live-cycle frequency. dry_run_override=true means the global
      CHUMP_INTEGRATOR_DRY_RUN flag forced DRY-RUN regardless of roll.
    consumers: [kpi-report, ops-audit]
    fields_required: [ts, kind, cycle_id, roll, threshold, decision, dry_run_override, cycle_is_live]
    expected_volume: "1 per cycle attempt"
    status: stable
    # payload: {cycle_id: string, roll: number, threshold: number, decision: "live"|"dry_run",
    #           dry_run_override: bool, cycle_is_live: bool, ts: string}

  # META-127 C3c — curator-opus-velocity-tracker (INFRA-2221)
  - kind: velocity_tracker_tick
    effect_metric: credible
    emitter: .claude/agents/curator-opus-velocity-tracker.md (INFRA-2221)
    trigger: >
      Emitted once per 7-day window by curator-opus-velocity-tracker after
      computing P50 ship-time, throughput (ships/day), and flake rate
      (test_flake events per merge) from ambient.jsonl + state.db.
      Carries week-over-week deltas and regressed=true flags for any metric
      that crossed its regression threshold (>20% for ship-time/throughput,
      >50% for flake rate). Emitted with force=true if operator dispatched
      an out-of-window tick.
    consumers: [orchestrator, kpi-report, curator-opus-incident-commander]
    fields_required: [ts, kind, session, p50_ship_time_s, p50_delta_pct, throughput_ships_per_day, throughput_delta_pct, flake_rate_per_ship, flake_delta_pct, window_days]
    expected_volume: "1 per 7-day window; operator may force additional ticks"
    # payload: {p50_ship_time_s: number, p50_delta_pct: number, throughput_ships_per_day: number, throughput_delta_pct: number, flake_rate_per_ship: number, flake_delta_pct: number, regressions: string[], window_days: number, low_sample: boolean, force: boolean, ts: string}

  # META-127 C3d — curator-opus-incident-commander (INFRA-2222)
  - kind: incident_commander_engaged
    effect_metric: resilient
    emitter: .claude/agents/curator-opus-incident-commander.md (INFRA-2222)
    trigger: >
      Emitted when curator-opus-incident-commander activates in response to
      a trunk-red condition (fleet_wedge, pr_stuck cluster ≥3 in 2h,
      operator_recall with condition=CI_BROKEN, or trunk_red event).
      Re-emitted at each rescue playbook step milestone (step=1..5) and
      again when the incident is resolved (status=resolved), escalated
      (status=escalated), or determined to be a false positive
      (status=false_positive). Enables post-incident timeline reconstruction
      without log parsing.
    consumers: [orchestrator, ops-audit, fleet-brief, kpi-report]
    fields_required: [ts, kind, session, status, trigger]
    expected_volume: "0-1 per incident declaration; N step-milestone events per active incident"
    # payload: {status: "active"|"resolved"|"escalated"|"false_positive", trigger: string, step: number|null, action: string|null, ts: string}

  # META-127 C3e — curator-opus-architecture-coach (INFRA-2223)
  - kind: arch_fit_query
    effect_metric: credible
    emitter: .claude/agents/curator-opus-architecture-coach.md (INFRA-2223)
    trigger: >
      Emitted by curator-opus-architecture-coach when it begins evaluating
      a gap for architectural fit. Marks the start of the review so
      consumers can detect gaps that received a query but no subsequent
      arch_fit_decision (stalled reviews). Carries the gap_id and effort
      so kpi-report can compute coach coverage by effort tier.
    consumers: [orchestrator, kpi-report]
    fields_required: [ts, kind, session, gap_id, effort]
    expected_volume: "0-N per session (one per gap reviewed, effort >= m by default)"
    # payload: {gap_id: string, effort: string, needs_prior_art: boolean, ts: string}

  - kind: arch_fit_decision
    effect_metric: credible
    emitter: .claude/agents/curator-opus-architecture-coach.md (INFRA-2223)
    trigger: >
      Emitted by curator-opus-architecture-coach after completing an
      architectural fit review for a gap. Carries the rating
      (fit | stretch | fork), rationale citing a specific architectural
      principle, and confidence level. A retracted decision is re-emitted
      with rating=retracted and the original_rating field set.
      Downstream: orchestrator decides whether to hold work on fork ratings;
      decompose uses the rating as one input to sub-gap slicing.
    consumers: [orchestrator, decompose, kpi-report, ops-audit]
    fields_required: [ts, kind, session, gap_id, rating, rationale, confidence]
    related_kinds: [arch_fit_query]
    expected_volume: "0-N per session (paired with arch_fit_query events)"
    # payload: {gap_id: string, rating: "fit"|"stretch"|"fork"|"retracted", rationale: string, confidence: "high"|"med"|"low", original_rating: string|null, correction: boolean, ts: string}
  - kind: bisect_step_evaluated
    effect_metric: resilient
    emitter: scripts/dev/integration-bisect-step.sh (INFRA-2168)
    trigger: >
      Emitted after each git bisect step completes in the integration-cycle
      bisect oracle. Records whether the commit was classified as good (0),
      bad (1), or skip/flake (125), along with which flake class matched (if
      any) and the wall-clock duration. Enables post-hoc analysis of bisect
      efficiency and flake-class hit rates without log parsing.
    consumers: [ci-audit, ops-audit, kpi-report]
    fields_required: [ts, kind, commit, outcome, duration_s, matched_class]
    expected_volume: "N per bisect run (one per commit evaluated)"
    status: stable
    # payload: {commit: string (short sha), outcome: "good"|"bad"|"skip", duration_s: float, matched_class: string (id or "none"), ts: string}
  - kind: opus_inbox_surfaced
    effect_metric: effective
    emitter: scripts/coord/ambient-context-inject.sh INFRA-1797 SessionStart block
    trigger: >
      Emitted at SessionStart when the opus-inbox surfacer renders one or
      more unread messages from .chump-locks/opus-inbox/. Carries
      unread_count and the curator session_id so consumers (operator
      dashboards, ambient-tail tools) can track inbox-surfacing patterns
      without re-scanning the inbox files.
    consumers: [ops-audit, fleet-doctor, pwa-dashboard]
    fields_required: [ts, kind, unread_count, session]
  - kind: gap_shipped_in_set
    effect_metric: credible
    emitter: crates/chump-gap-store/src/lib.rs (INFRA-2134)
    trigger: >
      Emitted by GapStore::set_shipped_in() whenever a shipped_in JSON blob
      is written for a gap. Enables audit trails for both integration-cycle
      and per-PR shipping paths. Contains the gap_id and the shape of the
      shipped_in record (integration vs per_pr).
    consumers: [fleet-brief, kpi-report, ops-audit]
    fields_required: [ts, kind, gap_id, shape]
    related_kinds: [cycle_shipped, pr_merged]
    expected_volume: "1 per gap shipped via any path"
    status: stable
    # payload: {gap_id: string, shape: "integration"|"per_pr", ts: string}
  - kind: ship_autofetch_pulled
    effect_metric: resilient
    emitter: crates/chump-gap-store/src/lib.rs GapStore::ship() (INFRA-2423)
    trigger: >
      Emitted when chump gap ship auto-fetches origin/main, detects local main
      is N commits behind, finds a clean working tree, and successfully pulls
      with --ff-only. Indicates the stale-local-main case was recovered
      automatically without operator intervention.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap_id, behind]
    expected_volume: "occasional; spikes indicate workers falling behind main"
    status: stable
    # payload: {gap_id: string, behind: u64, ts: string}
  - kind: ship_autofetch_blocked_dirty
    effect_metric: resilient
    emitter: crates/chump-gap-store/src/lib.rs GapStore::ship() (INFRA-2423)
    trigger: >
      Emitted when chump gap ship auto-fetches origin/main, detects local main
      is N commits behind, but finds the working tree is dirty (staged or
      unstaged changes). Ship is blocked; the caller must stash or commit
      before retrying. This is a friction signal for fleet health dashboards.
    consumers: [fleet-brief, ops-audit, fleet-doctor]
    fields_required: [ts, kind, gap_id, behind]
    expected_volume: "rare; should be near-zero in healthy fleet operation"
    status: stable
    # payload: {gap_id: string, behind: u64, ts: string}
  - kind: per_gap_branch_pruned
    effect_metric: zero-waste
    emitter: scripts/ops/chump-post-integration-prune.sh (INFRA-2138)
    trigger: >
      Emitted when chump-post-integration-prune.sh deletes a per-gap
      branch from origin after the 24h grace window following an
      integration_cycle_shipped event. One event per branch pruned.
      Also emitted in dry-run mode (dry_run=true) for auditability.
    consumers: [fleet-brief, ops-audit, waste-tally]
    fields_required: [ts, kind, branch, gap_id, cycle_id, dry_run]
    related_kinds: [integration_cycle_shipped]
    expected_volume: "N per integration cycle (one per gap in cycle)"
    status: stable
    # payload: {branch: string, gap_id: string, cycle_id: string, dry_run: bool, ts: string}

  # ── INFRA-1120: A2A Layer 2c — capability manifest + session discovery ───────
  - kind: capability_published
    effect_metric: effective
    emitter: crates/chump-coord/src/capability.rs (INFRA-1120) publish_manifest()
    trigger: >
      Emitted when a worker session publishes or heartbeats its CapabilityManifest
      to the chump_capabilities NATS KV bucket. Fires at worker startup and every
      30 seconds thereafter (HEARTBEAT_INTERVAL_SECS). Also appends a snapshot to
      .chump-locks/capabilities/<session-id>.jsonl for forensic audit when NATS is
      unavailable. scanner-anchor present in capability.rs.
    consumers: [picker, ops-audit, fleet-doctor, kpi-report]
    fields_required: [ts, kind, session_id, schema_version, harness, model_tier, skills]
    expected_volume: "1 per worker per 30s (one per heartbeat interval)"
    status: stable
    # payload: {session_id: string, schema_version: "chump-capability-v1",
    #           harness: string, model_tier: string, skills: [string],
    #           machine: string|null, ttl_seconds: int, heartbeat_at: RFC3339}
  - kind: capability_heartbeat
    effect_metric: effective
    emitter: crates/chump-coord/src/capability.rs (INFRA-1120) heartbeat_loop()
    trigger: >
      Emitted by the heartbeat_loop() task every HEARTBEAT_INTERVAL_SECS (30s) to
      refresh a session's CapabilityManifest TTL in the chump_capabilities KV
      bucket. Stale manifests (heartbeat_at > ttl_seconds old) are excluded from
      picker routing decisions. scanner-anchor present in capability.rs.
    consumers: [picker, fleet-doctor, ops-audit]
    fields_required: [ts, kind, session_id, heartbeat_at]
    expected_volume: "1 per worker per 30s"
    status: stable
    # payload: {session_id: string, heartbeat_at: RFC3339, ttl_seconds: int}

  - kind: cascade_unblocked
    effect_metric: resilient
    emitter: scripts/coord/cascade-unblock-detector.sh (INFRA-2070)
    trigger: >
      Emitted by cascade-unblock-detector.sh after a wedge_auto_fix PR merges and
      the detector identifies open PRs whose pr_failed events share the same
      failure signature. Records the source (auto-fix) PR, the signature hash,
      all matched PR numbers, and rebase attempt counts (success vs conflict).
      Emitted once per merged fix PR per run, even when some rebases fail.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, source, source_pr, signature_hash, matched_pr_numbers, rebase_attempt_count]
    expected_volume: "0–5 per wizard-daemon cycle (one per merged wedge_auto_fix PR)"
    status: stable
    # payload: {source_pr: int, signature_hash: str, matched_pr_numbers: csv-str,
    #           rebase_attempt_count: int, success_count: int, conflict_count: int,
    #           skipped_count: int}

  - kind: cascade_unblock_skipped
    effect_metric: resilient
    emitter: scripts/coord/cascade-unblock-detector.sh (INFRA-2070)
    trigger: >
      Emitted per-PR when a candidate PR is skipped during cascade-unblock.
      Reason field encodes which safety guard fired:
        operator_recent_comment — operator commented on the PR within OPERATOR_WINDOW_S (default 30min)
        chump_hold_label        — PR carries the CHUMP_HOLD label
        rebase_conflict         — gh pr update-branch returned non-zero (conflict or API error)
        rate_limit_reached      — CHUMP_UNBLOCK_RATE_LIMIT exceeded for this run
    consumers: [ops-audit]
    fields_required: [ts, kind, source, source_pr, signature_hash, pr_number, reason]
    expected_volume: "0–N per cascade run (N = blocked PRs that hit safety guards)"
    status: stable
    # payload: {source_pr: int, signature_hash: str, pr_number: int, reason: str}
  # INFRA-2280: META-118 scheduling activation — daemon lifecycle events
  - kind: meta_118_daemon_started
    effect_metric: effective
    emitter: scripts/setup/install-meta-118-daemons.sh (INFRA-2280)
    # scanner-anchor: "kind":"meta_118_daemon_started"
    trigger: >
      Emitted once by install-meta-118-daemons.sh immediately after both
      launchd plists are loaded. Confirms the META-118 scheduling chain has
      been activated on this host. Carries labels (array of launchd label
      strings for the two installed daemons).
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, source, labels]
    expected_volume: "1 per host install/reinstall"
    status: stable
    # payload: {labels: [str], source: str}

  - kind: meta_118_daemon_tick
    effect_metric: effective
    emitter: scripts/coord/novel-wedge-classifier.sh (INFRA-2280) + scripts/coord/cascade-unblock-detector.sh (INFRA-2280)
    # scanner-anchor: "kind":"meta_118_daemon_tick"
    trigger: >
      Emitted at the start of each scheduled run by the two META-118 daemon
      scripts: novel-wedge-classifier.sh (every 15 min) and
      cascade-unblock-detector.sh (every 5 min). Carries daemon name so
      fleet-doctor and INFRA-2278 SessionStart surface can count ticks and
      confirm liveness. Missing ticks indicate launchd misconfiguration or
      script crash.
    consumers: [ops-audit, fleet-doctor, infra-2278-session-start]
    fields_required: [ts, kind, daemon]
    expected_volume: "~6/hr (4 cascade + 1 classifier) per host"
    status: stable
    # payload: {daemon: str (novel-wedge-classifier | cascade-unblock-detector)}

  # INFRA-2006: A2A inbox-routing fix — alias resolution events
  - kind: a2a_inbox_alias_resolved
    effect_metric: resilient
    emitter: scripts/coord/chump-inbox.sh (INFRA-2006 reader-side union fix) + scripts/coord/lib/inbox-routing.sh resolve_inbox_target
    trigger: >
      Emitted by chump-inbox.sh read when it finds new messages in a
      lease-id alias inbox (i.e., an inbox file named after the session's
      claim lease-id rather than its primary env-session-id). This is the
      normal path after broadcast.sh --to <lease-session-id> delivers to
      the lease-id file while the reader's CHUMP_SESSION_ID differs.
      Also emitted by resolve_inbox_target when a sender-side lookup maps
      a lease-id or gap-id to a canonical inbox path.
      Carries primary_session, alias_session, message_count.
    consumers: [ops-audit, kpi-report, fleet-doctor]
    fields_required: [ts, kind, session, alias_session, message_count]
    expected_volume: "0-N per session read cycle; frequency drops to 0 once sender-side is fixed to use primary session ids (INFRA-2034)"
    # payload: {primary_session: string, alias_session: string, message_count: int, ts: string}

  - kind: a2a_inbox_message_orphan
    effect_metric: resilient
    emitter: scripts/coord/chump-inbox.sh (INFRA-2006 reader-side union fix)
    trigger: >
      Advisory event emitted when a message is found in an inbox file
      (.chump-locks/inbox/<id>.jsonl) that no live session lease owns.
      The message is NOT delivered (no live reader) but is flagged so
      operators can detect persistent orphan accumulation. Not fatal —
      archive-on-dead-session logic cleans these up eventually.
      Carries inbox_file, message_count.
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, inbox_file, message_count]
    expected_volume: "0-N; non-zero is advisory, not actionable unless persistent"
    # payload: {inbox_file: string, message_count: int, ts: string}
  - kind: trunk_cascade_broken
    effect_metric: resilient
    emitter: scripts/ops/break-trunk-cascade.sh (INFRA-2087)
    trigger: >
      Emitted on every successful invocation of break-trunk-cascade.sh —
      records the bypass for audit. Carries pr_number, reason,
      snapshot_contexts (the original required_status_checks list),
      bypass_duration_ms (how long protections were dropped),
      merge_exit, and operator session id. Combined with INFRA-2201
      required_status_checks_empty kind, this forms the full audit
      trail for any cascade-break operation.
    consumers: [ops-audit, fleet-doctor]
    fields_required: [ts, kind, pr, reason, snapshot_contexts, bypass_duration_ms]

  - kind: trunk_cascade_rate_limited
    effect_metric: resilient
    emitter: scripts/ops/break-trunk-cascade.sh (INFRA-2087)
    trigger: >
      Emitted when break-trunk-cascade.sh refuses to run because
      CHUMP_BREAK_CASCADE_PER_HOUR (default 1) has been exceeded within
      a rolling 1h window. Carries pr_number, limit, recent_count so the
      operator can audit pattern (frequent rate-limited attempts = signal
      the trunk-cascade root cause needs structural fix, not repeated
      bypass).
    consumers: [ops-audit]
    fields_required: [ts, kind, pr, limit, recent_count]
  # ── INFRA-1813: Marcus M-B HITL trust-gate ────────────────────────────────
  # Vendored shape from BEAST-MODE approve/reject route handlers (CP-003).
  - kind: hitl_approval_required
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-1813)
    # scanner-anchor: "kind":"hitl_approval_required"
    trigger: >
      Emitted when bot-merge encounters a PR that needs human-in-the-loop
      approval (require_hitl is on AND no approval signal present). The
      auto-merge arm is skipped; PR stays open. Carries pr/branch/files +
      approve_hint so the operator surface (PWA tray, tail script) can
      present an actionable prompt.
    consumers: [ops-audit, pwa-hitl-tray, waste-tally]
    fields_required: [ts, kind, pr, branch, files, session, approve_hint]
    expected_volume: "0..N per repo per day (off by default; opt-in)"
    status: stable
    # payload: {pr: number, branch: string, files: string (comma list), session: string, approve_hint: string, ts: string}

  - kind: hitl_approval_granted
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-1813)
    # scanner-anchor: "kind":"hitl_approval_granted"
    trigger: >
      Emitted by bot-merge when an HITL approval signal is detected
      (env CHUMP_HITL_APPROVED=1, file .chump-locks/hitl-approved-<PR>.flag,
      OR PR label hitl-approved) and auto-merge proceeds. Pairs with
      hitl_approval_required for audit-trail completeness.
    consumers: [ops-audit, pwa-hitl-tray, waste-tally]
    fields_required: [ts, kind, pr, signal, session]
    expected_volume: "matches hitl_approval_required volume on resolution"
    status: stable
    # payload: {pr: number, signal: string (env:X | file:Y | label:Z), session: string, ts: string}
    # payload: {cycle_id: string, gap_id: string, decision: "include"|"exclude", reason: string, ts: string}

  # ── INFRA-2295: stale-pr-rebase-bot (SCALE-C safety net) ──────────────────

  - kind: stale_pr_auto_rebased
    effect_metric: resilient
    emitter: scripts/coord/stale-pr-rebase-bot.sh (INFRA-2295)
    trigger: >
      Emitted when a stale, auto-merge-armed PR is successfully rebased —
      either via gh pr update-branch (method=gh_update_branch) or via a
      local worktree rebase with force-with-lease push (method=local_rebase_fallback,
      the INFRA-1958 false-positive path). Complements pr_auto_rebased from
      pr-auto-rebase.sh; adds 3-strike doctrine and trunk-RED guard.
    consumers: [fleet-brief, waste-tally, ops-audit]
    fields_required: [ts, kind, pr, branch, method, prior_strikes]
    expected_volume: "0-N per 15-min cadence (one per rebased PR)"
    status: stable

  - kind: stale_pr_rebase_failed
    effect_metric: resilient
    emitter: scripts/coord/stale-pr-rebase-bot.sh (INFRA-2295)
    trigger: >
      Emitted when both gh pr update-branch and the local worktree rebase
      fallback fail for a given PR on this cycle. Strike counter is
      incremented in .chump-locks/rebase-bot-strikes/<PR>.json. Not yet
      actionable — operator acts only after stale_pr_unrebaseable (3-strike).
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, pr, branch, strikes, strike_limit]
    expected_volume: "0-N per 15-min cadence; bursts during merge conflicts"
    status: stable

  - kind: stale_pr_unrebaseable
    effect_metric: resilient
    emitter: scripts/coord/stale-pr-rebase-bot.sh (INFRA-2295)
    trigger: >
      Emitted when a PR's strike counter reaches CHUMP_REBASE_BOT_STRIKE_LIMIT
      (default 3). A WARN broadcast is also sent via broadcast.sh. The PR is
      NEVER auto-closed — operator must decide (resolve conflict, close, or
      exempt). Strike state at .chump-locks/rebase-bot-strikes/<PR>.json.
    consumers: [fleet-brief, ops-audit, operator-digest]
    fields_required: [ts, kind, pr, branch, strikes, note]
    expected_volume: "rare; signals genuine merge conflict needing operator attention"
    status: stable

  - kind: stale_pr_rebase_bot_holding_for_trunk_red
    effect_metric: resilient
    emitter: scripts/coord/stale-pr-rebase-bot.sh (INFRA-2295)
    trigger: >
      Emitted at the top of each bot cycle when trunk-red-detector-state.json
      exists with a non-empty last_failed_sha. The entire rebase cycle is
      skipped — rebasing branches onto a broken main wastes CI capacity and
      queues work behind failing trunk commits.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind]
    expected_volume: "once per 15-min cycle while trunk is RED"
    status: stable

  # ── INFRA-2274: consensus-merge-gate (replaces 2026-05-30 admin-merge bypass) ──
  - kind: consensus_gate_approved
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-2274)
    # scanner-anchor: "kind":"consensus_gate_approved"
    trigger: >
      Emitted by bot-merge.sh when the INFRA-2274 consensus merge gate
      queries chump consensus-tally and receives verdict=PASSED (>=3 +1
      votes from curators). The auto-merge proceeds normally. Pairs with
      consensus_gate_blocked / consensus_gate_would_block / consensus_bypass_used
      for full audit trail of the 2026-05-30 admin-merge replacement.
    consumers: [ops-audit, consensus-bypass-leaderboard]
    fields_required: [ts, kind, pr, corr_id, verdict, mode, gap_id]
    expected_volume: "1 per merge once enforce mode is active"
    status: shadow
    # payload: {pr: number, corr_id: string (pr-N), verdict: "PASSED", mode: "1"|"enforce", gap_id: string, ts: string}

  - kind: consensus_gate_would_block
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-2274)
    # scanner-anchor: "kind":"consensus_gate_would_block"
    trigger: >
      Emitted by bot-merge.sh in SHADOW mode when consensus verdict is
      NOT PASSED (FAILED, NO_QUORUM, or EXTENDED). The merge proceeds
      anyway (shadow = log only); this event signals the merge WOULD have
      been blocked in enforce mode. 7-day observation window before flip
      to enforce. Drives the consensus-bypass-leaderboard "shadow_would_block"
      column.
    consumers: [ops-audit, consensus-bypass-leaderboard]
    fields_required: [ts, kind, pr, corr_id, verdict, mode, gap_id, note]
    expected_volume: "high during 7-day shadow window; trends down as fleet adopts vote discipline"
    status: shadow
    # payload: {pr: number, corr_id: string, verdict: "FAILED"|"NO_QUORUM"|"EXTENDED", mode: "shadow", gap_id: string, note: string, ts: string}

  - kind: consensus_gate_blocked
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-2274)
    # scanner-anchor: "kind":"consensus_gate_blocked"
    trigger: >
      Emitted by bot-merge.sh in ENFORCE mode when consensus verdict is
      NOT PASSED. bot-merge exits non-zero; PR stays open. Operator can
      cast votes via `chump vote pr-N +1 --reason "..."` or use the
      CHUMP_OPERATOR_CONSENSUS_BYPASS escape hatch.
    consumers: [ops-audit, consensus-bypass-leaderboard]
    fields_required: [ts, kind, pr, corr_id, verdict, mode, gap_id, note]
    expected_volume: "low; spikes signal fleet-wide vote-cast discipline drift"
    status: planned
    # payload: {pr: number, corr_id: string, verdict: string, mode: "enforce", gap_id: string, note: string, ts: string}

  - kind: consensus_bypass_used
    effect_metric: credible
    emitter: scripts/coord/bot-merge.sh (INFRA-2274)
    # scanner-anchor: "kind":"consensus_bypass_used"
    trigger: >
      Emitted by bot-merge.sh when CHUMP_OPERATOR_CONSENSUS_BYPASS=<reason>
      is set. The merge gate is skipped entirely; this is the operator
      escape hatch that replaces today's drop-required-checks pattern.
      Every use is auditable via consensus-bypass-leaderboard.sh; daily
      bypass_ratio > 10% should trigger a structural review (not
      another bypass).
    consumers: [ops-audit, consensus-bypass-leaderboard, pwa-hitl-tray]
    fields_required: [ts, kind, pr, corr_id, reason, operator_session_id, mode, gap_id]
    expected_volume: "low; target <10% of total merges once enforce mode is active"
    status: stable
    # payload: {pr: number, corr_id: string, reason: string, operator_session_id: string, mode: string, gap_id: string, ts: string}

  # ── INFRA-2324: Trunk health sentinel ───────────────────────────────────────
  - kind: trunk_sentinel_tick
    effect_metric: self
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_sentinel_tick"
    trigger: >
      Emitted on every tick of the trunk-health-sentinel daemon (60s cadence
      via launchd com.chump.trunk-sentinel). Records the current derived
      trunk state (TRUNK_GREEN/AMBER/RED/UNKNOWN), the prev state, the
      observed run_id/head_sha/conclusion, and the failing-job fingerprint.
      Pure observability — no follow-up action.
    consumers: [fleet-brief, observability]
    fields_required: [ts, kind, state, prev_state, run_id, head_sha, conclusion, fingerprint]
    expected_volume: "high; one per minute while installed"
    status: stable

  - kind: trunk_state_change
    effect_metric: self
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_state_change"
    trigger: >
      Emitted once when the trunk state transitions (GREEN→RED, RED→GREEN,
      UNKNOWN→GREEN, etc.). Records `from` + `to` + run_id + head_sha so
      a downstream consumer can detect "trunk just turned red at HH:MM" with
      no polling.
    consumers: [fleet-brief, observability, pwa-trunk-banner]
    fields_required: [ts, kind, from, to, conclusion, run_id, head_sha]
    expected_volume: "low; only on actual state flip"
    status: stable

  - kind: trunk_red_persistent
    effect_metric: trunk_recovered_within_60min
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_red_persistent"
    trigger: >
      Emitted once per failing-job fingerprint when trunk has been RED for
      >=5 min. Also files a P0 RESILIENT fix-trunk gap with the failing-job
      fingerprint embedded for dedup. Subsequent ticks with the SAME
      fingerprint do NOT re-emit.
    consumers: [pwa-trunk-banner, ops-audit]
    fields_required: [ts, kind, red_minutes, fingerprint, failing_jobs, run_id, head_sha]
    expected_volume: "very low; only when trunk is RED past 5min"
    status: stable

  - kind: trunk_red_dispatch
    effect_metric: trunk_recovered_within_60min
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_red_dispatch"
    trigger: >
      Emitted when trunk has been RED for >=15 min. Writes a CodeFixContract
      brief to .chump/trunk-sentinel-briefs/<fp>.md and shells out to
      `claude -p` for autonomous diff drafting. Dedupe is per fingerprint.
    consumers: [pwa-trunk-banner, ops-audit]
    fields_required: [ts, kind, fingerprint, run_id, head_sha, gap_id, brief_path, dispatched]
    expected_volume: "very low; only when trunk RED past 15min"
    status: stable

  - kind: trunk_red_operator_recall
    effect_metric: operator_response_within_60min
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_red_operator_recall"
    trigger: >
      Emitted when trunk has been RED for >=60 min (T4 halt-class).
      Also calls scripts/dispatch/operator-recall.sh with condition CI_BROKEN
      so the operator-recall surface pages the operator.
    consumers: [pwa-trunk-banner, ops-audit, operator-recall]
    fields_required: [ts, kind, fingerprint, run_id, head_sha, red_minutes, failing_jobs]
    expected_volume: "very low; should be zero in a healthy week"
    status: stable

  - kind: trunk_recovered
    effect_metric: self
    emitter: scripts/coord/trunk-sentinel-daemon.sh (INFRA-2324)
    # scanner-anchor: "kind":"trunk_recovered"
    trigger: >
      Emitted on RED→GREEN transition. The daemon also walks the fix-trunk
      gaps it filed during the RED window and closes each via
      `chump gap set --status done`. Records how many gaps closed.
    consumers: [pwa-trunk-banner, ops-audit, fleet-brief]
    fields_required: [ts, kind, closed_gaps_count]
    expected_volume: "very low; one per recovery"
    status: stable

  - kind: fix_trunk_priority_signal
    effect_metric: trunk_recovered_within_60min
    emitter: scripts/dispatch/fix-trunk-dispatcher.sh (INFRA-2341)
    # scanner-anchor: "kind":"fix_trunk_priority_signal"
    trigger: >
      Emitted by the fix-trunk-dispatcher (default mode=signal) after it
      atomically claims a fix-trunk gap during trunk_red. Replaces the
      legacy `claude -p` headless dispatch path (INFRA-2340 era) with a
      SessionStart-hook signal that the operator's running Claude Code IDE
      picks up via inbox-check-urgent.sh — respecting Max subscription
      billing instead of forcing console.anthropic.com API balance. The
      dispatcher also writes a paired CRIT entry to .chump-locks/URGENT-INBOX.jsonl
      with kind=fix_trunk_priority_signal so the next PostToolUse / SessionStart
      hook surfaces a "FIX-TRUNK PRIORITY SIGNAL" system-reminder banner.
    consumers: [inbox-check-urgent, pwa-trunk-banner, ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, priority, worktree, filing_session, dispatch_mode]
    expected_volume: "very low; only when trunk RED past 5min AND mode=signal"
    status: stable

  - kind: fix_trunk_session_acknowledged
    effect_metric: trunk_recovered_within_60min
    emitter: scripts/coord/inbox-check-urgent.sh (INFRA-2341)
    # scanner-anchor: "kind":"fix_trunk_session_acknowledged"
    trigger: >
      Emitted by inbox-check-urgent.sh when it surfaces a
      fix_trunk_priority_signal entry to the running IDE session for the
      first time (cursor advance is the "this has been delivered" boundary).
      One event per distinct gap_id surfaced in that tick — closes the
      handshake loop with fix_trunk_priority_signal so the 60-min
      operator-recall watchdog (sentinel) knows the signal was delivered,
      not silently dead-lettered.
    consumers: [ops-audit, fleet-brief, trunk-sentinel-daemon]
    fields_required: [ts, kind, gap_id, priority, worktree]
    expected_volume: "very low; mirrors fix_trunk_priority_signal volume"
    status: stable

  - kind: fix_trunk_dispatched
    effect_metric: trunk_recovered_within_60min
    emitter: scripts/dispatch/fix-trunk-dispatcher.sh (INFRA-2324, INFRA-2341)
    # scanner-anchor: "kind":"fix_trunk_dispatched"
    trigger: >
      Emitted by the fix-trunk-dispatcher when it claims a fix-trunk gap
      AND spawns a headless `claude -p` sub-agent against the legacy
      subprocess path (CHUMP_FIX_TRUNK_DISPATCH_MODE=subprocess, opt-in
      post-INFRA-2341). This event remains for users who explicitly want
      ANTHROPIC_API_KEY / CLAUDE_CODE_OAUTH_TOKEN headless billing without
      an interactive IDE session open.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, model, pid, worktree, dispatch_mode]
    expected_volume: "very low; only when trunk RED AND mode=subprocess (opt-in)"
    status: stable

  - kind: fix_trunk_no_candidate
    effect_metric: self
    emitter: scripts/dispatch/fix-trunk-dispatcher.sh (INFRA-2324)
    # scanner-anchor: "kind":"fix_trunk_no_candidate"
    trigger: >
      Emitted when trunk is RED (within the lookback window) but the gap
      registry contains no open gap matching the configured skill tag
      (default fix_trunk). Signal to file a remediation gap or audit the
      sentinel's gap-filing path.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, lookback_minutes, skill_tag]
    expected_volume: "very low; only on misconfigured fleets"
    status: stable

  - kind: fix_trunk_skipped
    effect_metric: self
    emitter: scripts/dispatch/fix-trunk-dispatcher.sh (INFRA-2324)
    # scanner-anchor: "kind":"fix_trunk_skipped"
    trigger: >
      Emitted when a prior dispatch (signal or subprocess) is still
      reflected in the dispatcher lockfile and the PID is alive — the
      parallelism cap of 1 holds for the current trunk-red incident.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, reason, prior_pid, prior_gap_id]
    expected_volume: "low; one per skipped tick while incident is open"
    status: stable
  # ── INFRA-2069: wedge-fixer template library (META-118 sub-gap 3) ──────────
  - kind: wedge_fixer_template_rendered
    effect_metric: resilient
    emitter: scripts/coord/wedge-fixer-dispatch.sh (INFRA-2069)
    trigger: >
      Emitted by wedge-fixer-dispatch.sh when the operator runs with --execute
      (not --dry-run). Carries the template_name selected, the gap_id being fixed,
      and dry_run=false. Used by ambient consumers to track how often each template
      class is dispatched and whether the dispatch-to-ship ratio is healthy.
      Auto-dispatch (consume kind=wedge_class_detected → auto-render-and-dispatch)
      is deferred to the follow-up PR that ships once INFRA-2068 lands.
    consumers: [ops-audit, kpi-report, ci-audit]
    fields_required: [ts, kind, template_name, gap_id, dry_run]
    expected_volume: "0-N per session (operator-initiated only until INFRA-2068 ships)"
    status: stable
    # payload: {template_name: "fmt-drift"|"orphan-event"|"printf-grep", gap_id: string, dry_run: boolean, ts: string}
  - kind: required_status_checks_empty
    effect_metric: resilient
    emitter: scripts/coord/fleet-doctor-strict.sh check_required_status_checks (INFRA-2201)
    trigger: >
      Emitted when fleet-doctor's required_status_checks invariant fails —
      both branch-protection AND ruleset 15133729 have empty
      required_status_checks lists, meaning main is silently
      unprotected. Catches the silent-open-gate class that bit the
      shepherd 2026-05-29 22:30Z when admin-merge-cycle ran without
      restoring required checks.
    consumers: [fleet-doctor, ops-audit]
    fields_required: [ts, kind, bp_count, ruleset_count, source]
  # ── INFRA-2214: curator role-doc template — confidence calibration loop ──────
  - kind: curator_confidence_calibrated
    effect_metric: credible
    emitter: any curator-opus-* role (.claude/agents/*.md confidence calibration loop)
    trigger: >
      Emitted by any curator when a subsequent verification proves a prior
      finding or recommendation wrong (e.g. claimed a file was missing but
      it exists on main). The emitting curator drops its confidence tier by
      one for the rest of the session. Carries role, original_confidence,
      new_confidence, and reason so the operator can audit calibration
      accuracy across the curator fleet. Operationally proven useful on
      2026-05-29 when handoff curator false-positived on a file existence
      check and needed to calibrate down to 0.75.
    consumers: [ops-audit, fleet-brief, kpi-report]
    fields_required: [ts, kind, role, original_confidence, new_confidence, reason]
    expected_volume: "0-5 per curator session (rare; only on verified misfires)"
    status: stable
    # payload: {role: string, original_confidence: "high"|"med"|"low", new_confidence: "high"|"med"|"low", reason: string, ts: string}
  # ── INFRA-2258: chump voice — Voice-of-Agent VOA filing ──────────────────────
  - kind: voice_of_agent_filed
    effect_metric: effective
    emitter: src/commands/voice.rs (INFRA-2258 — chump voice subcommand)
    trigger: >
      Emitted by `chump voice` after successfully writing both
      docs/gaps/VOA-NNNN.yaml and docs/voice/VOA-NNNN-FULL.yaml.
      Carries voa_id, wedge_class, minutes_lost, fix_shape, and
      target_repo (always anonymized — "anonymous", "opt-in:slug", or
      "opt-in:full" — never the raw repo slug unless user has explicitly
      consented via ~/.chump/voice-opt-in.toml AND the --target-repo flag
      uses the opt-in: prefix). session field is the filing agent's
      CHUMP_SESSION_ID.
    consumers: [ops-audit, kpi-report, fleet-brief]
    fields_required: [ts, kind, voa_id, wedge_class, minutes_lost, fix_shape, target_repo, session]
    # payload: {voa_id: str, wedge_class: str, minutes_lost: int, fix_shape: str, target_repo: str, session: str}

  - kind: tech_debt_finding
    effect_metric: zero_waste
    emitter: src/inventory.rs insert_finding (META-271 / INFRA-2368 — inventory detectors)
    trigger: >
      Emitted by every inventory detector at the moment a finding is
      INSERTed into tech_debt_findings. ALWAYS lands at tier=0 in this
      PR's scope — never files a gap, never removes code. The operator
      promotes a finding_class from tier 0 → 2 via
      `chump inventory promote <class>` only after calibration via
      `chump inventory review` (≥10 reviewed AND ≥70% REAL_POSITIVE).
      Tier-2 auto-file machinery is deferred to INFRA-2374.
    consumers: [ops-audit, fleet-brief, inventory-cli]
    fields_required: [ts, kind, finding_id, finding_class, severity, tier, detail]
    # payload: {finding_id: int, finding_class: str, severity: str, tier: int (0|1|2),
    #           detail: str, artifact_path?: str, pr_number?: int, gap_id?: str}

  # ── INFRA-2397: main-preflight-watchdog daemon ───────────────────────────────
  - kind: main_preflight_red
    effect_metric: main_preflight_recovered_within_60min
    emitter: scripts/coord/main-preflight-watchdog-daemon.sh (INFRA-2397)
    # scanner-anchor: "kind":"main_preflight_red"
    trigger: >
      Emitted on every tick where `chump preflight` exits non-zero against a
      fresh origin/main worktree. Carries the sorted list of failing gate
      names, a sha256 fingerprint of that set (for dedup), the filed P0 gap
      ID (or UNFILED on reserve failure), and the current head SHA.
      Fingerprint dedup prevents re-filing a gap for the same failure set on
      subsequent ticks — a new gate appearing in the set produces a new
      fingerprint and a new gap.
    consumers: [fleet-brief, ops-audit, observability]
    fields_required: [ts, kind, tick_id, failing_gates, fingerprint, gap_id, head_sha, dry_run]
    expected_volume: "very low; should be zero in a healthy week"
    status: stable

  - kind: main_preflight_recovered
    effect_metric: self
    emitter: scripts/coord/main-preflight-watchdog-daemon.sh (INFRA-2397)
    # scanner-anchor: "kind":"main_preflight_recovered"
    trigger: >
      Emitted on RED->GREEN transition when `chump preflight` exits 0 on
      origin/main after a previous tick recorded RED state. The daemon walks
      all P0 gaps it auto-filed during the RED window (tracked in
      .chump/main-preflight-state.json) and closes each via
      `chump gap set --status done`. Records the previous fingerprint and
      list of closed gap IDs.
    consumers: [fleet-brief, ops-audit, observability]
    fields_required: [ts, kind, previous_fingerprint, head_sha, gaps_closed, closed_count, tick_id, dry_run]
    expected_volume: "very low; one per recovery event"
    status: stable

  - kind: main_preflight_disabled
    effect_metric: self
    emitter: scripts/coord/main-preflight-watchdog-daemon.sh (INFRA-2397)
    # scanner-anchor: "kind":"main_preflight_disabled"
    trigger: >
      Emitted at daemon startup when CHUMP_MAIN_PREFLIGHT_DISABLED is set to
      a non-empty value. The daemon exits 0 immediately after emitting this
      event — no preflight is run, no gaps are filed. Used for controlled
      maintenance windows or environments where the worktree approach is
      unavailable.
    consumers: [ops-audit]
    fields_required: [ts, kind, dry_run]
    expected_volume: "zero in normal operation; non-zero only when explicitly disabled"
    status: stable

  # ── INFRA-2278: ship-assist context hook ─────────────────────────────────────
  - kind: ship_assist_context_surfaced
    effect_metric: effective
    emitter: scripts/coord/ambient-context-inject.sh (INFRA-2278 SessionStart block)
    # scanner-anchor: "kind":"ship_assist_context_surfaced"
    trigger: >
      Emitted at SessionStart when the ship-assist context block renders the
      top-3 wedge classes from the last 7d VOA reports. Carries aggregate
      counts only (no per-VOA identifiers) to preserve privacy. Bypass:
      CHUMP_SHIP_ASSIST_HOOK=0.
    consumers: [ops-audit, kpi-report]
    fields_required: [ts, kind, voa_count_last_7d, top_class_count]
    expected_volume: "1 per SessionStart (when CHUMP_SHIP_ASSIST_HOOK != 0)"
    status: stable
    # payload: {voa_count_last_7d: int, top_class_count: int, ts: string}

  # ── INFRA-2366: agent-awareness sync ─────────────────────────────────────────
  - kind: inventory_agent_sync_finding
    effect_metric: self
    emitter: scripts/coord/inventory-agent-sync.sh (INFRA-2366)
    # scanner-anchor: "kind":"inventory_agent_sync_finding"
    trigger: >
      Emitted once per shipped artifact (last N days, default 30) that has
      introducing_pr set in artifact_index but whose path/basename/gap-ID
      appears in zero scanned agent docs (.claude/agents/*.md, docs/process/*.md,
      AGENTS.md, CLAUDE.md). Advisory only — does not gate CI.
      Bypass: pass --dry-run to suppress emission.
    consumers: [ops-audit, fleet-brief, quartermaster]
    fields_required: [ts, kind, path, introducing_pr, introducing_gap, last_modified_at, scanned_doc_count]
    # payload: {path: str, introducing_pr: str, introducing_gap: str,
    #           last_modified_at: int (epoch), scanned_doc_count: int}
    status: stable

  - kind: inventory_agent_sync_run
    effect_metric: self
    emitter: scripts/coord/inventory-agent-sync.sh (INFRA-2366)
    # scanner-anchor: "kind":"inventory_agent_sync_run"
    trigger: >
      Emitted once per invocation of inventory-agent-sync.sh (unless --dry-run).
      Summarizes the scan: total docs scanned, window in days, total artifacts
      examined, and count of unmentioned artifacts surfaced.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, scanned_doc_count, window_days, total_artifacts, unmentioned_count]
    # payload: {scanned_doc_count: int, window_days: int, total_artifacts: int, unmentioned_count: int}
    status: stable

  # ── INFRA-1121: A2A Layer 3d — shared KV scratchpad activation ──────────────
  - kind: scratchpad_set
    effect_metric: self
    emitter: crates/chump-coord/src/scratchpad.rs (INFRA-1121)
    # scanner-anchor: "kind":"scratchpad_set"
    trigger: >
      Emitted when a LastWriterWins seed key is successfully written via
      scratchpad::set(). Not emitted for CAS writes (those emit
      scratchpad_cas_conflict on failure; success is silent).
      Allows ops-audit to track fleet.size and pillar.focus update frequency.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, key, session_id]
    status: stable

  - kind: scratchpad_cas_conflict
    effect_metric: self
    emitter: crates/chump-coord/src/scratchpad.rs (INFRA-1121)
    # scanner-anchor: "kind":"scratchpad_cas_conflict"
    trigger: >
      Emitted when a CAS write fails because the current value does not match
      the expected value (ScratchError::CASConflict). Indicates a concurrent
      writer race; caller should retry from a fresh read. High frequency on a
      single key signals hot contention.
    consumers: [ops-audit, watchdog]
    fields_required: [ts, kind, key, session_id]
    status: stable

  - kind: scratchpad_injected
    effect_metric: effective
    emitter: src/briefing.rs collect_scratchpad_context (INFRA-1121)
    # scanner-anchor: "kind":"scratchpad_injected"
    trigger: >
      Emitted at session start when collect_scratchpad_context() successfully
      reads at least one prompt-injectable seed key and includes it in the
      chump --briefing output. key_count reflects how many keys were set at
      snapshot time. Measures adoption: once fleet workers write main.head.sha,
      key_count should be >=1 on every briefing call.
    consumers: [ops-audit, kpi-report]
    fields_required: [ts, kind, key_count, session_id]
    status: stable

  - kind: scratch_cache_hit
    effect_metric: effective
    emitter: src/commands (future callers of chump_coord::scratchpad::get)
    # scanner-anchor: "kind":"scratch_cache_hit"
    trigger: >
      Emitted when a caller retrieves a value from the scratchpad instead of
      making an external call (e.g. git ls-remote for main.head.sha). Measures
      AC #4: once this event rate exceeds the git-ls-remote call rate, the >80%
      reduction target is met. key field identifies which seed key was served
      from cache.
    consumers: [ops-audit, kpi-report]
    fields_required: [ts, kind, key, session_id]
    status: stable

  - kind: scratch_cache_miss
    effect_metric: effective
    emitter: src/commands (future callers of chump_coord::scratchpad::get)
    # scanner-anchor: "kind":"scratch_cache_miss"
    trigger: >
      Emitted when a caller requests a scratchpad key that is absent or expired,
      requiring a fallback to an external data source (e.g. git ls-remote).
      Paired with scratch_cache_hit to compute hit-rate telemetry. High miss
      rate for main.head.sha indicates the scratchpad writer has stopped
      updating the key.
    consumers: [ops-audit, kpi-report]
    fields_required: [ts, kind, key, session_id]
    status: stable

  # ── INFRA-2406: pre-merge cross-PR contract gate ───────────────────────────
  - kind: cross_pr_contract_mismatch
    effect_metric: cross_pr_contract_resolved_before_merge
    emitter: scripts/ci/test-cross-pr-contract.sh (INFRA-2406)
    # scanner-anchor: "kind":"cross_pr_contract_mismatch"
    trigger: >
      Emitted when chump contract-scan --in-flight detects a key mismatch
      between a writer PR and a reader PR on a shared IPC surface (state file,
      ambient event payload, or Rust struct serialized to disk). Root cause
      (INFRA-2404): watchdog (INFRA-2397) wrote {state, updated_at, last_tick_id}
      while claim-gate (INFRA-2398) read {last_status, last_tick_at, failing_gates}
      — keys never matched; procedure layer shipped silently inert.
      This event fires in the audit-shard CI gate; bot-merge.sh refuses to arm
      auto-merge for any PR in the affected writer_pr/reader_pr pair until the
      mismatch is resolved or added to cross-pr-allowlist.txt.
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, writer_pr, reader_pr, missing_keys, gap]
    # payload: {writer_pr: str (path or PR#), reader_pr: str (path or PR#),
    #           missing_keys: str (comma-separated keys reader expects but writer omits),
    #           gap: "INFRA-2406"}
  - kind: worktree_build_cache_provisioned
    effect_metric: self
    emitter: src/worktree_build_cache.rs emit_provisioned (INFRA-2183)
    # scanner-anchor: "kind":"worktree_build_cache_provisioned"
    trigger: >
      Emitted by chump claim after successfully writing a per-worktree
      .cargo/config.toml that wires sccache (rustc-wrapper) and sets an
      isolated CARGO_TARGET_DIR so parallel Sonnet workers do not collide on
      shared build artefacts.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap_id, sccache_path, target_dir]
    # payload: {gap_id: str, sccache_path: str, target_dir: str}
    status: stable

  - kind: worktree_build_cache_skip
    effect_metric: self
    emitter: src/worktree_build_cache.rs emit_skip (INFRA-2183)
    # scanner-anchor: "kind":"worktree_build_cache_skip"
    trigger: >
      Emitted by chump claim when sccache wiring cannot be provisioned
      (host .cargo/config.toml missing, sccache binary absent, I/O error,
      or CHUMP_WORKTREE_BUILD_CACHE_DISABLED=1 override). Claim continues — a
      cold build is preferable to a failed claim.
    consumers: [fleet-brief, ops-audit]
    fields_required: [ts, kind, gap_id, reason]
    # payload: {gap_id: str, reason: str}
    status: stable

  # ── INFRA-2417: daemon-exit-loop-watcher ─────────────────────────────────
  # NOTE: these 3 kinds were dropped by a merge-conflict resolution in
  # INFRA-1121 (commit 25d4219af). Restored by INFRA-2496.
  - kind: daemon_exit_loop_detected
    effect_metric: daemon_exit_loop_recovered_within_45min
    emitter: scripts/coord/daemon-exit-loop-watcher-daemon.sh (INFRA-2417)
    # scanner-anchor: "kind":"daemon_exit_loop_detected"
    trigger: >
      Emitted when a launchd daemon label has consecutive non-zero exits
      >= CHUMP_DAEMON_EXIT_LOOP_THRESHOLD (default 3) as reported by
      `launchctl print gui/<uid>/<label>`. Root cause: com.chump.integrator-daemon
      crash-looped 145 times over 37+ hours (2026-06-02) with zero alerts.
      This daemon closes that observation gap; worst-case detection latency is
      3 × 15min = 45min. A P0 gap is auto-filed (idempotent via fingerprint =
      sha256(label) first 12 chars). The `dedup` field is true on subsequent
      ticks for the same label (no new gap filed).
    consumers: [ops-audit, fleet-brief, operator-recall]
    fields_required: [ts, kind, label, fingerprint, gap_id, runs, last_exit_code, threshold]
    # payload: {label: str, fingerprint: str, gap_id: str, runs: int,
    #           last_exit_code: int, threshold: int, tick_id: str, dedup?: bool}
    status: stable

  - kind: daemon_exit_loop_recovered
    effect_metric: self
    emitter: scripts/coord/daemon-exit-loop-watcher-daemon.sh (INFRA-2417)
    # scanner-anchor: "kind":"daemon_exit_loop_recovered"
    trigger: >
      Emitted when a daemon previously in LOOP_DETECTED state (last_exit_code != 0)
      returns to last_exit_code = 0. The auto-filed P0 gap is closed automatically
      via `chump gap set <id> --status done`. Signals operator that the daemon
      is healthy again without manual gap management.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, label, fingerprint, gap_id, runs, last_exit_code]
    # payload: {label: str, fingerprint: str, gap_id: str, runs: int,
    #           last_exit_code: int (=0), tick_id: str}
    status: stable

  - kind: daemon_exit_loop_disabled
    effect_metric: self
    emitter: scripts/coord/daemon-exit-loop-watcher-daemon.sh (INFRA-2417)
    # scanner-anchor: "kind":"daemon_exit_loop_disabled"
    trigger: >
      Emitted when CHUMP_DAEMON_EXIT_LOOP_DISABLED is set to a non-empty value.
      The daemon exits 0 immediately without querying any labels or filing gaps.
      Audit trail only — allows operator to temporarily suppress the watcher
      without leaving a silent gap in observability history.
    consumers: [ops-audit]
    fields_required: [ts, kind]
    # payload: {}
    status: stable

  # ── INFRA-2629: last-mile rescuer daemon ──────────────────────────────────
  # ── sub_agent_completed: Sonnet sub-agent completion signal ─────────────────
  # Natural pair for kind=sub_agent_dispatched (INFRA-1922). Consumed by
  # last-mile-rescuer (INFRA-2629) trigger3 to determine whether a dispatched
  # sub-agent has finished. Emitted by any curator that dispatches a Sonnet
  # sub-agent and receives its result (Claude Code Agent tool return, or
  # harness-neutral completion wrapper). Filed here because the last-mile
  # rescuer's stall-detection references it as a "completion signal" — the
  # registry guard requires registration of any kind literal that appears in
  # staged diffs.
  - kind: sub_agent_completed
    effect_metric: self
    emitter: >
      any curator role that dispatches a Sonnet sub-agent via the Agent tool
      (Claude Code) or harness-neutral equivalent; expected from
      scripts/coord/handoff-loop.sh and fleet worker completion paths.
    # scanner-anchor: "kind":"sub_agent_completed"
    trigger: >
      A Sonnet sub-agent dispatched by an Opus curator (any role) has
      completed its task — either shipped a PR or exited cleanly. Emitted
      by the dispatching curator when it receives the sub-agent result.
      Paired with kind=sub_agent_dispatched by gap_id / session fields.
      Consumed by last-mile-rescuer (INFRA-2629) trigger3 to suppress stall
      alerts for gaps where the sub-agent already completed successfully.
    consumers: [ops-audit, fleet-brief, last-mile-rescuer]
    fields_required: [ts, kind, session, gap, role]
    # payload: {gap: str, role: str, session: str}
    status: stable

  # ── INFRA-2629: last-mile rescuer daemon ──────────────────────────────────
  - kind: last_mile_rescue_triggered
    effect_metric: resilient
    emitter: scripts/coord/last-mile-rescuer.sh (INFRA-2629)
    # scanner-anchor: "kind":"last_mile_rescue_triggered"
    trigger: >
      The last-mile rescuer daemon has identified a committed-but-never-pushed
      claim branch and is about to attempt a rescue. Fired by either of two
      detection paths: (1) consuming a kind=orphan_worktree_detected event from
      ambient.jsonl within the last CHUMP_LAST_MILE_ORPHAN_WINDOW_S seconds
      (default 600), or (2) scanning local claim branches for commits ahead of
      origin/main with no open PR. Emitted before the rebase+push attempt so the
      operator can see intent even if the push fails. In CHUMP_LAST_MILE_DRY_RUN=1
      mode this is the only rescue event emitted — no actual push occurs.
      Motivating incident 2026-06-03: INFRA-2458 + INFRA-2484 commits sat
      unpushed for hours; rescued manually when orchestrator returned.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, session, branch, worktree_path, gap_id, source, commits_ahead, dry_run]
    # payload: {branch: str, worktree_path: str, gap_id: str|null,
    #           source: "trigger1_orphan_event"|"trigger2_branch_scan",
    #           commits_ahead: int, dry_run: 0|1}
    status: stable

  - kind: last_mile_rescue_completed
    effect_metric: resilient
    emitter: scripts/coord/last-mile-rescuer.sh (INFRA-2629)
    # scanner-anchor: "kind":"last_mile_rescue_completed"
    trigger: >
      The last-mile rescuer daemon successfully pushed a rescued claim branch
      and armed auto-merge via bot-merge.sh. Emitted after a confirmed push —
      the PR is open and auto-merge is armed. Pairs with a prior
      last_mile_rescue_triggered event on the same branch.
      Not emitted in CHUMP_LAST_MILE_DRY_RUN=1 mode (dry-run stops at triggered).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, session, branch, worktree_path, gap_id, commits_ahead, source]
    # payload: {branch: str, worktree_path: str, gap_id: str|null,
    #           commits_ahead: int, source: str}
    status: stable

  - kind: last_mile_rescue_failed
    effect_metric: resilient
    emitter: scripts/coord/last-mile-rescuer.sh (INFRA-2629)
    # scanner-anchor: "kind":"last_mile_rescue_failed"
    trigger: >
      The last-mile rescuer attempted to rescue a claim branch but could not
      complete the push. Two failure modes: (1) rebase_conflict — git rebase
      onto origin/main produced a merge conflict; the rebase is aborted and the
      branch is left as-is for operator inspection. (2) push_failed — bot-merge.sh
      or the bare git push returned a non-zero exit code (e.g. force-push refused,
      GitHub API error). In both cases the branch is NOT modified beyond the
      aborted rebase. Operator action: inspect the branch and resolve manually.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, session, branch, gap_id, reason, source]
    # payload: {branch: str, gap_id: str|null,
    #           reason: "rebase_conflict"|"push_failed",
    #           exit_code?: int, source: str}
    status: stable

  # ── RESILIENT-068: dev.chump.farmer events ───────────────────────────────

  - kind: farmer_heartbeat
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_heartbeat"
    trigger: >
      Emitted at the end of every successful farmer tick (60s cadence). Absence
      of this event for >120s indicates the farmer itself is stalled — the
      external watchdog (RESILIENT-071) uses heartbeat staleness to page the
      operator.
    consumers: [ops-audit, fleet-brief, farmer-watchdog]
    fields_required: [ts, kind]
    # payload: {dry_run: bool}
    status: stable

  - kind: farmer_pause_lifted
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_pause_lifted"
    trigger: >
      The farmer detected .chump/fleet-paused and confirmed that chump health
      --slo-check now passes (via sqlite3 read-only ghost-gap count). Sentinel
      removed; control-plane daemons kicked back to life. Two sub-reasons:
      slo_recovered (fresh sentinel, SLO just passed) or stale_sentinel_slo_pass
      (sentinel >15min old when SLO passed).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, reason, age_s]
    # payload: {reason: "slo_recovered"|"stale_sentinel_slo_pass", age_s: int}
    status: stable

  - kind: farmer_daemon_kicked
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_daemon_kicked"
    trigger: >
      The farmer issued a launchctl kickstart -k for a control-plane daemon that
      had a non-zero exit code (exit-78 pause-blocked, exit-127 binary-missing,
      or other crash). Emitted once per kick action.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, label]
    # payload: {label: str}
    status: stable

  - kind: farmer_auth_dead
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_auth_dead"
    trigger: >
      The farmer detected that ~/.chump/oauth-token.json is absent or older than
      FARMER_OAUTH_MAX_AGE_S (default 3600s). Also emits operator_recall with
      class=halt so the external watchdog pages the operator.
    consumers: [ops-audit, fleet-brief, operator-recall-handler]
    fields_required: [ts, kind]
    # payload: {reason?: "token_file_absent", token_age_s?: int}
    status: stable

  - kind: farmer_silent_worker
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_silent_worker"
    trigger: >
      A claim lease file in .chump-locks/ has not been touched for
      FARMER_SILENT_WORKER_S (default 1800s) and the session has no recent
      ambient event. The farmer kicks com.chump.reap-stale-leases to release
      the orphaned lease.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, session, lease_age_s, ambient_age_s]
    # payload: {session: str, lease_age_s: int, ambient_age_s: int}
    status: stable

  - kind: farmer_escalated
    effect_metric: resilient
    emitter: scripts/coord/farmer.sh (RESILIENT-068)
    # scanner-anchor: "kind":"farmer_escalated"
    trigger: >
      A control-plane daemon has been kicked >= FARMER_KICK_ESCALATE_N (default 3)
      times within FARMER_KICK_WINDOW_S (default 600s) and is still not healthy.
      The farmer stops kicking and emits operator_recall with class=halt
      (RESILIENT-058 restart-intensity threshold). Only emitted once per daemon
      per escalation window (marked in .chump/farmer-kick-state.json).
    consumers: [ops-audit, fleet-brief, operator-recall-handler]
    fields_required: [ts, kind, label, exit_code]
    # payload: {label: str, exit_code: int}
    status: stable

  - kind: last_mile_agent_stall_detected
    effect_metric: resilient
    emitter: scripts/coord/last-mile-rescuer.sh (INFRA-2629)
    # scanner-anchor: "kind":"last_mile_agent_stall_detected"
    trigger: >
      A kind=sub_agent_dispatched event in ambient.jsonl is older than
      CHUMP_LAST_MILE_AGENT_STALL_S (default 1800s = 30 min) and has no paired
      kind=sub_agent_completed event AND no open PR on the expected claim branch
      (chump/<gap-id-lowercase>-claim). This is a notification-only signal —
      the rescuer does NOT auto-push for stalled sub-agent dispatches; the
      operator or orchestrator must inspect and decide whether to re-dispatch
      or rescue manually. Near-miss incident 2026-06-03: RESILIENT-058 Sonnet
      committed bd5457680, lease expired before push completed; orchestrator
      caught it and rebased+pushed, only to find Sonnet had already finished.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, session, gap_id, agent_id, dispatched_at, elapsed_s]
    # payload: {gap_id: str, agent_id: str, dispatched_at: ISO8601,
    #           elapsed_s: int}
    status: stable

  # ── RESILIENT-059: durable gap execution journal ───────────────────────────

  - kind: durable_journal_step_started
    effect_metric: self
    emitter: src/commands/durable_execution_journal.rs (RESILIENT-059)
    # scanner-anchor: "kind":"durable_journal_step_started"
    trigger: >
      A DurableExecutor begins executing a named activity step for a gap run.
      Fired by Journal::start_step before the activity closure is invoked.
      Enables tracing which steps were in-flight at the moment of a crash.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, run_id, step_name, step_index]
    # payload: {gap_id: str, run_id: int, step_name: str, step_index: int}
    status: stable

  - kind: durable_journal_step_completed
    effect_metric: self
    emitter: src/commands/durable_execution_journal.rs (RESILIENT-059)
    # scanner-anchor: "kind":"durable_journal_step_completed"
    trigger: >
      A DurableExecutor activity step completed successfully and its result has
      been persisted to durable_journal. Fired by Journal::complete_step.
      elapsed_ms is the wall-clock time from started_at to completed_at.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, gap_id, run_id, step_name, step_index, elapsed_ms]
    # payload: {gap_id: str, run_id: int, step_name: str, step_index: int,
    #           elapsed_ms: int}
    status: stable

  - kind: durable_journal_resumed
    effect_metric: resilient
    emitter: src/commands/durable_execution_journal.rs (RESILIENT-059)
    # scanner-anchor: "kind":"durable_journal_resumed"
    trigger: >
      A DurableExecutor::resume() call found a prior incomplete run and is
      reattaching to it rather than starting from scratch. replayed_steps is the
      count of already-completed steps whose results will be served from cache
      without re-executing. Fires once per resume, before the first activity call.
      Absence of this event when a crash is known to have occurred indicates the
      gap was re-claimed from zero (the failure mode RESILIENT-059 prevents).
    consumers: [ops-audit, fleet-brief, waste-tally]
    fields_required: [ts, kind, gap_id, prior_run_id, replayed_steps]
    # payload: {gap_id: str, prior_run_id: int, replayed_steps: int}
    status: stable

  # ── INFRA-2265: chump bootstrap net-new entrypoint ─────────────────────────

  - kind: bootstrap_initiated
    effect_metric: effective
    emitter: src/commands/bootstrap.rs (INFRA-2265)
    # scanner-anchor: "kind":"bootstrap_initiated"
    trigger: >
      `chump bootstrap <intent>` begins execution. Fires immediately after the
      CLI args are parsed and before any filesystem mutation. Fields: intent
      (the product intent string), target_dir (absolute path), session_id.
      Absence of this event when bootstrap is expected indicates the CLI
      was not reached (arg-parse failure or binary not in PATH).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, intent, target_dir, session_id]
    # payload: {intent: str, target_dir: str, session_id: str}
    status: stable

  - kind: bootstrap_completed
    effect_metric: effective
    emitter: src/commands/bootstrap.rs (INFRA-2265)
    # scanner-anchor: "kind":"bootstrap_completed"
    trigger: >
      `chump bootstrap <intent>` completed successfully: git repo initialized,
      scaffold written, first commit made, and umbrella gap reserved. Fields:
      intent, target_dir, gap_ids (comma-separated gap IDs reserved, may be
      empty if gap reserve was not available), duration_ms, files_created
      (comma-separated list of files written).
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, intent, target_dir, gap_ids, duration_ms, files_created]
    # payload: {intent: str, target_dir: str, gap_ids: str, duration_ms: int,
    #           files_created: str}
    status: stable

  - kind: bootstrap_failed
    effect_metric: effective
    emitter: src/commands/bootstrap.rs (INFRA-2265)
    # scanner-anchor: "kind":"bootstrap_failed"
    trigger: >
      `chump bootstrap <intent>` failed mid-flight. failure_class is one of:
      arch_decision_timeout (LLM not wired, exit 2),
      scaffolding_write_failed (filesystem error writing scaffold or README),
      gap_reserve_failed (chump gap reserve returned non-zero),
      git_init_failed (git init or first commit failed).
      Best-effort cleanup of created files is attempted before this event fires.
      Fields: failure_class, intent, target_dir.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, failure_class, intent, target_dir]
    # payload: {failure_class: str, intent: str, target_dir: str}
  - kind: sccache_hit_rate_low
    effect_metric: credible
    emitter: scripts/ci/check-sccache-hit-rate.sh (CREDIBLE-085)
    # scanner-anchor: "kind":"sccache_hit_rate_low"
    trigger: >
      A CI build measured sccache cache hit rate below the threshold (default
      10%). Strong signal of R2 substrate health regression — credentials
      rotated, bucket unreachable, or sccache server misconfigured. Fires from
      the post-build gate after cargo-test / clippy / check jobs. measured_pct
      is the actual rate the gate observed; threshold_pct is the configured
      minimum (CHUMP_SCCACHE_HIT_RATE_MIN). commit is GITHUB_SHA, job is
      GITHUB_JOB. Cold-cache builds legitimately fire this — operator can
      bypass with CHUMP_SCCACHE_HIT_RATE_CHECK=0.
    consumers: [ops-audit, fleet-brief, substrate-health]
    fields_required: [ts, kind, measured_pct, threshold_pct, commit, job]
    # payload: {measured_pct: str, threshold_pct: str, commit: str, job: str}
    status: stable

  - kind: sccache_hit_rate_check_bypassed
    effect_metric: credible
    emitter: scripts/ci/check-sccache-hit-rate.sh (CREDIBLE-085)
    # scanner-anchor: "kind":"sccache_hit_rate_check_bypassed"
    trigger: >
      The sccache hit-rate gate was skipped via CHUMP_SCCACHE_HIT_RATE_CHECK=0.
      Audited so the operator can see how often the bypass fires — frequent
      use is a signal the threshold needs tuning or the cache is structurally
      cold.
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, reason]
    # payload: {reason: str}
    status: stable

  # ── INFRA-2275: external-repo plist installer (BEAST overnight loop slice 1/3) ─

  - kind: external_repo_scheduled
    effect_metric: effective
    emitter: src/onboard.rs (INFRA-2275)
    # scanner-anchor: "kind":"external_repo_scheduled"
    trigger: chump onboard --schedule loaded a per-repo launchd plist
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, repo_path, plist_label]
    status: stable

  - kind: external_repo_unscheduled
    effect_metric: effective
    emitter: src/onboard.rs (INFRA-2275)
    # scanner-anchor: "kind":"external_repo_unscheduled"
    trigger: chump onboard --unschedule unloaded a per-repo launchd plist
    consumers: [ops-audit, fleet-brief]
    fields_required: [ts, kind, repo_path, reason]
  - kind: ci_triage_verdict
    effect_metric: credible
    emitter: scripts/ci/triage-cargo-test-failure.sh (CREDIBLE-013)
    # scanner-anchor: "kind":"ci_triage_verdict"
    trigger: >
      cargo-test step failed; triage script classified failures and emitted
      verdict to GitHub job summary + ambient. Verdict is one of: flake (all
      failures are in KNOWN_FLAKES.yaml), real (genuine regression, fix needed),
      known-bug (matches open tracked gap), unknown (empty/unparseable input).
      Fires from the CI failure triage step added to the cargo-test job.
    consumers: [fleet-brief, kpi-report]
    fields_required: [ts, kind, verdict, failed_test_count, commit, job]
    # payload: {verdict: str, failed_test_count: int, flake_test_count: int,
    #           real_test_count: int, commit: str, job: str, pr_number: str}
    status: stable
