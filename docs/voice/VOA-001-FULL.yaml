# VOA-001 — Voice of the Agent, full report.
# Schema-defining example for the Voice-of-Agent protocol (see docs/process/VOICE_OF_AGENT.md).
# The lightweight gap-registry entry is at docs/gaps/VOA-001.yaml; this file is the full report.
#
# Filed retroactively as a test case after a 17-cycle dogfood session uncovered
# 7 distinct wedge classes. Future VOA reports will be auto-written by
# `chump voice` (INFRA-2258); this one is hand-written to define the shape.

id: VOA-001
filed_at: "2026-05-29T05:00:00Z"

reporter:
  agent_role: curator-opus-shepherd-2026-05-29
  chump_version: "post-PR-2774-pre-PR-2796"  # mid-session-evolving; commits a3c5c370f → in-flight
  target_repo: "repairman29/chump"  # SELF-target — this is a dogfood session
  target_repo_disclosure: opt-in:full  # operator explicitly authorized full disclosure
  session_id: "sha256:7df0d8...(elided in full disclosure for brevity)"
  session_duration_minutes: 420  # 17 cycles × ~10min avg + operator-direct extensions
  prs_shipped_this_session: 7  # before VOA filing: #2774, #2782, #2784, #2787, #2793, #2795, #2796 (ARMED), #2809 (ARMED)

wedge_observations:

  - wedge_class: fmt-drift-queue-wide
    minutes_lost: 30
    reproducibility: deterministic
    workaround_used:
      summary: "Reserved INFRA-2216, ran cargo fmt --all on clean worktree, shipped PR #2782"
      pr_number: 2782
    proposed_fix:
      shape: gate
      fix: "chump preflight (INFRA-1670 shipped) already runs cargo fmt --check — but parity gap between preflight and CI allowed the drift to land. INFRA-2120 preflight-vs-CI parity smoke is the structural fix."
      existing_gap: INFRA-2120
    impact_estimate_minutes_per_session: 25
    evidence:
      - "PR #2769 fast-checks job 78610314224 — Diff in crates/chump-integrator/tests/test_cycle_dry_run.rs:71"
      - "Pattern 14 verification: 22 ARMED-blocked PRs at 21m all showed identical fast-checks failure shape"

  - wedge_class: raw-gh-allowlist-miss
    minutes_lost: 25
    reproducibility: deterministic
    workaround_used:
      summary: "Reserved INFRA-2218, one-line append to scripts/ci/raw-gh-allowlist.txt with INFRA-2218 migration ref, shipped PR #2784"
      pr_number: 2784
    proposed_fix:
      shape: gate
      fix: "When INFRA-1274 raw-gh-lint gate flags a new violator, file a follow-up allowlist gap automatically; pair with the gate so the next PR adding the script also adds the allowlist entry"
      proposed_gap: new — file as INFRA-VOA-followup-A
    impact_estimate_minutes_per_session: 20
    evidence:
      - "audit job 78610605880 step 'raw-gh lint gate' failed on backfill-shipped-gaps.sh:87"
      - "PR #2710 (INFRA-2121) added the script without filing allowlist follow-up — every audit job since has failed silently"

  - wedge_class: sccache-R2-pair-mismatch
    minutes_lost: 90
    reproducibility: deterministic
    workaround_used:
      summary: "Manual atomic GH-secret update with new R2 token pair (operator-side regen via CF dashboard, then `gh secret set` both halves back-to-back in one shell)"
      pr_numbers: [2793, 2795]
    proposed_fix:
      shape: new-subcommand
      fix: "scripts/ops/rotate-sccache-r2-gh-only.sh — atomic both-halves update with length validation + audit fingerprints. CF-API automation backup at scripts/ops/rotate-sccache-r2-token.sh. Both shipped this session."
      existing_gaps: [INFRA-2237, INFRA-2240, INFRA-2127]
    impact_estimate_minutes_per_session: 90
    evidence:
      - "cargo-test job 78617364204 — sccache: error: Server startup failed: ... S3Error code: Unauthorized"
      - "gh secret list timestamps: R2_ACCESS_KEY_ID 2026-05-29T12:25Z, R2_SECRET_ACCESS_KEY 2026-05-28T02:46Z (2 days stale → half-rotation pair mismatch)"

  - wedge_class: bot-merge-silent-wedge
    minutes_lost: 60  # 3 occurrences × ~20 min each
    reproducibility: flaky  # 3/N over the session; not 100%
    workaround_used:
      summary: "SIGTERM bot-merge processes, manual recovery: gh push + create + merge --auto + rm lease file. Bypass-Trailer: Bot-Merge-Bypass + Test-Gate-Bypass"
      occurrences: ["cycle 5 INFRA-2216", "cycle 6 INFRA-2218", "cycle 13 INFRA-2071"]
    proposed_fix:
      shape: tooling
      fix: "Wall-clock progress monitor with auto-bail to manual fallback after 5 min no-output. Process state inspection: lsof TCP/FIFO; abort if no network activity for 60s"
      existing_gap: INFRA-1399  # filed pre-today, not yet shipped
    impact_estimate_minutes_per_session: 60
    evidence:
      - "Cycle 5: bot-merge.sh PID 17340 in S state, no stdout/stderr output for 4:26m"
      - "Documented in SHIP_ASSIST_PLAYBOOK.md §1 Class 4 + this VOA"

  - wedge_class: sonnet-mid-task-stall
    minutes_lost: 90  # full implementation + my recovery attempt that also wiped state
    reproducibility: one-off  # 1 of 5 dispatches this session
    workaround_used:
      summary: "Cycle 13: Sonnet stalled at 7/8 smoke tests on a test-design bug. My recovery attempt used `chump claim --force-recover` which wiped Sonnet's uncommitted work. Cycle 17 re-dispatched with explicit recovery instructions — second Sonnet shipped 19/19 PASS via PR #2807."
      stalled_session: "claim-infra-2071-87893-1780108520"
      recovery_pr: 2807
    proposed_fix:
      shape: tooling
      fix: "(a) Sub-agent should commit-as-WIP every N minutes so a stall doesn't lose work. (b) chump claim --force-recover should refuse when target worktree has uncommitted changes (filed as INFRA-2235)."
      existing_gap: INFRA-2235
    impact_estimate_minutes_per_session: 60  # average expected per-stall after fix
    evidence:
      - "INFRA-2071 notes contain the cycle 13 + cycle 17 recovery chain"
      - "PR #2807 from cycle 17 was an EXACT re-execution of cycle 13's intent, succeeded with the recovery plan written into the dispatch prompt"

  - wedge_class: claim-force-recover-wip-loss
    minutes_lost: 90  # paired with the Sonnet stall above — same incident
    reproducibility: deterministic  # any --force-recover with uncommitted state will wipe
    workaround_used:
      summary: "Filed INFRA-2235 as the systemic fix; not a per-incident workaround"
      pr_number: null  # not shipped yet
    proposed_fix:
      shape: tooling
      fix: "Detect uncommitted changes in target worktree before --force-recover wipes it; refuse by default; offer --discard-wip flag for legitimate destroy; audit emit kind=force_recover_wip_loss with count + paths"
      existing_gap: INFRA-2235
    impact_estimate_minutes_per_session: 30  # how often --force-recover gets reached for
    evidence:
      - "Cycle 13 incident; Sonnet's 90% impl + my 2 test fixes lost"

  - wedge_class: gap-status-auto-flip-silent-noop
    minutes_lost: 25  # 5 batch-flips × ~5 min Pattern-14 + manual flip per
    reproducibility: deterministic  # specific to PR/gap pairs that don't trigger INFRA-2121
    workaround_used:
      summary: "Manual ship-flip with bypass envs: CHUMP_GAP_SHIP_SKIP_STALE_CHECK=1 CHUMP_BYPASS_PROOF_OF_MERGE=1 chump gap ship X --closed-pr N --update-yaml"
      cycles: ["cycle 7 INFRA-2208", "cycle 10 INFRA-2202/2200/2151/DOC-058/2184"]
    proposed_fix:
      shape: tooling
      fix: "(a) Diagnose why INFRA-2121 auto-flip-on-merge silently misses certain PR/gap pairs. (b) Add nightly catch-up scan that flips any merged-but-still-open gap."
      existing_gap: INFRA-2230
    impact_estimate_minutes_per_session: 15
    evidence:
      - "6 gaps observed in this state across cycles 7 + 10"
      - "INFRA-2230 captures the systemic fix"

session_totals:
  total_minutes_lost_to_chump_friction: 410  # sum of above
  approximate_session_throughput_minus_friction: "would-be 7 PRs in ~50% of the time"
  voa_filing_overhead_minutes: 30  # this report itself

proposed_fixes_summary:
  immediate_shippable:
    - "INFRA-2237 (rotate-sccache-r2-token.sh CF-API) — SHIPPED this session, PR #2793"
    - "INFRA-2240 (rotate-sccache-r2-gh-only.sh primary) — SHIPPED this session, PR #2795"
    - "INFRA-2256 (SHIP_ASSIST_PLAYBOOK) — SHIPPED this session, PR #2809"
  pending_dispatch:
    - "INFRA-2071 (admin-merge circuit-breaker) — SHIPPED via Sonnet cycle 17, PR #2807"
    - "INFRA-2251 (local CI gate, P0) — Sonnet dispatched this session"
    - "INFRA-2069 (wedge-fixer template library) — Sonnet dispatched this session"
    - "INFRA-1713 (chump paramedic CI_FAILURE) — Sonnet dispatched but appears confused, will follow up"
  filed_but_not_dispatched:
    - "INFRA-1399 (bot-merge wall-clock monitor)"
    - "INFRA-2230 (gap-status auto-flip drift diagnosis)"
    - "INFRA-2235 (force-recover WIP-loss safety)"
    - "INFRA-2068/2070/2075 (META-118 chain remainders)"

cross_reference:
  ship_assist_playbook: "docs/process/SHIP_ASSIST_PLAYBOOK.md (PR #2809)"
  offline_first_roadmap: "docs/strategy/OFFLINE_ROADMAP_2026Q2.md (PR #2796)"
  mission_layer_interface: "docs/design/MISSION_LAYER_INTERFACE.md (PR #2796)"
  shepherd_loop_playbook: "docs/process/SHEPHERD_LOOP_PLAYBOOK.md (Pattern 14, Pattern 15)"
  subagent_dispatch: "docs/process/SUBAGENT_DISPATCH.md (META-069)"

notes: |
  This is the first VOA filing. The shape was operator-approved during the
  filing session; future VOA reports will be auto-written by `chump voice`
  (INFRA-2258) and aggregated by `chump voice top` + the WEDGE_HEATMAP cron
  (INFRA-2260). VOA-001 lives as a schema-defining example + a single-session
  retro that documents 7 wedge classes worth of friction in one place.

  Reporter identity: this VOA self-discloses full attribution because the
  reporter (curator-opus-shepherd-2026-05-29) operated on a self-target
  (repairman29/chump) during a dogfood session. Future VOAs from agents
  operating on external repos will default to target_repo_disclosure=anonymous
  unless the user opts in.
