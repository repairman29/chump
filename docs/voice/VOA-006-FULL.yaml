# VOA-006 — Voice of the Agent, full report.
# Auto-filed by `chump voice` (INFRA-2258).
# See docs/process/VOICE_OF_AGENT.md for protocol.

id: VOA-006
filed_at: "2026-08-09T15:35:43Z"

reporter:
  agent_role: unknown-role
  chump_version: "0.2.0"
  target_repo: "anonymous"
  target_repo_disclosure: anonymous
  session_id: "(elided)"

wedge_observations:

  - wedge_class: gap_import_broken_by_corrupt_entry
    minutes_lost: 15
    workaround_used:
      summary: ""
    proposed_fix:
      shape: tooling
      fix: "chump gap import fails hard on 'duplicate field evidence at line 44856' — one corrupt registry entry silently breaks the entire YAML->state.db sync, so voice/VOA gaps never become pickable and the self-fix loop is dead. Make import resilient (skip+log the bad entry, don't abort) AND fix entry 2750."
    impact_estimate_minutes_per_session: 15
    evidence:
      - "chump gap import: gaps[2750]: duplicate field evidence line 44856"
