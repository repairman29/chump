---
doc_tag: archive
last_audited: 2026-05-12
---

# Archived strategy docs — 2026-04 cycle

These three docs were active during the 2026-04 strategic planning cycle but are deferred from the current 30-day execution roadmap. They are preserved here (not deleted) so the strategic thinking is recoverable if a counterparty surfaces.

## Why archived

As of 2026-05-12, neither strand has a named sponsor or scheduled engagement:

- **`HIGH_ASSURANCE_AGENT_PHASES.md`** — defense / enterprise eval-aligned work packages. Required a counterparty (named pilot customer, sponsor org, or scheduled demo) to be load-bearing on the roadmap. None active.
- **`PRODUCT_ROADMAP_CHIEF_OF_STAFF.md`** — Chief of Staff (COS) autonomous staff + product factory concept (60 user stories, 4 waves). Internal product theory; no external pull.
- **`EXTERNAL_PLAN_ALIGNMENT.md`** — alignment doc that mapped the above two strands against the active roadmap. Without active strands, no alignment to maintain.

## Reactivation criteria

Move a doc back to `docs/strategy/` if:
- A named external counterparty surfaces (pilot customer, sponsor, partner) and asks for the work
- An internal experiment shows the concept needs to be the next thing built (rather than a backlog item)
- The operator explicitly wants to resume the strand (note it in `docs/strategy/ROADMAP.md` and reactivate)

Do **not** quietly reactivate by editing the archived file in place — move it back to `docs/strategy/` with an explicit commit message naming the trigger.

## Read history, not direction

Bots planning current work should NOT treat these docs as authoritative for the current 30-day roadmap (see `docs/ROADMAP.md`). Read them for context only.
