# Portable Cursor skill: Chump UI week (optional)

**In-repo copies (copy to `~/.cursor/skills/` if you want the same skill globally):**

- Rule: [`.cursor/rules/chump-ui-week-dogfood.mdc`](../.cursor/rules/chump-ui-week-dogfood.mdc)
- Skill: [`.cursor/skills/chump-ui-week/SKILL.md`](../.cursor/skills/chump-ui-week/SKILL.md)

To reuse the discipline **outside** this clone:

1. Copy `SKILL.md` to `~/.cursor/skills/chump-ui-week/SKILL.md` (or merge into your global skill set).
2. Keep **When to use**, **Must read** (`docs/UI_WEEK_SMOKE_PROMPTS.md`, `docs/TAURI_FRONTEND_PLAN.md`, `docs/OPERATIONS.md`), and **Rules** (one regression note per day; no drive-by refactors; `cargo check --bin chump` + `cargo check -p chump-desktop` after web/desktop changes).
