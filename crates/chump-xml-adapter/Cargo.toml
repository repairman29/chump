[package]
name = "chump-xml-adapter"
version = "0.1.0"
edition = "2021"
description = "XML-to-native tool-call adapter for models that emit <tool_call> tags"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
# no extra deps for tests
