[package]
name = "chump-mcp-gaps"
version = "0.1.0"
edition = "2021"
description = "MCP server: Chump gap registry queries via JSON-RPC over stdio"
repository = "https://github.com/repairman29/chump"
homepage = "https://github.com/repairman29/chump/tree/main/crates/mcp-servers/chump-mcp-gaps"
license = "MIT"
keywords = ["mcp", "gaps", "agent", "llm", "json-rpc"]
categories = ["command-line-utilities", "development-tools"]
readme = "README.md"

[[bin]]
name = "chump-mcp-gaps"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"

[package.metadata.dist]
dist = false
