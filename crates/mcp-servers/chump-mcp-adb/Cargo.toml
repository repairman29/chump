[package]
name = "chump-mcp-adb"
version = "0.1.0"
edition = "2021"
description = "MCP server: Android Debug Bridge (ADB) operations via JSON-RPC over stdio"
repository = "https://github.com/repairman29/chump"
homepage = "https://github.com/repairman29/chump/tree/main/crates/mcp-servers/chump-mcp-adb"
license = "MIT"
keywords = ["mcp", "adb", "android", "agent", "llm"]
categories = ["command-line-utilities", "development-tools"]
readme = "README.md"

[[bin]]
name = "chump-mcp-adb"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"

[package.metadata.dist]
dist = false
