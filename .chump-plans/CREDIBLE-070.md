# Plan: CREDIBLE-070 — CREDIBLE: chump fleet launchd-status + event-registry bypass audit — pair with plist work (TBD-INFRA-1546)

## Planned files

_(none enumerated by heuristic — agent will discover at runtime)_

## Open-PR overlap

_no overlap with currently-open PRs_

## Rationale (top of acceptance criteria)

- chump fleet launchd-status subcommand outputs a table of all known launchd plists (agents + daemons) with columns: name, plist_path, loaded (yes/no), running (yes/no), exit_code (if last exit was non-zero)
- Audit mode chump fleet launchd-status --audit compares installed plists vs a registry in docs/infra/LAUNCHD_REGISTRY.yaml; plists in registry but not installed are WARN; plists installed but not in registry are WARN
- Event-registry bypass audit: chump fleet launchd-status --audit also scans .chump-locks/ambient.jsonl (last 24h) for events emitted with kind values not present in EVENT_REGISTRY.yaml; outputs a list of unregistered kinds
- Emits kind=launchd_audit_complete to ambient.jsonl with fields: plists_checked, plists_missing, plists_extra, unregistered_event_kinds[]
- Smoke test scripts/ci/test-fleet-launchd-status.sh: inject 2 synthetic plist entries into LAUNCHD_REGISTRY.yaml, run the subcommand, assert both appear in output with correct loaded/running columns; inject an unregistered event into a synthetic ambient.jsonl, assert audit surfaces it

---
_Auto-generated by `chump --execute-gap` plan-mode gate (INFRA-060)._
