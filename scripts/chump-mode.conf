# Chump mode: slim the machine for the model on 8000 (14B default). Ollama + embed server are always stopped by the script.
# Every uncommented line below is killed when you run Enter Chump mode. Comment out any app you want to keep.
# Format: ProcessName (killall) or bundle:BUNDLE_ID (graceful quit). Never add: rust-agent, vllm, Python (protected).

# --- Browsers ---
Google Chrome
Safari
Firefox
Microsoft Edge
Arc
Brave Browser

# --- Electron / desktop apps ---
Slack
Discord
Zoom
Microsoft Teams
Notion
Obsidian
Figma

# --- Dev / IDEs (comment out if you need to code while Chump runs) ---
Code
# Cursor

# --- Apple apps ---
Mail
Music
TV
Calendar
Notes
Reminders

# --- Optional: quit by bundle ID (uncomment if killall misses an app) ---
# bundle:com.google.Chrome
# bundle:com.tinyspeck.slackmacgap
# bundle:com.hnc.Discord
# bundle:company.thebrowser.Browser
