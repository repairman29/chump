#
#     aggregate, product_drought — emitters never shipped)
#   - 3 truly dead entries were PRUNED from EVENT_REGISTRY.yaml (adversary_alert,
#   - obs_coverage_test_fixture → reserved (test-only, explicitly marked in registry)
#   - remaining 84 → reserved (planned for specific features; see reason comments)
# (`ambient-emit.sh "capabilities_registry_refreshed"`) — bash arg form,
# (emit_ambient "KIND" — no leading underscore, quoted arg). Registered in
# BOTH directions (emit-without-register AND register-without-emit).
# Blank lines and lines starting with `#` are ignored.
# Both emit sites use the local `_emit_ambient` helper (or direct printf) and
# CAPABILITIES_REGISTRY generator. These are NOT ambient event kinds —
# Disposition per kind:
# EVENT_REGISTRY.yaml; reserved here so coverage gate does not flag them.
# EVENT_REGISTRY.yaml; reserved here to satisfy register-without-emit.
# EVENT_REGISTRY.yaml; reserved here to satisfy strict-mode register-without-emit.
# Emit sites use the local `_emit_ambient KIND ...` helper (underscore-prefixed,
# Emitted by scripts/ops/audit-allowlist-staleness.sh via python3 heredoc write
# Emitted via `respawn_emit()` helper in scripts/ops/stale-pr-reaper.sh —
# Format: one kind per line, optional inline `# reason: ...` comment.
# INFRA-1287: seeded with 88 register-without-emit orphans after analysis.
# INFRA-1340: PRODUCT-109 follow-ups — kinds registered for future wiring
# INFRA-1456: test fixture placeholder kind in src/inspect_cmd.rs::tests — not a real emit
# INFRA-1695 / META-066: pre-existing lease-reaper telemetry surfaced after
# INFRA-1729: emit site uses positional arg to ambient-emit.sh
# INFRA-1729: false-positive "kind":"<value>" literals inside the
# INFRA-1744: pre-push test gate timeout — emitted from scripts/git-hooks/pre-push
# INFRA-1868: audit-allowlist-staleness.sh daily cron
# INFRA-1875: github-liaison.sh webhook-health probe + polling fallback.
# INFRA-1876: GitHub Liaison Phase 2 slice δ — offline-mode hard gate.
# INFRA-1893: open-PR scan warning discipline — two new forensic kinds
# Kinds that are EXEMPT from EVENT_REGISTRY.yaml drift enforcement in
# Remove an entry here once the emit site is implemented and CI runs clean.
# Use sparingly. Each entry must include a `# reason:` comment explaining
# apart from real emit sites, so allowlist with a reason instead.
# are not grep-scannable as `kind=X` literals by Pattern 5b's regex.
# as a `"kind":"..."` literal in the production path scan. Registered in
# but the scan paths missed scripts/coord/*reaper*.sh; reserved with reason.
# gap once src/web_server.rs lease clears. Reserved here to unblock CI in
# helper-function emit pattern (printf with variable kind) is not grep-scannable
# in the web-server approval handler; emission wiring lands in a follow-up
# mcp_tool / skill / event / script). The literal scan can't tell them
# not a `"kind":"X"` literal the gate's regex looks for. Registered in
# positional bare-string kind arg) which doesn't match Pattern 5b's regex
# scripts/ci/event-registry-reserved.txt — INFRA-1237
# scripts/content-bots/ was added to PROD_PATHS — these were always emitted
# state, exactly what this allowlist is for).
# the meantime (registered-without-emit is the "intent declared, code WIP"
# they're enum values of the registry's own `kind` field (cli / crate /
# to ambient.jsonl — not a grep-scannable `kind=X` literal in the prod path.
# undeclared internal probe, etc.).
# when cargo test exceeds CHUMP_PREPUSH_TEST_TIMEOUT_S budget under sibling
# why this kind is reserved (planned-but-not-yet-implemented, intentionally
# worktree contention; one-off operator-facing signal, not a steady-state event.
# ── Autonomous mode tracking ───────────────────────────────────────────────────
# ── Bot/merge observability ────────────────────────────────────────────────────
# ── Branch / protection ────────────────────────────────────────────────────────
# ── CI / gate ──────────────────────────────────────────────────────────────────
# ── Cascade routing ────────────────────────────────────────────────────────────
# ── Change management ──────────────────────────────────────────────────────────
# ── Claims ────────────────────────────────────────────────────────────────────
# ── Cognition / eval ──────────────────────────────────────────────────────────
# ── Cost / quota ──────────────────────────────────────────────────────────────
# ── Curator / decompose ───────────────────────────────────────────────────────
# ── Disk / fleet safety ───────────────────────────────────────────────────────
# ── Eval / behavior ───────────────────────────────────────────────────────────
# ── Feature monitoring ────────────────────────────────────────────────────────
# ── Flake auto-rerun ──────────────────────────────────────────────────────────
# ── Fleet / FTUE ──────────────────────────────────────────────────────────────
# ── Gap lifecycle ──────────────────────────────────────────────────────────────
# ── GitHub shim ───────────────────────────────────────────────────────────────
# ── Guard / hook bypass ───────────────────────────────────────────────────────
# ── Hook-emitted (not grep-scannable) ─────────────────────────────────────────
# ── INFRA-1410: PR-stuck SLO + auto-respawn ──────────────────────────────────
# ── Integration test pipeline (INFRA-849) ─────────────────────────────────────
# ── Intent system (INFRA-1116/1117) ──────────────────────────────────────────
# ── Invariant tracking ────────────────────────────────────────────────────────
# ── Lessons system ────────────────────────────────────────────────────────────
# ── Network monitoring ────────────────────────────────────────────────────────
# ── Opus / roadmap ────────────────────────────────────────────────────────────
# ── Orphan PR handling ────────────────────────────────────────────────────────
# ── PR lifecycle ──────────────────────────────────────────────────────────────
# ── Picker priority ───────────────────────────────────────────────────────────
# ── Pillar balance ────────────────────────────────────────────────────────────
# ── Planner ───────────────────────────────────────────────────────────────────
# ── Preflight ────────────────────────────────────────────────────────────────
# ── Reaper observability ──────────────────────────────────────────────────────
# ── Review handoff ────────────────────────────────────────────────────────────
# ── Roadmap proposals ────────────────────────────────────────────────────────
# ── Rust-first policy ────────────────────────────────────────────────────────
# ── SLO / health ──────────────────────────────────────────────────────────────
# ── Speculative work ──────────────────────────────────────────────────────────
# ── Stale gaps ────────────────────────────────────────────────────────────────
# ── Test fixtures ─────────────────────────────────────────────────────────────
# ── Test gate ────────────────────────────────────────────────────────────────
# ── Watchdog ──────────────────────────────────────────────────────────────────
# ── Webhook cache observability (INFRA-1873) ─────────────────────────────────
INFRA-1866 — audit-flake-catalog.sh CHUMP_AUDIT_FLAKE_CATALOG=0 opt-out
INFRA-1872 — emitted by scripts/ops/ci-qa-score.sh daily; rollup telemetry kind
a2a_rpc_stub_called  # reason: INFRA-1759 stub diagnostic emitted from crates/chump-coord/src/rpc.rs::call_rpc + serve_rpc while real impl (NATS subject routing) is unwired; allowlist until INFRA-1119 slice 2/4 lands
a2a_subscribe_stub_invoked  # reason: INFRA-1758 stub diagnostic emitted from crates/chump-coord/src/events.rs::subscribe_events while real impl (NATS push consumer) is unwired; allowlist until INFRA-1118 slice 2/4 lands
ac_coverage_disabled    # reason: INFRA-1541 operator-override audit emit from same gate; outside PROD_PATHS
ac_coverage_miss        # reason: INFRA-1541 pre-merge AC coverage gate; emitter scripts/ci/test-pr-ac-coverage.sh runs only in pr-hygiene job, outside PROD_PATHS grep scan
ac_coverage_no_ac       # reason: INFRA-1541 pass-through marker when gap has no AC bullets; outside PROD_PATHS
ac_coverage_no_gap_ref  # reason: INFRA-1541 pass-through marker for hotfix PRs without gap ref; outside PROD_PATHS
ac_coverage_waived      # reason: INFRA-1541 waiver trailer audit emit from same gate; outside PROD_PATHS
allowlist_stale_entry  # reason: INFRA-1868 — audit-allowlist-staleness.sh daily staleness detector; emitted via python3 heredoc, not grep-scannable as kind=X literal
ast_crawler_unsupported_language  # reason: INFRA-1719 emitted from crates/ast-crawler when an unsupported language is encountered; emit site uses struct-field syntax not grep-scannable as `kind=X` literal
audit_allowlist_staleness_bypassed  # reason: INFRA-1868 — audit-allowlist-staleness.sh skip-emit when CHUMP_AUDIT_ALLOWLIST_STALENESS=0; printf form not grep-scannable
audit_no_verify  # reason: INFRA-1834 emitted from chump-commit.sh + bot-merge.sh when --no-verify bypass used; reason field is operator-supplied, kind name stable for daily audit grep
auto_capability_bypassed  # reason: INFRA-1825 — capability-publish.sh CHUMP_AUTO_CAPABILITY=0 opt-out; printf-direct JSON not grep-scannable as kind=X literal
auto_envvar_applied # reason: INFRA-1853 emitted from scripts/coord/chump-commit.sh when new CHUMP_* env refs were auto-appended to env-vars-internal.txt (default path)
auto_envvar_bypassed # reason: INFRA-1853 emitted from scripts/coord/chump-commit.sh when CHUMP_AUTO_ENVVAR=0 disables the auto-append block
auto_fmt_applied # reason: INFRA-1833 emitted from scripts/coord/chump-commit.sh when cargo fmt --all was run pre-commit (default path)
auto_fmt_bypassed # reason: INFRA-1833 emitted from scripts/coord/chump-commit.sh when CHUMP_AUTO_FMT=0 disables the auto-fmt block
auto_merge_armed           # reason: planned for bot-merge.sh --auto-merge success confirmation
bot_merge_auto_armed       # reason: duplicate of auto_merge_armed (harmonize on ship)
bot_merge_bypassed      # reason: emitted from scripts/git-hooks/pre-push (shell); not in PROD_PATHS scanned by coverage check
bot_merge_watchdog_killed  # reason: planned for bot-merge watchdog kill path
bot_merge_watchdog_stuck   # reason: planned for bot-merge stuck-detection
# META-156: per-step observability + fail-fast kinds
bot_merge_step_started   # reason: META-156 AC#1 — emitted by _bm_emit_step_started() in bot-merge.sh at each named step (init/preflight/claim/push/pr_create/pr_merge_arm/pr_wait_merge/post_ship); helper uses variable-assembled printf so literal "kind":"bot_merge_step_started" not grep-scannable
bot_merge_step_done      # reason: META-156 AC#1 — emitted by _bm_emit_step_done() in bot-merge.sh at each named step with duration_ms and rc; same variable-assembled printf pattern
bot_merge_aborted_no_auth  # reason: META-156 AC#3 — emitted by bot-merge.sh on GraphQL 401 / graphql_exhausted hard-fail-fast in init step; variable-assembled printf
bot_merge_budget_warn    # reason: META-156 AC#6 — emitted by bot-merge.sh total-budget watchdog at 50%/75%/90% thresholds; variable-assembled printf
bot_merge_completed      # reason: META-156 AC#7 — emitted by bot-merge.sh graceful-exit roll-up with gap_id/pr_number/duration_ms/terminal_state; variable-assembled printf
bypass_threshold_breach  # reason: INFRA-1837 — emitted by scripts/ops/audit-bypass-frequency.sh from inside a python3 heredoc dict (per breaching session); kind literal lives in python dict, not grep-scannable as kind=X
capabilities_registry_refreshed  # reason: INFRA-1729 emit site is `ambient-emit.sh "<kind>"` positional arg, not grep-scannable as a JSON `"kind":"X"` literal
capability_published  # reason: INFRA-1120 / INFRA-1825 — crates/chump-coord/src/capability.rs publish_manifest(); scanner-anchor present in source; kind literal in comment block not grep-scannable as "kind":"X" JSON literal
capability_heartbeat  # reason: INFRA-1120 — crates/chump-coord/src/capability.rs heartbeat_loop(); scanner-anchor present in source; kind literal in comment block not grep-scannable as "kind":"X" JSON literal
chump_bin_resolved # reason: registered for INFRA-1618 active-fix flow; emit pending
chump_claim_force_recover  # reason: emitted via escaped Rust format string in src/atomic_claim.rs (INFRA-1439); regex pattern miss
force_recover_wip_loss     # reason: INFRA-2235 emitted via escaped Rust format string in src/atomic_claim.rs emit_force_recover_wip_loss; same regex pattern miss as chump_claim_force_recover
force_recover_wip_discarded  # reason: INFRA-2235 emitted via escaped Rust format string in src/atomic_claim.rs emit_force_recover_wip_discarded; same regex pattern miss as chump_claim_force_recover
chump_gap_set_legacy_delim  # reason: INFRA-1799 emitted from src/main.rs `chump gap set --acceptance-criteria` legacy delimiter path; struct-init form `kind: "X".to_string()` not grep-scannable as `kind = "X"`
ci_cascade_cancelled  # reason: INFRA-1870 emitted via _emit_ambient dict-payload in github-webhook-receiver.py workflow_run handler; dict-construction emit pattern not grep-scannable as "kind":"ci_cascade_cancelled" literal in PROD_PATHS
ci_parity_drift            # reason: emitted by scripts/ci/test-preflight-ci-parity.sh (INFRA-1867); scripts/ci/ is outside PROD_PATHS grep scan
ci_regression_guard_blocked  # reason: emitted by scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421); git-hooks/ outside PROD_PATHS grep scan
ci_regression_guard_missing  # reason: emitted by scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421); git-hooks/ outside PROD_PATHS grep scan
ci_regression_guard_suite_failed  # reason: emitted by scripts/git-hooks/pre-push-ci-regression-guard.sh (INFRA-1421); git-hooks/ outside PROD_PATHS grep scan
ci_yml_row_add_merged  # reason: emitted from scripts/git/merge-driver-ci-yml-add-row.sh (shell); not in PROD_PATHS
circuit_breaker_opened       # reason: RESILIENT-011 emitted via Rust match-arm literal in src/circuit_breaker.rs:emit_state_change_event; grep scanner pattern `kind = "..."` misses match-arm form `=> "circuit_breaker_opened"`
circuit_breaker_state_change # reason: RESILIENT-011 same emit site; catch-all match-arm `_ => "circuit_breaker_state_change"` not grep-scannable
claim_duplicate_bypassed   # reason: INFRA-1442 #2358 emitted from src/atomic_claim.rs emit_claim_duplicate_bypassed; allowlist while EVENT_REGISTRY entry catches up
claim_duplicate_gap_blocked   # reason: INFRA-1970 emitted from src/atomic_claim.rs emit_claim_duplicate_gap_event when same gap_id claimed by two sessions; scanner-anchor present in source
claim_duplicate_gap_bypassed  # reason: INFRA-1970 bypass variant (CHUMP_CLAIM_ALLOW_DUPLICATE_GAP=1); same emit site as claim_duplicate_gap_blocked
cli  # reason: INFRA-1729 — JSON enum value for primitive.kind in build-capabilities-registry.sh, not an ambient kind
commit  # reason: emitted by Claude Code PostToolUse hook (Bash git commit), not by scripts/; hooks are outside grep production paths
crate  # reason: INFRA-1729 — JSON enum value for primitive.kind in build-capabilities-registry.sh, not an ambient kind
curator_decision           # reason: planned for opus-curator gap-ranking decision emit
infra_watcher_finding      # reason: META-102 — emitted from scripts/coord/infra-watcher-loop.sh via direct printf JSON; emit uses dynamic {category,severity,detail} fields assembled at runtime, not grep-scannable as a `"kind":"infra_watcher_finding"` literal in the production path scan
disk_critical              # reason: emitter scripts/ops/disk-watchdog.sh uses variable-mediated emit (disk_alert_kind); grep pattern misses it; will fix in follow-up
external_collab_finding    # reason: META-104 emitted by scripts/coord/external-collab-loop.sh; categories: voice_drift | surface_stale | marcus_at_risk | partnership_stalled
external_collab_scope_override  # reason: META-104 emitted when CHUMP_EXTERNAL_COLLAB_SCOPE_OVERRIDE=1 bypasses lane guard
fixture_commit_dropped  # reason: planned by INFRA-1408 #2179 pre-push fixture detector; allow orphan until that PR lands
flake_catalog_orphan  # reason: INFRA-1866 — audit-flake-catalog.sh orphan catalog entry
fleet_doctor_run        # reason: INFRA-1427 emitted via positional arg `bash "$AMBIENT_EMIT" fleet_doctor_run` in scripts/coord/fleet-doctor-strict.sh; grep scanner expects "kind":"..." JSON literal and misses this form
fleet_version_skew         # reason: planned for fleet version drift detector
gap_dup_archived       # reason: emitted but not registered; allowlist while EVENT_REGISTRY entry is drafted
gap_reserve_open_pr_scan_failed  # reason: INFRA-1893 emitted from crates/chump-gap-store/src/lib.rs when scan fails AND gh smoke also fails — genuine auth failure telemetry; no EVENT_REGISTRY entry yet
gap_reserve_open_pr_scan_inconsistent  # reason: INFRA-1893 emitted from crates/chump-gap-store/src/lib.rs when scan fails but gh smoke (gh api user) returns 200 — spurious-401 forensics; no EVENT_REGISTRY entry yet
gap_resumed                # reason: INFRA-1456 #2351 emitted from src/resume_cmd.rs; allowlist while EVENT_REGISTRY entry catches up
gap_scrapped               # reason: INFRA-1456 #2351 emitted from src/scrap_cmd.rs; allowlist while EVENT_REGISTRY entry catches up
gap_blocked                # reason: INFRA-2055 — emitted by src/main.rs --execute-gap on every non-clean exit; scanner-anchor in execute_gap.rs
gap_deferred               # reason: INFRA-2055 — emitted by src/main.rs --execute-gap when agent explicitly defers; scanner-anchor in execute_gap.rs
gh_shim_worktree_path_resolved   # reason: planned for INFRA-779 gitdir repair confirmation
github_app_fallback  # reason: emitted by scripts/coord/lib/github.sh _chump_gh_lane_token (INFRA-1076); helper function emit not grep-scannable as literal kind string
handoff_lane_override  # reason: INFRA-1922 — emitted by scripts/coord/handoff-loop.sh only when operator sets CHUMP_HANDOFF_LANE_OVERRIDE=1 (rare cross-lane work path); registered in EVENT_REGISTRY.yaml + scanner-anchor will exist once a real emit lands
hidden_gems_refreshed  # reason: INFRA-1727 — emitted by scripts/dev/build-hidden-gems.sh after rebuild; python3 heredoc write to ambient.jsonl, not grep-scannable as kind=X literal
inbox_auto_poll_surfaced  # reason: INFRA-1860 emitted by scripts/coord/inbox-poll.sh PostToolUse hook helper, ambient-emit positional-arg form not grep-scannable as kind=X literal
inbox_session_derived  # reason: INFRA-1879 — emitted by scripts/coord/inbox-poll.sh on session-id derivation; positional-arg form
integration_test_fail      # reason: emitted by scripts/ci/test-system-integration.sh which is excluded from PROD_PATHS (CI scripts legitimately mention events without standard emit pattern)
integration_test_pass      # reason: emitted by scripts/ci/test-system-integration.sh which is excluded from PROD_PATHS (CI scripts legitimately mention events without standard emit pattern)
keystone_cascade_fired     # reason: INFRA-1420 #2361 emitted from src/paramedic.rs action_keystone_cascade; allowlist while EVENT_REGISTRY entry catches up
lease_reaper_deleted  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
lease_reaper_dry_run  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
lease_reaper_error  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
lease_reaper_skipped_active  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
lease_reaper_skipped_in_progress  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
lease_reaper_skipped_invalid  # reason: stale-lease-reaper telemetry (scripts/coord/), pre-existing
liaison_cache_offline_read  # reason: INFRA-1876 — emitted by scripts/coord/lib/github_cache.sh::_emit_offline_read_event when cache helpers run with CHUMP_GITHUB_MODE=offline; per-helper 60s debounce
liaison_cache_stale  # reason: INFRA-1874 emitted from scripts/coord/lib/github_cache.sh::cache_lookup_pr when age_s > CHUMP_LIAISON_CACHE_STALE_S (default 600); operator-visible 'cache degraded' signal separate from per-call cache_miss
liaison_elected            # reason: INFRA-1317 #2373 GitHub Liaison election event from scripts/ops/github-liaison.sh; printf emit not grep-scannable
liaison_heartbeat          # reason: INFRA-1317 #2373 GitHub Liaison heartbeat from scripts/ops/github-liaison.sh
liaison_offline_mode_gated  # reason: INFRA-1876 — emitted by scripts/ops/github-liaison.sh when CHUMP_GITHUB_MODE=offline blocks daemon start; _emit_ambient helper form
liaison_polling_fallback_active  # reason: INFRA-1875 emit at scripts/ops/github-liaison.sh _refresh_cycle alongside liaison_webhook_unhealthy; same helper-not-scannable issue
liaison_takeover           # reason: INFRA-1317 #2373 GitHub Liaison takeover event
liaison_webhook_recovered  # reason: INFRA-1875 emit at scripts/ops/github-liaison.sh _refresh_cycle on first successful probe after fallback; same helper-not-scannable issue
liaison_webhook_unhealthy  # reason: INFRA-1875 emit at scripts/ops/github-liaison.sh _refresh_cycle when probe fails CHUMP_LIAISON_WEBHOOK_HEALTH_MAX_FAILS times; _emit_ambient helper not grep-scannable
liaison_yielded            # reason: INFRA-1317 #2373 GitHub Liaison yield event
mcp_tool  # reason: INFRA-1729 — JSON enum value for primitive.kind in build-capabilities-registry.sh, not an ambient kind
merge_preview_dirty     # reason: planned kind (no emitter yet); allowlist until emitter ships to unblock audit cluster
merge_preview_skipped  # reason: emitted from scripts/coord/ (shell); not in PROD_PATHS
merge_queue_health      # reason: CREDIBLE-068 #2359 emitted from scripts/coord/monitor-merge-queue.sh via printf to ambient.jsonl; grep scanner does not see the shell printf pattern as a `kind=...` literal
obs_coverage_test_fixture  # reason: synthetic test-fixture kind; emitter is scripts/ci/ (excluded from prod grep); status=test-fixture in registry
operator_pr_action     # reason: emitted but not registered; allowlist while EVENT_REGISTRY entry is drafted
opus_message_sent  # reason: INFRA-1800 — emitted by scripts/coord/broadcast.sh after addressed-async DM delivery (META-061 opus-message v0 retarget)
opus_shepherd_plan  # reason: META-091 — emitted by scripts/coord/opus-shepherd-triage.sh via python3 heredoc, not grep-scannable as kind=X literal
opus_shepherd_triage  # reason: META-091 — emitted by scripts/coord/opus-shepherd-triage.sh via python3 heredoc, not grep-scannable as kind=X literal
opus_shepherd_triage_skipped  # reason: META-091 — emitted by scripts/coord/opus-shepherd-triage.sh via python3 heredoc, not grep-scannable as kind=X literal
orchestrate_session_summary  # reason: emitted by INFRA-1363 src/orchestrate.rs; used as test fixture here while that PR is in flight
picker_priority_stale      # reason: planned for INFRA-1258 picker priority stale detection
pr_auto_closed_for_respawn         # reason: INFRA-1410 emitted via respawn_emit() helper in stale-pr-reaper.sh; variable-kind printf not grep-scannable
pr_auto_rearmed  # reason: INFRA-1907 emitted by scripts/coord/pr-auto-rearm.sh safety-net sweeper when a BLOCKED+disarmed PR gets re-armed; printf-direct JSON not grep-scannable as kind=X literal
pr_auto_rebase_fallback # reason: INFRA-1958 emitted by scripts/coord/pr-auto-rebase.sh when gh pr update-branch returned false-positive but local git rebase succeeded; printf-direct JSON not grep-scannable as kind=X literal
pr_auto_rescue_invoked # reason: registered for INFRA-1600 pr-rescue MVP; emit pending
pr_bounced_relanded        # reason: emitter scripts/coord/ uses _emit function (confirmed in grep); adding to reserved while INFRA-1287 grep fixes land
pr_bounced_unfinished      # reason: emitter scripts/coord/bounced-pr-detector.sh; _emit pattern may not cover all code paths
pr_rebase_daemon_run    # reason: INFRA-1406 pr-rebase-daemon heartbeat
pr_rebase_failed        # reason: INFRA-1406 pr-rebase-daemon rebase failure
pr_rebased_auto         # reason: INFRA-1406 pr-rebase-daemon successful auto-rebase
pr_stuck_cycle_1_rebase_attempted  # reason: INFRA-1410 emitted via respawn_emit() helper in stale-pr-reaper.sh; variable-kind printf not grep-scannable
pr_stuck_exempt                    # reason: INFRA-1410 emitted via respawn_emit() helper in stale-pr-reaper.sh; variable-kind printf not grep-scannable
pr_triage_run  # reason: emitted from scripts/coord/chump-pr-triage.sh (shell); not in PROD_PATHS
pre_commit_ac_test_missing  # reason: emitted by scripts/git-hooks/pre-commit-ac-completeness.sh (INFRA-1401); git-hooks/ outside PROD_PATHS grep scan
preflight_acgate_bypassed # reason: INFRA-1791 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_ACGATE=1; allowlist while EVENT_REGISTRY entry catches up (mirrors INFRA-1731 pattern)
preflight_docsdelta_bypassed # reason: INFRA-1788 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_DOCSDELTA=1; struct-field emit (EmitArgs { kind: "..." }) not detected by Pattern 1 grep — allowlisted while orphan-warn lands
preflight_envvars_bypassed # reason: INFRA-1787 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_ENVVARS=1; allowlist while EVENT_REGISTRY entry catches up (mirrors INFRA-1731 preflight_registry_bypassed pattern)
preflight_gapsint_bypassed # reason: INFRA-1831 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_GAPSINT=1; allowlist while EVENT_REGISTRY entry catches up (mirrors INFRA-1731 pattern)
preflight_mdlinks_bypassed # reason: INFRA-1790 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_MDLINKS=1; allowlist while EVENT_REGISTRY entry catches up (mirrors INFRA-1731 pattern)
preflight_registry_bypassed # reason: INFRA-1731 #2377 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_REGISTRY=1; allowlist while EVENT_REGISTRY entry catches up
preflight_subcmdhelp_bypassed # reason: INFRA-1789 emitted from src/preflight.rs when CHUMP_PREFLIGHT_SKIP_SUBCMDHELP=1; allowlist while EVENT_REGISTRY entry catches up (mirrors INFRA-1731 preflight_registry_bypassed pattern)
prepush_head_drift  # reason: emitted by scripts/git-hooks/pre-push (INFRA-1372); git-hooks/ dir is outside PROD_PATHS grep scan
prepush_test_timeout  # reason: INFRA-1744 emitted from scripts/git-hooks/pre-push timeout branch; printf-formatted from bash, not via emit_event helper
pwa_brief_loaded  # reason: emitted from web/v2/daily-brief.js via sendBeacon (JS not in PROD_PATHS); emit added in PRODUCT-078
pwa_gap_list_filtered  # reason: emitted from web/v2/gap-list.js via sendBeacon (JS not in PROD_PATHS); emit added in PRODUCT-102
pwa_impact_viewed  # reason: emitted from web/v2/impact.js via sendBeacon (JS, not in PROD_PATHS)
queue_health_check_failed # reason: CREDIBLE-068 #2359 same emit pattern as merge_queue_health (printf in monitor-merge-queue.sh, not grep-scannable)
reaper_run                 # reason: planned for reaper start/stop lifecycle emit
reaper_safety_gate_triggered  # reason: INFRA-1786 — emitted by reap-orphan-claude-procs.sh when fg_pid=none on non-headless macOS; safety gate refuses reap-all
reaper_self_paused  # reason: reaper telemetry, pre-existing
redundancy_bypass_used  # reason: emitted by scripts/git-hooks/pre-commit-redundancy.sh (META-063); git hooks are outside PROD_PATHS grep scan
restart  # reason: daemon-restart telemetry, pre-existing — too generic to register without scope
runner_migration_step           # reason: INFRA-1535 migration pipeline state-transition emit
runner_scaled                   # reason: INFRA-1535 autoscale daemon scale-up/down emit
rust_first_blocked  # reason: emitted by scripts/git-hooks/pre-commit-rust-first.sh on the block path (INFRA-1448); git-hooks/ outside PROD_PATHS grep scan
rust_first_bypass_audit  # reason: emitted by scripts/dev/rust-first-bypass-audit.sh (INFRA-1580); scripts/dev/ outside PROD_PATHS grep scan
rust_first_strict_blocked  # reason: emitted by scripts/git-hooks/pre-commit-rust-first.sh strict layer (INFRA-1580); git-hooks/ outside PROD_PATHS grep scan
self_doctor_budget_exceeded    # reason: INFRA-1595 only fires under stress (>3 dispatches/10min); rare path not exercised in PROD_PATHS smoke
self_doctor_failed             # reason: INFRA-1595 only fires on action failure (install error / execute-gap spawn error); rare path not exercised in PROD_PATHS smoke
self_doctor_healed             # reason: INFRA-1595 same struct-init pattern as self_doctor_tick; emit-site not grep-scannable
self_doctor_tick               # reason: INFRA-1595 emitted from src/fleet_self_doctor.rs via EmitArgs{kind: "..".to_string()} struct-init form; grep pattern `kind = "X"` does not match `kind: "X".to_string()`
self_hosted_runner_run          # reason: INFRA-1534 per-run completion (planned)
skill  # reason: INFRA-1729 — JSON enum value for primitive.kind in build-capabilities-registry.sh, not an ambient kind
stale_branch_auto_rebased  # reason: INFRA-1429 #2356 emitted from src/paramedic.rs action_rebase_dirty; allowlist while EVENT_REGISTRY entry catches up
synthesis_gap_filed     # reason: declared kind awaiting emitter (INFRA-1684 synthesis-truth audit wip); allowlist orphan to unblock audit cluster (#2337, et al.)
target_artifact_critical_reap  # reason: INFRA-1431 critical-mode variant; emitted via shell variable $_event_kind in target-dir-reaper.sh (not a grep-scannable literal); same emit site as target_artifact_reaped
target_artifact_reaped  # reason: INFRA-1349 target/ artifact reaper kind; emitted when build dirs are cleaned under disk pressure
tool_approval_escalated  # reason: INFRA-1340 PRODUCT-109; emission wiring in follow-up gap
tool_approval_policy_changed  # reason: INFRA-1340 PRODUCT-109; emission wiring in follow-up gap
tool_auto_approved  # reason: INFRA-1340 PRODUCT-109; emission wiring in follow-up gap
watchdog_silent            # reason: planned for watchdog heartbeat absence detector
webhook_cache_write  # reason: INFRA-1873 emitted via _emit_ambient dict-payload in github-webhook-receiver.py (_upsert_pr + _upsert_check_runs call sites); dict-payload emit not grep-scannable as "kind":"webhook_cache_write" literal
observability_finding  # reason: META-103 emitted by scripts/coord/observability-loop.sh via printf in _emit_finding helper; structured payload {category,severity,kind_or_subject,detail} not grep-scannable as bare "kind":"observability_finding" literal
kind_that_fires   # reason: META-103 test-fixture synthetic kind used in scripts/ci/test-observability-loop.sh Test 1; not a production emit
kind_that_never_fires  # reason: META-103 test-fixture synthetic kind used in scripts/ci/test-observability-loop.sh Test 1; not a production emit
loud_detector     # reason: META-103 test-fixture synthetic kind used in scripts/ci/test-observability-loop.sh Test 5; not a production emit
noisy_kind        # reason: META-103 test-fixture synthetic kind used in scripts/ci/test-observability-loop.sh Test 2; not a production emit
normal_kind       # reason: META-103 test-fixture synthetic kind used in scripts/ci/test-observability-loop.sh Test 4; not a production emit
x  # reason: INFRA-1456 test-fixture placeholder ("x" used as kind in synthetic ambient.jsonl test inputs); not a production emit
test_file_fallback  # reason: INFRA-1118 test-fixture kind used in crates/chump-coord/tests/a2a_layer1a.rs file-fallback receipt test; not a production emit
test_roundtrip      # reason: INFRA-1118 test-fixture kind used in crates/chump-coord/tests/a2a_layer1a.rs NATS roundtrip latency test; not a production emit
test_backpressure   # reason: INFRA-1118 test-fixture kind used in crates/chump-coord/tests/a2a_layer1a.rs backpressure detection test; not a production emit
oracle_refresh_drift  # reason: META-088 — scripts/coord/oracle-refresh.sh emits on THE_PATH.md hash change; printf-direct JSON not grep-scannable as kind=X literal
oracle_refresh_noop  # reason: META-088 — scripts/coord/oracle-refresh.sh emits on idempotent no-op (same hash); printf-direct JSON not grep-scannable
oracle_refresh_skipped  # reason: META-088 — scripts/coord/oracle-refresh.sh emits when claude CLI missing or burst empty; printf-direct JSON not grep-scannable
oracle_refresh_pr_opened  # reason: META-088 — scripts/coord/oracle-refresh.sh emits after auto-PR opens; printf-direct JSON not grep-scannable

# ── META-093: Opus parallel sub-fleet slot tracker ───────────────────────────
opus_slot_dispatched  # reason: META-093 emitted by scripts/coord/opus-slot-tracker.sh dispatch subcommand via python3 heredoc; not grep-scannable as kind=X literal in PROD_PATHS
opus_slot_reaped      # reason: META-093 emitted by scripts/coord/opus-slot-tracker.sh reap subcommand via python3 heredoc; not grep-scannable as kind=X literal in PROD_PATHS

# ── INFRA-1522: W-007 required-check health gate ─────────────────────────────
required_check_health_warn  # reason: INFRA-1522 emitted by src/required_check_health.rs::emit_warn_for_unhealthy when chump fleet up/doctor finds flake>20% or skipped streak; formal EVENT_REGISTRY.yaml entry in follow-up (file currently held by INFRA-1977 lease)
required_check_health_bypass  # reason: INFRA-1522 emitted by src/required_check_health.rs::emit_bypass when operator passes --force to fleet up; audit trail for intentional override; formal EVENT_REGISTRY.yaml entry in follow-up

# ── INFRA-1987 / META-106 (THE FLOOR Phase 1): cluster detector ──────────────
ci_failure_cluster  # reason: INFRA-1987 emitted by scripts/coord/cluster-detector.sh when ≥3 OPEN PRs share IDENTICAL failing-check set; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS; formal EVENT_REGISTRY.yaml entry pending file lease
ci_failure_cluster_resolved  # reason: INFRA-1987 emitted by scripts/coord/cluster-detector.sh when a previously-detected cluster_id disappears from BLOCKED PRs (resolution signal); formal EVENT_REGISTRY.yaml entry pending file lease

# ── INFRA-1992 / META-106 (THE FLOOR Phase 1): floor-temperature signal ──────
floor_temp  # reason: INFRA-1992 emitted by src/floor_temp.rs::emit_floor_temp on every chump health --temp invocation; carries COLD/WARM/HOT classification + component counts; formal EVENT_REGISTRY.yaml entry pending file lease

# ── INFRA-1989 / META-106 (THE FLOOR Phase 1 finisher): blame bot ────────────
regression_attributed  # reason: INFRA-1989 emitted by scripts/coord/blame-bot.sh when green→red CI transition is mapped to suspect commits via git log green..HEAD; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
regression_inattributable  # reason: INFRA-1989 emitted by scripts/coord/blame-bot.sh when no green baseline OR no commits in window OR mapped paths empty; printf-direct JSON not grep-scannable

# ── INFRA-1988 / META-106 (THE FLOOR Phase 2): hook silent-noop alarm ────────
hook_silent_passthrough  # reason: INFRA-1988 emitted by scripts/git-hooks/pre-push EXIT trap when main Guard 1/2/3 loop never entered on non-trivial push (the exact INFRA-1986 silent-regression pattern); printf-direct JSON not grep-scannable in scanner's PROD_PATHS

# ── RESILIENT-031: admin-merge discipline audit trail ─────────────────────────
admin_merge_forced  # reason: RESILIENT-031 emitted by scripts/ops/admin-merge-cycle.sh _emit() via printf — emit site uses a # scanner-anchor comment (INFRA-1237 pattern 8b) but formal EVENT_REGISTRY.yaml entry added in same commit; reserved here to satisfy strict-mode register-without-emit until the scanner can resolve scanner-anchor patterns

# ── INFRA-2071: admin-merge burst circuit-breaker ─────────────────────────────
admin_merge_cycle_run          # reason: INFRA-2071 emitted by scripts/ops/admin-merge-cycle.sh after successful merge — the counter event the burst threshold counts; scanner-anchor in _emit_cycle_run helper
admin_merge_burst              # reason: INFRA-2071 emitted by scripts/ops/admin-merge-cycle.sh _check_burst when count >= threshold; scanner-anchor in _check_burst helper; operator_action_needed=true
admin_merge_burst_overridden   # reason: INFRA-2071 emitted by scripts/ops/admin-merge-cycle.sh burst override block when --force-admin or CHUMP_ADMIN_MERGE_FORCE=1 bypasses a tripped circuit-breaker; scanner-anchor in burst override block

# ── INFRA-1993 / META-106 (THE FLOOR Phase 3): operator-recovery queue ───────
operator_recovery_requested  # reason: INFRA-1993 emitted by scripts/coord/recovery-queue-emit.sh (worker-facing CLI) when a worker requests an admin-merge cycle for a PR cluster; the queue service consumes these events
operator_recovery_executed   # reason: INFRA-1993 emitted by scripts/coord/recovery-queue-service.sh after each successful drop+merge+re-arm cycle with merged/failed PR lists + ruleset_id
operator_recovery_failed     # reason: INFRA-1993 emitted by scripts/coord/recovery-queue-service.sh on snapshot/drop/restore failure (CRITICAL when ruleset restore fails — operator action required)
recovery_queue_rate_limited  # reason: INFRA-1993 emitted by scripts/coord/recovery-queue-service.sh when 3-per-hour rate budget exhausted; deferred request waits for window to slide
recovery_queue_paused        # reason: INFRA-1993 emitted when CHUMP_RECOVERY_QUEUE_PAUSE=1 disables the daemon

# ── INFRA-1994 / META-106 (THE FLOOR Phase 3): wedge state machine ───────────
wedge_remediation_requested  # reason: INFRA-1994 emitted by scripts/coord/wedge-state-machine.sh per-class remediation router; carries action + detail for operator/audit
wedge_remediation_rate_limited  # reason: INFRA-1994 emitted when same wedge_class fires within rate window (default 30min); deferred
wedge_chronic                # reason: INFRA-1994 emitted when same wedge_class fires ≥3x in 24h (default); escalation signal to file META gap for permanent fix

# ── INFRA-2003: event-registry strict-mode trunk-red unblock (#2587 flipped strict) ──
#
# THE FLOOR Phase 1 (#2587, INFRA-1987) flipped CHUMP_REGISTRY_GATE_MODE=strict
# in the audit job, surfacing 5 pre-existing latent violations that had been
# advisory-warning-only until that point. Result: every PR in CI started failing
# the event-registry coverage gate. INFRA-2003 unblocks the trunk by reserving
# these 5 kinds with explicit reasons so the strict gate passes; follow-up
# work owns the proper resolution per kind (wire emit OR remove from registry).
pr_oversight_snapshot  # reason: emit-without-register surfaced 2026-05-25; emit site not yet wired into the audit scanner's PROD_PATHS; reserved while a follow-up gap restructures the PR-oversight emit path
subagent_idle_without_pr  # reason: emit-without-register surfaced 2026-05-25 by INFRA-1987 strict-mode flip; emit logic exists in subagent watchdog but not in scanner's PROD_PATHS; reserved pending follow-up to either register or rehome the emit
preflight_ci_agreement  # reason: register-without-emit orphan from INFRA-1927 (#2537 area); emit site is gated by CHUMP_PREFLIGHT_CI_AGREEMENT env var so scanner reports no production emit; reserved pending decision to wire unconditionally or remove from registry
voice_lint_bypassed  # reason: register-without-emit orphan; voice-lint half-state class (critique H6 / INFRA-1975); kind is registered for the eventual policy decision (required-OR-removed); reserved until INFRA-1975 lands
voice_lint_violation  # reason: register-without-emit orphan; same class as voice_lint_bypassed; reserved until INFRA-1975 voice-lint policy decision lands

# ── INFRA-1923 / META-097 (ci-audit role productize, shipped 17:22Z 2026-05-25 as #2590) ──
ci_audit_heartbeat  # reason: emitted by scripts/coord/ci-audit-loop.sh::heartbeat (per INFRA-1923 productization); printf-direct JSON not grep-scannable by PROD_PATHS scanner; reserved pending formal EVENT_REGISTRY.yaml entry by ci-audit role owner
ci_cluster_detected  # reason: emitted by scripts/coord/ci-audit-loop.sh::audit when ≥N PRs share failing-check set; same scanner-scope class as ci_audit_heartbeat; reserved pending formal EVENT_REGISTRY.yaml entry by ci-audit role owner

# ── INFRA-2014 / META-106: inbox-injector (the 'magic' A2A live signal) ──────
inbox_injection_executed  # reason: INFRA-2014 emitted by scripts/coord/inbox-injector.sh after tmux send-keys interrupt delivered to a recipient pane; carries urgency + sender + pane target
inbox_injector_paused     # reason: INFRA-2014 emitted when CHUMP_INBOX_INJECTOR_PAUSE=1 disables daemon

# ── md-links agent events (now registered in EVENT_REGISTRY.yaml via RESILIENT-033, 2026-05-27) ──
md_links_heartbeat  # reason: emitted by scripts/coord/md-links-loop.sh (scanner-anchor present); registered via RESILIENT-033
md_links_lane_override  # reason: emitted by scripts/coord/md-links-loop.sh (scanner-anchor present); registered via RESILIENT-033
md_links_scan_done  # reason: emitted by scripts/coord/md-links-loop.sh (scanner-anchor present); registered via RESILIENT-033

# ── INFRA-2015: broadcast.sh --urgency typed routing ─────────────────────────
urgent_broadcast          # reason: INFRA-2015 emitted by scripts/coord/broadcast.sh route_by_urgency() for WARN/CRIT/EMERGENCY tier; secondary ambient marker for 5-min loop tick consumers; scanner-anchor: "kind":"urgent_broadcast"

# ── INFRA-2016: global URGENT-INBOX + PostToolUse check ──────────────────────
urgent_broadcast_sent     # reason: INFRA-2016 emitted by scripts/coord/broadcast-urgent.sh when CRIT/EMERGENCY message written to .chump-locks/URGENT-INBOX.jsonl global channel; bypasses session-keyed routing
inbox_urgent_surfaced     # reason: INFRA-2016 emitted by scripts/coord/inbox-check-urgent.sh after surfacing global-urgent messages as <system-reminder> via PostToolUse hook; carries message count + new cursor

# ── INFRA-2026: post-push PR integrity watch daemon ──────────────────────────
post_push_auto_close_recovered  # reason: INFRA-2026 emitted by scripts/coord/post-push-integrity-watch.sh when a chump/* PR is detected as auto-closed within 120s of a push (stale-base force-push pattern); carries pr, branch, state_reason, reopen_status
post_push_integrity_watch_ok    # reason: INFRA-2026 heartbeat from post-push-integrity-watch.sh when scan completes with no incidents
post_push_integrity_watch_err   # reason: INFRA-2026 emitted when daemon encounters a gh API or config error (gh_not_found, remote_parse_failed, gh_pr_list_failed)

# ── INFRA-2027: recovery-queue mid-flight checkpoint + auto-restore ───────────
operator_recovery_aborted_recovered  # reason: INFRA-2027 emitted by recovery-queue-service.sh when startup detects an orphaned in-flight checkpoint (age > 2× cycle interval) and successfully restores the ruleset from backup; carries aborted_step, checkpoint_age_s, backup_file, ruleset_id

# ── INFRA-2013: fleet-brief leading-indicator stall detection ─────────────────
fleet_stalled  # reason: INFRA-2013 emitted by scripts/dispatch/fleet-brief.sh when ships_1h==0 AND open BLOCKED PRs>=2; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS; formal EVENT_REGISTRY.yaml entry pending

# ── INFRA-2008 + INFRA-2029 (THE FLOOR Phase 2): worker prelude floor-signal reads ──
worker_floor_signal_read  # reason: INFRA-2008 emitted by scripts/dispatch/worker.sh prelude before each claim cycle; carries signal=(fleet_hold|floor_temp), hold=0|1 or temp=COLD|WARM|HOT; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
worker_stuck              # reason: INFRA-2029 emitted by scripts/dispatch/worker.sh _emit_worker_stuck helper in every exit-without-ship code path (stand_down, preflight_fail, gap_already_done, worktree_create_fail, no_pickable_gap); carries reason field; printf-direct JSON not grep-scannable

# ── INFRA-2009 (THE FLOOR Phase 2): daemon silent-noop alarm ─────────────────
daemon_silent_noop  # reason: INFRA-2009 emitted by scripts/coord/lib/silent-noop-guard.sh EXIT trap when a floor daemon (cluster_detector, wedge_state_machine, recovery_queue_service) exits rc=0 without executing its main work body despite non-empty input; generalizes the INFRA-1988 hook_silent_passthrough pattern; carries source=<daemon_tag> + reason=main_work_body_skipped_despite_input; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS

# ── INFRA-2025: cross-daemon coordination — cluster-detector defers during recovery-queue drop-window ──
cluster_detection_deferred_for_recovery  # reason: INFRA-2025 emitted by scripts/coord/cluster-detector.sh when recovery-cycle-in-flight.flag is present; prevents mis-classification of PRs as passing during the required_status_checks drop-window; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS

# ── INFRA-2030: real auto-remediations wired into wedge-state-machine ────────
wedge_remediated_real          # reason: INFRA-2030 emitted by scripts/coord/wedge-state-machine.sh _remediate_w002/_remediate_w007/_remediate_wagg after invoking a REAL remediation primitive (not just an advisory emit); carries class, action, detail; replaces advisory-only wedge_remediation_requested for W-002/W-007/W-AGG; printf-direct JSON not grep-scannable as kind=X literal
cluster_detection_requested    # reason: INFRA-2030 emitted by scripts/coord/wedge-state-machine.sh _remediate_wagg when W-AGG fires; signals cluster-detector.sh to run its IDENTICAL-check-set discrimination on next tick; carries source_wedge, reason, detail; printf-direct JSON not grep-scannable as kind=X literal

# ── INFRA-2007: W-002 binary-cache-lag permanent fix — event-driven refresh + cron fallback ─────────
binary_main_updated  # reason: INFRA-2007 emitted by scripts/coord/bot-merge.sh after successful gap ship + merge; triggers binary-refresh-event-watcher.sh to rebuild immediately; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
binary_refresh_triggered_event  # reason: INFRA-2007 emitted by scripts/coord/binary-refresh-event-watcher.sh when binary_main_updated event triggers an immediate rebuild (event-driven path); printf-direct JSON not grep-scannable as kind=X literal
binary_event_watcher_rate_limited  # reason: INFRA-2007 emitted by binary-refresh-event-watcher.sh when rebuild skipped because last rebuild was within CHUMP_BINARY_EVENT_RATE_LIMIT_S window; safety valve against merge bursts
binary_event_watcher_no_tool  # reason: INFRA-2007 emitted by binary-refresh-event-watcher.sh when fswatch is absent; signals that polling fallback (tail -F) is active instead of zero-overhead FSEvents path

# ── META-109 Phase 1: wizard-daemon — autonomous DRIVE primitive ──────────────
wizard_daemon_action          # reason: META-109 emitted by scripts/coord/wizard-daemon.sh on every classification and decision; carries step, target, decision, rate_limit_state, and step-specific fields; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
wizard_daemon_paused          # reason: META-109 emitted by scripts/coord/wizard-daemon.sh at startup when CHUMP_WIZARD_DAEMON_PAUSE=1 kill-switch is active; carries reason field; operator kill-switch confirmation signal
wizard_daemon_safety_refusal  # reason: META-109 emitted by scripts/coord/wizard-daemon.sh when a mandatory safety guard fires (floor_temp_HOT or pr_conflicting); carries reason + target; safety audit trail — never suppressed

# ── INFRA-2040: silent-fleet-death watchdog ───────────────────────────────────
silent_fleet_death             # reason: INFRA-2040 emitted by scripts/coord/fleet-doctor-strict.sh and scripts/dispatch/fleet-brief.sh when last merge into origin/main is >=SILENT_DEATH_MERGE_HOURS (default 12h) old AND at least one com/dev.chump.* launchd daemon has last exit code != 0; catches the 2026-05-26 class of 24h dead-floor where fleet-brief still said "healthy"; carries merge_age_h, dead_daemon_count, dead_daemons, source; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
silent_fleet_death_autohealed  # reason: INFRA-2040 emitted by scripts/coord/fleet-doctor-strict.sh when CHUMP_DOCTOR_AUTOHEAL=1 and a daemon with exit=127 (command not found) is healed by restoring its script from origin/main + launchctl kickstart; carries label, script_path, source; opt-in auto-heal audit trail

# ── META-107: wizard-daemon Phase 2 (steps 3+4+5) ────────────────────────────
wizard_dispatch_executed        # reason: META-107 emitted by scripts/coord/wizard-daemon.sh step4 when chump --execute-gap <ID> is spawned in background; carries gap_id, pid, active_before; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS
wizard_dispatch_rate_limited    # reason: META-107 emitted by scripts/coord/wizard-daemon.sh step4 when active dispatch count >= CHUMP_WIZARD_MAX_PARALLEL; carries active_count, max_parallel; rate-limit audit trail
wizard_gap_skipped              # reason: META-107 emitted by scripts/coord/wizard-daemon.sh step4 when a gap has wizard_skip:true in its notes field; carries gap_id, reason; skip audit trail
wizard_cascade_rebase_triggered # reason: META-107 emitted by scripts/coord/wizard-daemon.sh step5 when gh pr update-branch succeeds for a BEHIND sibling PR after a gap_shipped event; carries pr_number, author, head_ref; cascade-rebase audit trail
wizard_classify_deferred        # reason: INFRA-2042 emitted by scripts/coord/wizard-daemon.sh step1 when a PR's mergeStateStatus=UNKNOWN (GitHub hasn't computed mergeability yet); PR skipped this iteration, re-evaluated next cycle; prevents DIRTY misclassification that silences recovery-queue routing
wizard_dispatch_cooldown        # reason: INFRA-2051 emitted by scripts/coord/wizard-daemon.sh step4 when a gap is skipped because it had a FAILED dispatch within CHUMP_WIZARD_DISPATCH_GAP_COOLDOWN_S (default 1800s); prevents wheel-spin on consistently failing gaps; carries gap_id, last_fail_age_s, cooldown_s
wizard_dispatch_giveup          # reason: INFRA-2051 emitted by scripts/coord/wizard-daemon.sh step4 when a gap hits >= CHUMP_WIZARD_MAX_DISPATCH_ATTEMPTS (default 3) FAILEDs in 24h; gap is tagged wizard_skip:true via chump gap set --add-note; carries gap_id, failed_attempts, max_attempts; needs operator clearance

# ── RESILIENT-025: off-rails guard ───────────────────────────────────────────
off_rails_bypassed              # reason: RESILIENT-025 emitted by scripts/git-hooks/pre-commit when a commit bypasses the off-rails gap-ID check via 'Off-Rails-Bypass: <reason>' trailer; carries claimed_gap, reason; audit trail for intentional pre-req integrations; printf-direct JSON not grep-scannable as kind=X literal in PROD_PATHS

# ── RESILIENT-026: orphan worktree watchdog ───────────────────────────────────
orphan_worktree_detected        # reason: RESILIENT-026 emitted by scripts/coord/orphan-worktree-watchdog.sh when a /tmp/chump-* worktree has uncommitted/unpushed work + dead owning process + >15min idle; scanner-anchor present in script header; registered in EVENT_REGISTRY.yaml with full field spec

# ── META-115: per-session source-freshness preamble (META-114 cluster) ───────
freshness_critical_stale_bypassed  # reason: META-115 emitted by scripts/coord/freshness-gate.sh when a CRITICAL_STALE classification (commits-behind > CHUMP_FRESHNESS_COMMITS_THRESHOLD or binary-age > CHUMP_FRESHNESS_BINARY_AGE_S or cron-health=FAIL) is overridden via CHUMP_ACCEPT_STALE=1; carries commits_behind, binary_age_s; bypass audit trail — never suppressed; Phase 1 ships reserved here, EVENT_REGISTRY.yaml integration deferred to follow-up sub-gap

# ── META-116 / META-119: Sonnet hang-detection (META-114 cluster) ────────────
dispatch_hung_hook_detected     # reason: META-116 emitted by scripts/coord/dispatch-health-check.sh when a git-commit or pre-commit child process exceeds CHUMP_DISPATCH_HUNG_THRESHOLD_S (default 120s); carries pid, age_s, cmd; precedent 2026-05-27 14:56Z INFRA-2000 dispatch where pre-commit wedged 5+ min and looked like Sonnet-abandoned; killing the hook PID unblocked Sonnet's own commit — the kill IS the rescue, not the takeover; Phase 1 ships reserved here, EVENT_REGISTRY.yaml integration deferred to follow-up sub-gap

# ── INFRA-2096: trunk-RED SKIP guard (test-side, scripts/ci/) ────────────────
trunk_red_skip                  # reason: INFRA-2096 emitted by scripts/ci/test-mcp-coord-smoke.sh (and future similar SKIP guards in scripts/ci/test-*.sh) when a CI smoke test SKIPs because a runtime prerequisite is missing (cargo not on PATH, build returned non-zero, binary missing after exit-0 build). Scanner does NOT include scripts/ci/ in PROD_PATHS (correct — that's test-script territory, not production), so reserved here to satisfy register-without-emit. Operator audits via grep '"kind":"trunk_red_skip"' .chump-locks/ambient.jsonl. Registered in EVENT_REGISTRY.yaml under "RESILIENT-trunk-RED guard" block.

# ── META-122: autopilot curator session launcher ─────────────────────────────
autopilot_heartbeat             # reason: META-122 / META-090; emitted by scripts/coord/fleet-autopilot.sh cmd_heartbeat every 5 min via launchd cron; carries loaded/total daemon counts + curators_alive/curators_total (META-122 addition). Was scanner-anchor-only prior to META-122; now also emitted from heartbeat path. Operator audits via grep '"kind":"autopilot_heartbeat"' .chump-locks/ambient.jsonl to confirm cron is alive.
curator_session_launched        # reason: META-122; emitted by fleet-autopilot.sh curator_spawn_one when a curator tmux window is newly created; carries role, session_id, loop_script. Signals successful spawn of one of the 6 curator roles (shepherd/target/handoff/ci-audit/decompose/md-links). Absence after autopilot start = curator launch failed.
curator_session_respawned       # reason: META-122; emitted by fleet-autopilot.sh curator_check_and_respawn (called from heartbeat) when a curator tmux window is found dead and restarted; carries role, session_id. Persistent respawns of the same role = underlying crash loop, operator investigation needed.
curator_sessions_stopped        # reason: META-122; emitted by fleet-autopilot.sh cmd_stop_curators when the chump-curators tmux session is killed; carries tmux_session name. One-per-stop lifecycle event; absence during stop = tmux session was already gone.
curator_heartbeat               # reason: META-122; emitted by stub curator loop (for shepherd/target roles that lack a productized *-loop.sh yet) on each cadence tick; carries role, session. Stops when those roles get real loop scripts (INFRA-2103). Operator audits to confirm stub curators are alive between productization PRs.
dispatch_flatline               # reason: INFRA-2101; emitted by scripts/ops/dispatch-flatline-detector.sh when sub_agent_dispatched count is 0 for a rolling window (default 2h) AND autopilot_heartbeat > 0 (alive-but-idle). Carries window_h, dispatched, heartbeats, daemon_ticks, note. The catch-net for "wizard-retirement daemons heartbeat but dispatch chain produces nothing" — precedent 2026-05-28 24h flatline.

# ── INFRA-2122: oracle-refresh silent-failure visibility ─────────────────────
oracle_refresh_failed             # reason: INFRA-2122 emitted by scripts/coord/oracle-refresh.sh when claude -p exits non-zero (auth failures like "Not logged in" / OAuth chain breakage); replaces the stderr-swallowing 2>/dev/null path that masked INFRA-2124-class failures and let THE_PATH.md go 4d stale. Carries exit_code, elapsed_s, stderr (truncated 500 chars). printf-direct JSON; scanner-anchor present in source.
oracle_refresh_empty              # reason: INFRA-2122 emitted by scripts/coord/oracle-refresh.sh when claude -p exits 0 but returns empty/too-short output (distinguishes "model returned empty" from "auth failed" — the old oracle_refresh_skipped conflated them). Carries exit_code, elapsed_s, body_len, stderr. printf-direct JSON; scanner-anchor present.
oracle_stale_despite_heartbeat    # reason: INFRA-2122 META-alarm — detector-for-the-detector. Emitted by scripts/coord/oracle-refresh.sh when THE_PATH.md mtime > CHUMP_ORACLE_STALE_THRESHOLD_S (12h default) AND oracle-refresh-state.jsonl heartbeat is recent (< CHUMP_ORACLE_HEARTBEAT_WINDOW_S, 6h default) — i.e., daemon is alive but output is stale. Catches the same silent-failure class as INFRA-2101 binary-refresh staleness. Carries path_age_s, state_age_s, stale_threshold_s.

# ── INFRA-2124: OAuth token refresh daemon (CLAUDE.md INFRA-622 promise) ─────
oauth_token_refreshed           # reason: INFRA-2124 emitted by scripts/coord/oauth-token-refresh.sh refresh-once on successful extraction of claudeAiOauth.accessToken from macOS Keychain (service: "Claude Code-credentials") and atomic write to ~/.chump/oauth-token.json. Scanner-anchor in script header. Carries source, prev_age_seconds, new_age_seconds, token_len. Required for Oracle / JIT scheduler / dispatch_flatline headless `claude -p` subprocesses to inherit a fresh CLAUDE_CODE_OAUTH_TOKEN. Registered in EVENT_REGISTRY.yaml.
oauth_token_refresh_failed      # reason: INFRA-2124 emitted by scripts/coord/oauth-token-refresh.sh refresh-once on keychain miss, JSON parse fail, missing claudeAiOauth.accessToken field, or atomic write failure. Carries reason (keychain_miss|json_parse_or_missing_field|empty_access_token), prev_age_seconds. Distinguishes "operator hasn't installed plist" (no event) from "daemon ran but failed" (event fires). Registered in EVENT_REGISTRY.yaml.
oauth_token_stale_despite_daemon  # reason: INFRA-2124 emitted by scripts/coord/infra-watcher-loop.sh cmd_check_oauth_freshness when ~/.chump/oauth-token.json is older than CHUMP_OAUTH_STALE_S (default 900s = 15min) AND launchctl shows com.chump.oauth-refresh plist loaded. Carries token_file, age, daemon status. The wedged-refresher catch-net — if the daemon is loaded but the file is stale, something is broken inside the refresher (keychain permission revoked, python3 gone from PATH, etc.). Registered in EVENT_REGISTRY.yaml.

# ── INFRA-2174: chump-fleet-recorder (INFRA-2164 capture layer) ──────────────
fleet_recorder_ttl_pruned         # reason: INFRA-2174 emitted by chump-fleet-recorder in-process TTL pruner task (fires every 60min) when it deletes events older than CHUMP_FLEET_RECORDER_TTL_DAYS (default 7d). Carries deleted_count, ttl_days, cutoff_ms. scanner-anchor present in crates/chump-fleet-recorder/src/main.rs.

# ── INFRA-2132: META-124 integration-cycle pipeline event kinds ──────────────
integration_cycle_started         # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped — reserved so coverage gate stays green until emitter lands.
integration_candidates_selected   # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
integration_cycle_merges          # reason: INFRA-2132 / META-124; per-cycle AGGREGATE kind (observability-curator amendment replacing per-merge fanout); registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
integration_preflight_started     # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
integration_preflight_failed      # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
ship_bisect_root_cause            # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
bisect_quarantine                 # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
integration_cycle_shipped         # reason: INFRA-2132 / META-124; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped; related_kinds: [pr_merged] cross-ref set in registry.
cycle_sampling_decision           # reason: INFRA-2132 / META-124 Phase 2 marker; registered in EVENT_REGISTRY.yaml; emitter INFRA-2130 not yet shipped.
bisect_step_evaluated             # reason: INFRA-2168 / META-124 C18; registered in EVENT_REGISTRY.yaml; emitter scripts/dev/integration-bisect-step.sh.
fleet_scale_changed               # reason: INFRA-2198 (META-128/C7); disk-plan gate + fleet auto-scale emitter; fields: from, to, reason.
external_repo_ship                # reason: INFRA-2169 / META-124 Mode D; registered in EVENT_REGISTRY.yaml; emitter scripts/coord/pr-rescue.sh (INFRA-2114 fork-aware path).

# ── CREDIBLE-084: tick_outcome event for no-idle policy audit + observability ──
tick_outcome                      # reason: CREDIBLE-084 emitted by scripts/coord/ambient-context-inject.sh --tick-outcome at end of each productized curator-loop tick. Carries role, session, outcome (shipped|dispatched_sub|blocked_defensible|picked_nextbest|none), exit_code, optional gap_id. Drives the no-idle-policy compliance audit (INFRA-2208) + fleet-wire emit/consumer-ack leaderboard (CREDIBLE-081). Scanner-anchor: scripts/coord/ambient-context-inject.sh.

# ── INFRA-1118: A2A Layer 1a — file-fallback + degraded/recovered event kinds ──
# Emit sites in crates/chump-coord/src/events.rs use Rust format! macro patterns
# not grep-scannable as bare "kind":"<value>" literals in PROD_PATHS scanner.
fleet_a2a_degraded    # reason: INFRA-1118 emitted via append_ambient() in events.rs; format!() emit not grep-scannable as literal
fleet_a2a_recovered   # reason: INFRA-1118 emitted via append_ambient() in events.rs; format!() emit not grep-scannable as literal
fleet_a2a_backpressure # reason: INFRA-1118 emitted via append_ambient() in events.rs; format!() emit not grep-scannable as literal

# ── INFRA-2239: chump-curator-supervisor L3 daemon ───────────────────────────
curator_failure_paged             # reason: INFRA-2239 emitted by chump-curator-supervisor (crates/chump-curator-supervisor/src/main.rs) when a novel curator failure fingerprint is detected AND a gap has been filed via `chump gap reserve`. Carries role, gap_id, priority, fingerprint, summary. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_respawned                 # reason: INFRA-2239 emitted by chump-curator-supervisor after tmux respawn-pane succeeds AND a fresh curator_heartbeat is detected within 60s. Carries role. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_respawn_failed            # reason: INFRA-2239 emitted by chump-curator-supervisor when tmux respawn-pane was attempted but no curator_heartbeat appeared within 60s. Carries role, reason. Triggers operator broadcast via scripts/coord/broadcast.sh. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
# ── INFRA-2070: cascade-unblock detector (META-118 sub-gap 4) ─────────────────
cascade_unblocked                 # reason: INFRA-2070 emitted by scripts/coord/cascade-unblock-detector.sh after a wedge_auto_fix PR merges and N open PRs with matching failure signature are rebased. Carries source_pr, signature_hash, matched_pr_numbers (csv), rebase_attempt_count, success_count, conflict_count. Scanner-anchor in cascade-unblock-detector.sh.
cascade_unblock_skipped           # reason: INFRA-2070 emitted by scripts/coord/cascade-unblock-detector.sh per-PR when a safety guard fires (operator_recent_comment, chump_hold_label, rebase_conflict, rate_limit_reached). Carries source_pr, signature_hash, pr_number, reason. Scanner-anchor in cascade-unblock-detector.sh.
# ── INFRA-2006: A2A inbox-routing fix ────────────────────────────────────────
a2a_inbox_alias_resolved        # reason: INFRA-2006; emitted by scripts/coord/chump-inbox.sh read when messages are found in a lease-id alias inbox (primary env-session-id != broadcast --to target); also by scripts/coord/lib/inbox-routing.sh resolve_inbox_target on sender-side lookup. Carries primary_session, alias_session, message_count. Registered in EVENT_REGISTRY.yaml.
a2a_inbox_message_orphan        # reason: INFRA-2006; advisory event emitted by scripts/coord/chump-inbox.sh when a message is found in an inbox file no live session owns. Not fatal; archive-on-dead-session cleans up. Carries inbox_file, message_count. Registered in EVENT_REGISTRY.yaml.
queue_driver_iter_attempted

# ── INFRA-2295: stale-pr-rebase-bot (SCALE-C safety net) ─────────────────────
stale_pr_auto_rebased                      # reason: INFRA-2295 emitted by scripts/coord/stale-pr-rebase-bot.sh when a stale armed PR is successfully rebased via gh pr update-branch (method=gh_update_branch) or local worktree fallback (method=local_rebase_fallback). Carries pr, branch, method, prior_strikes. Complements pr_auto_rebased from pr-auto-rebase.sh — bot adds 3-strike doctrine and trunk-RED guard on top.
stale_pr_rebase_failed                     # reason: INFRA-2295 emitted by scripts/coord/stale-pr-rebase-bot.sh when both gh pr update-branch and local worktree rebase fail for a given PR on this cycle. Carries pr, branch, strikes (current count after increment), strike_limit. Not fatal — operator acts only after stale_pr_unrebaseable (3-strike threshold).
stale_pr_unrebaseable                      # reason: INFRA-2295 emitted by scripts/coord/stale-pr-rebase-bot.sh when a PR's strike counter reaches CHUMP_REBASE_BOT_STRIKE_LIMIT (default 3). Carries pr, branch, strikes, note=operator_action_required. A WARN broadcast is also sent. PR is never auto-closed — operator decides. Strike state at .chump-locks/rebase-bot-strikes/<PR>.json.
stale_pr_rebase_bot_holding_for_trunk_red  # reason: INFRA-2295 emitted by scripts/coord/stale-pr-rebase-bot.sh at the top of each cycle when trunk-red-detector-state.json exists with a non-empty last_failed_sha. Signals the whole cycle was skipped to avoid wasting rebase capacity while CI is broken on main.

# ── INFRA-2130 SCALE-A: integrator-daemon LIVE-mode toggle safety rails ──────
integration_cycle_completed   # reason: INFRA-2130 SCALE-A; emitted by chump-integrator daemon when a LIVE cycle successfully pushes the integration branch and arms auto-merge. Carries cycle_id, gap_count, total_loc, gap_ids, pr_url. Replaces the stubbed integration_cycle_shipped path.
integration_cycle_failed      # reason: INFRA-2130 SCALE-A; emitted by chump-integrator daemon when a LIVE cycle's git push or gh pr create/merge fails. Carries cycle_id, gap_ids, error, circuit_breaker=armed. Triggers dry-run for the next cycle.
integration_dry_run_skip      # reason: INFRA-2130 SCALE-A; emitted by chump-integrator daemon when a cycle is forced to dry-run by the circuit breaker (reason=circuit_breaker). Distinguishes planned dry-runs from circuit-breaker-forced ones.
integration_trunk_red_hold    # reason: INFRA-2130 SCALE-A; emitted by chump-integrator daemon when a LIVE cycle is held because trunk-red-detector-state.json shows is_red=true. Carries cycle_id. Cycle is skipped entirely (no selection, no merge).
integrator_stale_sla_fired    # reason: INFRA-2418; emitted by crates/chump-integrator/src/daemon.rs (Rust emit_event) when stale-SLA fallback fires — candidate count below CHUMP_INTEGRATOR_VOLUME_THRESHOLD but oldest candidate age >= CHUMP_INTEGRATOR_STALE_SLA_HOURS. Scanner-anchor: crates/chump-integrator/src/daemon.rs.

# ── META-207: no-operator-escalation doctrine ─────────────────────────────────
operator_escalation_unjustified  # reason: META-207 codifies no-escalation doctrine; detector is future work, emission point is currently AGENTS.md self-audit only. Scanner-anchor: docs/process/SUBAGENT_DISPATCH.md.

# ── RESILIENT-041 / INFRA-2291 (2026-05-30): trunk-red rescue — EMIT-NO-REG sweep ─
# 32 event kinds were emitted by recently-landed code without registration,
# causing the event-registry-audit gate to fail queue-wide for every PR that
# isn't docs-only. The 9 PRs that landed in the prior hour took the audit-stub
# path (docs-only) or admin-merged via INFRA-1813 HITL — the sweep is the
# durable fix so non-docs PRs can flow through again.

# sccache R2 token rotation (INFRA-2237 — rotate-sccache-r2-token.sh + rotate-sccache-r2-gh-only.sh)
sccache_r2_token_rotated          # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-token.sh on successful CF API + GH secret rotation. Scanner-anchor: scripts/ops/rotate-sccache-r2-token.sh.
sccache_r2_token_rotation_failed  # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-token.sh on rotation failure. Carries step (create|parse_response|gh_set). Scanner-anchor: scripts/ops/rotate-sccache-r2-token.sh.
sccache_r2_token_rotation_partial # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-token.sh when CF token created but GH secret push failed (leaves orphan CF token). Scanner-anchor: scripts/ops/rotate-sccache-r2-token.sh.
sccache_r2_gh_rotated             # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-gh-only.sh when pushing existing R2 cred to GH secret (no CF API call). Scanner-anchor: scripts/ops/rotate-sccache-r2-gh-only.sh.
sccache_r2_gh_rotation_failed     # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-gh-only.sh per-secret on gh set failure. Carries step (set_access_key_id|set_secret_access_key). Scanner-anchor: scripts/ops/rotate-sccache-r2-gh-only.sh.
sccache_r2_gh_rotation_partial    # reason: INFRA-2237 emitted by scripts/ops/rotate-sccache-r2-gh-only.sh when one of two secrets pushed but the other failed (leaves R2 in mismatched state). Scanner-anchor: scripts/ops/rotate-sccache-r2-gh-only.sh.

# bot-merge resilience (INFRA-2272 — per-step progress ledger + gtimeout wrapping)
bot_merge_step_stalled            # reason: INFRA-2272 emitted by scripts/coord/bot-merge.sh when a wrapped step exceeds its gtimeout budget. Carries step, elapsed_s, budget_s. Scanner-anchor: scripts/coord/bot-merge.sh.
bot_merge_mode_fallback           # reason: INFRA-2272 emitted by scripts/coord/bot-merge.sh when an --auto-merge attempt falls back to admin-merge or manual recovery. Carries from_mode, to_mode, reason. Scanner-anchor: scripts/coord/bot-merge.sh.

# Curator supervisor (INFRA-2239, RESILIENT-035, RESILIENT-040)
curator_supervisor_circuit_broken           # reason: RESILIENT-035 emitted by chump-curator-supervisor when per-role spawn cap or flapping threshold tripped. Carries role, cap_kind (max_spawns_per_hour|flapping_threshold), counter. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_supervisor_dry_run                  # reason: INFRA-2239 emitted when supervisor is run with --dry-run; advisory, no remediation. Carries role, would_action. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_supervisor_gap_already_filed        # reason: RESILIENT-040 emitted when pre-file fingerprint check finds an open gap with matching fingerprint already filed by an earlier supervisor tick. Carries role, gap_id, fingerprint. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_supervisor_spawn_skipped_claim_collision # reason: RESILIENT-040 emitted when pre-spawn claim check finds the target gap is already actively claimed by another session. Carries role, gap_id, claim_session. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_sentinel_created          # reason: INFRA-2239 emitted when supervisor writes a dedup sentinel after spawning Sonnet on a fingerprint. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.
curator_sentinel_removed          # reason: INFRA-2239 emitted when sentinel pruner clears expired sentinels. Scanner-anchor: crates/chump-curator-supervisor/src/main.rs.

# Cascade detector / unblock automation (INFRA-2070)
cascade_auto_resolved             # reason: INFRA-2070 emitted by scripts/coord/cascade-unblock-detector.sh when a cascade is auto-resolved via successful rebase wave. Scanner-anchor in cascade-unblock-detector.sh.
cascade_resolve_skipped_semantic  # reason: INFRA-2070 emitted by scripts/coord/cascade-unblock-detector.sh when semantic-conflict detection skips an otherwise-rebasable PR. Scanner-anchor in cascade-unblock-detector.sh.

# Consensus / deliberator (INFRA-2274, META-157)
consensus_resolved                # reason: INFRA-2274 emitted by ConsensusCoordinator when quorum reached and decision final. Carries corr_id, decision, votes_for, votes_against. Scanner-anchor: crates/chump-coord/src/consensus.rs.
consensus_result                  # reason: META-157 emitted by scripts/coord/deliberator-loop.sh after tallying vote/proposal FEEDBACK events for a corr_id. Carries corr_id, winning_position, vote_count. Scanner-anchor: scripts/coord/deliberator-loop.sh:148.
deliberator_heartbeat             # reason: META-157 emitted by scripts/coord/deliberator-loop.sh per-tick liveness probe. Scanner-anchor: scripts/coord/deliberator-loop.sh.

# Vote / preference / proposal — FEEDBACK kinds, deliberator inputs (META-157)
vote                              # reason: META-157 FEEDBACK kind emitted via broadcast.sh FEEDBACK with kind=vote for consensus tallying. Read by scripts/coord/deliberator-loop.sh:190. Scanner-anchor: scripts/coord/deliberator-loop.sh.
preference                        # reason: META-157 FEEDBACK kind emitted via broadcast.sh FEEDBACK with kind=preference for soft-signal aggregation. Scanner-anchor: scripts/coord/deliberator-loop.sh.
proposal                          # reason: META-157 FEEDBACK kind emitted via broadcast.sh FEEDBACK with kind=proposal for option-ranking. Read by scripts/coord/decompose-loop.sh:138 + deliberator-loop.sh:283. Scanner-anchor: scripts/coord/decompose-loop.sh.

# FEEDBACK fanout (META-157)
feedback_fanout_delivered         # reason: META-157 emitted by feedback-fanout.sh when a FEEDBACK message is replicated from broadcast to N curator inboxes. Carries corr_id, recipient_count. Scanner-anchor: scripts/coord/feedback-fanout.sh.
feedback_fanout_skipped           # reason: META-157 emitted by feedback-fanout.sh when no recipients found for a corr_id. Carries corr_id, reason. Scanner-anchor: scripts/coord/feedback-fanout.sh.

# Queue / gap routing (INFRA-2271)
gap_requeued                      # reason: INFRA-2271 emitted by queue-driver when a semantic-skip releases a gap back to pickable pool. Carries gap_id, skip_reason. Scanner-anchor: scripts/dispatch/queue-driver.sh.
gap_routed_to_batched             # reason: INFRA-2271 emitted by queue-driver when a gap is routed to batched-under track (META-124 C7). Carries gap_id, target_branch. Scanner-anchor: scripts/dispatch/queue-driver.sh.

# Operator escalation surface (META-095)
operator_escalation               # reason: META-095 emitted by scripts/dispatch/operator-recall.sh when a halt-class condition triggers paging. Carries condition, reason. Scanner-anchor: scripts/dispatch/operator-recall.sh.

# Sibling status protocol (META-157 inter-curator coordination)
sibling_status_polled             # reason: META-157 emitted by curator loops when a sibling-status check is run before claiming overlapping paths. Carries sibling_session, sibling_paths. Scanner-anchor: scripts/coord/lib/sibling-protocol.sh.

# Test fixtures (false-positive matches from substring scanner)
bad                               # reason: false-positive — scanner matched `bad_new_kind` literal in scripts/ci/test-event-registry-effect-metric-hook.sh:60 (test fixture YAML, not a real emit).
good                              # reason: false-positive — scanner matched `good_new_kind` literal in scripts/ci/test-event-registry-effect-metric-hook.sh:91 (test fixture YAML, not a real emit).
skip                              # reason: false-positive — scanner matched bare `kind=skip` mention in queue-driver tests; not an ambient emit.
test_event                        # reason: test fixture used by crates/chump-fleet-recorder/src/main.rs:869 unit test + scripts/ci/test-inbox-prune.sh:139. Not a production event kind.

# ── INFRA-2290 strict-mode follow-up (2026-05-30): 76 register-without-emit orphans ─
# INFRA-2290's ci.yml audit tightened CHUMP_REGISTRY_GATE_MODE=strict in the
# audit job, which fails on registered-without-emit drift in addition to
# emit-without-register. 76 pre-existing orphan registrations (kinds documented
# in EVENT_REGISTRY.yaml whose emit calls were never wired or were removed)
# were not swept before strict mode was enabled — folded into RESILIENT-041's
# trunk-red rescue so the audit gate can pass while individual feature gaps
# wire their emitters lazily. Each entry below cites INFRA-2290 strict-mode
# sweep; replace with a feature-specific reason as emitters land.
arch_fit_decision                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
arch_fit_query                            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
asks_clarification                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
autonomous_ship_rate_regression           # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
avoids_tool                               # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
bot_autonomous_check_failed               # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
bot_autonomous_check_passed               # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
bot_merge_aborted_no_worktree             # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
branch_protection_drift                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
cascade_report                            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
change_approved                           # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
change_rolled_back                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
ci_yml_merge_driver_abort                 # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
claim_aborted_disk_full                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
cognition_ab_comparison                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
cognition_ab_run_start                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
cost_quota_exceeded                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
cost_quota_warning                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
curator_auto_decompose                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
decomposition_hint                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
feature_silent_failure                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
flake_autorerun_initiated                 # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
flake_autorerun_persisted                 # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
flake_autorerun_recovered                 # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
flake_autorerun_skipped                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
fleet_paused_disk_critical                # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
ftue_init_smoke_passed                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gap_auto_flip_failed                      # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gap_auto_flipped                          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gap_check_false_positive                  # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gap_perf_sample                           # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gate_check_result                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gate_check_start                          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
gh_shim_worktree_install_blocked          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
guard_bypassed                            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
incident_commander_engaged                # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
intent                                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
intent_parse_ok                           # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
intent_parse_unknown                      # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
invariant_recovered                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
invariant_violation                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
lesson_not_applied                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
lessons_audit_run                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
lessons_pruned                            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
network_restored                          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
network_unavailable                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
opus_roadmap_published                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
orchestrate_intent                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
orchestrate_session_end                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
picker_used_priority                      # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pillar_balance_block                      # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pillar_balance_warn                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
planner_rank_ran                          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_bundle_blocked                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_dedup_blocked                          # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_dedup_bypass_rejected                  # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_dedup_bypassed                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_fmt_auto_fixed                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_fmt_shepherd_run                       # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
pr_scope_violation                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
preflight_baseline_diff                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
preflight_dupe_pr                         # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
preflight_dupe_worktree                   # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
premature_closure_auto_fixed              # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
review_handoff_escalated                  # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
review_handoff_timeout                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
roadmap_update_proposal_cost              # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
roadmap_update_proposal_failed            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
roadmap_update_proposal_opened            # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
roadmap_update_proposal_skipped           # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
rust_first_bypass_used                    # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
slo_recovered                             # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
speculative_race_loss                     # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
stale_post_merge_gap                      # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
test_gate_bypassed                        # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
velocity_tracker_tick                     # reason: INFRA-2290 strict-mode sweep — pre-existing registered kind without active emitter; documented in EVENT_REGISTRY.yaml; emitter wiring deferred to per-feature gap.
# ── META-181: PR-shepherd daemon skeleton (META-180 slice 1) ─────────────────
pr_shepherd_tick    # reason: META-181 emitted by scripts/coord/pr-shepherd-daemon.sh on each tick; carries open_pr_count, dry_run. Scanner-anchor: scripts/coord/pr-shepherd-daemon.sh.
# ── INFRA-2300: DigitalOcean droplet self-hosted Linux runner (CI-speed Tier 3) ──
do_droplet_runner_install_failed  # reason: INFRA-2300 emitted by scripts/setup/install-do-droplet-runner.sh on any step failure (validate_env, create_droplet, wait_ssh, remote_install, wait_runner_online). Carries step, reason, droplet fields. Also emitted by uninstall-do-droplet-runner.sh on teardown completion (step=uninstall_complete) to confirm operator-requested teardown. NOT auto-filed as a gap — operator debugs the droplet manually. Off-Rails-Bypass: INFRA-2300 ships 2 new ambient kinds; reserved.txt entry is part of same atomic unit.
do_droplet_runner_installed       # reason: INFRA-2300 emitted by scripts/setup/install-do-droplet-runner.sh on successful runner install + online confirmation. Carries droplet, ip, id, labels, runner_version fields. Confirms the new Linux burst runner is registered and the fleet has +1 persistent runner slot. Off-Rails-Bypass: INFRA-2300 ships 2 new ambient kinds; reserved.txt entry is part of same atomic unit.
# ── INFRA-2309: auto-merge-rearm-daemon ──────────────────────────────────────
auto_merge_rearmed          # reason: INFRA-2309 emitted by scripts/coord/auto-merge-rearm-daemon.sh when a CLEAN+allowlisted PR has auto-merge re-armed via gh pr merge --auto --squash. Carries pr, reason, dry_run. Scanner-anchor in daemon header. Registered in EVENT_REGISTRY.yaml under "Auto-merge rearm daemon (INFRA-2309)".
auto_merge_rearm_skipped    # reason: INFRA-2309 emitted by scripts/coord/auto-merge-rearm-daemon.sh when a CLEAN PR is found but its title prefix is not in fix-class-allowlist.txt. Carries pr, reason, dry_run. Safety brake — feat-class PRs skip without CHUMP_AUTO_MERGE_REARM_OPEN=1.
auto_merge_rearm_failed     # reason: INFRA-2309 emitted by scripts/coord/auto-merge-rearm-daemon.sh when gh pr merge --auto --squash returns non-zero for a CLEAN+allowlisted PR. Carries pr, reason, dry_run. Signals auth issue or branch-protection block needing operator attention.
# ── META-184/186: PR-shepherd daemon action events ────────────────────────────
pr_action_taken             # reason: META-184/186 emitted by scripts/coord/pr-shepherd-daemon.sh per actioned PR per tick; action enum: rebase|rebase_failed|rebase_skipped (META-184); arm_auto_merge|arm_auto_merge_failed|arm_auto_merge_skipped|file_followup_gap|file_followup_gap_skipped (META-186); reason field carries guard name when guard fires (trunk_red|claim|debounce|throttle|conflict|dedup); optional new_gap_id present on file_followup_gap. Off-Rails-Bypass: META-186 extends existing pr_action_taken kind with new action values; reserved.txt update is part of same atomic unit.
# ── INFRA-2326: Fix-Trunk Priority Lane dispatcher (RESILIENT) ───────────────
fix_trunk_dispatched        # reason: INFRA-2326 emitted by scripts/dispatch/fix-trunk-dispatcher.sh when a fix_trunk-tagged gap is claimed and a Sonnet sub-agent is launched via claude -p. Carries gap_id, model, pid, worktree (or dry_run=true in DRY_RUN mode). Scanner-anchor in script header. Off-Rails-Bypass: INFRA-2326 ships 3 new ambient kinds atomically; reserved.txt entry is part of same atomic unit; sibling INFRA-2324 holds EVENT_REGISTRY.yaml lease for trunk_red companion ship — full registry entry deferred to follow-up after both land.
fix_trunk_no_candidate      # reason: INFRA-2326 emitted by scripts/dispatch/fix-trunk-dispatcher.sh when trunk_red is active but no open gap has skills_required containing fix_trunk — signals the operator that the auto-fix lane is armed but starved of candidates (file a fix_trunk-tagged gap). Carries lookback_minutes, skill_tag. Off-Rails-Bypass: INFRA-2326 ships 3 new ambient kinds atomically; reserved.txt entry is part of same atomic unit.
fix_trunk_skipped           # reason: INFRA-2326 emitted by scripts/dispatch/fix-trunk-dispatcher.sh when a prior dispatch is still alive (singleton lockfile parallelism cap=1) — prevents launchd over-fire from burning N redundant Sonnets on the same incident. Carries reason=prior_active, prior_pid, prior_gap_id. Off-Rails-Bypass: INFRA-2326 ships 3 new ambient kinds atomically; reserved.txt entry is part of same atomic unit.
# ── META-184: PR-shepherd daemon auto-rebase action ───────────────────────────
pr_action_taken             # reason: META-184 emitted by scripts/coord/pr-shepherd-daemon.sh once per BEHIND PR per tick; action=rebase|rebase_failed|rebase_skipped; reason=trunk_red|claim|debounce|throttle|conflict when guard fires; carries pr_number, action, reason, gap_id, dry_run. Off-Rails-Bypass: META-184 ships new pr_action_taken ambient kind; reserved.txt entry is part of same atomic unit.
# ── INFRA-2069: wedge-fixer template library ──────────────────────────────────
wedge_fixer_template_rendered     # reason: INFRA-2069; emitted by scripts/coord/wedge-fixer-dispatch.sh (--execute mode only); registered in EVENT_REGISTRY.yaml; auto-dispatch consumer (INFRA-2068) not yet shipped so event fires only on operator-initiated manual dispatch; deferred full auto-dispatch until INFRA-2068 lands.
# ── INFRA-2214: curator role-doc template — confidence calibration loop ────────
curator_confidence_calibrated     # reason: INFRA-2214 / META-127; emitted by any curator-opus-* role when a verified misfire causes a confidence tier downgrade (high→med or med→low) for the rest of the session. Carries role, original_confidence, new_confidence, reason. Registered in EVENT_REGISTRY.yaml. Scanner-anchor present in each .claude/agents/*.md confidence calibration loop section.
# ── INFRA-2258: chump voice — Voice-of-Agent VOA filing ──────────────────────
voice_of_agent_filed            # reason: INFRA-2258; emitted by src/commands/voice.rs (chump voice subcommand) after writing both docs/gaps/VOA-NNNN.yaml and docs/voice/VOA-NNNN-FULL.yaml. Carries voa_id, wedge_class, minutes_lost, fix_shape, target_repo (always anonymized). Scanner-anchor: src/commands/voice.rs.
# ── INFRA-2280: META-118 scheduling activation — daemon lifecycle events ──────
meta_118_daemon_started           # reason: INFRA-2280; emitted by scripts/setup/install-meta-118-daemons.sh once after both launchd plists are loaded. Confirms META-118 scheduling chain activated on this host. Carries labels array. Scanner-anchor: install-meta-118-daemons.sh.
meta_118_daemon_tick              # reason: INFRA-2280; emitted at the start of each scheduled run by scripts/coord/novel-wedge-classifier.sh (15-min) and scripts/coord/cascade-unblock-detector.sh (5-min). Carries daemon name. Scanner-anchor: both daemon scripts.
reaper_holding_for_trunk_red    # reason: RESILIENT-050; emitted by scripts/ops/stale-pr-reaper.sh when trunk-RED state is detected (last_failed_sha != null within 60min window) before the INFRA-1410 auto-respawn loop. Carries would_bounce_count (PRs spared), reason, window_s. Prevents the 2026-05-30T15:46Z class of incident where 28 in-flight PRs were destroyed because trunk was RED. Scanner-anchor present in stale-pr-reaper.sh.
reaper_stuck_class_skipped      # reason: RESILIENT-050; emitted by scripts/ops/stale-pr-reaper.sh when a BLOCKED PR's failure class is shared by >= CHUMP_REAPER_SHARED_CLASS_THRESHOLD (default 3) other PRs — indicating a queue-wide blocker rather than an individual stuck PR. These PRs receive a STUCK broadcast instead of being bounced. Carries pr, shared_count, failure_class.

# ── INFRA-2396 batch: 16 orphan kinds shipped without EVENT_REGISTRY.yaml entries ──
# Each was added by a recent PR without DOC-026/INFRA-1237 follow-up. Reserved here
# rather than registered so the unwedge ships small; full registration deferred to
# follow-up gap "INFRA-NEW: backfill EVENT_REGISTRY entries for INFRA-2396 cohort".
daemon_activator_failed         # reason: INFRA-2396; emitted by scripts/coord/daemon-activator.sh on failure; registration pending follow-up gap.
daemon_auto_activated           # reason: INFRA-2396; emitted by scripts/coord/daemon-activator.sh after successful activation; registration pending.
daemon_silence_monitor_tick     # reason: INFRA-2396; emitted by scripts/coord/daemon-silence-monitor.sh per tick (heartbeat class); registration pending.
detector_disabled               # reason: INFRA-2396; emitted by various detector daemons when explicitly disabled via env var; registration pending.
ghost_pr_closed                 # reason: INFRA-2396; emitted by scripts/coord/ghost-pr-closer.sh when a phantom PR is closed; registration pending.
handoff_contract_vote           # reason: INFRA-2396; emitted by handoff curator when voting on typed contract acceptance; registration pending.
handoff_reactor_cooldown_skip   # reason: INFRA-2396; emitted when handoff reactor skips candidate due to cooldown; registration pending.
investigate_dispatch_failed     # reason: INFRA-2396; emitted by investigate-class daemon when sub-agent dispatch fails; registration pending.
investigate_dispatched          # reason: INFRA-2396; emitted on successful investigate dispatch; registration pending.
investigate_dryrun              # reason: INFRA-2396; emitted in dry-run mode of investigate daemon; registration pending.
investigate_pending             # reason: INFRA-2396; emitted when an investigate candidate is queued but not dispatched; registration pending.
main_worktree_drift_detected    # reason: INFRA-2396; emitted by main-worktree-drift-detector.sh when local main is N commits behind origin; registration pending.
quartermaster_heartbeat         # reason: INFRA-2396; emitted by quartermaster curator role per heartbeat cycle; registration pending.
shelfware_audit_run             # reason: INFRA-2396; emitted by quartermaster-shelfware-audit.sh per audit run; registration pending.
shelfware_detected              # reason: INFRA-2396; emitted by shelfware audit per detected artifact; registration pending.
wedge_class_detected            # reason: INFRA-2396; emitted by novel-wedge-classifier.sh when a new wedge class is identified; registration pending.

# ── INFRA-2396 register-without-emit batch: 5 orphans ─────────────────────────
# These kinds are registered in EVENT_REGISTRY.yaml but no source currently
# emits them. Likely emitters were renamed/removed during recent auto-merge-
# armer + gh-token-rotate refactors. Reserved here so strict-mode CI gate
# passes; proper cleanup (either re-add emit sites or prune from registry)
# tracked under follow-up gap "INFRA-NEW: prune dead EVENT_REGISTRY entries".
auto_merge_arm_dropped     # reason: INFRA-2396; registered in EVENT_REGISTRY.yaml but no source emit found; prior emitter likely lost in INFRA-2289 watcher refactor
auto_merge_arm_restored    # reason: INFRA-2396; same as above
auto_merge_arm_skipped     # reason: INFRA-2396; same as above
gh_token_rotate_noop       # reason: INFRA-2396; registered but no source emit found; likely lost in gh-token-rotate refactor
gh_token_rotated           # reason: INFRA-2396; same as above

# ── INFRA-2406: cross-PR contract gate ────────────────────────────────────────
# Emitted by scripts/ci/test-cross-pr-contract.sh — a CI-gate script in
# scripts/ci/ which is intentionally excluded from PROD_PATHS (CI gates can
# legitimately mention "kind":"X" as test fixtures; including them would produce
# false-positive emit-without-register hits). The kind IS registered in
# EVENT_REGISTRY.yaml. Reserved here so strict-mode CI gate passes.
cross_pr_contract_mismatch  # reason: INFRA-2406; emitted by scripts/ci/test-cross-pr-contract.sh (CI-gate script, excluded from PROD_PATHS scan); registered in EVENT_REGISTRY.yaml
prepush_lease_mismatch  # reason: INFRA-2446; emitted by scripts/git-hooks/pre-push Guard 5 (RESILIENT-026) via direct printf JSON; git-hooks/ dir is outside PROD_PATHS grep scan

# ── INFRA-2534 stitch: 4 walker false-positives blocking the substrate cascade ──
# These kinds ARE emitted in source — the audit walker has false-positive bugs
# (likely the heredoc-in-Rust pattern at src/main.rs:7772 and the _emit helper
# at scripts/coord/waste-spike-detector.sh:58). Reserving here so the strict-mode
# CI gate passes while a follow-up gap addresses the walker bug.
gap_reserve_blocked              # reason: INFRA-2534; emitted at src/main.rs:7772 via heredoc raw-string r#"..."# pattern; walker misses heredoc kind literals
worker_paused_waste_spike_bypass # reason: INFRA-2534; emitted at scripts/coord/waste-spike-detector.sh:58 via _emit helper; walker fails to resolve _emit args as the kind name
scratch_cache_hit                # reason: INFRA-2534; emitted by chump-coord scratchpad layer (INFRA-1121 A2A Layer 3d); Rust struct serialization path not parsed by walker
scratch_cache_miss               # reason: INFRA-2534; emitted by chump-coord scratchpad layer (INFRA-1121 A2A Layer 3d); Rust struct serialization path not parsed by walker
