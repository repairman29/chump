# scripts/ci/dead-grep-detector-allowlist.txt — CREDIBLE-237
#
# Known false positives from dead-grep-detector.sh's line-heuristic scan.
# Format: "rel/path:line  # reason". Keep entries narrow (exact file:line)
# so this file can't silently absorb a real dead grep target — each entry
# was individually verified not to be the vacuous-pass class the detector
# exists to catch (grep target file/dir genuinely present, just not
# resolvable by the heuristic).
scripts/ci/test-dead-grep-detector.sh:45  # grep call is literal text inside this test's own heredoc fixture (the string being written to a tmp file to test the detector itself), not a real invocation in this file
scripts/ci/test-claim-redirect-on-red.sh:23  # guarded by `if [[ -f "$REPO_ROOT/.cargo/config.toml" ]]` on the preceding line; heuristic can't see the guard
scripts/ci/test-launchd-installer-paths.sh:70  # target is a glob (install-*-launchd.sh matching multiple files); heuristic mis-tokenizes the quoted prefix + glob suffix as one literal path
scripts/ci/test-worktree-base-honored.sh:62  # multi-line grep call with several file arguments continued across lines; heuristic captures a mid-expression fragment, not the real last argument
scripts/ci/test-gap-doctor-safe-sweep.sh:129  # docs/gaps/INFRA-B1.yaml is a fixture the test itself writes at runtime (yaml_write "INFRA-B1" ...), not committed source
scripts/ci/test-circuit-breaker.sh:35  # genuine dead target (src/circuit_breaker.rs / mod circuit_breaker no longer exist), but this script is not wired into ci.yml (0 refs) — orphaned pre-existing test, not a live vacuous-pass gate. Tracked: INFRA-4063
scripts/ci/test-code-reviewer-agent.sh:40  # genuine dead target (docs/gaps.yaml retired by ZERO-WASTE-020), but this script is not wired into ci.yml (0 refs) — orphaned pre-existing test, not a live vacuous-pass gate. Tracked: INFRA-4063
