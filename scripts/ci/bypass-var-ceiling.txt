234
# EFFECTIVE-094 — bypass-var DEBT-CEILING (the addition tax).
# Each net-positive new bypass var requires this number to be raised
# (operator action) AND a corresponding deletion gap filed.
#
# History:
#   233 → 234: INFRA-2435 introduced CHUMP_GAP_RESERVE_SKIP_PR as a
#              test-isolation escape hatch for the new gap-reserve PR-title
#              similarity check. Deletion path: state.db-based PR-title
#              cache so tests don't need the bypass.
