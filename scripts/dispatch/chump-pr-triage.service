[Unit]
Description=Chump unmerged-work triage reporter (EFFECTIVE-435)
[Service]
Type=oneshot
Environment=HOME=/root
ExecStart=/bin/bash -lc "set -a; source /root/.chump/providers.env 2>/dev/null; set +a; cd /root/Projects/chump; exec bash scripts/ops/pr-triage-reporter.sh --apply"
